//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IGHardwareGuC {
public: // (access info unavailable from symbols alone)
    IGHardwareGuC();
    IGHardwareGuC(OSMetaClass const*);
    auto acquireDoorbell(UK_KM_CONTEXT_DESCRIPTOR_REC*, bool);
    auto allocContextId(unsigned long long, bool);
    auto allocDoorbellId(UK_CONTEXT_PRIORITY);
    auto allocUkDoorbell(unsigned int, bool);
    auto createUkContext(unsigned long long, UK_CONTEXT_PRIORITY);
    auto dumpContextInfo(unsigned int);
    auto dumpLogBuffers();
    auto dumpStatus(char*, int);
    auto dumpWorkItemHistory();
    auto free();
    auto getContextCpuAddress(unsigned int);
    auto getContextGttAddress(unsigned int);
    auto getDoorbellRangeForPriority(UK_CONTEXT_PRIORITY, int&, int&);
    auto getEngineForHwCsType(IGHwCsType);
    auto getGuCContextInfo(unsigned int);
    auto getMetaClass() const;
    auto handleHardwareGuCToHostInterrupt(OSObject*, void*);
    auto handleSoftwareGuCToHostInterrupt(IOInterruptEventSource*, int);
    auto handleTimerInterrupt(IOTimerEventSource*);
    auto hostToGuCAction(unsigned int, unsigned int, int, unsigned int*);
    auto inSubmitQueue(IGHwCsType, SCHED_CONTEXT_ENGINE_PRESENCE_STRUCT volatile*);
    auto initDoorbells();
    auto initEngine(unsigned int, IGHwCsType, SGfxContextDescriptor const&, unsigned long long, unsigned long);
    auto initInterrupts();
    auto initSchedControl();
    auto initWithOptions(IntelAccelerator*);
    auto initWorkItemHistory(unsigned int);
    auto invalidateTLB();
    auto isContextIdle(unsigned int);
    auto isGuCIdle();
    auto loadGuCBinary();
    auto logRingAvailable();
    auto logRingInit(unsigned long);
    auto logRingRead(void*, unsigned int);
    auto logRingSetEnabled(bool);
    auto logRingWrite(void const*, unsigned int);
    auto pauseScheduler();
    auto reacquireDoorbell(unsigned int);
    auto recordWorkItem(unsigned int, IGHwCsType, unsigned int, unsigned int, unsigned int, unsigned int, KM_SCHED_WORK_QUEUE_ITEM_REC*);
    auto registerForInterrupts();
    auto releaseContextId(unsigned int);
    auto releaseDoorbell(UK_KM_CONTEXT_DESCRIPTOR_REC*);
    auto releaseDoorbellId(unsigned short);
    auto releaseUkContext(unsigned int);
    auto resumeScheduler();
    auto setDoorbellPinning(unsigned short, bool);
    auto setDoorbellUsage(unsigned short, bool);
    auto setupAdditionalDataStructs();
    auto setupContextPool(int);
    auto setupLogBuffers(unsigned int, int, int, int);
    auto stealDoorbellId(UK_CONTEXT_PRIORITY);
    auto submitWorkItem(unsigned int, unsigned int, IGHwCsType, unsigned int, unsigned int, unsigned int);
    auto withOptions(IntelAccelerator*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IGHardwareGuC();
};
