//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IGHardwareRingBuffer {
public: // (access info unavailable from symbols alone)
    IGHardwareRingBuffer(OSMetaClass const*);
    auto addRingResources(unsigned int, unsigned int*, unsigned long long, unsigned int);
    auto alignRing(unsigned int);
    auto canUseSemaphoreWaitPoll(int);
    auto checkBatchRegs(debugRegisters&);
    auto checkEngineStatus(debugRegisters&);
    auto checkExecListRegs(debugRegisters&);
    auto checkHangLogMemory();
    auto checkInstRegs(debugRegisters&);
    auto checkPageFault(debugRegisters&);
    auto checkRingRegs(debugRegisters&);
    auto clearEventWait();
    auto clearSemaphoreWait();
    auto commitStampCommand(unsigned int, bool);
    auto debugGraphicsEngine();
    auto describeDriverAllocations(IOAccelAllocationInfo*, char const*, int);
    auto doHangAnalysis();
    auto doSchedulerReset(IGHardwareContext*);
    auto drvErrLog(char const*, ...);
    auto drvErrLog(char const*, ...)::fullWarning;
    auto dumpBatchBuffer(debugRegisters&, ResourceCacheElement*);
    auto dumpBuffer(char const*, unsigned int*, unsigned long long, unsigned int);
    auto dumpExecListRegs(debugRegisters&);
    auto dumpExecutionListStatus(debugRegisters&);
    auto dumpHangAnalysis();
    auto dumpInstDone2Status(debugRegisters&, int);
    auto dumpInstDone3Status(debugRegisters&, int, int);
    auto dumpInstDone4Status(debugRegisters&, int, int);
    auto dumpInstDoneStatus(debugRegisters&);
    auto dumpPageTableStatus(debugRegisters&);
    auto dumpRegisterRange(unsigned int, unsigned int);
    auto dumpRegisters();
    auto dumpRegistersBCS();
    auto dumpRegistersDebug();
    auto dumpRegistersMCS();
    auto dumpRegistersRCS();
    auto dumpRegistersSysConfig();
    auto dumpRegistersVECS();
    auto dumpRingBuffer(debugRegisters&);
    auto dumpRingStatus();
    auto dumpSchedStatus();
    auto dumpSemaphoreRegs();
    auto dumpStampInfo();
    auto free();
    auto generateHangSig();
    auto getBatchBufferAddressSpaceIndicator();
    auto getFlushSpace();
    auto getFlushTLBSpace();
    auto getGTTWriteMode();
    auto getMetaClass() const;
    auto getRingResources(ResourceCacheElement**);
    auto getSemaphoreWaitBuffer(unsigned int, int);
    auto getSemaphoreWaitPollSpace(int);
    auto getStampSpace();
    auto getUtilizationData(IGHardwareContextUtilizationData*) const;
    auto init(IGHardwareContext*);
    auto initDebugEngine();
    auto insertSemaphoreWaitPoll(unsigned int, int);
    auto insertSemaphoreWaitPollCommand(IGSemaphoreWaitBufferQueue::SemaphoreWaitBufferDescriptor const*);
    auto notifyComplete(IOAccelEvent*);
    auto patchFrequencyRequest(unsigned int, unsigned int);
    auto refreshRing();
    auto resetGraphicsEngine(IGHardwareContext*);
    auto resetRing();
    auto resubmitStampToRing(unsigned int, unsigned int*);
    auto sampleTimestamp(unsigned long long);
    auto sampleTimestampHiLoHi(unsigned long long);
    auto setBlockFenceTimeWriteAddress(unsigned long long);
    auto setStampIndex(int);
    auto sleepForStamp(unsigned int*, unsigned int, unsigned int);
    auto submitBatchBuffer(unsigned long long, unsigned int, unsigned int*, unsigned int, bool, bool);
    auto submitToRing();
    auto submitToRing() (.cold.1);
    auto waitForSemaphore(unsigned int, unsigned int);
    auto waitForSpace(unsigned int);
    auto waitTimeout(bool () block_pointer);
    auto waitTimeout(bool () block_pointer) (.cold.1);
    auto withHardwareContext(IGHardwareContext*);
    auto writeBlockFenceMMIOTimestamp();
    auto writeBuffer(unsigned int*, unsigned int);
    auto writeDWord(unsigned int);
    auto writeFlush();
    auto writeFlushTLB();
    auto writeQWord(unsigned long long);
    auto writeStamp(unsigned int, bool);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IGHardwareRingBuffer();
};
