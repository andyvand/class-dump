//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class Vega10TimingGenerator {
public: // (access info unavailable from symbols alone)
    Vega10TimingGenerator();
    Vega10TimingGenerator(OSMetaClass const*);
    auto areTimingRegistersLocked();
    auto blankCrtc(bool, unsigned int);
    auto calculateCrtcOffset(unsigned int);
    auto enableCrtc(bool, bool);
    auto enablePrefetch(bool);
    auto getFrameCount();
    auto getMetaClass() const;
    auto initWithController(unsigned char, ATIController*);
    auto initWithController(unsigned char, ATIController*)::__desc;
    auto isCounterMoving();
    auto isCrtcEnabled();
    auto isGraphicUpdatePending();
    auto isInVerticalBlank();
    auto isMasterLocked();
    auto isTestPatternEnabled();
    auto lockGrphSurfaceRegisters();
    auto lockMaster();
    auto lockTimingRegisters();
    auto programMasterUpdateMode();
    auto programTiming();
    auto readHwTiming(AmdHwCrtcTiming&);
    auto resetFlipControl();
    auto resetTestPattern();
    auto setBarPattern(CrtcTestPattern, unsigned int);
    auto setBarPattern(CrtcTestPattern, unsigned int)::srcBpc;
    auto setColorRampPattern(unsigned int);
    auto setColorRampPattern(unsigned int)::srcBpc;
    auto setColorSquaresPattern(CrtcTestPattern, unsigned int);
    auto setTestPattern(CrtcTestPattern, unsigned int);
    auto translateColorFormat(unsigned int);
    auto translateColorMode(CrtcTestPattern, Vega10TimingGenerator::CrtcTestPatternColorFormat);
    auto unlockGrphSurfaceRegisters();
    auto unlockMaster();
    auto unlockTimingRegisters();
    auto waitForCrtcUpdate();
    auto waitForCrtcUpdate()::CRTC_UPDATE_PENDING_CHECK_COUNT;
    auto waitForCrtcUpdate()::CRTC_UPDATE_PENDING_SLEEP_TIME;
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~Vega10TimingGenerator();
};
