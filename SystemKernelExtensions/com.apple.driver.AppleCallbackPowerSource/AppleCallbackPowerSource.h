//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleCallbackPowerSource {
public: // (access info unavailable from symbols alone)
    AppleCallbackPowerSource();
    AppleCallbackPowerSource(OSMetaClass const*);
    doMergeUpdateData(OSDictionary**, OSSymbol const**, unsigned long, OSDictionary*);
    extractSubDicts(OSDictionary*, OSDictionary**, OSDictionary**, OSDictionary**);
    fixupPropertyType(OSSymbol const*, OSObject*);
    fixupPropertyType(OSSymbol const*, OSObject*)::_os_log_fmt;
    fixupPropertyTypes();
    free();
    freeGated();
    getMetaClass() const;
    getPendingUpdateDict();
    getWorkLoop() const;
    handlePropertyOverrideGated(OSDictionary*);
    init(OSDictionary*);
    logEvent(OSSymbol const*, OSDictionary*);
    logEvent(OSSymbol const*, OSDictionary*)::_os_log_fmt;
    mergeUpdateBDArrayData(OSDictionary**, OSArray**);
    mergeUpdateData(OSDictionary**, OSDictionary**);
    psCallback(OSSymbol const*, OSDictionary*);
    psCallbackThread();
    psCallbackThreadGated(bool);
    psCallbackThreadGated(bool)::firstrun;
    setProperties(OSObject*);
    start(IOService*);
    updatePendingUpdateDict(OSDictionary*);
    updateStatus(bool);
    updateStatus(bool)::_os_log_fmt;
    updateStatus(bool)::earlyUpdateCount;
    updateStatus(bool)::lastAtCriticalLevel;
    updateStatus(bool)::lastChargeCapable;
    updateStatus(bool)::lastExternalConnected;
    updateStatus(bool)::lastIsCharging;
    updateStatus(bool)::lastRawExternalConnected;
    updateStatus(bool)::lastSoc;
    updateStatus(bool)::lastUpdateTs;
    updateStatus(bool)::timerActive;
    ~AppleCallbackPowerSource();
};
