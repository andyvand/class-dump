//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IOGPUWorkQueue {
public: // (access info unavailable from symbols alone)
    IOGPUWorkQueue(OSMetaClass const*);
    auto addCommandToTail(IOGPUCommandDescriptor*);
    auto addCommandToTail_slow(IOGPUCommandDescriptor*);
    auto copyWorkQueueState() const;
    auto describeAllocations(IOGPUAllocationInfo*);
    auto didSubmitCommands(int);
    auto finalize();
    auto findCommandDescriptorWithStampValue(int, unsigned int) const;
    auto free();
    auto free()::kalloc_type_view_117;
    auto getEventMachine() const;
    auto getFirstPendingCommand() const;
    auto getHardwareDiagnosisReport(unsigned int*, unsigned long*);
    auto getMetaClass() const;
    auto init(IOGPU*, IOGPUCommandQueue const*, int);
    auto init(IOGPU*, IOGPUCommandQueue const*, int)::kalloc_type_view_34;
    auto logRingBuffer();
    auto performBlockOnSubmittedCommands(void (IOGPUCommandDescriptor*) block_pointer) const;
    auto prepare_next_command(unsigned int, bool);
    auto pruneRingBuffer();
    auto resetHardwareAndContinue();
    auto restart();
    auto restart() (.cold.1);
    auto restart()::_os_log_fmt;
    auto setSchedulerPriority(int);
    auto set_error_on_all_commands(unsigned int);
    auto submit_next_command(int);
    auto updateSchedulerState(eIOGPUWorkQueueSchedulerState*);
    auto waitForAllSubmittedToBeCompleted();
    auto willSubmitCommands();
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IOGPUWorkQueue();
};
