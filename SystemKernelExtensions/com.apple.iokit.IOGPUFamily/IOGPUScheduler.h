//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IOGPUScheduler {
public: // (access info unavailable from symbols alone)
    IOGPUScheduler();
    IOGPUScheduler(OSMetaClass const*);
    auto allowCPUPriorityBoost(IOGPUWorkQueue*) const;
    auto areWorkQueuesEnabled() const;
    auto cancelProgressTimer();
    auto collectGartWirings();
    auto configure_gart_collector(unsigned int, unsigned int, unsigned long long);
    auto copy_busy_workqueue_states() const;
    auto decrement_gart_collector_disable_count();
    auto decrement_gart_collector_disable_count() (.cold.1);
    auto decrement_gart_collector_disable_count()::_os_log_fmt;
    auto describeAllocations(IOGPUAllocationInfo*);
    auto disableWorkQueues();
    auto disable_gart_collector();
    auto display_footprint_changed();
    auto display_footprint_changed_callback(void*);
    auto enableWorkQueues();
    auto enable_gart_collector();
    auto eventTimeout(int);
    auto flush_scheduler(eIOGPUKickReason);
    auto free();
    auto free()::kalloc_type_view_292;
    auto freeToWireMemory(IOGPUMemory*);
    auto freeToWireMemory(IOGPUMemory*)::_os_log_fmt;
    auto freeToWireSysMemory(IOGPUMemory*);
    auto gart_collector_thread();
    auto gart_wired_cache_size() const;
    auto getCompletionGate() const;
    auto getGPURestartClientErrorCode(IOGPUCommandQueue const*);
    auto getGPURestartSeed(IOGPUCommandQueue const*);
    auto getMetaClass() const;
    auto getSingleSubmissionMode() const;
    auto getSubmissionGate() const;
    auto gpuRestartBegin();
    auto gpuRestartCompleted();
    auto gpu_progress_check_interrupt(IOInterruptEventSource*, int);
    auto gpu_progress_check_timer(IOTimerEventSource*);
    auto hardware_error_interrupt(IOInterruptEventSource*, int);
    auto hardware_error_interrupt(IOInterruptEventSource*, int)::_os_log_fmt;
    auto increment_gart_collector_disable_count();
    auto increment_gart_collector_disable_count() (.cold.1);
    auto increment_gart_collector_disable_count()::_os_log_fmt;
    auto init(IOGPU*);
    auto init(IOGPU*)::_os_log_fmt;
    auto init(IOGPU*)::kalloc_type_view_141;
    auto isCompletionLockedByCurrentThread() const;
    auto isSubmissionLockedByCurrentThread() const;
    auto kick_completed();
    auto kick_scheduler(eIOGPUKickReason);
    auto notifyResourceCompleted();
    auto progressTimerCheckInterrupt();
    auto progressTimerCheckInterrupt()::_os_log_fmt;
    auto prune_busy_queues();
    auto prune_busy_queues() (.cold.1);
    auto prune_busy_queues() (.cold.2);
    auto queue_did_submit(IOGPUWorkQueue*);
    auto registerCommandQueue(IOGPUCommandQueue const*);
    auto registerCommandQueue(IOGPUCommandQueue const*)::_os_log_fmt;
    auto registerCommandQueue(IOGPUCommandQueue const*)::kalloc_type_view_2080;
    auto registerCommandQueue(IOGPUCommandQueue const*)::kalloc_type_view_2088;
    auto retainOldestCommand(unsigned long long*, bool);
    auto scheduleWorkqueue(IOGPUWorkQueue*);
    auto schedule_blocked_queues();
    auto schedule_workqueue(IOGPUWorkQueue*, bool);
    auto schedule_workqueue(IOGPUWorkQueue*, bool) (.cold.1);
    auto schedule_workqueue(IOGPUWorkQueue*, bool) (.cold.2);
    auto schedule_workqueue(IOGPUWorkQueue*, bool) (.cold.3);
    auto schedule_workqueue(IOGPUWorkQueue*, bool) (.cold.4);
    auto schedule_workqueue(IOGPUWorkQueue*, bool) (.cold.5);
    auto serializeSchedulerState(OSDictionary*) const;
    auto serialize_scheduler(void*, void*, OSSerialize*);
    auto setGPURestartClientErrorCode(IOGPUCommandQueue const*, unsigned int);
    auto setGPURestartErrorCodeOnAllClients(unsigned int);
    auto setSingleSubmissionMode(bool);
    auto signalHardwareError(eRestartRequest, int);
    auto signalHardwareError(eRestartRequest, int)::_os_log_fmt;
    auto startHardwareProgressTimer();
    auto stopHardwareProgressTimer();
    auto unregisterCommandQueue(IOGPUCommandQueue const*);
    auto unwireAllSysMemory();
    auto updateGartCollectorLowWaterMark(unsigned long long);
    auto update_gart_cache_size();
    auto update_gart_collector_age();
    auto wakeProgressTimerAtTime(unsigned long long);
    auto workCompleted();
    auto work_completed(unsigned long long, unsigned long long, IOGPUWorkQueue*);
    auto work_completion_interrupt(IOInterruptEventSource*, int);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IOGPUScheduler();
};
