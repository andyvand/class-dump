//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class DDCProxy {
public: // (access info unavailable from symbols alone)
    DDCProxy();
    DDCProxy(OSMetaClass const*);
    auto areBothDataAndClock(unsigned short, unsigned char);
    auto areEitherDataOrClock(unsigned short, unsigned char);
    auto errorRecovery(unsigned short, unsigned char);
    auto errorRecovery(unsigned short, unsigned char)::RECOVERY_LOOP_LIMIT;
    auto freeClock(unsigned short, unsigned char);
    auto freeData(unsigned short, unsigned char);
    auto getClock(unsigned short);
    auto getData(unsigned short);
    auto getDelayTime(unsigned char);
    auto getLastOperationByteCount();
    auto getMetaClass() const;
    auto init(ATIController*);
    auto initWithController(ATIController*);
    auto readBytesFromOffset(unsigned char, unsigned short, unsigned char, unsigned char, unsigned char*, unsigned int);
    auto readComponentDongle(unsigned short);
    auto readEdid(unsigned short, unsigned char, Edid*);
    auto readEdidBlock(unsigned short, unsigned char, unsigned char, Edid*);
    auto readEdidSegment(unsigned short, unsigned char, unsigned char, Edid*, bool);
    auto receiveBit(unsigned short, unsigned char, bool&);
    auto receiveBlock(unsigned short, unsigned char, unsigned long long, unsigned char*);
    auto receiveByte(unsigned short, unsigned char, unsigned char&);
    auto receiveByteWithAck(unsigned short, unsigned char, unsigned char&, unsigned char);
    auto sendBit(unsigned short, unsigned char, unsigned char);
    auto sendBlock(unsigned short, unsigned char, unsigned long long, unsigned char*);
    auto sendByte(unsigned short, unsigned char, unsigned char);
    auto sense(unsigned short, unsigned char);
    auto setClock(unsigned short, unsigned char, unsigned int);
    auto setData(unsigned short, unsigned char, unsigned int);
    auto setStart(unsigned short, unsigned char);
    auto setStop(unsigned short, unsigned char);
    auto setupEdidRead(unsigned short, unsigned char, unsigned char);
    auto setupEdidSegment(unsigned short, unsigned char, unsigned char);
    auto transferByDDCci(unsigned short, unsigned char, bool, IOI2CRequest*);
    auto transferI2c(unsigned short, unsigned char, bool, IOI2CRequest*);
    auto transferWithOffset(unsigned char, unsigned short, unsigned char, unsigned char, unsigned char*, unsigned int);
    auto waitClockHigh(unsigned short, unsigned char);
    auto waitClockHigh(unsigned short, unsigned char)::LOOP_LIMIT;
    auto writeBytesToOffset(unsigned char, unsigned short, unsigned char, unsigned char, unsigned char*, unsigned int);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~DDCProxy();
};
