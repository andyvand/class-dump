//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class ATIConnector {
public: // (access info unavailable from symbols alone)
    ATIConnector();
    ATIConnector(OSMetaClass const*);
    auto assignDriverCrtcIndex(unsigned char, unsigned char, unsigned char);
    auto assignPllIndex(unsigned char);
    auto assignRADToDriverIndex(unsigned char, unsigned char, unsigned int);
    auto cacheHpdSenseState(bool);
    auto calculateScanOutRequirementFor(unsigned char);
    auto calculateVramRequirementFor(unsigned char);
    auto calculateVramRequirementFor(unsigned char)::HDTV_WINDOW;
    auto calculateVramRequirementFor(unsigned char)::MAX_WINDOW;
    auto checkDDCConnections(unsigned int, unsigned int, Edid*, unsigned char);
    auto checkDPCDConnections(unsigned int, unsigned char);
    auto checkDPNonDDCConnections(unsigned int, DpcdInfo*);
    auto checkGMuxConnection();
    auto checkNonDDCConnections(unsigned int, unsigned int);
    auto clearChangeEvents(bool);
    auto connectionChanged();
    auto detectAux();
    auto detectDisplays(unsigned int, unsigned char);
    auto detectHdmiDongle();
    auto detectHdmiDongleCombined();
    auto detectHdmiDongleSimple();
    auto disableConnector();
    auto disableHdcp(bool);
    auto findEdidForConnector(unsigned int, Edid*, unsigned char);
    auto free();
    auto getActiveConnection(unsigned char);
    auto getActiveConnectionFor(unsigned char);
    auto getActiveDigitalEncoder(unsigned char);
    auto getActiveDpcd();
    auto getActiveEdid(unsigned char);
    auto getActiveEdidFor(unsigned char);
    auto getAssignedDriverIndex(unsigned char);
    auto getAssignedDriverWithRAD(unsigned int);
    auto getAssignedPllIndex();
    auto getAuxUtil();
    auto getCachedHpdSenseState();
    auto getConnectedConnection(unsigned char);
    auto getConnectedConnectionCount();
    auto getConnectedConnectionFor(unsigned char);
    auto getConnectedConnectionNumber();
    auto getConnectedDpcd();
    auto getConnectedEdid(unsigned char);
    auto getConnectedEdidFor(unsigned char);
    auto getConnectionChangeMask();
    auto getConnectionFlags();
    auto getConnectorId();
    auto getConnectorType();
    auto getCurrentActiveInfoStructureForCrtc(unsigned char);
    auto getCurrentActiveInfoStructureForDev(unsigned char);
    auto getDacIndex();
    auto getDefaultMode(unsigned char);
    auto getDefaultModeFor(unsigned char);
    auto getDetailedTimings(unsigned char);
    auto getDetailedTimingsFor(unsigned char);
    auto getDeviceIndexFor(unsigned char);
    auto getDeviceIndexWithRAD(unsigned int);
    auto getDigIndex();
    auto getDpServices(unsigned char);
    auto getDpServicesFor(unsigned char);
    auto getDpServicesForRAD(unsigned int);
    auto getEnabledFeatures();
    auto getFeatures();
    auto getHdcpProxy();
    auto getHdmiDongleROMPtr(DPDongleROM**);
    auto getHotPlugId();
    auto getI2CInfo(I2CInfo&);
    auto getInfoStructure();
    auto getMaxScanOutFor(unsigned char);
    auto getMetaClass() const;
    auto getNonDDCConnections();
    auto getOutstandingChangeEvent();
    auto getOutstandingNotification(bool);
    auto getPanel();
    auto getPriorityIndex();
    auto getPropertiesForUserClient();
    auto getPropertiesForUserClient()::CONNECTOR_STRINGS;
    auto getRADForAssignedDriver(unsigned char, bool);
    auto getRADForDeviceIndex(unsigned char, bool);
    auto getSenseId();
    auto getSenseInfo();
    auto getState(unsigned int);
    auto getTotalMaxScanOut();
    auto getTransmitterLink();
    auto getTransmitterNumber();
    auto getVramSize(unsigned char);
    auto hasConnections(unsigned int);
    auto hasEdid(unsigned char);
    auto hasEdidFor(unsigned char);
    auto hasFeature(unsigned long long);
    auto hasFeatures(unsigned long long);
    auto hasOverrideEdid();
    auto init(unsigned char, AuxChannelProxy*, ConnectorInfo const*, ATIController*);
    auto initWithData(unsigned char, AuxChannelProxy*, ConnectorInfo const*, ATIController*);
    auto initializeConnections();
    auto initializeTimingList(unsigned char);
    auto insertChangeEvent(ConnectionChangeEvent);
    auto isAssigned(unsigned char);
    auto isConnected(unsigned char);
    auto isDigitalWithAnalogPanel(unsigned char);
    auto isDpPCIE();
    auto isDpPPLL();
    auto isEmcDisplayAttached();
    auto isHpdSenseSameWithCached();
    auto isMstConnected();
    auto isOnline();
    auto isOutputEnabled();
    auto isTypeConnected(ConnectionNumber);
    auto linkTrainingFailed();
    auto populateGoldenSettings();
    auto programActiveConnection(CRTC_PARAMS*, bool);
    auto programAudio(bool);
    auto programBaobabInfoPacket(CRTC_PARAMS*);
    auto programHdcp(bool);
    auto readEdid(unsigned int, unsigned char, Edid*, unsigned char);
    auto readOverrideEdid(char const*, Edid*, unsigned char);
    auto readPanelEdid(unsigned int, Edid*, unsigned char);
    auto reduceScanOutsAvailable();
    auto refreshActiveInfo(unsigned char, AtiDetailedTimingInformation const*, CRTC_PARAMS*);
    auto refreshActiveInfoFor(unsigned char, AtiDetailedTimingInformation const*, CRTC_PARAMS*);
    auto resetCacheHpdSenseState();
    auto resetConnections();
    auto sense(unsigned int);
    auto setActiveDigitalEncoder(unsigned char, unsigned char);
    auto setBlank(unsigned int, bool);
    auto setConnection(unsigned int, unsigned char);
    auto setConnectionType(ConnectionNumber);
    auto setOffline();
    auto setOverrideEdid(Edid*, unsigned int, unsigned char*);
    auto setPriorityIndex(unsigned char);
    auto shouldCheckForDdcConnections(unsigned int, unsigned int);
    auto shouldUseHdmi(Edid*);
    auto sufficientScanOutsForTiming(AtiDetailedTimingInformation*);
    auto turnOffConnectionsFor(CRTC_PARAMS*, ConnectionNumber);
    auto unassignDriverCrtcIndex(unsigned char);
    auto updateActiveInfo(unsigned char, AtiDetailedTimingInformation const*, CRTC_PARAMS*, bool);
    auto updateActiveInfoFor(unsigned char, AtiDetailedTimingInformation const*, CRTC_PARAMS*, bool);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~ATIConnector();
};
