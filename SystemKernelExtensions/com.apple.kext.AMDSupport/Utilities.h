//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class Utilities {
public: // (access info unavailable from symbols alone)
    auto abs(int);
    auto areOutputFormatsEqual(AtiDetailedTimingInformation const*, AtiDetailedTimingInformation const*);
    auto atoi(char const*);
    auto calculateRefreshRate(AtiDetailedTimingInformation const*);
    auto ceil(double);
    auto colorAdd(IOColorEntry const&, IOColorEntry const&);
    auto colorOr(IOColorEntry const&, IOColorEntry const&);
    auto colorScalarDivide(IOColorEntry const&, int);
    auto colorScalarMultiply(float, IOColorEntry const&);
    auto colorScalarMultiply(int, IOColorEntry const&);
    auto colorShiftLeft(IOColorEntry const&, unsigned int);
    auto colorSubtract(IOColorEntry const&, IOColorEntry const&);
    auto compareViewports(Window const&, Window const&);
    auto convertBytesToUInt16(unsigned char const*);
    auto convertBytesToUInt32(unsigned char const*);
    auto convertBytesToUInt64(unsigned char const*);
    auto convertColorFormat(unsigned int, unsigned int, unsigned char);
    auto convertTrainingSettingstoOSLinkConfig(DPTrainingSettings const*, AGDCDPLinkConfig_t*);
    auto copyProperty(IORegistryEntry*, IORegistryEntry*, char const*, char const*);
    auto cosf(float);
    auto crc4(unsigned char const*, unsigned long);
    auto crc8(unsigned char const*, unsigned long);
    auto decodeFloatPixelFormat(unsigned short);
    auto doRangesIntersect(Range const&, Range const&, Range*);
    auto encodeFloatPixelFormat(float);
    auto endOfRange(Range const&);
    auto endOfRange(unsigned int, unsigned int);
    auto exp(double);
    auto fabs(double);
    auto findConnectionNumber(unsigned int);
    auto findIndexOfStandardMode(unsigned int, unsigned int, unsigned int);
    auto findIndexOfStandardModeId(int);
    auto findPciDevice(unsigned char, unsigned char, unsigned char);
    auto findPlistProperty(OSObject*, char const*);
    auto findPriorityConnectionNumber(unsigned int);
    auto findStandardMode(unsigned int, unsigned int, unsigned int);
    auto findStandardModeId(int);
    auto floor(double);
    auto getCEAPixelRepetition(AtiDetailedTimingInformation const*);
    auto getCEAPixelRepetition(unsigned char);
    auto getCEATimingID(AtiDetailedTimingInformation const*);
    auto getColorFormatFromTimingIOPixelEncoding(unsigned int, AtiDetailedTimingInformation const*);
    auto getConnectorInfoProperties(ConnectorInfo const*);
    auto getDeepColorDepth(unsigned int);
    auto getDetailedTimingProperties(AtiDetailedTimingInformation const*);
    auto getExtendedColorimetryFromTiming(AtiDetailedTimingInformation const*, unsigned int&);
    auto getOSObjectData(OSObject*, void*, unsigned long);
    auto getOSObjectSize(OSObject*);
    auto getOutputDepthFromTimingBPC(unsigned int, AtiDetailedTimingInformation const*);
    auto getPixelFormatFrom(IOPixelInformation const*);
    auto getPlistProperty(IORegistryEntry*, char const*, char const*, void*, unsigned int);
    auto getPllIndexFrom(ClockSourceId);
    auto getRegistryProperty(IORegistryEntry*, char const*, void*, unsigned int);
    auto getRegistryPropertySize(IORegistryEntry*, char const*);
    auto getReturnCodeStr(int);
    auto getRotationAngelFrom(AtiDetailedTimingInformation const*);
    auto getSignalTypeFrom(ConnectionNumber);
    auto getTimingColorFormat(unsigned int, AtiDetailedTimingInformation const*);
    auto getViewport(AtiDetailedTimingInformation const*);
    auto isActiveTimingEqual(AtiDetailedTimingInformation const*, AtiDetailedTimingInformation const*);
    auto isCEAHDTVTiming(AtiDetailedTimingInformation const*);
    auto isCEAHDTVTimingID(unsigned char);
    auto isCEASDTVTiming(AtiDetailedTimingInformation const*);
    auto isCEASDTVTimingID(unsigned char);
    auto isCEATiming(AtiDetailedTimingInformation const*);
    auto isCEAYCbCr601Timing(AtiDetailedTimingInformation const*);
    auto isCEAYCbCr709Timing(AtiDetailedTimingInformation const*);
    auto isCEAYCbCrTiming(AtiDetailedTimingInformation const*);
    auto isComponentTiming(AtiDetailedTimingInformation const*);
    auto isCustomMode(int);
    auto isCustomScaledMode(int);
    auto isDetailedTimingEqual(AtiDetailedTimingInformation const*, AtiDetailedTimingInformation const*);
    auto isFiniteDouble(double);
    auto isFiniteFloat(float);
    auto isInSet(unsigned int*, unsigned int, unsigned int);
    auto isMultiDisplayMode(int);
    auto isNTSCTiming(AtiDetailedTimingInformation const*);
    auto isNanDouble(double);
    auto isNanFloat(float);
    auto isPALTiming(AtiDetailedTimingInformation const*);
    auto isTimingInterlaced(AtiDetailedTimingInformation const*);
    auto isTimingScaled(AtiDetailedTimingInformation const*);
    auto isTvTiming(AtiDetailedTimingInformation const*);
    auto itoa(int, int, char*, unsigned long);
    auto itoa(int, int, char*, unsigned long)::num;
    auto lengthOfRange(unsigned int, unsigned int);
    auto printChildrenNodes(IORegistryEntry*);
    auto rotationMinus(unsigned int, unsigned int);
    auto rotationPlus(unsigned int, unsigned int);
    auto simplifyRatio(unsigned int*, unsigned int*);
    auto sin(double);
    auto sinf(float);
    auto sqrt(unsigned int);
    auto str_cat(char*, char const*, unsigned long);
    auto str_copy(char*, char const*, unsigned long);
    auto str_replace(char*, char, char);
    auto str_reverse(char*);
    auto str_reverse(char*, unsigned long);
    auto stringifyBitsPerColorComponent(unsigned short);
    auto stringifyColorimetry(unsigned short);
    auto stringifyDynamicRange(unsigned short);
    auto stringifyPixelEncoding(unsigned short);
    auto strncmp_prefix(char const*, char const*, unsigned long);
    auto strncmp_suffix(char const*, char const*, unsigned long);
    auto swap(bool*, bool*);
    auto swap(float*, float*);
    auto swap(int*, int*);
    auto swap(unsigned char*, unsigned char*);
    auto swap(unsigned int*, unsigned int*);
    auto swap(unsigned short*, unsigned short*);
    auto takeNextIndex(unsigned char&, bool);
    auto trunacte(double);
};
