//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class GammaTable {
public: // (access info unavailable from symbols alone)
    GammaTable();
    GammaTable(OSMetaClass const*);
    auto convertColorBits(IOColorEntry&, unsigned char, unsigned char);
    auto convertColorBits(unsigned short, unsigned char, unsigned char);
    auto copyTable(GammaTable*);
    auto doGammaCorrection(IOColorEntry*, unsigned short, unsigned short);
    auto doGammaCorrection(IOColorEntry*, unsigned short, unsigned short, unsigned short);
    auto doGammaCorrectionForComponent(unsigned short, unsigned short, GammaTable::Color);
    auto encodeCLUTEntry2PixelColor(unsigned short, IOPixelInformation const&, bool);
    auto fillWithColor(unsigned int);
    auto getChannelCount();
    auto getColor(GammaTable::Color, unsigned short);
    auto getColorBits();
    auto getColorBytes();
    auto getColorDataWithPointer(unsigned char*, unsigned short, unsigned short);
    auto getEntry(unsigned short);
    auto getMetaClass() const;
    auto getSize();
    auto init(unsigned short, unsigned short, unsigned short);
    auto initTable(unsigned short, unsigned short, unsigned short);
    auto refresh(GammaTable*);
    auto refresh(_AMD_PIPE_GAMMA_TABLE*);
    auto refresh(unsigned short, unsigned short, unsigned short, void*);
    auto reset(unsigned short, unsigned short, unsigned short);
    auto reset(unsigned short, unsigned short, unsigned short)::BLACK_ENTRY;
    auto setColor(GammaTable::Color, unsigned short, unsigned short);
    auto setEntry(unsigned short, IOColorEntry const&);
    auto setEntry(unsigned short, IOColorEntry const&, unsigned char);
    auto setHWMatchStatus(bool);
    auto setInternals(unsigned short, unsigned short, unsigned short);
    auto setLinearRamp();
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~GammaTable();
};
