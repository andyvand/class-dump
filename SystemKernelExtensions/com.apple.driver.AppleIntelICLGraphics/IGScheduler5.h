//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IGScheduler5 {
public: // (access info unavailable from symbols alone)
    IGScheduler5();
    IGScheduler5(OSMetaClass const*);
    auto bind(IGHardwareContext*, int, unsigned char);
    auto checkForProgress(IGHwCsType);
    auto checkForProgress(IGHwCsType) (.cold.1);
    auto cleanupSharedPrivateData(IGHardwareContext*);
    auto disableContextSwitchInterrupts();
    auto disableErrorInterrupts();
    auto disableInterrupts();
    auto disableStampInterrupt(int);
    auto dumpStatus(char*, int);
    auto enableContextSwitchInterrupts();
    auto enableErrorInterrupts();
    auto enableInterrupts();
    auto enableStampInterrupt(int);
    auto free();
    auto getActiveContexts(IGHwCsType, IGHardwareContext**, unsigned long long*, IGHardwareContext**);
    auto getCurrentContext(IGHwCsType) const;
    auto getLastContextRunTimeNS(unsigned char) const;
    auto getMetaClass() const;
    auto getNextStampIndex(IGHwCsType, unsigned char);
    auto getPmUtilizationSnapshot();
    auto getWorkLoop() const;
    auto handleHighPriThrottleTimerInterrupt(IOTimerEventSource*);
    auto handleKickDPSMInterrupt(IOInterruptEventSource*, int);
    auto handleLowPriThrottleTimerInterrupt(IOTimerEventSource*);
    auto handleNormalHighPriThrottleTimerInterrupt(IOTimerEventSource*);
    auto handleNormalPriThrottleTimerInterrupt(IOTimerEventSource*);
    auto initPriorityConfig();
    auto initPriorityConfig() (.cold.1);
    auto initSharedPrivateData(IGHardwareContext*);
    auto initWithAccelerator(IOGraphicsAccelerator2*);
    auto isContextIdle(IGHardwareContext const*) const;
    auto isGpuIdle() const;
    auto isHung(unsigned int);
    auto isStampInterruptEnabled(int);
    auto notify(IGHwCsType);
    auto notifyAsyncEngines(int);
    auto notifyAsyncEngines(int) (.cold.1);
    auto pause();
    auto prepareGPUReset(IGHwCsType);
    auto processGPUReset(IGHwCsType, IGHardwareContext*);
    auto push(IGHardwareContext*, unsigned int, unsigned int, bool, bool);
    auto rebind(IGHardwareContext*, unsigned char);
    auto resetCommandStreamersOnWake();
    auto resetCredits();
    auto resetLastContextRunTimeNS(unsigned char);
    auto resume();
    auto setPerfLogLevel(unsigned long long);
    auto setPmAttributes(void const*);
    auto setThermalAttribute(unsigned int);
    auto signalDisplayStampUpdate(int);
    auto systemDidWake();
    auto systemWillSleep();
    auto unbind(IGHardwareContext*);
    auto updateCredits();
    auto updateIdleState(IGHwCsType, unsigned int);
    auto updateKnobs();
    auto updateOptions();
    auto updateSharedPrivateData(IGHardwareContext*);
    auto updateThermalStallConfigs(unsigned int, unsigned int);
    auto updateThermalStallStats(ThermalStallStats*);
    auto verifyThrottling(bool*);
    auto waitForGpuIdle();
    auto withAccelerator(IOGraphicsAccelerator2*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IGScheduler5();
};
