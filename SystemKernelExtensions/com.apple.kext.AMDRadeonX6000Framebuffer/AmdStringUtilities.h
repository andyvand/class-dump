//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AmdStringUtilities {
public: // (access info unavailable from symbols alone)
    auto stringifyBitsPerColorComponent(unsigned short);
    auto stringifyChangeEvent(AmdLinkChangeEvent);
    auto stringifyChannelId(channel_id);
    auto stringifyColorimetry(unsigned short);
    auto stringifyConnectorId(graphics_object_id);
    auto stringifyDetectReason(AmdFbDetectReason);
    auto stringifyDongleType(AmdFbDisplayDongleType);
    auto stringifyDynamicRange(unsigned short);
    auto stringifyEngineId(engine_id);
    auto stringifyFbEvents(unsigned int);
    auto stringifyHpdSourceId(hpd_source_id);
    auto stringifyLinkRate(AmdFbDpLinkRate);
    auto stringifyMemoryType(VideoMemoryType);
    auto stringifyPixelEncoding(unsigned short);
    auto stringifyReturnCode(int);
    auto stringifyRotation(unsigned short);
    auto stringifySignalType(signal_type);
    auto stringifyStreamState(AmdLinkStreamState);
    auto stringifySurfaceFormat(_AMD_HW_SURFACE_PIXEL_FORMAT);
    auto stringifyTransactionBit(unsigned long long);
    auto stringifyTransmitterId(transmitter);
    auto stringifyWsaaState(unsigned int);
    auto strlen_va(char const*, __va_list_tag*);
    auto strncmp_prefix(char const*, char const*, unsigned long);
    auto strncmp_suffix(char const*, char const*, unsigned long);
};
