//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AMDRadeonX6000_AmdTimingList {
public: // (access info unavailable from symbols alone)
    AMDRadeonX6000_AmdTimingList();
    AMDRadeonX6000_AmdTimingList(OSMetaClass const*);
    auto addMode(AmdDetailedTimingInformation const*);
    auto countDriverModes() const;
    auto countOsModes() const;
    auto findActiveWindow(AmdFbRect const&, AmdDetailedTimingInformation&) const;
    auto findDisplayMode(int, AmdDetailedTimingInformation&) const;
    auto findMatchingMode(AmdDetailedTimingInformation const*, AmdDetailedTimingInformation&, bool, bool) const;
    auto findMaxClock(AmdDetailedTimingInformation&) const;
    auto findMaxViewport(AmdDetailedTimingInformation&) const;
    auto getDriverModeList(int*) const;
    auto getMetaClass() const;
    auto getTimingAt(unsigned int) const;
    auto indexOfTiming(int) const;
    auto merge(OSArray const*);
    auto shouldAddObject(OSObject*) const;
    auto subtract(OSArray const*);
    auto withCapacity(unsigned int);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~AMDRadeonX6000_AmdTimingList();
};
