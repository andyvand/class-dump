//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AmdTimingUtilities {
public: // (access info unavailable from symbols alone)
    auto areActiveTimingsEqual(AmdDetailedTimingInformation const*, AmdDetailedTimingInformation const*);
    auto areDetailedTimingsEqual(AmdDetailedTimingInformation const*, AmdDetailedTimingInformation const*);
    auto areOutputFormatsEqual(AmdDetailedTimingInformation const*, AmdDetailedTimingInformation const*);
    auto areViewportsEqual(AmdFbViewport const&, AmdFbViewport const&);
    auto calculateRefreshRate(AmdDetailedTimingInformation const*);
    auto calculateScaledRectangle(AmdFbRect const&, AmdFbRect const&, AmdFbRect&);
    auto calculateScaledViewport(AmdFbRect const&, AmdFbRect const&, AmdFbViewport&);
    auto clearPathChangeFlags(AmdFbDisplayPath*);
    auto compareWindows(AmdFbRect const&, AmdFbRect const&);
    auto createDisplayPath(unsigned char);
    auto getViewport(AmdDetailedTimingInformation const*);
    auto hasPathChanged(AmdFbDisplayPath const*);
    auto initializeDisplayPath(unsigned char, AmdFbDisplayPath*);
    auto isTimingDownScaled(AmdDetailedTimingInformation const*);
    auto isTimingInterlaced(AmdDetailedTimingInformation const*);
    auto isTimingRotated(AmdDetailedTimingInformation const*);
    auto isTimingScaled(AmdDetailedTimingInformation const*);
    auto isTimingTv(AmdDetailedTimingInformation const*);
    auto setOffline(AmdFbDisplayPath*);
    auto translateAmdDpTraining(IOFBDPLinkConfig*, AmdDpLinkConfig const*);
    auto translateDepthMode(unsigned int);
    auto translateDetailedTimingToDebugProperty(AmdDetailedTimingInformation const*);
    auto translateIopRect(AmdFbViewport&, _AMD_PIPE_RECTL const&);
    auto translateOsDpTraining(AmdDpLinkConfig*, IOFBDPLinkConfig const*);
    auto translateTimingListToDebugProperty(AMDRadeonX6000_AmdTimingList const*);
    auto translateTimingRangeToDebugProperty(IODisplayTimingRangeV2 const*);
    auto translateViewport(_AMD_PIPE_RECTL&, AmdFbViewport const&);
    auto updateStreamComposition(AmdFbDisplayPath const*, AmdFbViewport*, unsigned int, unsigned int);
    auto validateTimingRange(AmdDetailedTimingInformation const*, IODisplayTimingRangeV2 const*, unsigned int);
};
