//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AmdTimingUtilities {
public: // (access info unavailable from symbols alone)
    areActiveTimingsEqual(AmdDetailedTimingInformation const*, AmdDetailedTimingInformation const*);
    areDetailedTimingsEqual(AmdDetailedTimingInformation const*, AmdDetailedTimingInformation const*);
    areOutputFormatsEqual(AmdDetailedTimingInformation const*, AmdDetailedTimingInformation const*);
    areViewportsEqual(AmdFbViewport const&, AmdFbViewport const&);
    calculateRefreshRate(AmdDetailedTimingInformation const*);
    calculateScaledRectangle(AmdFbRect const&, AmdFbRect const&, AmdFbRect&);
    calculateScaledViewport(AmdFbRect const&, AmdFbRect const&, AmdFbViewport&);
    clearPathChangeFlags(AmdFbDisplayPath*);
    compareWindows(AmdFbRect const&, AmdFbRect const&);
    createDisplayPath(unsigned char);
    getViewport(AmdDetailedTimingInformation const*);
    hasPathChanged(AmdFbDisplayPath const*);
    initializeDisplayPath(unsigned char, AmdFbDisplayPath*);
    isTimingDownScaled(AmdDetailedTimingInformation const*);
    isTimingInterlaced(AmdDetailedTimingInformation const*);
    isTimingRotated(AmdDetailedTimingInformation const*);
    isTimingScaled(AmdDetailedTimingInformation const*);
    isTimingTv(AmdDetailedTimingInformation const*);
    setOffline(AmdFbDisplayPath*);
    translateAmdDpTraining(IOFBDPLinkConfig*, AmdDpLinkConfig const*);
    translateDepthMode(unsigned int);
    translateDetailedTimingToDebugProperty(AmdDetailedTimingInformation const*);
    translateIopRect(AmdFbViewport&, _AMD_PIPE_RECTL const&);
    translateOsDpTraining(AmdDpLinkConfig*, IOFBDPLinkConfig const*);
    translateTimingListToDebugProperty(AMDRadeonX6000_AmdTimingList const*);
    translateTimingRangeToDebugProperty(IODisplayTimingRangeV2 const*);
    translateViewport(_AMD_PIPE_RECTL&, AmdFbViewport const&);
    updateStreamComposition(AmdFbDisplayPath const*, AmdFbViewport*, unsigned int, unsigned int);
    validateTimingRange(AmdDetailedTimingInformation const*, IODisplayTimingRangeV2 const*, unsigned int);
};
