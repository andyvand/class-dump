//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class hv_vmx_vma_t {
public: // (access info unavailable from symbols alone)
    auto TASK_TRAP_vm_intr_msi(hv_vmx_vm_t*, unsigned long long);
    auto TASK_TRAP_vm_request(hv_vmx_vm_t*, unsigned long);
    auto create(hv_vmx_vma_t*, hv_vmx_vm_create_args_t const*);
    auto deinit();
    auto disable_vcpu_lookup(unsigned int);
    auto disable_vcpu_lookup(unsigned int)::wmesg;
    auto dispatch_request(hv_vma_userarg_t const*);
    auto dump_ilog(bool);
    auto dump_ilog(bool) (.cold.1);
    auto enable_vcpu_lookup(unsigned int);
    auto free();
    auto getMetaClass() const;
    auto hold_vcpu(unsigned int);
    auto ilog(char const*, ...);
    auto init(hv_vmx_vm_create_args_t const*);
    auto inject_extint(unsigned int);
    auto inject_nmi(unsigned int);
    auto lapic_intr_msi(unsigned long long, unsigned long long);
    auto lapic_is_boot_state(unsigned int, hv_boot_state);
    auto lapic_set_boot_state(unsigned int, hv_boot_state);
    auto lapic_set_intr(unsigned int, int, hv_intr_trigger);
    auto lapic_set_local_intr(unsigned int, hv_apic_lvt_flavor);
    auto rele_vcpu(hv_vmx_vcpua_t*);
    auto rele_vcpu(unsigned int);
    auto set_apic_bus_frequency(unsigned long long);
    auto vcpua_interrupt(unsigned long long);
    auto vcpua_interrupt(unsigned long long) (.cold.1);
    auto vilog(hv_vmx_vcpua_t*, char const*, __va_list_tag*);
    auto vlapic_calcdest(unsigned long long*, unsigned int, bool, bool, bool);
    auto vlapic_deliver_intr(hv_intr_trigger, unsigned int, bool, int, int);
    auto vm_vcpu_hold(unsigned int);
    hv_vmx_vma_t();
    hv_vmx_vma_t(OSMetaClass const*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~hv_vmx_vma_t();
};
