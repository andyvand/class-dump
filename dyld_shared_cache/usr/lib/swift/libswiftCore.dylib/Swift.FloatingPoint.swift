//
//     Generated by class-dump 3.5 (64 bit) — Swift symbol-derived view.
//

extension Swift.FloatingPoint {
    addingProduct(A, A) -> A
    maximum(A, A) -> A
    maximumMagnitude(A, A) -> A
    minimum(A, A) -> A
    minimumMagnitude(A, A) -> A
    remainder(dividingBy: A) -> A
    rounded(Swift.FloatingPointRoundingRule) -> A
    squareRoot() -> A
    truncatingRemainder(dividingBy: A) -> A
}
