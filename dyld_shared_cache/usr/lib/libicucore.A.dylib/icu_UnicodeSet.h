//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class icu::UnicodeSet {
public: // (access info unavailable from symbols alone)
    UnicodeSet();
    UnicodeSet(icu::UnicodeSet const&);
    UnicodeSet(icu::UnicodeSet const&, signed char);
    UnicodeSet(icu::UnicodeString const&, UErrorCode&);
    UnicodeSet(icu::UnicodeString const&, icu::ParsePosition&, unsigned int, icu::SymbolTable const*, UErrorCode&);
    UnicodeSet(icu::UnicodeString const&, unsigned int, icu::SymbolTable const*, UErrorCode&);
    UnicodeSet(int, int);
    UnicodeSet(unsigned short const*, int, icu::UnicodeSet::ESerialization, UErrorCode&);
    auto _add(icu::UnicodeString const&);
    auto _appendToPat(icu::UnicodeString&, icu::UnicodeString const&, signed char);
    auto _appendToPat(icu::UnicodeString&, int, int, signed char);
    auto _appendToPat(icu::UnicodeString&, int, signed char);
    auto _generatePattern(icu::UnicodeString&, signed char) const;
    auto _toPattern(icu::UnicodeString&, signed char) const;
    auto add(icu::UnicodeString const&);
    auto add(int const*, int, signed char);
    auto add(int);
    auto add(int, int);
    auto addAll(icu::UnicodeSet const&);
    auto addAll(icu::UnicodeString const&);
    auto addMatchSetTo(icu::UnicodeSet&) const;
    auto allocateStrings(UErrorCode&);
    auto applyFilter(signed char (*)(int, void*), void*, icu::UnicodeSet const*, UErrorCode&);
    auto applyIntPropertyValue(UProperty, int, UErrorCode&);
    auto applyPattern(icu::RuleCharacterIterator&, icu::SymbolTable const*, icu::UnicodeString&, unsigned int, icu::UnicodeSet& (icu::UnicodeSet::*)(int), int, UErrorCode&);
    auto applyPattern(icu::UnicodeString const&, UErrorCode&);
    auto applyPattern(icu::UnicodeString const&, icu::ParsePosition&, unsigned int, icu::SymbolTable const*, UErrorCode&);
    auto applyPattern(icu::UnicodeString const&, unsigned int, icu::SymbolTable const*, UErrorCode&);
    auto applyPatternIgnoreSpace(icu::UnicodeString const&, icu::ParsePosition&, icu::SymbolTable const*, UErrorCode&);
    auto applyPropertyAlias(icu::UnicodeString const&, icu::UnicodeString const&, UErrorCode&);
    auto applyPropertyPattern(icu::RuleCharacterIterator&, icu::UnicodeString&, UErrorCode&);
    auto applyPropertyPattern(icu::UnicodeString const&, icu::ParsePosition&, UErrorCode&);
    auto charAt(int) const;
    auto clear();
    auto clone() const;
    auto cloneAsThawed() const;
    auto closeOver(int);
    auto closeOverAddCaseMappings();
    auto closeOverCaseInsensitive(bool);
    auto compact();
    auto complement();
    auto complement(icu::UnicodeString const&);
    auto complement(int);
    auto complement(int, int);
    auto complementAll(icu::UnicodeSet const&);
    auto complementAll(icu::UnicodeString const&);
    auto contains(icu::UnicodeString const&) const;
    auto contains(int) const;
    auto contains(int, int) const;
    auto containsAll(icu::UnicodeSet const&) const;
    auto containsAll(icu::UnicodeString const&) const;
    auto containsNone(icu::UnicodeSet const&) const;
    auto containsNone(icu::UnicodeString const&) const;
    auto containsNone(int, int) const;
    auto copyFrom(icu::UnicodeSet const&, signed char);
    auto createFrom(icu::UnicodeString const&);
    auto createFromAll(icu::UnicodeString const&);
    auto ensureBufferCapacity(int);
    auto ensureCapacity(int);
    auto exclusiveOr(int const*, int, signed char);
    auto findCodePoint(int) const;
    auto freeze();
    auto getDynamicClassID() const;
    auto getRangeCount() const;
    auto getRangeEnd(int) const;
    auto getRangeStart(int) const;
    auto getSingleCP(icu::UnicodeString const&);
    auto getStaticClassID();
    auto getString(int) const;
    auto hasStrings() const;
    auto hashCode() const;
    auto indexOf(int) const;
    auto isEmpty() const;
    auto matchRest(icu::Replaceable const&, int, int, icu::UnicodeString const&);
    auto matches(icu::Replaceable const&, int&, int, signed char);
    auto matchesIndexValue(unsigned char) const;
    auto nextCapacity(int);
    auto operator=(icu::UnicodeSet const&);
    auto operator==(icu::UnicodeSet const&) const;
    auto releasePattern();
    auto remove(icu::UnicodeString const&);
    auto remove(int);
    auto remove(int, int);
    auto removeAll(icu::UnicodeSet const&);
    auto removeAll(icu::UnicodeString const&);
    auto removeAllStrings();
    auto resemblesPattern(icu::UnicodeString const&, int);
    auto resemblesPropertyPattern(icu::RuleCharacterIterator&, int);
    auto resemblesPropertyPattern(icu::UnicodeString const&, int);
    auto retain(icu::UnicodeString const&);
    auto retain(int const*, int, signed char);
    auto retain(int);
    auto retain(int, int);
    auto retainAll(icu::UnicodeSet const&);
    auto retainAll(icu::UnicodeString const&);
    auto serialize(unsigned short*, int, UErrorCode&) const;
    auto set(int, int);
    auto setPattern(char16_t const*, int);
    auto setToBogus();
    auto size() const;
    auto span(char16_t const*, int, USetSpanCondition) const;
    auto spanBack(char16_t const*, int, USetSpanCondition) const;
    auto spanBackUTF8(char const*, int, USetSpanCondition) const;
    auto spanUTF8(char const*, int, USetSpanCondition) const;
    auto stringsContains(icu::UnicodeString const&) const;
    auto stringsSize() const;
    auto swapBuffers();
    auto toPattern(icu::UnicodeString&, signed char) const;
    non-virtual thunk to addMatchSetTo(icu::UnicodeSet&) const;
    non-virtual thunk to matches(icu::Replaceable const&, int&, int, signed char);
    non-virtual thunk to matchesIndexValue(unsigned char) const;
    non-virtual thunk to toPattern(icu::UnicodeString&, signed char) const;
    non-virtual thunk to ~UnicodeSet();
    ~UnicodeSet();
};
