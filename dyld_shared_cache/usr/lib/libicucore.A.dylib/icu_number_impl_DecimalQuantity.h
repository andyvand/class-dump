//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class icu::number::impl::DecimalQuantity {
public: // (access info unavailable from symbols alone)
    DecimalQuantity();
    DecimalQuantity(icu::number::impl::DecimalQuantity const&);
    DecimalQuantity(icu::number::impl::DecimalQuantity&&);
    auto _setToDecNum(icu::number::impl::DecNum const&, UErrorCode&);
    auto _setToDoubleFast(double);
    auto _setToInt(int);
    auto _setToLong(long long);
    auto adjustExponent(int);
    auto adjustMagnitude(int);
    auto adjustToZeroScale();
    auto appendDigit(signed char, int, bool);
    auto applyMaxInteger(int);
    auto checkHealth() const;
    auto clear();
    auto compact();
    auto convertToAccurateDouble();
    auto copyBcdFrom(icu::number::impl::DecimalQuantity const&);
    auto copyFieldsFrom(icu::number::impl::DecimalQuantity const&);
    auto decreaseMinIntegerTo(int);
    auto divideBy(icu::number::impl::DecNum const&, UErrorCode&);
    auto ensureCapacity();
    auto ensureCapacity(int);
    auto fitsInLong(bool) const;
    auto fractionCount() const;
    auto fractionCountWithoutTrailingZeros() const;
    auto fromExponentString(icu::UnicodeString, UErrorCode&);
    auto getDigit(int) const;
    auto getDigitPos(int) const;
    auto getExponent() const;
    auto getLowerDisplayMagnitude() const;
    auto getMagnitude() const;
    auto getPluralOperand(icu::PluralOperand) const;
    auto getPositionFingerprint() const;
    auto getUpperDisplayMagnitude() const;
    auto getVisibleFractionCount(icu::UnicodeString);
    auto hasIntegerValue() const;
    auto increaseMinIntegerTo(int);
    auto isInfinite() const;
    auto isNaN() const;
    auto isNegative() const;
    auto isZeroish() const;
    auto moveBcdFrom(icu::number::impl::DecimalQuantity&);
    auto multiplyBy(icu::number::impl::DecNum const&, UErrorCode&);
    auto negate();
    auto operator=(icu::number::impl::DecimalQuantity const&);
    auto operator=(icu::number::impl::DecimalQuantity&&);
    auto operator==(icu::number::impl::DecimalQuantity const&) const;
    auto popFromLeft(int);
    auto readDecNumberToBcd(icu::number::impl::DecNum const&);
    auto readDoubleConversionToBcd(char const*, int, int);
    auto readIntToBcd(int);
    auto readLongToBcd(long long);
    auto resetExponent();
    auto roundToIncrement(unsigned long long, short, UNumberFormatRoundingMode, UErrorCode&);
    auto roundToInfinity();
    auto roundToMagnitude(int, UNumberFormatRoundingMode, UErrorCode&);
    auto roundToMagnitude(int, UNumberFormatRoundingMode, bool, UErrorCode&);
    auto roundToNickel(int, UNumberFormatRoundingMode, UErrorCode&);
    auto setBcdToZero();
    auto setDigitPos(int, signed char);
    auto setMinFraction(int);
    auto setToDecNum(icu::number::impl::DecNum const&, UErrorCode&);
    auto setToDecNumber(icu::StringPiece, UErrorCode&);
    auto setToDouble(double);
    auto setToInt(int);
    auto setToLong(long long);
    auto shiftLeft(int);
    auto shiftRight(int);
    auto signum() const;
    auto switchStorage();
    auto toDecNum(icu::number::impl::DecNum&, UErrorCode&) const;
    auto toDouble() const;
    auto toExponentString() const;
    auto toFractionLong(bool) const;
    auto toLong(bool) const;
    auto toPlainString() const;
    auto toScientificString() const;
    auto toString() const;
    auto truncate();
    ~DecimalQuantity();
};
