//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class icu::number::NumberFormatterSettings<icu::number::LocalizedNumberFormatter> {
public: // (access info unavailable from symbols alone)
    adoptPerUnit(icu::MeasureUnit*) &&;
    adoptPerUnit(icu::MeasureUnit*) const &;
    adoptSymbols(icu::NumberingSystem*) &&;
    adoptSymbols(icu::NumberingSystem*) const &;
    adoptUnit(icu::MeasureUnit*) &&;
    adoptUnit(icu::MeasureUnit*) const &;
    clone() &&;
    clone() const &;
    copyErrorTo(UErrorCode&) const;
    decimal(UNumberDecimalSeparatorDisplay) &&;
    decimal(UNumberDecimalSeparatorDisplay) const &;
    displayOptions(icu::DisplayOptions const&) &&;
    displayOptions(icu::DisplayOptions const&) const &;
    forDateFormat(bool) &&;
    forDateFormat(bool) const &;
    grouping(UNumberGroupingStrategy) &&;
    grouping(UNumberGroupingStrategy) const &;
    integerWidth(icu::number::IntegerWidth const&) &&;
    integerWidth(icu::number::IntegerWidth const&) const &;
    macros(icu::number::impl::MacroProps const&) &&;
    macros(icu::number::impl::MacroProps const&) const &;
    macros(icu::number::impl::MacroProps&&) &&;
    macros(icu::number::impl::MacroProps&&) const &;
    notation(icu::number::Notation const&) &&;
    notation(icu::number::Notation const&) const &;
    padding(icu::number::impl::Padder const&) &&;
    padding(icu::number::impl::Padder const&) const &;
    perUnit(icu::MeasureUnit const&) &&;
    perUnit(icu::MeasureUnit const&) const &;
    precision(icu::number::Precision const&) &&;
    precision(icu::number::Precision const&) const &;
    roundingMode(UNumberFormatRoundingMode) &&;
    roundingMode(UNumberFormatRoundingMode) const &;
    scale(icu::number::Scale const&) &&;
    scale(icu::number::Scale const&) const &;
    sign(UNumberSignDisplay) &&;
    sign(UNumberSignDisplay) const &;
    symbols(icu::DecimalFormatSymbols const&) &&;
    symbols(icu::DecimalFormatSymbols const&) const &;
    threshold(int) &&;
    threshold(int) const &;
    toSkeleton(UErrorCode&) const;
    unit(icu::MeasureUnit const&) &&;
    unit(icu::MeasureUnit const&) const &;
    unitDisplayCase(icu::StringPiece) &&;
    unitDisplayCase(icu::StringPiece) const &;
    unitWidth(UNumberUnitWidth) &&;
    unitWidth(UNumberUnitWidth) const &;
    usage(icu::StringPiece) &&;
    usage(icu::StringPiece) const &;
};
