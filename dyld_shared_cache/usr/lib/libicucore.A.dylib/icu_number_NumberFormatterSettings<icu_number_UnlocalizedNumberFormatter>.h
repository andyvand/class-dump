//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class icu::number::NumberFormatterSettings<icu::number::UnlocalizedNumberFormatter> {
public: // (access info unavailable from symbols alone)
    auto adoptPerUnit(icu::MeasureUnit*) &&;
    auto adoptPerUnit(icu::MeasureUnit*) const &;
    auto adoptSymbols(icu::NumberingSystem*) &&;
    auto adoptSymbols(icu::NumberingSystem*) const &;
    auto adoptUnit(icu::MeasureUnit*) &&;
    auto adoptUnit(icu::MeasureUnit*) const &;
    auto clone() &&;
    auto clone() const &;
    auto copyErrorTo(UErrorCode&) const;
    auto decimal(UNumberDecimalSeparatorDisplay) &&;
    auto decimal(UNumberDecimalSeparatorDisplay) const &;
    auto displayOptions(icu::DisplayOptions const&) &&;
    auto displayOptions(icu::DisplayOptions const&) const &;
    auto forDateFormat(bool) &&;
    auto forDateFormat(bool) const &;
    auto grouping(UNumberGroupingStrategy) &&;
    auto grouping(UNumberGroupingStrategy) const &;
    auto integerWidth(icu::number::IntegerWidth const&) &&;
    auto integerWidth(icu::number::IntegerWidth const&) const &;
    auto macros(icu::number::impl::MacroProps const&) &&;
    auto macros(icu::number::impl::MacroProps const&) const &;
    auto macros(icu::number::impl::MacroProps&&) &&;
    auto macros(icu::number::impl::MacroProps&&) const &;
    auto notation(icu::number::Notation const&) &&;
    auto notation(icu::number::Notation const&) const &;
    auto padding(icu::number::impl::Padder const&) &&;
    auto padding(icu::number::impl::Padder const&) const &;
    auto perUnit(icu::MeasureUnit const&) &&;
    auto perUnit(icu::MeasureUnit const&) const &;
    auto precision(icu::number::Precision const&) &&;
    auto precision(icu::number::Precision const&) const &;
    auto roundingMode(UNumberFormatRoundingMode) &&;
    auto roundingMode(UNumberFormatRoundingMode) const &;
    auto scale(icu::number::Scale const&) &&;
    auto scale(icu::number::Scale const&) const &;
    auto sign(UNumberSignDisplay) &&;
    auto sign(UNumberSignDisplay) const &;
    auto symbols(icu::DecimalFormatSymbols const&) &&;
    auto symbols(icu::DecimalFormatSymbols const&) const &;
    auto threshold(int) &&;
    auto threshold(int) const &;
    auto toSkeleton(UErrorCode&) const;
    auto unit(icu::MeasureUnit const&) &&;
    auto unit(icu::MeasureUnit const&) const &;
    auto unitDisplayCase(icu::StringPiece) &&;
    auto unitDisplayCase(icu::StringPiece) const &;
    auto unitWidth(UNumberUnitWidth) &&;
    auto unitWidth(UNumberUnitWidth) const &;
    auto usage(icu::StringPiece) &&;
    auto usage(icu::StringPiece) const &;
};
