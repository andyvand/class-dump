//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class icu::RegexCompile {
public: // (access info unavailable from symbols alone)
    RegexCompile(icu::RegexPattern*, UErrorCode&);
    auto allocateData(int);
    auto allocateStackData(int);
    auto appendOp(int);
    auto appendOp(int, int);
    auto blockTopLoc(signed char);
    auto buildOp(int, int);
    auto compile(UText*, UParseError&, UErrorCode&);
    auto compile(icu::UnicodeString const&, UParseError&, UErrorCode&);
    auto compileInlineInterval();
    auto compileInterval(int, int);
    auto compileSet(icu::UnicodeSet*);
    auto createSetForProperty(icu::UnicodeString const&, signed char);
    auto doParseActions(int);
    auto error(UErrorCode);
    auto findCaseInsensitiveStarters(int, icu::UnicodeSet*);
    auto fixLiterals(signed char);
    auto handleCloseParen();
    auto insertOp(int);
    auto literalChar(int);
    auto matchStartType();
    auto maxMatchLength(int, int);
    auto minMatchLength(int, int);
    auto nextChar(icu::RegexCompile::RegexPatternChar&);
    auto nextCharLL();
    auto peekCharLL();
    auto scanNamedChar();
    auto scanPosixProp();
    auto scanProp();
    auto setEval(int);
    auto setPushOp(int);
    auto stripNOPs();
    ~RegexCompile();
};
