//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class zhtok::WordLatticeController {
public: // (access info unavailable from symbols alone)
    WordLatticeController(__CFLocale const*, ChineseTokenizerOptions);
    auto addComposedCharacteracterInRange(unsigned short const*, CFRange);
    auto addCustomWordIfFound(unsigned short const*, long);
    auto addDynamicWord(void*, unsigned short const*, CFRange const&);
    auto addEmojiWords(unsigned short const*);
    auto addLatinWords(unsigned short const*, CFRange, bool);
    auto addNewline(CFRange);
    auto addNonChineseWord(unsigned short const*, CFRange);
    auto addSpace(CFRange);
    auto addUNKWord(CFRange);
    auto addWord(void*, long);
    auto addWord(void*, unsigned short const*, long, unsigned long);
    auto breakEmojiWords(unsigned short const*);
    auto connectEOS();
    auto connectToPreviousWord(zhtok::LatticeWord const*, zhtok::LatticeWord*, zhtok::ConnectionContext const*);
    auto connectToWordsEndingAtIndex(zhtok::LatticeWord*, unsigned long);
    auto finalize();
    auto getBestSegmentation(std::__1::vector<CFRange, std::__1::allocator<CFRange>>&, __CFString const*, long) const;
    auto reset();
    auto rewriteLatticeWithDynamicWords(unsigned short const*);
    auto setCustomWordCheckBlock(long, bool (unsigned short const*, long) block_pointer);
    auto setDynamicLexicon(zhtok::TokenizerLexicon*);
    auto updateLattice(zhtok::LatticeWord*);
    auto updateNonChineseTokenRange(unsigned short const*, CFRange, bool, bool);
    ~WordLatticeController();
};
