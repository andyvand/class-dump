//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class marisa::grimoire::trie::LoudsTrie {
public: // (access info unavailable from symbols alone)
    LoudsTrie();
    auto build(marisa::Keyset&, int);
    auto build_(marisa::Keyset&, marisa::grimoire::trie::Config const&);
    auto clear();
    auto common_prefix_search(marisa::Agent&) const;
    auto fill_cache();
    auto find_child(marisa::Agent&) const;
    auto io_size() const;
    auto lookup(marisa::Agent&) const;
    auto map(marisa::grimoire::io::Mapper&);
    auto map_(marisa::grimoire::io::Mapper&);
    auto map_(marisa::grimoire::io::Mapper&) (.cold.1);
    auto match_(marisa::Agent&, unsigned long) const;
    auto predictive_find_child(marisa::Agent&) const;
    auto predictive_search(marisa::Agent&) const;
    auto prefix_match_(marisa::Agent&, unsigned long) const;
    auto read(marisa::grimoire::io::Reader&);
    auto read_(marisa::grimoire::io::Reader&);
    auto read_(marisa::grimoire::io::Reader&) (.cold.1);
    auto reserve_cache(marisa::grimoire::trie::Config const&, unsigned long, unsigned long);
    auto restore_(marisa::Agent&, unsigned long) const;
    auto reverse_lookup(marisa::Agent&) const;
    auto reverse_lookup(marisa::Agent&) const (.cold.1);
    auto swap(marisa::grimoire::trie::LoudsTrie&);
    auto total_size() const;
    auto write(marisa::grimoire::io::Writer&) const;
    auto write_(marisa::grimoire::io::Writer&) const;
    void build_current_trie<marisa::grimoire::trie::Key>(marisa::grimoire::vector::Vector<marisa::grimoire::trie::Key>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa::grimoire::trie::Config const&, unsigned long);
    void build_current_trie<marisa::grimoire::trie::ReverseKey>(marisa::grimoire::vector::Vector<marisa::grimoire::trie::ReverseKey>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa::grimoire::trie::Config const&, unsigned long);
    void build_next_trie<marisa::grimoire::trie::Key>(marisa::grimoire::vector::Vector<marisa::grimoire::trie::Key>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa::grimoire::trie::Config const&, unsigned long);
    void build_next_trie<marisa::grimoire::trie::ReverseKey>(marisa::grimoire::vector::Vector<marisa::grimoire::trie::ReverseKey>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa::grimoire::trie::Config const&, unsigned long);
    void build_next_trie<marisa::grimoire::trie::ReverseKey>(marisa::grimoire::vector::Vector<marisa::grimoire::trie::ReverseKey>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa::grimoire::trie::Config const&, unsigned long) (.cold.1);
    void build_terminals<marisa::grimoire::trie::Key>(marisa::grimoire::vector::Vector<marisa::grimoire::trie::Key> const&, marisa::grimoire::vector::Vector<unsigned int>*) const;
    void build_terminals<marisa::grimoire::trie::ReverseKey>(marisa::grimoire::vector::Vector<marisa::grimoire::trie::ReverseKey> const&, marisa::grimoire::vector::Vector<unsigned int>*) const;
    void build_trie<marisa::grimoire::trie::Key>(marisa::grimoire::vector::Vector<marisa::grimoire::trie::Key>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa::grimoire::trie::Config const&, unsigned long);
    void build_trie<marisa::grimoire::trie::ReverseKey>(marisa::grimoire::vector::Vector<marisa::grimoire::trie::ReverseKey>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa::grimoire::trie::Config const&, unsigned long);
    void cache<marisa::grimoire::trie::Key>(unsigned long, unsigned long, float, char);
    void cache<marisa::grimoire::trie::ReverseKey>(unsigned long, unsigned long, float, char);
    ~LoudsTrie();
};
