//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class marisa::grimoire::trie::LoudsTrie {
public: // (access info unavailable from symbols alone)
    LoudsTrie();
    build(marisa::Keyset&, int);
    build_(marisa::Keyset&, marisa::grimoire::trie::Config const&);
    clear();
    common_prefix_search(marisa::Agent&) const;
    fill_cache();
    find_child(marisa::Agent&) const;
    io_size() const;
    lookup(marisa::Agent&) const;
    map(marisa::grimoire::io::Mapper&);
    map_(marisa::grimoire::io::Mapper&);
    match_(marisa::Agent&, unsigned long) const;
    predictive_find_child(marisa::Agent&) const;
    predictive_search(marisa::Agent&) const;
    prefix_match_(marisa::Agent&, unsigned long) const;
    read(marisa::grimoire::io::Reader&);
    read_(marisa::grimoire::io::Reader&);
    reserve_cache(marisa::grimoire::trie::Config const&, unsigned long, unsigned long);
    restore_(marisa::Agent&, unsigned long) const;
    reverse_lookup(marisa::Agent&) const;
    swap(marisa::grimoire::trie::LoudsTrie&);
    total_size() const;
    write(marisa::grimoire::io::Writer&) const;
    write_(marisa::grimoire::io::Writer&) const;
    ~LoudsTrie();
};
