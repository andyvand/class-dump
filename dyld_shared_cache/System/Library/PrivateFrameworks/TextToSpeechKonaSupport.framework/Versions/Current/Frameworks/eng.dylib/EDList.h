//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class EDList {
public: // (access info unavailable from symbols alone)
    EDList(EDList const&);
    auto addAfter(EListIter&, IndexPair);
    auto addBefore(EListIter&, IndexPair);
    auto addToHead(IndexPair);
    auto addToTail(IndexPair);
    auto head() const;
    auto isEmpty() const;
    auto newIter() const;
    auto operator=(EDList const&);
    auto removeAfter(EListIter&);
    auto removeAt(EListIter&);
    auto removeHead();
    auto removeTail();
    auto tail() const;
    ~EDList();
};
