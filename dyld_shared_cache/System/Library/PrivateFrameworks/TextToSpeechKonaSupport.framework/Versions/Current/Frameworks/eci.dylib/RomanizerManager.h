//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class RomanizerManager {
public: // (access info unavailable from symbols alone)
    RomanizerManager(SynthThread*);
    auto MBCSToUnicode(unsigned int, char const*, unsigned short**);
    auto UnicodeToMBCS(unsigned int, unsigned short const*, char**, unsigned char);
    auto addParam(char const*, int);
    auto addText(char const*, int, unsigned char);
    auto convertText(unsigned char*);
    auto countAdditionSpace(unsigned char*, unsigned char);
    auto getLibraryName(char const*);
    auto getRom(unsigned int);
    auto getRomanizerInst(unsigned char, unsigned char);
    auto initialRomanizer();
    auto insertIndex();
    auto isRomExist(int, int);
    auto processInternalAnnotation(char**, char**, char*, unsigned char);
    auto processRemaining(char**);
    auto processSentence(char**, unsigned char);
    auto processSpecial(char**, char**, char*, unsigned char);
    auto processText(char**, unsigned int, unsigned char);
    auto resume();
    auto romClearErrors();
    auto romErrorMessage(char*);
    auto romProgStatus();
    auto setActiveLanguage(unsigned char, unsigned char, RomInstance**);
    auto setParam(long, int);
    auto stop();
    ~RomanizerManager();
};
