//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::ConstantRange {
public: // (access info unavailable from symbols alone)
    ConstantRange(llvm::APInt);
    ConstantRange(llvm::APInt, llvm::APInt);
    ConstantRange(unsigned int, bool);
    auto abs(bool) const;
    auto add(llvm::ConstantRange const&) const;
    auto addWithNoWrap(llvm::ConstantRange const&, unsigned int, llvm::ConstantRange::PreferredRangeType) const;
    auto areInsensitiveToSignednessOfICmpPredicate(llvm::ConstantRange const&, llvm::ConstantRange const&);
    auto areInsensitiveToSignednessOfInvertedICmpPredicate(llvm::ConstantRange const&, llvm::ConstantRange const&);
    auto ashr(llvm::ConstantRange const&) const;
    auto binaryAnd(llvm::ConstantRange const&) const;
    auto binaryNot() const;
    auto binaryOp(llvm::Instruction::BinaryOps, llvm::ConstantRange const&) const;
    auto binaryOr(llvm::ConstantRange const&) const;
    auto binaryXor(llvm::ConstantRange const&) const;
    auto castOp(llvm::Instruction::CastOps, unsigned int) const;
    auto contains(llvm::APInt const&) const;
    auto contains(llvm::ConstantRange const&) const;
    auto difference(llvm::ConstantRange const&) const;
    auto dump() const;
    auto exactIntersectWith(llvm::ConstantRange const&) const;
    auto exactUnionWith(llvm::ConstantRange const&) const;
    auto fromKnownBits(llvm::KnownBits const&, bool);
    auto getActiveBits() const;
    auto getEquivalentICmp(llvm::CmpInst::Predicate&, llvm::APInt&) const;
    auto getEquivalentICmp(llvm::CmpInst::Predicate&, llvm::APInt&, llvm::APInt&) const;
    auto getEquivalentPredWithFlippedSignedness(llvm::CmpInst::Predicate, llvm::ConstantRange const&, llvm::ConstantRange const&);
    auto getMinSignedBits() const;
    auto getSignedMax() const;
    auto getSignedMin() const;
    auto getUnsignedMax() const;
    auto getUnsignedMin() const;
    auto icmp(llvm::CmpInst::Predicate, llvm::ConstantRange const&) const;
    auto intersectWith(llvm::ConstantRange const&, llvm::ConstantRange::PreferredRangeType) const;
    auto intrinsic(unsigned int, llvm::ArrayRef<llvm::ConstantRange>);
    auto inverse() const;
    auto isAllNegative() const;
    auto isAllNonNegative() const;
    auto isEmptySet() const;
    auto isFullSet() const;
    auto isIntrinsicSupported(unsigned int);
    auto isSignWrappedSet() const;
    auto isSizeLargerThan(unsigned long long) const;
    auto isSizeStrictlySmallerThan(llvm::ConstantRange const&) const;
    auto isUpperSignWrapped() const;
    auto isUpperWrapped() const;
    auto isWrappedSet() const;
    auto lshr(llvm::ConstantRange const&) const;
    auto makeAllowedICmpRegion(llvm::CmpInst::Predicate, llvm::ConstantRange const&);
    auto makeExactICmpRegion(llvm::CmpInst::Predicate, llvm::APInt const&);
    auto makeExactNoWrapRegion(llvm::Instruction::BinaryOps, llvm::APInt const&, unsigned int);
    auto makeGuaranteedNoWrapRegion(llvm::Instruction::BinaryOps, llvm::ConstantRange const&, unsigned int);
    auto makeSatisfyingICmpRegion(llvm::CmpInst::Predicate, llvm::ConstantRange const&);
    auto multiply(llvm::ConstantRange const&) const;
    auto overflowingBinaryOp(llvm::Instruction::BinaryOps, llvm::ConstantRange const&, unsigned int) const;
    auto print(llvm::raw_ostream&) const;
    auto sadd_sat(llvm::ConstantRange const&) const;
    auto sdiv(llvm::ConstantRange const&) const;
    auto sextOrTrunc(unsigned int) const;
    auto shl(llvm::ConstantRange const&) const;
    auto signExtend(unsigned int) const;
    auto signedAddMayOverflow(llvm::ConstantRange const&) const;
    auto signedSubMayOverflow(llvm::ConstantRange const&) const;
    auto smax(llvm::ConstantRange const&) const;
    auto smin(llvm::ConstantRange const&) const;
    auto smul_fast(llvm::ConstantRange const&) const;
    auto smul_sat(llvm::ConstantRange const&) const;
    auto srem(llvm::ConstantRange const&) const;
    auto sshl_sat(llvm::ConstantRange const&) const;
    auto ssub_sat(llvm::ConstantRange const&) const;
    auto sub(llvm::ConstantRange const&) const;
    auto subWithNoWrap(llvm::ConstantRange const&, unsigned int, llvm::ConstantRange::PreferredRangeType) const;
    auto subtract(llvm::APInt const&) const;
    auto truncate(unsigned int) const;
    auto uadd_sat(llvm::ConstantRange const&) const;
    auto udiv(llvm::ConstantRange const&) const;
    auto umax(llvm::ConstantRange const&) const;
    auto umin(llvm::ConstantRange const&) const;
    auto umul_sat(llvm::ConstantRange const&) const;
    auto unionWith(llvm::ConstantRange const&, llvm::ConstantRange::PreferredRangeType) const;
    auto unsignedAddMayOverflow(llvm::ConstantRange const&) const;
    auto unsignedMulMayOverflow(llvm::ConstantRange const&) const;
    auto unsignedSubMayOverflow(llvm::ConstantRange const&) const;
    auto urem(llvm::ConstantRange const&) const;
    auto ushl_sat(llvm::ConstantRange const&) const;
    auto usub_sat(llvm::ConstantRange const&) const;
    auto zeroExtend(unsigned int) const;
    auto zextOrTrunc(unsigned int) const;
};
