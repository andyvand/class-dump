//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::APInt {
public: // (access info unavailable from symbols alone)
    APInt(unsigned int, llvm::ArrayRef<unsigned long long>);
    APInt(unsigned int, llvm::StringRef, unsigned char);
    APInt(unsigned int, unsigned int, unsigned long long const*);
    auto Profile(llvm::FoldingSetNodeID&) const;
    auto andAssignSlowCase(llvm::APInt const&);
    auto ashrInPlace(llvm::APInt const&);
    auto ashrSlowCase(unsigned int);
    auto assignSlowCase(llvm::APInt const&);
    auto byteSwap() const;
    auto compare(llvm::APInt const&) const;
    auto compareSigned(llvm::APInt const&) const;
    auto concatSlowCase(llvm::APInt const&) const;
    auto countLeadingOnesSlowCase() const;
    auto countLeadingZerosSlowCase() const;
    auto countPopulationSlowCase() const;
    auto countTrailingOnesSlowCase() const;
    auto countTrailingZerosSlowCase() const;
    auto divide(unsigned long long const*, unsigned int, unsigned long long const*, unsigned int, unsigned long long*, unsigned long long*);
    auto dump() const;
    auto equalSlowCase(llvm::APInt const&) const;
    auto extractBits(unsigned int, unsigned int) const;
    auto extractBitsAsZExtValue(unsigned int, unsigned int) const;
    auto flipAllBitsSlowCase();
    auto flipBit(unsigned int);
    auto fromString(unsigned int, llvm::StringRef, unsigned char);
    auto getBitsNeeded(llvm::StringRef, unsigned char);
    auto getHiBits(unsigned int) const;
    auto getLoBits(unsigned int) const;
    auto getSplat(unsigned int, llvm::APInt const&);
    auto getSufficientBitsNeeded(llvm::StringRef, unsigned char);
    auto initFromArray(llvm::ArrayRef<unsigned long long>);
    auto initSlowCase(llvm::APInt const&);
    auto initSlowCase(unsigned long long, bool);
    auto insertBits(llvm::APInt const&, unsigned int);
    auto insertBits(unsigned long long, unsigned int, unsigned int);
    auto intersectsSlowCase(llvm::APInt const&) const;
    auto isSplat(unsigned int) const;
    auto isSubsetOfSlowCase(llvm::APInt const&) const;
    auto lshrInPlace(llvm::APInt const&);
    auto lshrSlowCase(unsigned int);
    auto multiplicativeInverse(llvm::APInt const&) const;
    auto nearestLogBase2() const;
    auto operator*(llvm::APInt const&) const;
    auto operator*=(llvm::APInt const&);
    auto operator*=(unsigned long long);
    auto operator++();
    auto operator+=(llvm::APInt const&);
    auto operator+=(unsigned long long);
    auto operator--();
    auto operator-=(llvm::APInt const&);
    auto operator-=(unsigned long long);
    auto orAssignSlowCase(llvm::APInt const&);
    auto print(llvm::raw_ostream&, bool) const;
    auto reallocate(unsigned int);
    auto reverseBits() const;
    auto rotl(llvm::APInt const&) const;
    auto rotl(unsigned int) const;
    auto rotr(llvm::APInt const&) const;
    auto rotr(unsigned int) const;
    auto roundToDouble(bool) const;
    auto sadd_ov(llvm::APInt const&, bool&) const;
    auto sadd_sat(llvm::APInt const&) const;
    auto sdiv(llvm::APInt const&) const;
    auto sdiv(long long) const;
    auto sdiv_ov(llvm::APInt const&, bool&) const;
    auto sdivrem(llvm::APInt const&, llvm::APInt const&, llvm::APInt&, llvm::APInt&);
    auto sdivrem(llvm::APInt const&, long long, llvm::APInt&, long long&);
    auto setBitsSlowCase(unsigned int, unsigned int);
    auto sext(unsigned int) const;
    auto sextOrSelf(unsigned int) const;
    auto sextOrTrunc(unsigned int) const;
    auto shlSlowCase(unsigned int);
    auto smul_ov(llvm::APInt const&, bool&) const;
    auto smul_sat(llvm::APInt const&) const;
    auto sqrt() const;
    auto srem(llvm::APInt const&) const;
    auto srem(long long) const;
    auto sshl_ov(llvm::APInt const&, bool&) const;
    auto sshl_sat(llvm::APInt const&) const;
    auto ssub_ov(llvm::APInt const&, bool&) const;
    auto ssub_sat(llvm::APInt const&) const;
    auto tcAdd(unsigned long long*, unsigned long long const*, unsigned long long, unsigned int);
    auto tcAddPart(unsigned long long*, unsigned long long, unsigned int);
    auto tcAssign(unsigned long long*, unsigned long long const*, unsigned int);
    auto tcClearBit(unsigned long long*, unsigned int);
    auto tcCompare(unsigned long long const*, unsigned long long const*, unsigned int);
    auto tcDivide(unsigned long long*, unsigned long long const*, unsigned long long*, unsigned long long*, unsigned int);
    auto tcExtract(unsigned long long*, unsigned int, unsigned long long const*, unsigned int, unsigned int);
    auto tcExtractBit(unsigned long long const*, unsigned int);
    auto tcFullMultiply(unsigned long long*, unsigned long long const*, unsigned long long const*, unsigned int, unsigned int);
    auto tcIsZero(unsigned long long const*, unsigned int);
    auto tcLSB(unsigned long long const*, unsigned int);
    auto tcMSB(unsigned long long const*, unsigned int);
    auto tcMultiply(unsigned long long*, unsigned long long const*, unsigned long long const*, unsigned int);
    auto tcMultiplyPart(unsigned long long*, unsigned long long const*, unsigned long long, unsigned long long, unsigned int, unsigned int, bool);
    auto tcNegate(unsigned long long*, unsigned int);
    auto tcSet(unsigned long long*, unsigned long long, unsigned int);
    auto tcSetBit(unsigned long long*, unsigned int);
    auto tcShiftLeft(unsigned long long*, unsigned int, unsigned int);
    auto tcShiftRight(unsigned long long*, unsigned int, unsigned int);
    auto tcSubtract(unsigned long long*, unsigned long long const*, unsigned long long, unsigned int);
    auto tcSubtractPart(unsigned long long*, unsigned long long, unsigned int);
    auto toString(llvm::SmallVectorImpl<char>&, unsigned int, bool, bool) const;
    auto trunc(unsigned int) const;
    auto truncOrSelf(unsigned int) const;
    auto truncSSat(unsigned int) const;
    auto truncUSat(unsigned int) const;
    auto uadd_ov(llvm::APInt const&, bool&) const;
    auto uadd_sat(llvm::APInt const&) const;
    auto udiv(llvm::APInt const&) const;
    auto udiv(unsigned long long) const;
    auto udivrem(llvm::APInt const&, llvm::APInt const&, llvm::APInt&, llvm::APInt&);
    auto udivrem(llvm::APInt const&, unsigned long long, llvm::APInt&, unsigned long long&);
    auto umul_ov(llvm::APInt const&, bool&) const;
    auto umul_sat(llvm::APInt const&) const;
    auto urem(llvm::APInt const&) const;
    auto urem(unsigned long long) const;
    auto ushl_ov(llvm::APInt const&, bool&) const;
    auto ushl_sat(llvm::APInt const&) const;
    auto usub_ov(llvm::APInt const&, bool&) const;
    auto usub_sat(llvm::APInt const&) const;
    auto xorAssignSlowCase(llvm::APInt const&);
    auto zext(unsigned int) const;
    auto zextOrSelf(unsigned int) const;
    auto zextOrTrunc(unsigned int) const;
};
