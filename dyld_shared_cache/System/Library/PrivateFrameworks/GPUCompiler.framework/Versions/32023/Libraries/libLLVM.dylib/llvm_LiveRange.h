//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::LiveRange {
public: // (access info unavailable from symbols alone)
    auto MergeSegmentsInAsValue(llvm::LiveRange const&, llvm::VNInfo*);
    auto MergeValueInAsValue(llvm::LiveRange const&, llvm::VNInfo const*, llvm::VNInfo*);
    auto MergeValueNumberInto(llvm::VNInfo*, llvm::VNInfo*);
    auto RenumberValues();
    auto addSegment(llvm::LiveRange::Segment);
    auto addSegmentToSet(llvm::LiveRange::Segment);
    auto append(llvm::LiveRange::Segment);
    auto covers(llvm::LiveRange const&) const;
    auto createDeadDef(llvm::SlotIndex, llvm::BumpPtrAllocatorImpl<llvm::MallocAllocator, 4096ul, 4096ul, 128ul>&);
    auto createDeadDef(llvm::VNInfo*);
    auto dump() const;
    auto extendInBlock(llvm::ArrayRef<llvm::SlotIndex>, llvm::SlotIndex, llvm::SlotIndex);
    auto extendInBlock(llvm::SlotIndex, llvm::SlotIndex);
    auto find(llvm::SlotIndex);
    auto flushSegmentSet();
    auto isLiveAtIndexes(llvm::ArrayRef<llvm::SlotIndex>) const;
    auto join(llvm::LiveRange&, int const*, int const*, llvm::SmallVectorImpl<llvm::VNInfo*>&);
    auto markValNoForDeletion(llvm::VNInfo*);
    auto overlaps(llvm::LiveRange const&, llvm::CoalescerPair const&, llvm::SlotIndexes const&) const;
    auto overlaps(llvm::SlotIndex, llvm::SlotIndex) const;
    auto overlapsFrom(llvm::LiveRange const&, llvm::LiveRange::Segment const*) const;
    auto print(llvm::raw_ostream&) const;
    auto removeSegment(llvm::LiveRange::Segment*, bool);
    auto removeSegment(llvm::SlotIndex, llvm::SlotIndex, bool);
    auto removeValNo(llvm::VNInfo*);
    auto removeValNoIfDead(llvm::VNInfo*);
};
