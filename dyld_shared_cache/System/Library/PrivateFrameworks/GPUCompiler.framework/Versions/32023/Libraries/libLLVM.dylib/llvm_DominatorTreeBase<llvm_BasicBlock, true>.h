//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::DominatorTreeBase<llvm::BasicBlock, true> {
public: // (access info unavailable from symbols alone)
    DominatorTreeBase(llvm::DominatorTreeBase<llvm::BasicBlock, true>&&);
    auto addNewBlock(llvm::BasicBlock*, llvm::BasicBlock*);
    auto addRoot(llvm::BasicBlock*);
    auto applyUpdates(llvm::ArrayRef<llvm::cfg::Update<llvm::BasicBlock*>>);
    auto applyUpdates(llvm::ArrayRef<llvm::cfg::Update<llvm::BasicBlock*>>, llvm::ArrayRef<llvm::cfg::Update<llvm::BasicBlock*>>);
    auto changeImmediateDominator(llvm::BasicBlock*, llvm::BasicBlock*);
    auto changeImmediateDominator(llvm::DomTreeNodeBase<llvm::BasicBlock>*, llvm::DomTreeNodeBase<llvm::BasicBlock>*);
    auto compare(llvm::DominatorTreeBase<llvm::BasicBlock, true> const&) const;
    auto createChild(llvm::BasicBlock*, llvm::DomTreeNodeBase<llvm::BasicBlock>*);
    auto createNode(llvm::BasicBlock*);
    auto deleteEdge(llvm::BasicBlock*, llvm::BasicBlock*);
    auto dominatedBySlowTreeWalk(llvm::DomTreeNodeBase<llvm::BasicBlock> const*, llvm::DomTreeNodeBase<llvm::BasicBlock> const*) const;
    auto dominates(llvm::BasicBlock const*, llvm::BasicBlock const*) const;
    auto dominates(llvm::DomTreeNodeBase<llvm::BasicBlock> const*, llvm::DomTreeNodeBase<llvm::BasicBlock> const*) const;
    auto eraseNode(llvm::BasicBlock*);
    auto findNearestCommonDominator(llvm::BasicBlock const*, llvm::BasicBlock const*) const;
    auto findNearestCommonDominator(llvm::BasicBlock*, llvm::BasicBlock*) const;
    auto getDescendants(llvm::BasicBlock*, llvm::SmallVectorImpl<llvm::BasicBlock*>&) const;
    auto getNode(llvm::BasicBlock const*) const;
    auto getRoot() const;
    auto getRootNode();
    auto getRootNode() const;
    auto insertEdge(llvm::BasicBlock*, llvm::BasicBlock*);
    auto isPostDominator() const;
    auto isReachableFromEntry(llvm::BasicBlock const*) const;
    auto isReachableFromEntry(llvm::DomTreeNodeBase<llvm::BasicBlock> const*) const;
    auto isVirtualRoot(llvm::DomTreeNodeBase<llvm::BasicBlock> const*) const;
    auto operator=(llvm::DominatorTreeBase<llvm::BasicBlock, true>&&);
    auto operator[](llvm::BasicBlock const*) const;
    auto print(llvm::raw_ostream&) const;
    auto properlyDominates(llvm::BasicBlock const*, llvm::BasicBlock const*) const;
    auto properlyDominates(llvm::DomTreeNodeBase<llvm::BasicBlock> const*, llvm::DomTreeNodeBase<llvm::BasicBlock> const*) const;
    auto recalculate(llvm::Function&);
    auto recalculate(llvm::Function&, llvm::ArrayRef<llvm::cfg::Update<llvm::BasicBlock*>>);
    auto reset();
    auto root_begin();
    auto root_begin() const;
    auto root_end();
    auto root_end() const;
    auto root_size() const;
    auto roots();
    auto roots() const;
    auto setNewRoot(llvm::BasicBlock*);
    auto splitBlock(llvm::BasicBlock*);
    auto updateDFSNumbers() const;
    auto verify(llvm::DominatorTreeBase<llvm::BasicBlock, true>::VerificationLevel) const;
    auto wipe();
};
