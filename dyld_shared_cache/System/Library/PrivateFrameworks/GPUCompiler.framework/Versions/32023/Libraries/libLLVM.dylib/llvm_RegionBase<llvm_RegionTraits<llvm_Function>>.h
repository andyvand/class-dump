//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::RegionBase<llvm::RegionTraits<llvm::Function>> {
public: // (access info unavailable from symbols alone)
    RegionBase(llvm::BasicBlock*, llvm::BasicBlock*, llvm::RegionInfo*, llvm::DominatorTree*, llvm::Region*);
    auto addSubRegion(llvm::Region*, bool);
    auto begin();
    auto begin() const;
    auto block_begin();
    auto block_begin() const;
    auto block_end();
    auto block_end() const;
    auto blocks();
    auto blocks() const;
    auto clearNodeCache();
    auto contains(llvm::BasicBlock const*) const;
    auto contains(llvm::Instruction const*) const;
    auto contains(llvm::Loop const*) const;
    auto contains(llvm::Region const*) const;
    auto dump() const;
    auto element_begin();
    auto element_begin() const;
    auto element_end();
    auto element_end() const;
    auto elements();
    auto elements() const;
    auto end();
    auto end() const;
    auto getBBNode(llvm::BasicBlock*) const;
    auto getDepth() const;
    auto getEnteringBlock() const;
    auto getEntry() const;
    auto getExit() const;
    auto getExitingBlock() const;
    auto getExitingBlocks(llvm::SmallVectorImpl<llvm::BasicBlock*>&) const;
    auto getExpandedRegion() const;
    auto getNameStr() const;
    auto getNode() const;
    auto getNode(llvm::BasicBlock*) const;
    auto getParent() const;
    auto getRegionInfo() const;
    auto getSubRegionNode(llvm::BasicBlock*) const;
    auto isSimple() const;
    auto isTopLevelRegion() const;
    auto outermostLoopInRegion(llvm::Loop*) const;
    auto outermostLoopInRegion(llvm::LoopInfo*, llvm::BasicBlock*) const;
    auto print(llvm::raw_ostream&, bool, unsigned int, llvm::RegionBase<llvm::RegionTraits<llvm::Function>>::PrintStyle) const;
    auto removeSubRegion(llvm::Region*);
    auto replaceEntry(llvm::BasicBlock*);
    auto replaceEntryRecursive(llvm::BasicBlock*);
    auto replaceExit(llvm::BasicBlock*);
    auto replaceExitRecursive(llvm::BasicBlock*);
    auto transferChildrenTo(llvm::Region*);
    auto verifyBBInRegion(llvm::BasicBlock*) const;
    auto verifyRegion() const;
    auto verifyRegionNest() const;
    auto verifyWalk(llvm::BasicBlock*, std::__1::set<llvm::BasicBlock*, std::__1::less<llvm::BasicBlock*>, std::__1::allocator<llvm::BasicBlock*>>*) const;
    ~RegionBase();
};
