//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::Constant {
public: // (access info unavailable from symbols alone)
    auto canTrap() const;
    auto containsConstantExpression() const;
    auto containsPoisonElement() const;
    auto containsUndefOrPoisonElement() const;
    auto destroyConstant();
    auto getAggregateElement(llvm::Constant*) const;
    auto getAggregateElement(unsigned int) const;
    auto getAllOnesValue(llvm::Type*);
    auto getIntegerValue(llvm::Type*, llvm::APInt const&);
    auto getNullValue(llvm::Type*);
    auto getRelocationInfo() const;
    auto getSplatValue(bool) const;
    auto getUniqueInteger() const;
    auto handleOperandChange(llvm::Value*, llvm::Value*);
    auto hasExactInverseFP() const;
    auto hasNLiveUses(unsigned int) const;
    auto hasOneLiveUse() const;
    auto hasZeroLiveUses() const;
    auto isAllOnesValue() const;
    auto isConstantUsed() const;
    auto isDLLImportDependent() const;
    auto isElementWiseEqual(llvm::Value*) const;
    auto isFiniteNonZeroFP() const;
    auto isManifestConstant() const;
    auto isMinSignedValue() const;
    auto isNaN() const;
    auto isNegativeZeroValue() const;
    auto isNormalFP() const;
    auto isNotMinSignedValue() const;
    auto isNotOneValue() const;
    auto isNullValue() const;
    auto isOneValue() const;
    auto isThreadDependent() const;
    auto isZeroValue() const;
    auto mergeUndefsWith(llvm::Constant*, llvm::Constant*);
    auto needsDynamicRelocation() const;
    auto needsRelocation() const;
    auto removeDeadConstantUsers() const;
    auto replaceUndefsWith(llvm::Constant*, llvm::Constant*);
};
