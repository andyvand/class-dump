//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::DGNode<llvm::DDGNode, llvm::DDGEdge> {
public: // (access info unavailable from symbols alone)
    DGNode(llvm::DDGEdge&);
    DGNode(llvm::DGNode<llvm::DDGNode, llvm::DDGEdge> const&);
    DGNode(llvm::DGNode<llvm::DDGNode, llvm::DDGEdge>&&);
    auto addEdge(llvm::DDGEdge&);
    auto back();
    auto back() const;
    auto begin();
    auto begin() const;
    auto clear();
    auto end();
    auto end() const;
    auto findEdgeTo(llvm::DDGNode const&) const;
    auto findEdgesTo(llvm::DDGNode const&, llvm::SmallVectorImpl<llvm::DDGEdge*>&) const;
    auto front();
    auto front() const;
    auto getDerived();
    auto getDerived() const;
    auto getEdges();
    auto getEdges() const;
    auto hasEdgeTo(llvm::DDGNode const&) const;
    auto isEqualTo(llvm::DDGNode const&) const;
    auto operator=(llvm::DGNode<llvm::DDGNode, llvm::DDGEdge> const&&);
    auto operator=(llvm::DGNode<llvm::DDGNode, llvm::DDGEdge> const&);
    auto removeEdge(llvm::DDGEdge&);
};
