//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::Type {
public: // (access info unavailable from symbols alone)
    auto canLosslesslyBitCastTo(llvm::Type*) const;
    auto dump() const;
    auto getBFloatPtrTy(llvm::LLVMContext&, unsigned int);
    auto getBFloatTy(llvm::LLVMContext&);
    auto getDoublePtrTy(llvm::LLVMContext&, unsigned int);
    auto getDoubleTy(llvm::LLVMContext&);
    auto getFP128PtrTy(llvm::LLVMContext&, unsigned int);
    auto getFP128Ty(llvm::LLVMContext&);
    auto getFPMantissaWidth() const;
    auto getFloatPtrTy(llvm::LLVMContext&, unsigned int);
    auto getFloatTy(llvm::LLVMContext&);
    auto getFloatingPointTy(llvm::LLVMContext&, llvm::fltSemantics const&);
    auto getFltSemantics() const;
    auto getHalfPtrTy(llvm::LLVMContext&, unsigned int);
    auto getHalfTy(llvm::LLVMContext&);
    auto getInt128Ty(llvm::LLVMContext&);
    auto getInt16PtrTy(llvm::LLVMContext&, unsigned int);
    auto getInt16Ty(llvm::LLVMContext&);
    auto getInt1PtrTy(llvm::LLVMContext&, unsigned int);
    auto getInt1Ty(llvm::LLVMContext&);
    auto getInt32PtrTy(llvm::LLVMContext&, unsigned int);
    auto getInt32Ty(llvm::LLVMContext&);
    auto getInt64PtrTy(llvm::LLVMContext&, unsigned int);
    auto getInt64Ty(llvm::LLVMContext&);
    auto getInt8PtrTy(llvm::LLVMContext&, unsigned int);
    auto getInt8Ty(llvm::LLVMContext&);
    auto getIntNPtrTy(llvm::LLVMContext&, unsigned int, unsigned int);
    auto getIntNTy(llvm::LLVMContext&, unsigned int);
    auto getLabelTy(llvm::LLVMContext&);
    auto getMetadataTy(llvm::LLVMContext&);
    auto getPPC_FP128PtrTy(llvm::LLVMContext&, unsigned int);
    auto getPPC_FP128Ty(llvm::LLVMContext&);
    auto getPointerTo(unsigned int) const;
    auto getPrimitiveSizeInBits() const;
    auto getPrimitiveType(llvm::LLVMContext&, llvm::Type::TypeID);
    auto getScalarSizeInBits() const;
    auto getTokenTy(llvm::LLVMContext&);
    auto getVoidTy(llvm::LLVMContext&);
    auto getX86_AMXPtrTy(llvm::LLVMContext&, unsigned int);
    auto getX86_AMXTy(llvm::LLVMContext&);
    auto getX86_FP80PtrTy(llvm::LLVMContext&, unsigned int);
    auto getX86_FP80Ty(llvm::LLVMContext&);
    auto getX86_MMXPtrTy(llvm::LLVMContext&, unsigned int);
    auto getX86_MMXTy(llvm::LLVMContext&);
    auto isEmptyTy() const;
    auto isIEEE() const;
    auto isIntegerTy(unsigned int) const;
    auto isOpaquePointerTy() const;
    auto isSizedDerivedType(llvm::SmallPtrSetImpl<llvm::Type*>*) const;
    auto print(llvm::raw_ostream&, bool, bool) const;
};
