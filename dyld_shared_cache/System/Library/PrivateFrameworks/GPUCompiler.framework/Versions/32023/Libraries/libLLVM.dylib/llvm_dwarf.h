//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::dwarf {
public: // (access info unavailable from symbols alone)
    auto AccessibilityString(unsigned int);
    auto ApplePropertyString(unsigned int);
    auto ArrayOrderString(unsigned int);
    auto AtomTypeString(unsigned int);
    auto AtomValueString(unsigned short, unsigned int);
    auto AttributeEncodingString(unsigned int);
    auto AttributeEncodingVendor(llvm::dwarf::TypeKind);
    auto AttributeEncodingVersion(llvm::dwarf::TypeKind);
    auto AttributeString(unsigned int);
    auto AttributeValueString(unsigned short, unsigned int);
    auto AttributeVendor(llvm::dwarf::Attribute);
    auto AttributeVersion(llvm::dwarf::Attribute);
    auto CallFrameString(unsigned int, llvm::Triple::ArchType);
    auto CaseString(unsigned int);
    auto ChildrenString(unsigned int);
    auto ConventionString(unsigned int);
    auto DecimalSignString(unsigned int);
    auto DefaultedMemberString(unsigned int);
    auto EndianityString(unsigned int);
    auto FormEncodingString(unsigned int);
    auto FormVendor(llvm::dwarf::Form);
    auto FormVersion(llvm::dwarf::Form);
    auto FormatString(bool);
    auto FormatString(llvm::dwarf::DwarfFormat);
    auto GDBIndexEntryKindString(llvm::dwarf::GDBIndexEntryKind);
    auto GDBIndexEntryLinkageString(llvm::dwarf::GDBIndexEntryLinkage);
    auto GnuMacroString(unsigned int);
    auto IndexString(unsigned int);
    auto InlineCodeString(unsigned int);
    auto LNExtendedString(unsigned int);
    auto LNStandardString(unsigned int);
    auto LanguageLowerBound(llvm::dwarf::SourceLanguage);
    auto LanguageString(unsigned int);
    auto LanguageVendor(llvm::dwarf::SourceLanguage);
    auto LanguageVersion(llvm::dwarf::SourceLanguage);
    auto LocListEncodingString(unsigned int);
    auto MacinfoString(unsigned int);
    auto MacroString(unsigned int);
    auto OperationEncodingString(unsigned int);
    auto OperationVendor(llvm::dwarf::LocationAtom);
    auto OperationVersion(llvm::dwarf::LocationAtom);
    auto RLEString(unsigned int);
    auto RangeListEncodingString(unsigned int);
    auto TagString(unsigned int);
    auto TagVendor(llvm::dwarf::Tag);
    auto TagVersion(llvm::dwarf::Tag);
    auto UnitTypeString(unsigned int);
    auto VirtualityString(unsigned int);
    auto VisibilityString(unsigned int);
    auto getAttributeEncoding(llvm::StringRef);
    auto getCallingConvention(llvm::StringRef);
    auto getFixedFormByteSize(llvm::dwarf::Form, llvm::dwarf::FormParams);
    auto getLanguage(llvm::StringRef);
    auto getMacinfo(llvm::StringRef);
    auto getMacro(llvm::StringRef);
    auto getOperationEncoding(llvm::StringRef);
    auto getTag(llvm::StringRef);
    auto getVirtuality(llvm::StringRef);
    auto isValidFormForVersion(llvm::dwarf::Form, unsigned int, bool);
};
