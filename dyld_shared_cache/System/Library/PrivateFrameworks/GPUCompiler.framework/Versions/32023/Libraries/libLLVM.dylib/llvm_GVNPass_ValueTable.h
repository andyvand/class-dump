//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::GVNPass::ValueTable {
public: // (access info unavailable from symbols alone)
    ValueTable();
    ValueTable(llvm::GVNPass::ValueTable const&);
    ValueTable(llvm::GVNPass::ValueTable&&);
    auto add(llvm::Value*, unsigned int);
    auto areAllValsInBB(unsigned int, llvm::BasicBlock const*, llvm::GVNPass&);
    auto areCallValsEqual(unsigned int, unsigned int, llvm::BasicBlock const*, llvm::BasicBlock const*, llvm::GVNPass&);
    auto assignExpNewValueNum(llvm::GVNPass::Expression&);
    auto clear();
    auto createCmpExpr(unsigned int, llvm::CmpInst::Predicate, llvm::Value*, llvm::Value*);
    auto createExpr(llvm::Instruction*);
    auto createExtractvalueExpr(llvm::ExtractValueInst*);
    auto erase(llvm::Value*);
    auto eraseTranslateCacheEntry(unsigned int, llvm::BasicBlock const&);
    auto exists(llvm::Value*) const;
    auto lookup(llvm::Value*, bool) const;
    auto lookupOrAdd(llvm::Value*);
    auto lookupOrAddCall(llvm::CallInst*);
    auto lookupOrAddCmp(unsigned int, llvm::CmpInst::Predicate, llvm::Value*, llvm::Value*);
    auto operator=(llvm::GVNPass::ValueTable const&);
    auto phiTranslate(llvm::BasicBlock const*, llvm::BasicBlock const*, unsigned int, llvm::GVNPass&);
    auto phiTranslateImpl(llvm::BasicBlock const*, llvm::BasicBlock const*, unsigned int, llvm::GVNPass&);
    auto verifyRemoved(llvm::Value const*) const;
    ~ValueTable();
};
