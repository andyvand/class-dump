//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::SCCPInstVisitor {
public: // (access info unavailable from symbols alone)
    auto getConstant(llvm::ValueLatticeElement const&) const;
    auto getFeasibleSuccessors(llvm::Instruction&, llvm::SmallVectorImpl<bool>&);
    auto handleCallArguments(llvm::CallBase&);
    auto handleCallOverdefined(llvm::CallBase&);
    auto handleCallResult(llvm::CallBase&);
    auto isEdgeFeasible(llvm::BasicBlock*, llvm::BasicBlock*) const;
    auto isStructLatticeConstant(llvm::Function*, llvm::StructType*);
    auto markArgInFuncSpecialization(llvm::Function*, llvm::SmallVectorImpl<llvm::ArgInfo> const&);
    auto markBlockExecutable(llvm::BasicBlock*);
    auto markConstant(llvm::ValueLatticeElement&, llvm::Value*, llvm::Constant*, bool);
    auto markEdgeExecutable(llvm::BasicBlock*, llvm::BasicBlock*);
    auto markOverdefined(llvm::ValueLatticeElement&, llvm::Value*);
    auto mergeInValue(llvm::ValueLatticeElement&, llvm::Value*, llvm::ValueLatticeElement, llvm::ValueLatticeElement::MergeOptions);
    auto pushToWorkList(llvm::ValueLatticeElement&, llvm::Value*);
    auto pushToWorkListMsg(llvm::ValueLatticeElement&, llvm::Value*);
    auto resolvedUndefsIn(llvm::Function&);
    auto solve();
    auto visitBinaryOperator(llvm::Instruction&);
    auto visitCallBase(llvm::CallBase&);
    auto visitCastInst(llvm::CastInst&);
    auto visitCmpInst(llvm::CmpInst&);
    auto visitExtractValueInst(llvm::ExtractValueInst&);
    auto visitGetElementPtrInst(llvm::GetElementPtrInst&);
    auto visitInsertValueInst(llvm::InsertValueInst&);
    auto visitInstruction(llvm::Instruction&);
    auto visitLoadInst(llvm::LoadInst&);
    auto visitPHINode(llvm::PHINode&);
    auto visitReturnInst(llvm::ReturnInst&);
    auto visitSelectInst(llvm::SelectInst&);
    auto visitStoreInst(llvm::StoreInst&);
    auto visitTerminator(llvm::Instruction&);
    auto visitUnaryOperator(llvm::Instruction&);
};
