//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::ConstantDataVector {
public: // (access info unavailable from symbols alone)
    auto get(llvm::LLVMContext&, llvm::ArrayRef<double>);
    auto get(llvm::LLVMContext&, llvm::ArrayRef<float>);
    auto get(llvm::LLVMContext&, llvm::ArrayRef<unsigned char>);
    auto get(llvm::LLVMContext&, llvm::ArrayRef<unsigned int>);
    auto get(llvm::LLVMContext&, llvm::ArrayRef<unsigned long long>);
    auto get(llvm::LLVMContext&, llvm::ArrayRef<unsigned short>);
    auto getFP(llvm::Type*, llvm::ArrayRef<unsigned int>);
    auto getFP(llvm::Type*, llvm::ArrayRef<unsigned long long>);
    auto getFP(llvm::Type*, llvm::ArrayRef<unsigned short>);
    auto getSplat(unsigned int, llvm::Constant*);
    auto getSplatValue() const;
    auto isSplat() const;
    auto isSplatData() const;
};
