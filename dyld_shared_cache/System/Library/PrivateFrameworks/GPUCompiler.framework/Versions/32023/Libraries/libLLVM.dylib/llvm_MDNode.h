//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::MDNode {
public: // (access info unavailable from symbols alone)
    MDNode(llvm::LLVMContext&, unsigned int, llvm::Metadata::StorageType, llvm::ArrayRef<llvm::Metadata*>, llvm::ArrayRef<llvm::Metadata*>);
    auto clone() const;
    auto concatenate(llvm::MDNode*, llvm::MDNode*);
    auto countUnresolvedOperands();
    auto decrementUnresolvedOperandCount();
    auto deleteAsSubclass();
    auto deleteTemporary(llvm::MDNode*);
    auto dropAllReferences();
    auto dropReplaceableUses();
    auto dumpTree() const;
    auto dumpTree(llvm::Module const*) const;
    auto eraseFromStore();
    auto getMostGenericAliasScope(llvm::MDNode*, llvm::MDNode*);
    auto getMostGenericAlignmentOrDereferenceable(llvm::MDNode*, llvm::MDNode*);
    auto getMostGenericFPMath(llvm::MDNode*, llvm::MDNode*);
    auto getMostGenericRange(llvm::MDNode*, llvm::MDNode*);
    auto getMostGenericTBAA(llvm::MDNode*, llvm::MDNode*);
    auto handleChangedOperand(void*, llvm::Metadata*);
    auto intersect(llvm::MDNode*, llvm::MDNode*);
    auto isTBAAVtableAccess() const;
    auto makeDistinct();
    auto makeUniqued();
    auto printTree(llvm::raw_ostream&, llvm::Module const*) const;
    auto printTree(llvm::raw_ostream&, llvm::ModuleSlotTracker&, llvm::Module const*) const;
    auto replaceOperandWith(unsigned int, llvm::Metadata*);
    auto replaceWithDistinctImpl();
    auto replaceWithPermanentImpl();
    auto replaceWithUniquedImpl();
    auto resolve();
    auto resolveAfterOperandChange(llvm::Metadata*, llvm::Metadata*);
    auto resolveCycles();
    auto setOperand(unsigned int, llvm::Metadata*);
    auto storeDistinctInContext();
    auto uniquify();
    void * operator new(unsigned long, unsigned int);
    void operator delete(void*);
};
