//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::LiveIntervals {
public: // (access info unavailable from symbols alone)
    LiveIntervals();
    auto addKillFlags(llvm::VirtRegMap const*);
    auto addSegmentToEndOfBlock(llvm::Register, llvm::MachineInstr&);
    auto checkRegMaskInterference(llvm::LiveInterval const&, llvm::BitVector&);
    auto computeDeadValues(llvm::LiveInterval&, llvm::SmallVectorImpl<llvm::MachineInstr*>*);
    auto computeLiveInRegUnits();
    auto computeRegMasks();
    auto computeRegUnitRange(llvm::LiveRange&, unsigned int);
    auto computeVirtRegInterval(llvm::LiveInterval&);
    auto computeVirtRegs();
    auto constructMainRangeFromSubranges(llvm::LiveInterval&);
    auto createInterval(llvm::Register);
    auto dumpInstrs() const;
    auto extendSegmentsToUses(llvm::LiveRange&, llvm::SmallVector<std::__1::pair<llvm::SlotIndex, llvm::VNInfo*>, 16u>&, llvm::Register, llvm::LaneBitmask);
    auto extendToIndices(llvm::LiveRange&, llvm::ArrayRef<llvm::SlotIndex>, llvm::ArrayRef<llvm::SlotIndex>);
    auto extendToUses(llvm::LiveInterval*);
    auto getAnalysisUsage(llvm::AnalysisUsage&) const;
    auto getLiveSubranges(llvm::LiveInterval const&, llvm::MachineRegisterInfo const&, llvm::SlotIndex const&) const;
    auto getSpillWeight(bool, bool, llvm::MachineBlockFrequencyInfo const*, llvm::MachineBasicBlock const*, float);
    auto getSpillWeight(bool, bool, llvm::MachineBlockFrequencyInfo const*, llvm::MachineInstr const&);
    auto handleMove(llvm::MachineInstr&, bool);
    auto handleMoveIntoNewBundle(llvm::MachineInstr&, bool);
    auto hasPHIKill(llvm::LiveInterval const&, llvm::VNInfo const*) const;
    auto intervalIsInOneMBB(llvm::LiveInterval const&) const;
    auto print(llvm::raw_ostream&, llvm::Module const*) const;
    auto printInstrs(llvm::raw_ostream&) const;
    auto pruneValue(llvm::LiveRange&, llvm::SlotIndex, llvm::SmallVectorImpl<llvm::SlotIndex>*);
    auto releaseMemory();
    auto removePhysRegDefAt(llvm::MCRegister, llvm::SlotIndex);
    auto removeVRegDefAt(llvm::LiveInterval&, llvm::SlotIndex);
    auto repairIntervalsInRange(llvm::MachineBasicBlock*, llvm::MachineInstrBundleIterator<llvm::MachineInstr, false>, llvm::MachineInstrBundleIterator<llvm::MachineInstr, false>, llvm::ArrayRef<llvm::Register>);
    auto repairOldRegInRange(llvm::MachineInstrBundleIterator<llvm::MachineInstr, false>, llvm::MachineInstrBundleIterator<llvm::MachineInstr, false>, llvm::SlotIndex, llvm::LiveRange&, llvm::Register, llvm::LaneBitmask);
    auto runOnMachineFunction(llvm::MachineFunction&);
    auto shrinkToUses(llvm::LiveInterval*, llvm::SmallVectorImpl<llvm::MachineInstr*>*);
    auto shrinkToUses(llvm::LiveInterval::SubRange&, llvm::Register);
    auto splitSeparateComponents(llvm::LiveInterval&, llvm::SmallVectorImpl<llvm::LiveInterval*>&);
    ~LiveIntervals();
};
