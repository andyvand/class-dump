//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::AliasSetTracker {
public: // (access info unavailable from symbols alone)
    auto add(llvm::AliasSetTracker const&);
    auto add(llvm::AnyMemSetInst*);
    auto add(llvm::AnyMemTransferInst*);
    auto add(llvm::BasicBlock&);
    auto add(llvm::Instruction*);
    auto add(llvm::LoadInst*);
    auto add(llvm::StoreInst*);
    auto add(llvm::VAArgInst*);
    auto add(llvm::Value*, llvm::LocationSize, llvm::AAMDNodes const&);
    auto addPointer(llvm::MemoryLocation, llvm::AliasSet::AccessLattice);
    auto addUnknown(llvm::Instruction*);
    auto clear();
    auto copyValue(llvm::Value*, llvm::Value*);
    auto deleteValue(llvm::Value*);
    auto dump() const;
    auto findAliasSetForUnknownInst(llvm::Instruction*);
    auto getAliasSetFor(llvm::MemoryLocation const&);
    auto mergeAliasSetsForPointer(llvm::Value const*, llvm::LocationSize, llvm::AAMDNodes const&, bool&);
    auto mergeAllAliasSets();
    auto print(llvm::raw_ostream&) const;
    auto removeAliasSet(llvm::AliasSet*);
};
