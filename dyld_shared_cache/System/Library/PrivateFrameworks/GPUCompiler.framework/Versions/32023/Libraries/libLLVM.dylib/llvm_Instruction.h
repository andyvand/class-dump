//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::Instruction {
public: // (access info unavailable from symbols alone)
    Instruction(llvm::Type*, unsigned int, llvm::Use*, unsigned int, llvm::BasicBlock*);
    Instruction(llvm::Type*, unsigned int, llvm::Use*, unsigned int, llvm::Instruction*);
    auto addAnnotationMetadata(llvm::StringRef);
    auto andIRFlags(llvm::Value const*);
    auto applyMergedLocation(llvm::DILocation const*, llvm::DILocation const*);
    auto clone() const;
    auto cloneImpl() const;
    auto comesBefore(llvm::Instruction const*) const;
    auto copyFastMathFlags(llvm::FastMathFlags);
    auto copyFastMathFlags(llvm::Instruction const*);
    auto copyIRFlags(llvm::Value const*, bool);
    auto copyMetadata(llvm::Instruction const&, llvm::ArrayRef<unsigned int>);
    auto dropLocation();
    auto dropPoisonGeneratingFlags();
    auto dropUndefImplyingAttrsAndUnknownMetadata(llvm::ArrayRef<unsigned int>);
    auto dropUnknownNonDebugMetadata(llvm::ArrayRef<unsigned int>);
    auto eraseFromParent();
    auto extractProfMetadata(unsigned long long&, unsigned long long&) const;
    auto extractProfTotalWeight(unsigned long long&) const;
    auto getAAMetadata() const;
    auto getAllMetadataImpl(llvm::SmallVectorImpl<std::__1::pair<unsigned int, llvm::MDNode*>>&) const;
    auto getFastMathFlags() const;
    auto getFunction() const;
    auto getMetadataImpl(llvm::StringRef) const;
    auto getMetadataImpl(unsigned int) const;
    auto getModule() const;
    auto getNextNonDebugInstruction(bool) const;
    auto getNumSuccessors() const;
    auto getOpcodeName(unsigned int);
    auto getPrevNonDebugInstruction(bool) const;
    auto getSuccessor(unsigned int) const;
    auto hasAllowContract() const;
    auto hasAllowReassoc() const;
    auto hasAllowReciprocal() const;
    auto hasApproxFunc() const;
    auto hasAtomicLoad() const;
    auto hasAtomicStore() const;
    auto hasNoInfs() const;
    auto hasNoNaNs() const;
    auto hasNoSignedWrap() const;
    auto hasNoSignedZeros() const;
    auto hasNoUnsignedWrap() const;
    auto hasPoisonGeneratingFlags() const;
    auto insertAfter(llvm::Instruction*);
    auto insertBefore(llvm::Instruction*);
    auto isAssociative() const;
    auto isAtomic() const;
    auto isCommutative() const;
    auto isDebugOrPseudoInst() const;
    auto isExact() const;
    auto isFast() const;
    auto isIdenticalTo(llvm::Instruction const*) const;
    auto isIdenticalToWhenDefined(llvm::Instruction const*) const;
    auto isLaunderOrStripInvariantGroup() const;
    auto isLifetimeStartOrEnd() const;
    auto isOnlyUserOfAnyOperand();
    auto isSafeToRemove() const;
    auto isSameOperationAs(llvm::Instruction const*, unsigned int) const;
    auto isUsedOutsideOfBlock(llvm::BasicBlock const*) const;
    auto isVolatile() const;
    auto mayHaveSideEffects() const;
    auto mayReadFromMemory() const;
    auto mayThrow() const;
    auto mayWriteToMemory() const;
    auto moveAfter(llvm::Instruction*);
    auto moveBefore(llvm::BasicBlock&, llvm::ilist_iterator<llvm::ilist_detail::node_options<llvm::Instruction, false, false, void>, false, false>);
    auto moveBefore(llvm::Instruction*);
    auto removeFromParent();
    auto replaceSuccessorWith(llvm::BasicBlock*, llvm::BasicBlock*);
    auto setAAMetadata(llvm::AAMDNodes const&);
    auto setFast(bool);
    auto setFastMathFlags(llvm::FastMathFlags);
    auto setHasAllowContract(bool);
    auto setHasAllowReassoc(bool);
    auto setHasAllowReciprocal(bool);
    auto setHasApproxFunc(bool);
    auto setHasNoInfs(bool);
    auto setHasNoNaNs(bool);
    auto setHasNoSignedWrap(bool);
    auto setHasNoSignedZeros(bool);
    auto setHasNoUnsignedWrap(bool);
    auto setIsExact(bool);
    auto setMetadata(llvm::StringRef, llvm::MDNode*);
    auto setMetadata(unsigned int, llvm::MDNode*);
    auto setParent(llvm::BasicBlock*);
    auto setSuccessor(unsigned int, llvm::BasicBlock*);
    auto swapProfMetadata();
    auto updateLocationAfterHoist();
    auto willReturn() const;
    ~Instruction();
};
