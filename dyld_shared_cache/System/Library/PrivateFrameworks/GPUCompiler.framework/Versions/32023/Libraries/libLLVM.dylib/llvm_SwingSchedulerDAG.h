//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::SwingSchedulerDAG {
public: // (access info unavailable from symbols alone)
    auto addConnectedNodes(llvm::SUnit*, llvm::NodeSet&, llvm::SetVector<llvm::SUnit*, std::__1::vector<llvm::SUnit*, std::__1::allocator<llvm::SUnit*>>, llvm::DenseSet<llvm::SUnit*, llvm::DenseMapInfo<llvm::SUnit*, void>>>&);
    auto addLoopCarriedDependences(llvm::AAResults*);
    auto applyInstrChange(llvm::MachineInstr*, llvm::SMSchedule&);
    auto calculateRecMII(llvm::SmallVector<llvm::NodeSet, 8u>&);
    auto calculateResMII();
    auto canUseLastOffsetValue(llvm::MachineInstr*, unsigned int&, unsigned int&, unsigned int&, long long&);
    auto changeDependences();
    auto checkNodeSets(llvm::SmallVector<llvm::NodeSet, 8u>&);
    auto checkValidNodeOrder(llvm::SmallVector<llvm::NodeSet, 8u> const&) const;
    auto colocateNodeSets(llvm::SmallVector<llvm::NodeSet, 8u>&);
    auto computeDelta(llvm::MachineInstr&, unsigned int&);
    auto computeNodeFunctions(llvm::SmallVector<llvm::NodeSet, 8u>&);
    auto computeNodeOrder(llvm::SmallVector<llvm::NodeSet, 8u>&);
    auto findCircuits(llvm::SmallVector<llvm::NodeSet, 8u>&);
    auto findDefInLoop(llvm::Register);
    auto finishBlock();
    auto fixupRegisterOverlaps(std::__1::deque<llvm::SUnit*, std::__1::allocator<llvm::SUnit*>>&);
    auto fuseRecs(llvm::SmallVector<llvm::NodeSet, 8u>&);
    auto groupRemainingNodes(llvm::SmallVector<llvm::NodeSet, 8u>&);
    auto isLoopCarriedDep(llvm::SUnit*, llvm::SDep const&, bool);
    auto postprocessDAG();
    auto registerPressureFilter(llvm::SmallVector<llvm::NodeSet, 8u>&);
    auto removeDuplicateNodes(llvm::SmallVector<llvm::NodeSet, 8u>&);
    auto schedule();
    auto schedulePipeline(llvm::SMSchedule&);
    auto setMAX_II();
    auto setMII(unsigned int, unsigned int);
    auto updatePhiDependences();
};
