//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::MachineRegisterInfo {
public: // (access info unavailable from symbols alone)
    MachineRegisterInfo(llvm::MachineFunction*);
    auto EmitLiveInCopies(llvm::MachineBasicBlock*, llvm::TargetRegisterInfo const&, llvm::TargetInstrInfo const&);
    auto addRegOperandToUseList(llvm::MachineOperand*);
    auto clearKillFlags(llvm::Register) const;
    auto clearVirtRegTypes();
    auto clearVirtRegs();
    auto cloneVirtualRegister(llvm::Register, llvm::StringRef);
    auto constrainRegAttrs(llvm::Register, llvm::Register, unsigned int);
    auto constrainRegClass(llvm::Register, llvm::TargetRegisterClass const*, unsigned int);
    auto createGenericVirtualRegister(llvm::LLT, llvm::StringRef);
    auto createIncompleteVirtualRegister(llvm::StringRef);
    auto createVirtualRegister(llvm::TargetRegisterClass const*, llvm::StringRef);
    auto disableCalleeSavedRegister(llvm::MCRegister);
    auto dumpUses(llvm::Register) const;
    auto freezeReservedRegs(llvm::MachineFunction const&);
    auto getCalleeSavedRegs() const;
    auto getLiveInPhysReg(llvm::Register) const;
    auto getLiveInVirtReg(llvm::MCRegister) const;
    auto getMaxLaneMaskForVReg(llvm::Register) const;
    auto getUniqueVRegDef(llvm::Register) const;
    auto getVRegDef(llvm::Register) const;
    auto hasAtMostUserInstrs(llvm::Register, unsigned int) const;
    auto hasOneNonDBGUse(llvm::Register) const;
    auto hasOneNonDBGUser(llvm::Register) const;
    auto isArgumentRegister(llvm::MachineFunction const&, llvm::MCRegister) const;
    auto isConstantPhysReg(llvm::MCRegister) const;
    auto isFixedRegister(llvm::MachineFunction const&, llvm::MCRegister) const;
    auto isGeneralPurposeRegister(llvm::MachineFunction const&, llvm::MCRegister) const;
    auto isLiveIn(llvm::Register) const;
    auto isPhysRegModified(llvm::MCRegister, bool) const;
    auto isPhysRegUsed(llvm::MCRegister, bool) const;
    auto isReservedRegUnit(unsigned int) const;
    auto markUsesInDebugValueAsUndef(llvm::Register) const;
    auto moveOperands(llvm::MachineOperand*, llvm::MachineOperand*, unsigned int);
    auto recomputeRegClass(llvm::Register);
    auto removeRegOperandFromUseList(llvm::MachineOperand*);
    auto replaceRegWith(llvm::Register, llvm::Register);
    auto setCalleeSavedRegs(llvm::ArrayRef<unsigned short>);
    auto setRegBank(llvm::Register, llvm::RegisterBank const&);
    auto setRegClass(llvm::Register, llvm::TargetRegisterClass const*);
    auto setType(llvm::Register, llvm::LLT);
    auto verifyUseList(llvm::Register) const;
    auto verifyUseLists() const;
};
