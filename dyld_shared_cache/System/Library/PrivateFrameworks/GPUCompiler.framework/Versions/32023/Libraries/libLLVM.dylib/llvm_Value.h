//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::Value {
public: // (access info unavailable from symbols alone)
    Value(llvm::Type*, unsigned int);
    auto DoPHITranslation(llvm::BasicBlock const*, llvm::BasicBlock const*) const;
    auto addMetadata(llvm::StringRef, llvm::MDNode&);
    auto addMetadata(unsigned int, llvm::MDNode&);
    auto assertModuleIsMaterializedImpl() const;
    auto canBeFreed() const;
    auto clearMetadata();
    auto deleteValue();
    auto destroyValueName();
    auto doRAUW(llvm::Value*, llvm::Value::ReplaceMetadataUses);
    auto dropDroppableUse(llvm::Use&);
    auto dropDroppableUses(llvm::function_ref<bool (llvm::Use const*)>);
    auto dropDroppableUsesIn(llvm::User&);
    auto dump() const;
    auto eraseMetadata(llvm::StringRef);
    auto eraseMetadata(unsigned int);
    auto getAllMetadata(llvm::SmallVectorImpl<std::__1::pair<unsigned int, llvm::MDNode*>>&) const;
    auto getContext() const;
    auto getMetadata(llvm::StringRef) const;
    auto getMetadata(llvm::StringRef, llvm::SmallVectorImpl<llvm::MDNode*>&) const;
    auto getMetadata(unsigned int) const;
    auto getMetadata(unsigned int, llvm::SmallVectorImpl<llvm::MDNode*>&) const;
    auto getName() const;
    auto getNumUses() const;
    auto getPointerAlignment(llvm::DataLayout const&) const;
    auto getPointerDereferenceableBytes(llvm::DataLayout const&, bool&, bool&) const;
    auto getSingleUndroppableUse();
    auto getUniqueUndroppableUser();
    auto getValueName() const;
    auto hasNUndroppableUses(unsigned int) const;
    auto hasNUndroppableUsesOrMore(unsigned int) const;
    auto hasNUses(unsigned int) const;
    auto hasNUsesOrMore(unsigned int) const;
    auto hasOneUser() const;
    auto isSwiftError() const;
    auto isTransitiveUsedByMetadataOnly() const;
    auto isUsedInBasicBlock(llvm::BasicBlock const*) const;
    auto print(llvm::raw_ostream&, bool) const;
    auto print(llvm::raw_ostream&, llvm::ModuleSlotTracker&, bool) const;
    auto printAsOperand(llvm::raw_ostream&, bool, llvm::Module const*) const;
    auto printAsOperand(llvm::raw_ostream&, bool, llvm::ModuleSlotTracker&) const;
    auto replaceAllUsesWith(llvm::Value*);
    auto replaceNonMetadataUsesWith(llvm::Value*);
    auto replaceUsesOutsideBlock(llvm::Value*, llvm::BasicBlock*);
    auto replaceUsesWithIf(llvm::Value*, llvm::function_ref<bool (llvm::Use&)>);
    auto reverseUseList();
    auto setMetadata(llvm::StringRef, llvm::MDNode*);
    auto setMetadata(unsigned int, llvm::MDNode*);
    auto setName(llvm::Twine const&);
    auto setNameImpl(llvm::Twine const&);
    auto setValueName(llvm::StringMapEntry<llvm::Value*>*);
    auto stripAndAccumulateConstantOffsets(llvm::DataLayout const&, llvm::APInt&, bool, bool, llvm::function_ref<bool (llvm::Value&, llvm::APInt&)>) const;
    auto stripInBoundsConstantOffsets() const;
    auto stripInBoundsOffsets(llvm::function_ref<void (llvm::Value const*)>) const;
    auto stripPointerCasts() const;
    auto stripPointerCastsAndAliases() const;
    auto stripPointerCastsForAliasAnalysis() const;
    auto stripPointerCastsSameRepresentation() const;
    auto takeName(llvm::Value*);
    ~Value();
};
