//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::AttrBuilder {
public: // (access info unavailable from symbols alone)
    AttrBuilder(llvm::LLVMContext&, llvm::AttributeSet);
    auto addAlignmentAttr(llvm::MaybeAlign);
    auto addAllocSizeAttr(unsigned int, llvm::Optional<unsigned int> const&);
    auto addAllocSizeAttrFromRawRepr(unsigned long long);
    auto addAttribute(llvm::Attribute);
    auto addAttribute(llvm::Attribute::AttrKind);
    auto addAttribute(llvm::StringRef, llvm::StringRef);
    auto addByRefAttr(llvm::Type*);
    auto addByValAttr(llvm::Type*);
    auto addDereferenceableAttr(unsigned long long);
    auto addDereferenceableOrNullAttr(unsigned long long);
    auto addInAllocaAttr(llvm::Type*);
    auto addPreallocatedAttr(llvm::Type*);
    auto addRawIntAttr(llvm::Attribute::AttrKind, unsigned long long);
    auto addStackAlignmentAttr(llvm::MaybeAlign);
    auto addStructRetAttr(llvm::Type*);
    auto addTypeAttr(llvm::Attribute::AttrKind, llvm::Type*);
    auto addUWTableAttr(llvm::UWTableKind);
    auto addVScaleRangeAttr(unsigned int, llvm::Optional<unsigned int>);
    auto addVScaleRangeAttrFromRawRepr(unsigned long long);
    auto clear();
    auto contains(llvm::Attribute::AttrKind) const;
    auto contains(llvm::StringRef) const;
    auto getAllocSizeArgs() const;
    auto getAttribute(llvm::Attribute::AttrKind) const;
    auto getAttribute(llvm::StringRef) const;
    auto getRawIntAttr(llvm::Attribute::AttrKind) const;
    auto getTypeAttr(llvm::Attribute::AttrKind) const;
    auto getVScaleRangeMax() const;
    auto getVScaleRangeMin() const;
    auto hasAlignmentAttr() const;
    auto merge(llvm::AttrBuilder const&);
    auto operator==(llvm::AttrBuilder const&) const;
    auto overlaps(llvm::AttributeMask const&) const;
    auto remove(llvm::AttributeMask const&);
    auto removeAttribute(llvm::Attribute::AttrKind);
    auto removeAttribute(llvm::StringRef);
};
