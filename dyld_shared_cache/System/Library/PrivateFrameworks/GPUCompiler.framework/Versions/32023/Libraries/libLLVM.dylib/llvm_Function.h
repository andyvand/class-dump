//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::Function {
public: // (access info unavailable from symbols alone)
    Function(llvm::FunctionType*, llvm::GlobalValue::LinkageTypes, unsigned int, llvm::Twine const&, llvm::Module*);
    auto BuildLazyArguments() const;
    auto Create(llvm::FunctionType*, llvm::GlobalValue::LinkageTypes, llvm::Twine const&, llvm::Module&);
    auto addAttributeAtIndex(unsigned int, llvm::Attribute);
    auto addDereferenceableOrNullParamAttr(unsigned int, unsigned long long);
    auto addDereferenceableParamAttr(unsigned int, unsigned long long);
    auto addFnAttr(llvm::Attribute);
    auto addFnAttr(llvm::Attribute::AttrKind);
    auto addFnAttr(llvm::StringRef, llvm::StringRef);
    auto addFnAttrs(llvm::AttrBuilder const&);
    auto addParamAttr(unsigned int, llvm::Attribute);
    auto addParamAttr(unsigned int, llvm::Attribute::AttrKind);
    auto addParamAttrs(unsigned int, llvm::AttrBuilder const&);
    auto addRetAttr(llvm::Attribute);
    auto addRetAttr(llvm::Attribute::AttrKind);
    auto addRetAttrs(llvm::AttrBuilder const&);
    auto allocHungoffUselist();
    auto callsFunctionThatReturnsTwice() const;
    auto clearArguments();
    auto clearGC();
    auto copyAttributesFrom(llvm::Function const*);
    auto createWithDefaultAttr(llvm::FunctionType*, llvm::GlobalValue::LinkageTypes, unsigned int, llvm::Twine const&, llvm::Module*);
    auto dropAllReferences();
    auto eraseFromParent();
    auto getAIRBitcodeValueType() const;
    auto getAttributeAtIndex(unsigned int, llvm::Attribute::AttrKind) const;
    auto getAttributeAtIndex(unsigned int, llvm::StringRef) const;
    auto getContext() const;
    auto getDenormalMode(llvm::fltSemantics const&) const;
    auto getEntryCount(bool) const;
    auto getFnAttribute(llvm::Attribute::AttrKind) const;
    auto getFnAttribute(llvm::StringRef) const;
    auto getGC() const;
    auto getImportGUIDs() const;
    auto getInstructionCount() const;
    auto getParamAttribute(unsigned int, llvm::Attribute::AttrKind) const;
    auto getPersonalityFn() const;
    auto getPrefixData() const;
    auto getPrologueData() const;
    auto getSectionPrefix() const;
    auto getSubprogram() const;
    auto hasAIRBitcodeValueType() const;
    auto hasAddressTaken(llvm::User const**, bool, bool, bool, bool) const;
    auto hasFnAttribute(llvm::Attribute::AttrKind) const;
    auto hasFnAttribute(llvm::StringRef) const;
    auto hasParamAttribute(unsigned int, llvm::Attribute::AttrKind) const;
    auto hasRetAttribute(llvm::Attribute::AttrKind) const;
    auto hasStackProtectorFnAttr() const;
    auto isConstrainedFPIntrinsic() const;
    auto isDebugInfoForProfiling() const;
    auto isDefTriviallyDead() const;
    auto isTargetIntrinsic() const;
    auto isTargetIntrinsic(unsigned int);
    auto lookupIntrinsicID(llvm::StringRef);
    auto nullPointerIsDefined() const;
    auto print(llvm::raw_ostream&, llvm::AssemblyAnnotationWriter*, bool, bool) const;
    auto recalculateIntrinsicID();
    auto removeAttributeAtIndex(unsigned int, llvm::Attribute::AttrKind);
    auto removeAttributeAtIndex(unsigned int, llvm::StringRef);
    auto removeFnAttr(llvm::Attribute::AttrKind);
    auto removeFnAttr(llvm::StringRef);
    auto removeFnAttrs(llvm::AttributeMask const&);
    auto removeFromParent();
    auto removeParamAttr(unsigned int, llvm::Attribute::AttrKind);
    auto removeParamAttr(unsigned int, llvm::StringRef);
    auto removeParamAttrs(unsigned int, llvm::AttributeMask const&);
    auto removeRetAttr(llvm::Attribute::AttrKind);
    auto removeRetAttr(llvm::StringRef);
    auto removeRetAttrs(llvm::AttributeMask const&);
    auto setAIRBitcodeValueType(llvm::FunctionType*);
    auto setEntryCount(llvm::Function::ProfileCount, llvm::DenseSet<unsigned long long, llvm::DenseMapInfo<unsigned long long, void>> const*);
    auto setEntryCount(unsigned long long, llvm::Function::ProfileCountType, llvm::DenseSet<unsigned long long, llvm::DenseMapInfo<unsigned long long, void>> const*);
    auto setGC(std::__1::basic_string<char, std::__1::char_traits<char>, std::__1::allocator<char>>);
    auto setPersonalityFn(llvm::Constant*);
    auto setPrefixData(llvm::Constant*);
    auto setPrologueData(llvm::Constant*);
    auto setSectionPrefix(llvm::StringRef);
    auto setSubprogram(llvm::DISubprogram*);
    auto setValueSubclassDataBit(unsigned int, bool);
    auto stealArgumentListFrom(llvm::Function&);
    auto viewCFG() const;
    auto viewCFG(bool, llvm::BlockFrequencyInfo const*, llvm::BranchProbabilityInfo const*) const;
    auto viewCFGOnly() const;
    auto viewCFGOnly(llvm::BlockFrequencyInfo const*, llvm::BranchProbabilityInfo const*) const;
    ~Function();
};
