//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::detail::IEEEFloat {
public: // (access info unavailable from symbols alone)
    IEEEFloat(double);
    IEEEFloat(float);
    IEEEFloat(llvm::detail::IEEEFloat const&);
    IEEEFloat(llvm::detail::IEEEFloat&&);
    IEEEFloat(llvm::fltSemantics const&);
    IEEEFloat(llvm::fltSemantics const&, llvm::APFloatBase::uninitializedTag);
    IEEEFloat(llvm::fltSemantics const&, llvm::APInt const&);
    IEEEFloat(llvm::fltSemantics const&, unsigned long long);
    auto add(llvm::detail::IEEEFloat const&, llvm::RoundingMode);
    auto addOrSubtract(llvm::detail::IEEEFloat const&, llvm::RoundingMode, bool);
    auto addOrSubtractSignificand(llvm::detail::IEEEFloat const&, bool);
    auto addOrSubtractSpecials(llvm::detail::IEEEFloat const&, bool);
    auto addSignificand(llvm::detail::IEEEFloat const&);
    auto assign(llvm::detail::IEEEFloat const&);
    auto bitcastToAPInt() const;
    auto bitwiseIsEqual(llvm::detail::IEEEFloat const&) const;
    auto changeSign();
    auto compare(llvm::detail::IEEEFloat const&) const;
    auto compareAbsoluteValue(llvm::detail::IEEEFloat const&) const;
    auto convert(llvm::fltSemantics const&, llvm::RoundingMode, bool*);
    auto convertBFloatAPFloatToAPInt() const;
    auto convertDoubleAPFloatToAPInt() const;
    auto convertF80LongDoubleAPFloatToAPInt() const;
    auto convertFloat8E4M3APFloatToAPInt() const;
    auto convertFloat8E4M3FNAPFloatToAPInt() const;
    auto convertFloat8E5M2APFloatToAPInt() const;
    auto convertFloatAPFloatToAPInt() const;
    auto convertFromAPInt(llvm::APInt const&, bool, llvm::RoundingMode);
    auto convertFromDecimalString(llvm::StringRef, llvm::RoundingMode);
    auto convertFromHexadecimalString(llvm::StringRef, llvm::RoundingMode);
    auto convertFromSignExtendedInteger(unsigned long long const*, unsigned int, bool, llvm::RoundingMode);
    auto convertFromString(llvm::StringRef, llvm::RoundingMode);
    auto convertFromStringSpecials(llvm::StringRef);
    auto convertFromUnsignedParts(unsigned long long const*, unsigned int, llvm::RoundingMode);
    auto convertFromZeroExtendedInteger(unsigned long long const*, unsigned int, bool, llvm::RoundingMode);
    auto convertHalfAPFloatToAPInt() const;
    auto convertNormalToHexString(char*, unsigned int, bool, llvm::RoundingMode) const;
    auto convertPPCDoubleDoubleAPFloatToAPInt() const;
    auto convertQuadrupleAPFloatToAPInt() const;
    auto convertToDouble() const;
    auto convertToFloat() const;
    auto convertToHexString(char*, unsigned int, bool, llvm::RoundingMode) const;
    auto convertToInteger(llvm::MutableArrayRef<unsigned long long>, unsigned int, bool, llvm::RoundingMode, bool*) const;
    auto convertToSignExtendedInteger(llvm::MutableArrayRef<unsigned long long>, unsigned int, bool, llvm::RoundingMode, bool*) const;
    auto copySignificand(llvm::detail::IEEEFloat const&);
    auto divide(llvm::detail::IEEEFloat const&, llvm::RoundingMode);
    auto divideSignificand(llvm::detail::IEEEFloat const&);
    auto divideSpecials(llvm::detail::IEEEFloat const&);
    auto exponentInf() const;
    auto exponentNaN() const;
    auto exponentZero() const;
    auto freeSignificand();
    auto fusedMultiplyAdd(llvm::detail::IEEEFloat const&, llvm::detail::IEEEFloat const&, llvm::RoundingMode);
    auto getExactInverse(llvm::APFloat*) const;
    auto handleOverflow(llvm::RoundingMode);
    auto incrementSignificand();
    auto initFromAPInt(llvm::fltSemantics const*, llvm::APInt const&);
    auto initFromBFloatAPInt(llvm::APInt const&);
    auto initFromDoubleAPInt(llvm::APInt const&);
    auto initFromF80LongDoubleAPInt(llvm::APInt const&);
    auto initFromFloat8E4M3APInt(llvm::APInt const&);
    auto initFromFloat8E4M3FNAPInt(llvm::APInt const&);
    auto initFromFloat8E5M2APInt(llvm::APInt const&);
    auto initFromFloatAPInt(llvm::APInt const&);
    auto initFromHalfAPInt(llvm::APInt const&);
    auto initFromPPCDoubleDoubleAPInt(llvm::APInt const&);
    auto initFromQuadrupleAPInt(llvm::APInt const&);
    auto initialize(llvm::fltSemantics const*);
    auto isDenormal() const;
    auto isInteger() const;
    auto isLargest() const;
    auto isSignaling() const;
    auto isSignificandAllOnes() const;
    auto isSignificandAllOnesExceptLSB() const;
    auto isSignificandAllZeros() const;
    auto isSmallest() const;
    auto makeInf(bool);
    auto makeLargest(bool);
    auto makeNaN(bool, bool, llvm::APInt const*);
    auto makeQuiet();
    auto makeSmallest(bool);
    auto makeSmallestNormalized(bool);
    auto makeZero(bool);
    auto mod(llvm::detail::IEEEFloat const&);
    auto modSpecials(llvm::detail::IEEEFloat const&);
    auto multiply(llvm::detail::IEEEFloat const&, llvm::RoundingMode);
    auto multiplySignificand(llvm::detail::IEEEFloat const&);
    auto multiplySignificand(llvm::detail::IEEEFloat const&, llvm::detail::IEEEFloat);
    auto multiplySpecials(llvm::detail::IEEEFloat const&);
    auto next(bool);
    auto normalize(llvm::RoundingMode, llvm::lostFraction);
    auto operator=(llvm::detail::IEEEFloat const&);
    auto operator=(llvm::detail::IEEEFloat&&);
    auto partCount() const;
    auto remainder(llvm::detail::IEEEFloat const&);
    auto remainderSpecials(llvm::detail::IEEEFloat const&);
    auto roundAwayFromZero(llvm::RoundingMode, llvm::lostFraction, unsigned int) const;
    auto roundSignificandWithExponent(unsigned long long const*, unsigned int, int, llvm::RoundingMode);
    auto roundToIntegral(llvm::RoundingMode);
    auto shiftSignificandLeft(unsigned int);
    auto shiftSignificandRight(unsigned int);
    auto significandLSB() const;
    auto significandMSB() const;
    auto significandParts();
    auto significandParts() const;
    auto subtract(llvm::detail::IEEEFloat const&, llvm::RoundingMode);
    auto subtractSignificand(llvm::detail::IEEEFloat const&, unsigned long long);
    auto toString(llvm::SmallVectorImpl<char>&, unsigned int, unsigned int, bool) const;
    auto zeroSignificand();
    ~IEEEFloat();
};
