//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::MachineInstr {
public: // (access info unavailable from symbols alone)
    MachineInstr(llvm::MachineFunction&, llvm::MCInstrDesc const&, llvm::DebugLoc, bool);
    MachineInstr(llvm::MachineFunction&, llvm::MachineInstr const&);
    auto addImplicitDefUseOperands(llvm::MachineFunction&);
    auto addMemOperand(llvm::MachineFunction&, llvm::MachineMemOperand*);
    auto addOperand(llvm::MachineFunction&, llvm::MachineOperand const&);
    auto addOperand(llvm::MachineOperand const&);
    auto addRegOperandsToUseLists(llvm::MachineRegisterInfo&);
    auto addRegisterDead(llvm::Register, llvm::TargetRegisterInfo const*, bool);
    auto addRegisterDefined(llvm::Register, llvm::TargetRegisterInfo const*);
    auto addRegisterKilled(llvm::Register, llvm::TargetRegisterInfo const*, bool);
    auto allDefsAreDead() const;
    auto bundleWithPred();
    auto bundleWithSucc();
    auto changeDebugValuesDefReg(llvm::Register);
    auto clearKillInfo();
    auto clearRegisterDeads(llvm::Register);
    auto clearRegisterKills(llvm::Register, llvm::TargetRegisterInfo const*);
    auto cloneInstrSymbols(llvm::MachineFunction&, llvm::MachineInstr const&);
    auto cloneMemRefs(llvm::MachineFunction&, llvm::MachineInstr const&);
    auto cloneMergedMemRefs(llvm::MachineFunction&, llvm::ArrayRef<llvm::MachineInstr const*>);
    auto collectDebugValues(llvm::SmallVectorImpl<llvm::MachineInstr*>&);
    auto copyFlagsFromInstruction(llvm::Instruction const&);
    auto copyIRFlags(llvm::Instruction const&);
    auto copyImplicitOps(llvm::MachineFunction&, llvm::MachineInstr const&);
    auto dropMemRefs(llvm::MachineFunction&);
    auto dump() const;
    auto dumpr(llvm::MachineRegisterInfo const&, unsigned int) const;
    auto dumprImpl(llvm::MachineRegisterInfo const&, unsigned int, unsigned int, llvm::SmallPtrSetImpl<llvm::MachineInstr const*>&) const;
    auto emitError(llvm::StringRef) const;
    auto eraseFromBundle();
    auto eraseFromParent();
    auto findFirstPredOperandIdx() const;
    auto findInlineAsmFlagIdx(unsigned int, unsigned int*) const;
    auto findRegisterDefOperandIdx(llvm::Register, bool, bool, llvm::TargetRegisterInfo const*) const;
    auto findRegisterUseOperandIdx(llvm::Register, bool, llvm::TargetRegisterInfo const*) const;
    auto findTiedOperandIdx(unsigned int) const;
    auto getBundleSize() const;
    auto getDebugExpression() const;
    auto getDebugExpressionOp();
    auto getDebugExpressionOp() const;
    auto getDebugInstrNum();
    auto getDebugInstrNum(llvm::MachineFunction&);
    auto getDebugLabel() const;
    auto getDebugVariable() const;
    auto getDebugVariableOp();
    auto getDebugVariableOp() const;
    auto getFoldedRestoreSize(llvm::TargetInstrInfo const*) const;
    auto getFoldedSpillSize(llvm::TargetInstrInfo const*) const;
    auto getInlineAsmDialect() const;
    auto getMF() const;
    auto getNumExplicitDefs() const;
    auto getNumExplicitOperands() const;
    auto getRegClassConstraint(unsigned int, llvm::TargetInstrInfo const*, llvm::TargetRegisterInfo const*) const;
    auto getRegClassConstraintEffect(unsigned int, llvm::TargetRegisterClass const*, llvm::TargetInstrInfo const*, llvm::TargetRegisterInfo const*) const;
    auto getRegClassConstraintEffectForVReg(llvm::Register, llvm::TargetRegisterClass const*, llvm::TargetInstrInfo const*, llvm::TargetRegisterInfo const*, bool) const;
    auto getRegClassConstraintEffectForVRegImpl(unsigned int, llvm::Register, llvm::TargetRegisterClass const*, llvm::TargetInstrInfo const*, llvm::TargetRegisterInfo const*) const;
    auto getRegInfo();
    auto getRestoreSize(llvm::TargetInstrInfo const*) const;
    auto getSpillSize(llvm::TargetInstrInfo const*) const;
    auto getTypeToPrint(unsigned int, llvm::SmallBitVector&, llvm::MachineRegisterInfo const&) const;
    auto hasComplexRegisterTies() const;
    auto hasOrderedMemoryRef() const;
    auto hasPropertyInBundle(unsigned long long, llvm::MachineInstr::QueryType) const;
    auto hasRegisterImplicitUseOperand(llvm::Register) const;
    auto hasUnmodeledSideEffects() const;
    auto isCandidateForCallSiteEntry(llvm::MachineInstr::QueryType) const;
    auto isConstantValuePHI() const;
    auto isDebugEntryValue() const;
    auto isDereferenceableInvariantLoad(llvm::AAResults*) const;
    auto isIdenticalTo(llvm::MachineInstr const&, llvm::MachineInstr::MICheckType) const;
    auto isLoadFoldBarrier() const;
    auto isSafeToMove(llvm::AAResults*, bool&) const;
    auto isStackAligningInlineAsm() const;
    auto mayAlias(llvm::AAResults*, llvm::MachineInstr const&, bool) const;
    auto mergeFlagsWith(llvm::MachineInstr const&) const;
    auto moveBefore(llvm::MachineInstr*);
    auto print(llvm::raw_ostream&, bool, bool, bool, bool, llvm::TargetInstrInfo const*) const;
    auto print(llvm::raw_ostream&, llvm::ModuleSlotTracker&, bool, bool, bool, bool, llvm::TargetInstrInfo const*) const;
    auto readsWritesVirtualRegister(llvm::Register, llvm::SmallVectorImpl<unsigned int>*) const;
    auto removeFromBundle();
    auto removeFromParent();
    auto removeOperand(unsigned int);
    auto removeRegOperandsFromUseLists(llvm::MachineRegisterInfo&);
    auto setExtraInfo(llvm::MachineFunction&, llvm::ArrayRef<llvm::MachineMemOperand*>, llvm::MCSymbol*, llvm::MCSymbol*, llvm::MDNode*);
    auto setHeapAllocMarker(llvm::MachineFunction&, llvm::MDNode*);
    auto setMemRefs(llvm::MachineFunction&, llvm::ArrayRef<llvm::MachineMemOperand*>);
    auto setPhysRegsDeadExcept(llvm::ArrayRef<llvm::Register>, llvm::TargetRegisterInfo const&);
    auto setPostInstrSymbol(llvm::MachineFunction&, llvm::MCSymbol*);
    auto setPreInstrSymbol(llvm::MachineFunction&, llvm::MCSymbol*);
    auto setRegisterDefReadUndef(llvm::Register, bool);
    auto shouldUpdateCallSiteInfo() const;
    auto substituteRegister(llvm::Register, llvm::Register, unsigned int, llvm::TargetRegisterInfo const&);
    auto tieOperands(unsigned int, unsigned int);
    auto unbundleFromPred();
    auto unbundleFromSucc();
};
