//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::SCCPSolver {
public: // (access info unavailable from symbols alone)
    SCCPSolver(llvm::DataLayout const&, std::__1::function<llvm::TargetLibraryInfo const& (llvm::Function&)>, llvm::LLVMContext&);
    auto addAnalysis(llvm::Function&, llvm::AnalysisResultsForFn);
    auto addArgumentTrackedFunction(llvm::Function*);
    auto addToMustPreserveReturnsInFunctions(llvm::Function*);
    auto addTrackedFunction(llvm::Function*);
    auto getArgumentTrackedFunctions();
    auto getConstant(llvm::ValueLatticeElement const&) const;
    auto getDTU(llvm::Function&);
    auto getLatticeValueFor(llvm::Value*) const;
    auto getMRVFunctionsTracked();
    auto getPredicateInfoFor(llvm::Instruction*);
    auto getStructLatticeValueFor(llvm::Value*) const;
    auto getTrackedGlobals();
    auto getTrackedRetVals();
    auto isArgumentTrackedFunction(llvm::Function*);
    auto isBlockExecutable(llvm::BasicBlock*) const;
    auto isEdgeFeasible(llvm::BasicBlock*, llvm::BasicBlock*) const;
    auto isStructLatticeConstant(llvm::Function*, llvm::StructType*);
    auto markArgInFuncSpecialization(llvm::Function*, llvm::SmallVectorImpl<llvm::ArgInfo> const&);
    auto markBlockExecutable(llvm::BasicBlock*);
    auto markFunctionUnreachable(llvm::Function*);
    auto markOverdefined(llvm::Value*);
    auto mustPreserveReturn(llvm::Function*);
    auto removeLatticeValueFor(llvm::Value*);
    auto resolvedUndefsIn(llvm::Function&);
    auto solve();
    auto trackValueOfGlobalVariable(llvm::GlobalVariable*);
    auto visit(llvm::Instruction*);
    auto visitCall(llvm::CallInst&);
    ~SCCPSolver();
};
