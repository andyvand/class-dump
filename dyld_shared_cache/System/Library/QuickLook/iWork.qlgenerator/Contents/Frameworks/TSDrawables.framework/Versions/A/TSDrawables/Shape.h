//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class Shape {
public: // (access info unavailable from symbols alone)
    Shape();
    auto AddChgt(int, int, Shape*&, int&, int, Shape*, int, Shape*, int);
    auto AddContour(Path*, int, Path**, int, int);
    auto AddEdge(int, int);
    auto AddEdge(int, int, int, int);
    auto AddPoint(float, float);
    auto AssembleAretes();
    auto AssemblePoints(Shape*);
    auto AssemblePoints(int, int);
    auto Avance(int, int, Shape*, int, Shape*, Shape*, bool_op);
    auto Booleen(Shape*, Shape*, bool_op);
    auto CalcBBox();
    auto CheckAdjacencies(int, int, Shape*, int);
    auto CheckEdges(int, int, Shape*, Shape*, bool_op);
    auto CleanupSweep();
    auto CmpToVert(float, float, float, float);
    auto ConnectEnd(int, int);
    auto ConnectStart(int, int);
    auto ConvertToForme(Path*);
    auto ConvertToForme(Path*, int, Path**);
    auto ConvertToShape(Shape*, fill_typ, bool);
    auto Copy(Shape*);
    auto CreateIncidence(Shape*, int, int);
    auto DisconnectEnd(int);
    auto DisconnectStart(int);
    auto DoEdgeTo(Shape*, int, int, bool, bool);
    auto Eulerian(bool);
    auto ForceToPolygon();
    auto GetFlag(int);
    auto GetWindings(Shape*, Shape*, bool_op, bool);
    auto HasContainedSize();
    auto Inverse(int);
    auto MakeBackData(bool);
    auto MakeEdgeData(bool);
    auto MakeOffset(Shape*, float, join_typ, float);
    auto MakePointData(bool);
    auto MakeQuickRasterData(bool);
    auto MakeRasterData(bool);
    auto MakeSweepDestData(bool);
    auto MakeSweepSrcData(bool);
    auto MakeVoronoiData(bool);
    auto PushIncidence(Shape*, int, int, float);
    auto ReFormeArcTo(int, int, Path*, Path*);
    auto ReFormeBezierChunk(float, float, float, float, Path*, int, int, Path*, int, float, float);
    auto ReFormeBezierTo(int, int, Path*, Path*);
    auto ReFormeCubicTo(int, int, Path*, Path*);
    auto ReFormeLineTo(int, int, Path*, Path*);
    auto Reoriente(Shape*);
    auto Reset(int, int);
    auto ResetSweep();
    auto SetFlag(int, bool);
    auto SortEdges();
    auto SortEdgesList(Shape::edge_list*, int, int);
    auto SortPoints();
    auto SortPoints(int, int);
    auto SortPointsByOldInd(int, int);
    auto SortPointsRounded();
    auto SortPointsRounded(int, int);
    auto SubEdge(int);
    auto SubPoint(int);
    auto SwapEdges(int, int);
    auto SwapEdges(int, int, int);
    auto SwapPoints(int, int);
    auto SwapPoints(int, int, int);
    auto TesteAdjacency(Shape*, int, float, float, int, bool);
    auto TesteIntersection(Shape*, Shape*, int, int, float&, float&, float&, float&, bool);
    auto TesteIntersection(SweepTree*, SweepTree*, float&, float&, float&, float&, bool);
    auto TesteIntersection(SweepTree*, bool, bool);
    auto Validate();
    auto Winding(float, float);
    auto Winding(int);
    auto reallocAretesArray(int);
    auto reallocBackData(int);
    auto reallocDestData(int);
    auto reallocEdgesData(int);
    auto reallocPointsArray(int);
    auto reallocPointsData(int);
    auto reallocRasterData(int);
    auto reallocSweepSrcData(int);
    auto reallocVoronoiEdgeData(int);
    auto reallocVoronoiPointData(int);
    ~Shape();
};
