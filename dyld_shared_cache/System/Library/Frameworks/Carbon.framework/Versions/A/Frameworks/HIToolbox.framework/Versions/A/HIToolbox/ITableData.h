//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class ITableData {
public: // (access info unavailable from symbols alone)
    auto AddColumn(unsigned long, DataBrowserPropertyDesc const&);
    auto CachedData(Cell64 const&) const;
    auto CachedData(Cell64 const&, BrowserObj*);
    auto CellID(Cell64 const&) const;
    auto CellIsSelected(Cell64 const&) const;
    auto ColumnFlags(unsigned long) const;
    auto ColumnFlags(unsigned long, unsigned int);
    auto ColumnIndex(unsigned long) const;
    auto DeleteColumn(unsigned long);
    auto FirstAndLastSelection(Cell64&, Cell64&) const;
    auto FirstSelection(Cell64&) const;
    auto ForEachCell(TableCellState, ICellCallback&) const;
    auto ForEachCell(TableCellState, ICellCallback&, bool) const;
    auto ItemHeight(unsigned long) const;
    auto ItemHeight(unsigned long, unsigned short);
    auto ItemRow(unsigned long) const;
    auto LastSelection(Cell64&) const;
    auto MoveColumn(unsigned long, unsigned long);
    auto MoveRow(unsigned long, unsigned long);
    auto NextSelection(Cell64&, bool, bool) const;
    auto PrevSelection(Cell64&, bool, bool) const;
    auto RowBasedData() const;
    auto RowCountChangedNotificationProc(__EventLoopTimer*, void*);
    auto RowHasCustomHeight(unsigned long) const;
    auto RowHeight(unsigned long) const;
    auto RowHeight(unsigned long, unsigned short);
    auto RowID(unsigned long) const;
    auto SelectCell(Cell64 const&, BinaryState);
    auto SelectedChildrenChangedNotificationProc(__EventLoopTimer*, void*);
    auto SendAccessibilityRowCountChangedNotification();
    auto SendAccessibilitySelectedChildrenChangedNotification();
    auto SynchronizeRows();
    auto View();
    auto View() const;
    ~ITableData();
};
