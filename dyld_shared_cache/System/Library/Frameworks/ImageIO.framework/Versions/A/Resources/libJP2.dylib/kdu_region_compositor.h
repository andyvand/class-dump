//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class kdu_region_compositor {
public: // (access info unavailable from symbols alone)
    auto access_codestream(kdrc_stream*);
    auto add_active_stream(int, int, bool);
    auto add_active_stream(int, int, bool) (.cold.1);
    auto add_active_stream(int, int, bool) (.cold.2);
    auto add_active_stream(int, int, bool) (.cold.3);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.1);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.2);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.3);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.4);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.5);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.6);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.1);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.2);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.3);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.4);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.5);
    auto add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.6);
    auto allocate_buffer(kdu_coords, kdu_coords&, bool);
    auto change_compositing_layer_frame(int, int);
    auto configure_overlays(bool, int, int);
    auto create(jpx_source*, int);
    auto create(kdu_compressed_source*, int);
    auto cull_inactive_layers(int);
    auto custom_paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords);
    auto delete_buffer(kdu_compositor_buf*);
    auto donate_compositing_buffer(kdu_compositor_buf*, kdu_dims, kdu_coords);
    auto donate_compositing_buffer(kdu_compositor_buf*, kdu_dims, kdu_coords) (.cold.1);
    auto donate_compositing_buffer(kdu_compositor_buf*, kdu_dims, kdu_coords) (.cold.2);
    auto donate_compositing_buffer(kdu_compositor_buf*, kdu_dims, kdu_coords) (.cold.3);
    auto find_codestream_region(int, int, bool);
    auto find_layer_region(int, int, bool);
    auto find_optimal_scale(kdu_dims, float, float, float, int*, int*, int*);
    auto find_point(kdu_coords, int&, int&);
    auto flush_composition_queue();
    auto get_codestream_info(kdrc_stream*, int&, int&, bounded::v1::bounded_ptr<int>, int*, float*, float*);
    auto get_codestream_info(kdrc_stream*, int&, int&, bounded::v1::bounded_ptr<int>, int*, float*, float*) (.cold.1);
    auto get_codestream_info(kdrc_stream*, int&, int&, int*, int*, float*, float*);
    auto get_codestream_info(kdrc_stream*, int&, int&, int*, int*, float*, float*) (.cold.1);
    auto get_codestream_packets(kdrc_stream*, kdu_dims, long long&, long long&, long long&);
    auto get_composition_buffer(kdu_dims&);
    auto get_composition_buffer(kdu_dims&) (.cold.1);
    auto get_max_available_quality_layers();
    auto get_next_codestream(kdrc_stream*, bool, bool);
    auto get_next_visible_codestream(kdrc_stream*, kdu_dims, bool);
    auto get_total_composition_dims(kdu_dims&);
    auto halt_processing();
    auto init();
    auto init(kdu_thread_env*, kdu_thread_queue*);
    auto inspect_composition_queue(int, long long*, int*);
    auto internal_allocate_buffer(kdu_coords, kdu_coords&, bool);
    auto internal_delete_buffer(kdu_compositor_buf*);
    auto map_region(int&, kdu_dims&);
    auto paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords);
    auto paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.1);
    auto paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.2);
    auto paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.3);
    auto paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.4);
    auto paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.5);
    auto paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.6);
    auto paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.7);
    auto paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.8);
    auto pop_composition_buffer();
    auto pre_destroy();
    auto process(int, kdu_dims&);
    auto process(int, kdu_dims&) (.cold.1);
    auto process(int, kdu_dims&) (.cold.2);
    auto push_composition_buffer(long long, int);
    auto push_composition_buffer(long long, int) (.cold.1);
    auto push_composition_buffer(long long, int) (.cold.2);
    auto refresh();
    auto remove_compositing_layer(int, bool);
    auto remove_compositing_layer(int, bool) (.cold.1);
    auto remove_compositing_layer(int, bool) (.cold.2);
    auto remove_compositing_layer(int, bool) (.cold.3);
    auto remove_stream(kdrc_stream*, bool);
    auto remove_stream(kdrc_stream*, bool) (.cold.1);
    auto remove_stream(kdrc_stream*, bool) (.cold.2);
    auto reset();
    auto retract_compositing_buffer(kdu_coords&);
    auto retract_compositing_buffer(kdu_coords&) (.cold.1);
    auto search_overlays(kdu_coords, int&);
    auto set_buffer_surface(kdu_dims, int);
    auto set_buffer_surface(kdu_dims, int) (.cold.1);
    auto set_buffer_surface(kdu_dims, int) (.cold.2);
    auto set_error_level(int);
    auto set_frame(jpx_frame_expander*);
    auto set_frame(jpx_frame_expander*) (.cold.1);
    auto set_frame(jpx_frame_expander*) (.cold.2);
    auto set_frame(jpx_frame_expander*) (.cold.3);
    auto set_frame(jpx_frame_expander*) (.cold.4);
    auto set_layer_buffer_surfaces();
    auto set_max_quality_layers(int);
    auto set_scale(bool, bool, bool, float);
    auto set_single_component(int, int, kdu_component_access_mode);
    auto set_single_component(int, int, kdu_component_access_mode) (.cold.1);
    auto set_single_component(int, int, kdu_component_access_mode) (.cold.2);
    auto set_single_component(int, int, kdu_component_access_mode) (.cold.3);
    auto set_thread_env(kdu_thread_env*, kdu_thread_queue*);
    auto update_composition();
    auto update_composition() (.cold.1);
    auto update_overlays(bool);
    kdu_region_compositor();
    kdu_region_compositor(jpx_source*, int);
    kdu_region_compositor(kdu_compressed_source*, int);
    kdu_region_compositor(kdu_thread_env*, kdu_thread_queue*);
    ~kdu_region_compositor();
};
