//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class kdrc_layer {
public: // (access info unavailable from symbols alone)
    auto activate(kdu_dims, kdu_dims, bool, bool, bool);
    auto activate(kdu_dims, kdu_dims, bool, bool, bool) (.cold.1);
    auto activate(kdu_dims, kdu_dims, bool, bool, bool) (.cold.2);
    auto activate(kdu_dims, kdu_dims, bool, bool, bool) (.cold.3);
    auto activate(kdu_dims, kdu_dims, bool, bool, bool, int, int);
    auto activate(kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.1);
    auto activate(kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.2);
    auto change_frame(int, bool);
    auto configure_overlay(bool, int, int);
    auto deactivate();
    auto find_optimal_scale(float, float, float);
    auto get_component_scale_factors(kdrc_stream*, double&, double&);
    auto get_component_scale_factors(kdrc_stream*, double&, double&) (.cold.1);
    auto get_visible_packet_stats(kdrc_stream*, kdu_dims, long long&, long long&, long long&);
    auto get_visible_packet_stats(kdrc_stream*, kdu_dims, long long&, long long&, long long&) (.cold.1);
    auto get_visible_packet_stats(kdrc_stream*, kdu_dims, long long&, long long&, long long&) (.cold.2);
    auto get_visible_packet_stats(kdrc_stream*, kdu_dims, long long&, long long&, long long&) (.cold.3);
    auto get_visible_packet_stats(kdrc_stream*, kdu_dims, long long&, long long&, long long&) (.cold.4);
    auto init(jpx_layer_source, kdu_dims, kdu_dims, bool, bool, bool);
    auto init(jpx_layer_source, kdu_dims, kdu_dims, bool, bool, bool) (.cold.1);
    auto init(mj2_video_source*, int, int, kdu_dims, bool, bool, bool);
    auto init(mj2_video_source*, int, int, kdu_dims, bool, bool, bool) (.cold.1);
    auto init(mj2_video_source*, int, int, kdu_dims, bool, bool, bool) (.cold.2);
    auto map_region(int&, kdu_dims&);
    auto measure_visible_area(kdu_dims, bool);
    auto process_overlay(kdu_dims&);
    auto process_overlay(kdu_dims&) (.cold.1);
    auto reinit();
    auto reinit() (.cold.1);
    auto search_overlay(kdu_coords, int&, bool&);
    auto set_buffer_surface(kdu_dims, kdu_dims, kdu_compositor_buf*, bool);
    auto set_buffer_surface(kdu_dims, kdu_dims, kdu_compositor_buf*, bool) (.cold.1);
    auto set_buffer_surface(kdu_dims, kdu_dims, kdu_compositor_buf*, bool) (.cold.2);
    auto set_scale(bool, bool, bool, float, int&);
    auto take_layer_buffer(bool);
    auto take_layer_buffer(bool) (.cold.1);
    auto take_layer_buffer(bool) (.cold.2);
    auto update_composition(kdu_dims, unsigned int);
    auto update_composition(kdu_dims, unsigned int) (.cold.1);
    auto update_composition(kdu_dims, unsigned int) (.cold.2);
    auto update_overlay(bool, bool);
    auto update_overlay(bool, bool) (.cold.1);
    kdrc_layer(kdu_region_compositor*);
    ~kdrc_layer();
};
