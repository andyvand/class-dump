//
//     Generated by class-dump 3.5 (64 bit).
//
//  Copyright (C) 1997-2019 Steve Nygard.
//

#import <Metal/MTLRenderPipelineColorAttachmentDescriptor.h>

__attribute__((visibility("hidden")))
@interface MTLRenderPipelineColorAttachmentDescriptorInternal : MTLRenderPipelineColorAttachmentDescriptor
{
    struct MTLRenderPipelineAttachmentDescriptorPrivate _private;
}

- (void)idth, vec4 color, float opacity) {
  float hw, dist1, dist2, interpolant;
  vec2 p1, p2, p, v, w, s;
  hw = width * 0.5;
  p = destCoord();
  p1 = pt1.rg;
  p2 = pt1.ba;
  v = p - p1;
  s = p2 - p1;
  dist1 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist1 = compare((v.x * s.x) + (v.y * s.y), length(v), dist1);
  w = p - p2;
  dist1 = compare((w.x * s.x) + (w.y * s.y), dist1, length(w));
  p1 = pt2.rg;
  p2 = pt2.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt3.rg;
  p2 = pt3.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt4.rg;
  p2 = pt4.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt5.rg;
  p2 = pt5.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt6.rg;
  p2 = pt6.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt7.rg;
  p2 = pt7.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt8.rg;
  p2 = pt8.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt9.rg;
  p2 = pt9.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt10.rg;
  p2 = pt10.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt11.rg;
  p2 = pt11.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt12.rg;
  p2 = pt12.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt13.rg;
  p2 = pt13.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt14.rg;
  p2 = pt14.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt15.rg;
  p2 = pt15.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt16.rg;
  p2 = pt16.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  interpolant = clamp(hw - dist1, 0.0, 1.0);
  interpolant = ((3.0 - (2.0 * interpolant)) * interpolant) * interpolant;
  return compare(vec4(dist1 - (hw - 1.0)), color, compare(vec4(dist1 - hw), color * interpolant, vec4(0.0))) * opacity;
}
;
- (void);
- (void);
- (unsigned long long);
- (unsigned long long);
- (id);
- (unsigned long long);
- (long long);
- (void);
- (void);
- (id);
- (void);
- (void);
- (unsigned long long);
- (unsigned long long);
- (unsigned long long);
- (_Bool);
- (void);
- (id);
- (_Bool);
- (void)(;
- (void);
- (unsigned long long)us{3 1 roll pop}if pop;
- (void);
- (id);
- (void);
- (unsigned long long);
- (unsigned long long)b/ÿÿ;
- (const struct MTLRenderPipelineAttachmentDescriptorPrivate *)ets;

@end

