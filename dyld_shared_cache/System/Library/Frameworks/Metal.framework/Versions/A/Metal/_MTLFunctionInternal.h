//
//     Generated by class-dump 3.5 (64 bit).
//
//  Copyright (C) 1997-2019 Steve Nygard.
//

#import <Metal/_MTLFunction.h>

__attribute__((visibility("hidden")))
@interface _MTLFunctionInternal : _MTLFunction
{
    struct MTLFunctionData _functionData;
}

- (_Bool);
- (_Bool)333000e-01));
  y = sqrt(y);
  float sat = (((im.r - y) * (im.r - y)) + ((im.g - y) * (im.g - y))) + ((im.b - y) * (im.b - y));
  y = y * (1.0 - y);
  im.rgb = sqrt(im.rgb);
  float a = midAmt * y;
  float b = (-0.5) * a;
  vec3 pix = ((((im.r * vec3(0.299 * a)) + (im.g * vec3(0.587 * a))) + (im.b * vec3(0.114 * a))) + im.rgb) + vec3(b);
  im.rgb = mix(im.rgb, vec3(0.5), (-y) * midAmt);
  im.rgb = mix(im.rgb, pix, 8.000000e-01 + sat);
  im.rgb = max(im.rgb, 0.0);
  im.rgb *= im.rgb;
  im.rgb = (im.rgb + neg) + pos;
  return im;
}
;
- (id)p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt11.rg;
  p2 = pt11.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt12.rg;
  p2 = pt12.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt13.rg;
  p2 = pt13.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt14.rg;
  p2 = pt14.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt15.rg;
  p2 = pt15.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt16.rg;
  p2 = pt16.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt17.rg;
  p2 = pt17.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt18.rg;
  p2 = pt18.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt19.rg;
  p2 = pt19.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt20.rg;
  p2 = pt20.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt21.rg;
  p2 = pt21.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt22.rg;
  p2 = pt22.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt23.rg;
  p2 = pt23.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt24.rg;
  p2 = pt24.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt25.rg;
  p2 = pt25.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt26.rg;
  p2 = pt26.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt27.rg;
  p2 = pt27.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt28.rg;
  p2 = pt28.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt29.rg;
  p2 = pt29.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt30.rg;
  p2 = pt30.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt31.rg;
  p2 = pt31.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  p1 = pt32.rg;
  p2 = pt32.ba;
  v = p - p1;
  s = p2 - p1;
  dist2 = abs(((v.x * s.y) - (v.y * s.x)) / max(length(s), 0.01));
  dist2 = compare((v.x * s.x) + (v.y * s.y), length(v), dist2);
  w = p - p2;
  dist2 = compare((w.x * s.x) + (w.y * s.y), dist2, length(w));
  dist1 = min(dist1, dist2);
  interpolant = clamp(hw - dist1, 0.0, 1.0);
  interpolant = ((3.0 - (2.0 * interpolant)) * interpolant) * interpolant;
  return compare(vec4(dist1 - (hw - 1.0)), color, compare(vec4(dist1 - hw), color * interpolant, vec4(0.0))) * opacity;
}
;
- (void);
- (_Bool);
- (void);
- (void *);
- (id);
- (id);
- (void);
- (void);
- (long long);
- (id);
- (id);
- (unsigned long long)ChannelPosition;
- (void);
- (id);
- (void);
- (long long);
- (id)omation support library before listening for connections.;
- (id)ndler:%p] (tsmDocumentID=%p) /* Error: Ran out of types for this method. */;
- (id)۲;;
- (void);
- (void);
- (unsigned long long);
- (void *);
- (struct SpecializedFunctionTrackingData *);
- (id);
- (CDStruct_41a22ec7);
- (void);
- (void);
- (id);
- (id);
- (id);
- (unsigned char)|;
- (id);
- (void)vector;
- (const struct MTLFunctionData *);
- (id)D85ADD51;
- (void);
- (void)
;
- (id);
- (void);
- (unsigned long long)c;
- (void);
- (void);
- (void);
- (id);
- (void);
- (void);
- (void);
- (id);
- (id)A;
- (void);
- (id)sageENS3_5blockEE_FvSD_SE_EEE;
- (unsigned int)rceEEEEEvRT_NS_10option_setINS0_12region_usageEjEERKNSt3__18functionIFvSA_NS0_5blockEEEEEUlSA_mmE_;
- (const CDStruct_41a22ec7 *)!øAùqs01Â0@ù
× ;
- (id)TLStatSampleRec=^vQ[0Q]}16@0:8 /* Error: Ran out of types for this method. */;
- (id)ionIdHash>, std::__unordered_map_equal<MTLHashKey, std::pair<const MTLHashKey, std::tuple<unsigned long long, unsigned long long, unsigned long long, unsigned long long>>, CompareFunctionIdHash, CompareFunctionIdHash>, std::allocator<std::pair<const MTLHashKey, std::tuple<unsigned long long, unsigned long long, unsigned long long, unsigned long long>>>>="__bucket_list_"{unique_ptr<std::__hash_node_base<std::__hash_node<std::__hash_value_type<MTLHashKey, std::tuple<unsigned long long, unsigned long long, unsigned long long, unsigned long long>>, void *> *> *[], std::__bucket_list_deallocator<std::allocator<std::__hash_node_base<std::__hash_node<std::__hash_value_type<MTLHashKey, std::tuple<unsigned long long, unsigned long long, unsigned long long, unsigned long long>>, void *> *> *>>>=""{?="__ptr_"^^v"__deleter_"{__bucket_list_deallocator<std::allocator<std::__hash_node_base<std::__hash_node<std::__hash_value_type<MTLHashKey, std::tuple<unsigned long long, unsigned long long, unsigned long long, unsigned long long>>, void *> *> *>>=""{?="__size_"Q}}}}""{?="__first_node_"{__hash_node_base<std::__hash_node<std::__hash_value_type<MTLHashKey, std::tuple<unsigned long long, unsigned long long, unsigned long long, unsigned long long>>, void *> *>="__next_"^v}}""{?="__size_"Q}""{?="__max_load_factor_"f}}} /* Error: Ran out of types for this method. */;
- (void)resourceBlobForByteCodeSignature:(id)arg1 resourceName:error: /* Error: Ran out of types for this method. */;
- (unsigned long long)destinationUniqueIdentifier: /* Error: Ran out of types for this method. */;
- (void)r;
- (id)aSupported;
- (id);
- (id);
- (id);
- (void);
- (unsigned long long)rriers in render;
- (void)WorkgroupSizeFnAttr;
- (id)ì3;

@end

