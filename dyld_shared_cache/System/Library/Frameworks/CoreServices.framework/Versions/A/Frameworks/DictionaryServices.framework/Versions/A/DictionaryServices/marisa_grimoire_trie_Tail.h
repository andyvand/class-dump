//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class marisa::grimoire::trie::Tail {
public: // (access info unavailable from symbols alone)
    Tail();
    auto build(marisa::grimoire::vector::Vector<marisa::grimoire::trie::Entry>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa_tail_mode_);
    auto build(marisa::grimoire::vector::Vector<marisa::grimoire::trie::Entry>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa_tail_mode_) (.cold.1);
    auto build(marisa::grimoire::vector::Vector<marisa::grimoire::trie::Entry>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa_tail_mode_) (.cold.2);
    auto build_(marisa::grimoire::vector::Vector<marisa::grimoire::trie::Entry>&, marisa::grimoire::vector::Vector<unsigned int>*, marisa_tail_mode_);
    auto clear();
    auto map(marisa::grimoire::io::Mapper&);
    auto map_(marisa::grimoire::io::Mapper&);
    auto match(marisa::Agent&, unsigned long) const;
    auto prefix_match(marisa::Agent&, unsigned long) const;
    auto read(marisa::grimoire::io::Reader&);
    auto read_(marisa::grimoire::io::Reader&);
    auto restore(marisa::Agent&, unsigned long) const;
    auto swap(marisa::grimoire::trie::Tail&);
    auto write(marisa::grimoire::io::Writer&) const;
    auto write_(marisa::grimoire::io::Writer&) const;
    ~Tail();
};
