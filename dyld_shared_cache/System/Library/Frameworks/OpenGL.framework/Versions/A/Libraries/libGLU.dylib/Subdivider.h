//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class Subdivider {
public: // (access info unavailable from symbols alone)
    Subdivider(Renderhints&, Backend&);
    auto addArc(float*, Quilt*, int);
    auto addArc(int, TrimVertex*, int);
    auto addQuilt(Quilt*);
    auto arc_split(Arc*, int, float, int);
    auto beginQuilts();
    auto ccwTurn_sl(Arc*, Arc*);
    auto ccwTurn_sr(Arc*, Arc*);
    auto ccwTurn_tl(Arc*, Arc*);
    auto ccwTurn_tr(Arc*, Arc*);
    auto check_s(Arc*, Arc*);
    auto check_t(Arc*, Arc*);
    auto classify_headonleft_s(Bin&, Bin&, Bin&, float);
    auto classify_headonleft_t(Bin&, Bin&, Bin&, float);
    auto classify_headonright_s(Bin&, Bin&, Bin&, float);
    auto classify_headonright_t(Bin&, Bin&, Bin&, float);
    auto classify_tailonleft_s(Bin&, Bin&, Bin&, float);
    auto classify_tailonleft_t(Bin&, Bin&, Bin&, float);
    auto classify_tailonright_s(Bin&, Bin&, Bin&, float);
    auto classify_tailonright_t(Bin&, Bin&, Bin&, float);
    auto clear();
    auto decompose(Bin&, float);
    auto drawCurves();
    auto drawSurfaces(int);
    auto findIrregularS(Bin&);
    auto findIrregularT(Bin&);
    auto freejarcs(Bin&);
    auto join_s(Bin&, Bin&, Arc*, Arc*);
    auto join_t(Bin&, Bin&, Arc*, Arc*);
    auto makeBorderTrim(float const*, float const*);
    auto monosplitInS(Bin&, int, int);
    auto monosplitInT(Bin&, int, int);
    auto monotonize(Arc*, Bin&);
    auto nonSamplingSplit(Bin&, Patchlist&, int, int);
    auto outline(Bin&);
    auto partition(Bin&, Bin&, Bin&, Bin&, Bin&, int, float);
    auto render(Bin&);
    auto samplingSplit(Bin&, Patchlist&, int, int);
    auto samplingSplit(Curvelist&, int);
    auto setJumpbuffer(JumpBuffer*);
    auto split(Bin&, Bin&, Bin&, int, float);
    auto splitInS(Bin&, int, int);
    auto splitInT(Bin&, int, int);
    auto subdivideInS(Bin&);
    auto tessellate(Arc*, float);
    auto tessellate(Bin&, float, float, float, float);
    auto tessellate(Bin&, float, float, float, float) (.cold.1);
    auto tessellation(Bin&, Patchlist&);
    ~Subdivider();
};
