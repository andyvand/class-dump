//
//     Generated by class-dump 3.5 (64 bit).
//
//  Copyright (C) 1997-2019 Steve Nygard.
//

#import <CoreImage/CGRenderer.h>

__attribute__((visibility("hidden")))
@interface PNGRenderer : CGRenderer
{
    struct __CFURL *fullURL;
}

- (_Bool);
- (void);
- (struct CGImage *);
- (void);
- (id)Ja;
- (void)float4x4 m, metal:(id)arg1:texture2d<float, access::write> t );
    coreimage::group::destination_h group::make_destination(float2 c, uint2 gid, float4 r, float4x4 m, metal::texture2d<half,  access::write> t );
    coreimage::sampler_h make_sampler(texture2d<half,  access::sample> t, metal::sampler s, constant float4x4& m, float2 dc );
    coreimage::sampler   make_sampler(texture2d<float, access::sample> t, metal::sampler s, constant float4x4& m, float2 dc );
#endif //__CIKERNEL_METAL_VERSION__ >= 300
}
#define mod(x,y) ((x) - (y) * floor((x)/(y))) // GLSL defines mod() differently than Metal's fmod()
#define inversesqrt(x) rsqrt(x)
#define lessThan(a,b) (1.0 - step(b,a))
float atan (float x, float y); // overload of Metal's atan(y_over_x)
#define destCoord() _dc
#define writeCoord() static_cast<float2>(_dst.gid())
#define writeImage(c,p) _dst.write(c,p)
#define writeImagePlane(c,p) _dst.writePlane(c,p)
#endif /* __METAL_VERSION__ */
#endif /* CIKERNELMETALLIBPRIVATE_H */
// Copyright 2015 Apple Inc.
#ifndef __BUILDING_RUNTIME_COMPILE_HEADER__
#include "CIKernelMetalLib.h"
#include "CIKernelMetalLibPrivate.h"
#endif //__BUILDING_RUNTIME_COMPILE_HEADER__
using namespace metal;
#define KERNEL_OUTPUT_SIZE(destTransform) destTransform[3].xy
#define KERNEL_THREAD_COUNT(destTransform) static_cast<uint2>(destTransform[3].zw)
#define CI_ALWAYS_INLINE __attribute__((always_inline))
static float2 _affineTransform(float4x4 m, float2 p)
{
    return float2(dot(float3(p,1.0), m[0].xyz),
                  dot(float3(p,1.0), m[1].xyz));
}
#ifndef _BUILDING_CORE_IMAGE_LIB_ //[
template <typename T, typename T4>
CI_ALWAYS_INLINE T4 _read_pixel(texture2d<T, access::sample> image, sampler s, float2 p, float3x3 transform)
{
    float3 loc = float3(p, 1.0) * transform;
    return image.sample(s, loc.xy);
}
template <typename T, typename T4>
CI_ALWAYS_INLINE T4 _read_pixel_420(texture2d<T, access::sample> Y, sampler sY, float2 p, float3x3 transform,
                                    texture2d<T, access::sample> CC, sampler sCC, float2 f)
{
    float3 loc = float3(p, 1.0) * transform;
    return T4(Y.sample(sY, loc.xy).r, CC.sample(sCC, f*loc.xy).rg, 1.0);
}
template <typename T, typename T4>
CI_ALWAYS_INLINE T4 _read_pixel_420_r_rg(texture2d<T, access::sample> Y, sampler sY, float2 p, float3x3 transform,
                                         texture2d<T, access::sample> CC, sampler sCC, float2 f)
{
    float3 loc = float3(p, 1.0) * transform;
    return T4(Y.sample(sY, loc.xy).r, CC.sample(sCC, f*loc.xy).rg, 1.0);
}
// The biplanar packed 10-bit 420 format 'pf20' and 'p420' combine the horizontally neighboring luma
// values in one 32-bit word for the luma plane.  We bind this format at RGB10A2 as it aligns with how
// they do this.  This code is used to index and pull out the right value.  For the Chroma it is a bit
// more complex as three pairs of CrCb are packed into two 32-bit words and the second one straddles
// two samples.
//TODO check that a scaled image is getting the chroma correctly interpolated
//TODO handle the left and right edges for the chroma
template <typename T, typename T2, typename T4>
CI_ALWAYS_INLINE T4 _read_pixel_420_packed(texture2d<T, access::sample> Y,  sampler sY,  float2 c, float3x3 transform,
                                           texture2d<T, access::sample> CC, sampler sCC, float2 f)
{
    int cInt = int(c.x);
    int cInt2 = int(c.x*0.5);
    int lumaX = cInt/3;
    float3 pLuma = float3(lumaX+0.5, c.y, 1.0) * transform;
    int chromaX = (cInt2/3)*2;
    float3 pChromaM1 = float3(2.0*(chromaX - 0.5), c.y, 1.0) * transform;
    float3 pChroma1 = float3(2.0*(chromaX + 0.5), c.y, 1.0) * transform;
    float3 pChroma2 = float3(2.0*(chromaX + 1.5), c.y, 1.0) * transform;
    float3 pChroma3 = float3(2.0*(chromaX + 2.5), c.y, 1.0) * transform;
    T4 lumaGroup = Y.sample(sY, pLuma.xy);
    T4 chromaM1 = CC.sample(sCC, pChromaM1.xy * f);
    T4 chroma1 = CC.sample(sCC, pChroma1.xy * f);
    T4 chroma2 = CC.sample(sCC, pChroma2.xy * f);
    T4 chroma3 = CC.sample(sCC, pChroma3.xy * f);
    T2 crcbM1 = chromaM1.yx;
    T2 crcb0 = chroma1.zy;
    T2 crcb1 = T2(chroma1.x,chroma2.z);
    T2 crcb2 = chroma2.yx;
    T2 crcb3 = chroma3.zy;
    T4 out = T4(0.0, 0.0, 0.0, 1.0);
    int lumaRemainder = cInt - lumaX*3;
    out.x = (lumaRemainder==0) ? lumaGroup.z :(lumaRemainder==1) ? lumaGroup.y :lumaGroup.x;
    //Perform the chroma interpolation
    int chromaRemainder = cInt - (cInt/6)*6;  // dont use  % operator
    if (chromaRemainder == 0){
        out.yz = mix(crcbM1, crcb0, 0.75);
    } else if (chromaRemainder == 1) {
        out.yz = mix(crcb0, crcb1, 0.25);
    } else if (chromaRemainder == 2) {
        out.yz = mix(crcb0, crcb1, 0.75);
    } else if (chromaRemainder == 3) {
        out.yz = mix(crcb1, crcb2, 0.25);
    } else if (chromaRemainder == 4) {
        out.yz = mix(crcb1, crcb2, 0.75);
    } else if (chromaRemainder == 5) {
        out.yz = mix(crcb2, crcb3, 0.25);
    }
    return out;
}
#endif //] _BUILDING_CORE_IMAGE_LIB_
template <typename T, typename T4>
CI_ALWAYS_INLINE void _write_pixel(texture2d<T, access::write> outputTexture, const T4 result, const uint2 gid, const float4 outputRect)
{
    uint2 loc = gid + static_cast<uint2>(outputRect.xy);
    loc.y = loc.y + lessThan(outputRect.w, 0) * ((-outputRect.w-1)-2*gid.y); // for 'needsVerticalFlip'
    outputTexture.write(result, loc);
}
template <typename T, typename T4>
CI_ALWAYS_INLINE void _destinationWrite(texture2d<T, access::write> t, float4x4 m, float4 r, T4 v, float2 dc)
{
    const float2 p = _affineTransform(m, dc);
    const uint2 gid = static_cast<uint2>(p - r.xy); // assuming p >= r.xy
    if (all(gid < static_cast<uint2>(abs(r.zw))))
    {
        _write_pixel<T,T4>(t, v, gid, r);
    }
}
#ifndef _BUILDING_CI_STDLIB_STITCHABLE_ //[
//MARK:- CIKernelMetalLib
namespace coreimage
{
#if __CIKERNEL_METAL_VERSION__ >= 300
coreimage::group::destination group::make_destination(float2 c, uint2 gid, float4 r, float4x4 m, metal::texture2d<float, access::write> t )
{
    return coreimage::group::Destination(c,gid,r,m,t);
}
coreimage::group::destination_h group::make_destination(float2 c, uint2 gid, float4 r, float4x4 m, metal::texture2d<half, access::write> t )
{
    return coreimage::group::Destination_h(c,gid,r,m,t);
}
coreimage::sampler_h make_sampler(texture2d<half, access::sample> t, metal::sampler s, constant float4x4& m, float2 dc )
{
    return coreimage::Sampler_h(t,s,m,dc);
}
coreimage::sampler make_sampler(texture2d<float, access::sample> t, metal::sampler s, constant float4x4& m, float2 dc )
{
    return coreimage::Sampler(t,s,m,dc);
}
#endif //__CIKERNEL_METAL_VERSION__ >= 300
    static float4 _samplerExtent(float4x4 m)
    {
        return m[3]; // extent packed in last (unused) row of 4x4 transform matrix
    }
#ifdef __CIKERNEL_METAL_SUPPORTS_GATHER__ //[
    static half2 _gatherTransform(half2x2 m, half2 p)
    {
        return p * m + 0.5;
    }
    #define GATHER_COORD_TO_IDX(p) short(p.x + p.y * 2)
    template <typename T>
    static T _gatherOrdered(float4x4 m, T v)
    {
        // reorder samples into linear buffer based on Metal's specified order of gathered samples:v[0] v[1]
        v = v.wzxy; //                                                                              v[2] v[3]
        // reduce transform to orientation matrix for unit extent of [-0.5 -0.5 1 1], centered about the origin
        half2x2 orientation = half2x2(sign(m[0][0]),sign(m[0][1]),
                                      sign(m[1][0]),sign(m[1][1]));
        // transform gather coords based on sampler transform
        half2 p00 = _gatherTransform(orientation, half2(-0.5,-0.5));
        half2 p10 = _gatherTransform(orientation, half2( 0.5,-0.5));
        half2 p11 = _gatherTransform(orientation, half2( 0.5, 0.5));
        half2 p01 = _gatherTransform(orientation, half2(-0.5, 0.5));
        // convert gather coords to buffer indices, and return samples in CCW order starting w/ sample at the lower left
        return T(v[GATHER_COORD_TO_IDX(p00)],
                 v[GATHER_COORD_TO_IDX(p10)],
                 v[GATHER_COORD_TO_IDX(p11)],
                 v[GATHER_COORD_TO_IDX(p01)]);
    }
#endif //] __CIKERNEL_METAL_SUPPORTS_GATHER__
#pragma mark - sampler
    // Returns the pixel value produced from sampler at the position p, where p is specified in sampler space.
    float4 sampler::sample(float2 p) const
    {
        return t.sample(s, p);
    }
    // Returns the position in the coordinate space of the source that is associated with the position defined in working-space coordinates.
    float2 sampler::transform(float2 p) const
    {
        return _affineTransform(m, p);
    }
    // Returns the position, in sampler space, of the sampler that is associated with the current output pixel (that is, after any transformation matrix
    // associated with sampler is applied). The sample space refers to the coordinate space of that you are texturing from. Note that if your source data is tiled,
    // the sample coordinate will have an offset (dx/dy). You can convert a destination location to the sampler location using the transform function.
    float2 sampler::coord() const
    {
        return transform(dc);
    }
    // Returns the extent of the sampler in world coordinates, as a four-element vector [x, y, width, height].
    float4 sampler::extent() const
    {
        return _samplerExtent(m);
    }
#ifdef __CIKERNEL_METAL_SUPPORTS_GATHER__ //[
    // Returns four samples that would be used for bilinear interpolation when sampling at the position p, where p is specified in sampler space.
    float4 sampler::gatherX(float2 p) const
    {
        return _gatherOrdered(m, t.gather(s, p, int2(0), component::x));
    }
    float4 sampler::gatherY(float2 p) const
    {
        return _gatherOrdered(m, t.gather(s, p, int2(0), component::y));
    }
    float4 sampler::gatherZ(float2 p) const
    {
        return _gatherOrdered(m, t.gather(s, p, int2(0), component::z));
    }
    float4 sampler::gatherW(float2 p) const
    {
        return _gatherOrdered(m, t.gather(s, p, int2(0), component::w));
    }
    // Returns four (unordered) samples that would be used for bilinear interpolation when sampling at the position p, where p is specified in sampler space.
    float4 sampler::gatherX_unordered(float2 p) const
    {
        return t.gather(s, p, int2(0), component::x);
    }
    float4 sampler::gatherY_unordered(float2 p) const
    {
        return t.gather(s, p, int2(0), component::y);
    }
    float4 sampler::gatherZ_unordered(float2 p) const
    {
        return t.gather(s, p, int2(0), component::z);
    }
    float4 sampler::gatherW_unordered(float2 p) const
    {
        return t.gather(s, p, int2(0), component::w);
    }
#endif //] __CIKERNEL_METAL_SUPPORTS_GATHER__
#pragma mark - sampler_h
    // Returns the pixel value produced from sampler at the position p, where p is specified in sampler space.
    half4 sampler_h::sample(float2 p) const
    {
        return t.sample(s, p);
    }
    // Returns the position in the coordinate space of the source that is associated with the position defined in working-space coordinates.
    float2 sampler_h::transform(float2 p) const
    {
        return _affineTransform(m, p);
    }
    // Returns the position, in sampler space, of the sampler that is associated with the current output pixel (that is, after any transformation matrix
    // associated with sampler is applied). The sample space refers to the coordinate space of that you are texturing from. Note that if your source data is tiled,
    // the sample coordinate will have an offset (dx/dy). You can convert a destination location to the sampler location using the transform function.
    float2 sampler_h::coord() const
    {
        return transform(dc);
    }
    // Returns the extent of the sampler in world coordinates, as a four-element vector [x, y, width, height].
    float4 sampler_h::extent() const
    {
        return _samplerExtent(m);
    }
#ifdef __CIKERNEL_METAL_SUPPORTS_GATHER__ //[
    // Returns four samples that would be used for bilinear interpolation when sampling at the position p, where p is specified in sampler space.
    half4 sampler_h::gatherX(float2 p) const
    {
        return _gatherOrdered(m, t.gather(s, p, int2(0), component::x));
    }
    half4 sampler_h::gatherY(float2 p) const
    {
        return _gatherOrdered(m, t.gather(s, p, int2(0), component::y));
    }
    half4 sampler_h::gatherZ(float2 p) const
    {
        return _gatherOrdered(m, t.gather(s, p, int2(0), component::z));
    }
    half4 sampler_h::gatherW(float2 p) const
    {
        return _gatherOrdered(m, t.gather(s, p, int2(0), component::w));
    }
    // Returns four (unordered) samples that would be used for bilinear interpolation when sampling at the position p, where p is specified in sampler space.
    half4 sampler_h::gatherX_unordered(float2 p) const
    {
        return t.gather(s, p, int2(0), component::x);
    }
    half4 sampler_h::gatherY_unordered(float2 p) const
    {
        return t.gather(s, p, int2(0), component::y);
    }
    half4 sampler_h::gatherZ_unordered(float2 p) const
    {
        return t.gather(s, p, int2(0), component::z);
    }
    half4 sampler_h::gatherW_unordered(float2 p) const
    {
        return t.gather(s, p, int2(0), component::w);
    }
#endif //] __CIKERNEL_METAL_SUPPORTS_GATHER__
#pragma mark -
    // Global equivalents of sampler struct member functions, to facilitate porting of non-Metal legacy CI kernels.
    float4 sample (sampler src, float2 p) { return src.sample(p); }
    float2 samplerTransform (sampler src, float2 p) { return src.transform(p); }
    float2 samplerCoord (sampler src) { return src.coord(); }
    float4 samplerExtent (sampler src) { return src.extent(); }
    float2 samplerOrigin (sampler src) { return src.origin(); }
    float2 samplerSize (sampler src) { return src.size(); }
    float4 gatherX (sampler src, float2 p) { return src.gatherX(p); }
    float4 gatherY (sampler src, float2 p) { return src.gatherY(p); }
    float4 gatherZ (sampler src, float2 p) { return src.gatherZ(p); }
    float4 gatherW (sampler src, float2 p) { return src.gatherW(p); }
    float4 _unordered_gatherX (sampler src, float2 p) { return src.gatherX_unordered(p); }
    float4 _unordered_gatherY (sampler src, float2 p) { return src.gatherY_unordered(p); }
    float4 _unordered_gatherZ (sampler src, float2 p) { return src.gatherZ_unordered(p); }
    float4 _unordered_gatherW (sampler src, float2 p) { return src.gatherW_unordered(p); }
    namespace group
    {
        // Writes 4 color values to the destination texture for the current 2x2 group of pixels.
        void destination::write(float4 v0, float4 v1, float4 v2, float4 v3)
        {
#if __CIKERNEL_SUPPORTS_VARIABLE_GROUPSIZE__
            float4x4 v = float4x4(v0, v1, v2, v3);
            for (short i = 0; i < 4; i++)
            {
                short dx = i % short(KERNEL_OUTPUT_SIZE(m).x);
                short dy = i / short(KERNEL_OUTPUT_SIZE(m).y);
                _destinationWrite(t, m, r, v[i], c + float2(dx,dy));
            }
#else
            _destinationWrite(t, m, r, v0, c);
            _destinationWrite(t, m, r, v1, c + float2(1,0));
            _destinationWrite(t, m, r, v2, c + float2(0,1));
            _destinationWrite(t, m, r, v3, c + float2(1,1));
#endif
        }
        // Writes 4 color values to the destination texture for the current 2x2 group of pixels.
        void destination_h::write(half4 v0, half4 v1, half4 v2, half4 v3)
        {
#if __CIKERNEL_SUPPORTS_VARIABLE_GROUPSIZE__
            half4x4 v = half4x4(v0, v1, v2, v3);
            for (short i = 0; i < 4; i++)
            {
                short dx = i % short(KERNEL_OUTPUT_SIZE(m).x);
                short dy = i / short(KERNEL_OUTPUT_SIZE(m).y);
                _destinationWrite(t, m, r, v[i], c + float2(dx,dy));
            }
#else
            _destinationWrite(t, m, r, v0, c);
            _destinationWrite(t, m, r, v1, c + float2(1,0));
            _destinationWrite(t, m, r, v2, c + float2(0,1));
            _destinationWrite(t, m, r, v3, c + float2(1,1));
#endif
        }
    }
#pragma mark - math
    float4 premultiply(float4 s) { return float4(s.xyz* s.w, s.w); }
    half4 premultiply(half4 s) { return half4(s.xyz* s.w, s.w); }
    float4 unpremultiply(float4 s) { return float4(s.xyz/ fmax(s.w, 0.00001), s.w); }
    half4 unpremultiply(half4 s) { return half4(s.xyz/ fmax(s.w, 0.0001h), s.w); }
    #define _srgb_to_linear(s,t) sign(s) * mix(abs(s)*0.077399380804954##t, pow(abs(s)*0.947867298578199##t + 0.052132701421801##t, 2.4##t), step(0.04045##t, abs(s)))
    float3 srgb_to_linear(float3 s) { return _srgb_to_linear(s, f); }
    half3 srgb_to_linear(half3 s) { return _srgb_to_linear(s, h); }
    float4 srgb_to_linear(float4 s)
    {
        s = float4(unpremultiply(s));
        s.rgb = _srgb_to_linear(s.rgb, f);
        return premultiply(s);
    }
    half4 srgb_to_linear(half4 s)
    {
        s = half4(unpremultiply(s));
        s.rgb = _srgb_to_linear(s.rgb, h);
        return premultiply(s);
    }
    #define _linear_to_srgb(s,t) sign(s) * mix(abs(s)*12.92##t, pow(abs(s), 0.4166667##t) * 1.055##t - 0.055##t, step(0.0031308##t, abs(s)))
    float3 linear_to_srgb(float3 s) { return _linear_to_srgb(s, f); }
    half3 linear_to_srgb(half3 s) { return _linear_to_srgb(s, h); }
    float4 linear_to_srgb(float4 s)
    {
      s = float4(unpremultiply(s));
      s.rgb = _linear_to_srgb(s.rgb, f);
      return premultiply(s);
    }
    half4 linear_to_srgb(half4 s)
    {
      s = half4(unpremultiply(s));
      s.rgb = _linear_to_srgb(s.rgb, h);
      return premultiply(s);
    }
    #define _compare(x,y,z) select(z, y, signbit(x)) // for each component, returns x < 0 ? y :z
    float  compare(float  x, float  y, float  z) { return _compare(x, y, z); }
    float2 compare(float2 x, float2 y, float2 z) { return _compare(x, y, z); }
    float3 compare(float3 x, float3 y, float3 z) { return _compare(x, y, z); }
    float4 compare(float4 x, float4 y, float4 z) { return _compare(x, y, z); }
    half   compare(half   x, half   y, half   z) { return _compare(x, y, z); }
    half2  compare(half2  x, half2  y, half2  z) { return _compare(x, y, z); }
    half3  compare(half3  x, half3  y, half3  z) { return _compare(x, y, z); }
    half4  compare(half4  x, half4  y, half4  z) { return _compare(x, y, z); }
    float2 cossin(float x)
    {
        float c; float s = metal::sincos(x, c);
        return float2(c, s);
    }
    float2 sincos(float x)
    {
        float c; float s = metal::sincos(x, c);
        return float2(s, c);
    }
}
//MARK:- CIKernelMetalLibPrivate
namespace coreimage
{
    half4 sample (sampler_h src, float2 p) { return src.sample(p); }
    float2 samplerTransform (sampler_h src, float2 p) { return src.transform(p); }
    float2 samplerCoord (sampler_h src) { return src.coord(); }
    float4 samplerExtent (sampler_h src) { return src.extent(); }
    float2 samplerOrigin (sampler_h src) { return src.origin(); }
    float2 samplerSize (sampler_h src) { return src.size(); }
    namespace priv
    {
        uint2 destination::gid(int2 offset) const
        {
            return as_type<uint2>(as_type<int2>(_gid) * static_cast<int2>(KERNEL_OUTPUT_SIZE(_transform)) + static_cast<int2>(_extent.xy) + offset);
        }
        uint2 destination_h::gid(int2 offset) const
        {
            return as_type<uint2>(as_type<int2>(_gid) * static_cast<int2>(KERNEL_OUTPUT_SIZE(_transform)) + static_cast<int2>(_extent.xy) + offset);
        }
    }
}
float atan (float x, float y) { return atan2(x,y); }
#endif //] _BUILDING_CI_STDLIB_STITCHABLE_
#ifndef _BUILDING_CORE_IMAGE_LIB_
//MARK:- Extern Functions
#ifndef _BUILDING_FOR_UBER_SHADER_
extern "C" {
#endif// _BUILDING_FOR_UBER_SHADER_
// base functions for all main programs
FUNCTION_ATTRIBUTES bool _ci_early_out_nothing(uint2 gid, float4x4 transform)
{
    return false;
}
FUNCTION_ATTRIBUTES bool _ci_early_out_imageblock_nothing(uint2 gid, float4 outputRect)
{
    return false;
}
FUNCTION_ATTRIBUTES bool _ci_early_out(uint2 gid, float4x4 transform)
{
    return any(gid >= KERNEL_THREAD_COUNT(transform));
}
FUNCTION_ATTRIBUTES bool _ci_early_out_imageblock(uint2 gid, float4 outputRect)
{
    return !all(outputRect.xy <= static_cast<float2>(gid) && static_cast<float2>(gid) < outputRect.xy + abs(outputRect.zw));
}
FUNCTION_ATTRIBUTES float2 _ci_init_destcoord(uint2 gid, float4 outputRect, float4x4 transform)
{
    float2 dc = static_cast<float2>(gid) * KERNEL_OUTPUT_SIZE(transform) + KERNEL_OUTPUT_SIZE(transform)/2 + outputRect.xy;
    return _affineTransform(transform, dc) - KERNEL_OUTPUT_SIZE(transform)/2 + 0.5;
}
FUNCTION_ATTRIBUTES float2 _ci_init_destcoord_imageblock(uint2 gid, float4 /*outputRect*/, float4x4 transform)
{
    float2 dc = static_cast<float2>(gid) * KERNEL_OUTPUT_SIZE(transform) + KERNEL_OUTPUT_SIZE(transform)/2;
    return _affineTransform(transform, dc) - KERNEL_OUTPUT_SIZE(transform)/2 + 0.5;
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE float4 _ci_read_pixel(texture2d<float, access::sample> image, sampler s, float2 p, float3x3 transform)
{
    return _read_pixel<float,float4>(image, s, p, transform);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE half4 _ci_read_pixel_h(texture2d<half, access::sample> image, sampler s, float2 p, float3x3 transform)
{
    return _read_pixel<half,half4>(image, s, p, transform);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE float4 _ci_read_pixel_420(texture2d<float, access::sample> Y, sampler sY, float2 p, float3x3 transform,
                          texture2d<float, access::sample> CC, sampler sCC, float2 f)
{
    return _read_pixel_420<float,float4>(Y, sY, p, transform, CC, sCC, f);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE half4 _ci_read_pixel_420_h(texture2d<half, access::sample> Y, sampler sY, float2 p, float3x3 transform,
                           texture2d<half, access::sample> CC, sampler sCC, float2 f)
{
    return _read_pixel_420<half,half4>(Y, sY, p, transform, CC, sCC, f);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE float4 _ci_read_pixel_420_r_rg(texture2d<float, access::sample> Y, sampler sY, float2 p, float3x3 transform,
                                                texture2d<float, access::sample> CC, sampler sCC, float2 f)
{
    return _read_pixel_420_r_rg<float,float4>(Y, sY, p, transform, CC, sCC, f);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE half4 _ci_read_pixel_420_r_rg_h(texture2d<half, access::sample> Y, sampler sY, float2 p, float3x3 transform,
                                                 texture2d<half, access::sample> CC, sampler sCC, float2 f)
{
    return _read_pixel_420_r_rg<half,half4>(Y, sY, p, transform, CC, sCC, f);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE float4 _ci_read_pixel_420_packed(texture2d<float, access::sample> Y, sampler sY, float2 p, float3x3 transform,
                                                  texture2d<float, access::sample> CC, sampler sCC, float2 f)
{
    return _read_pixel_420_packed<float,float2,float4>(Y, sY, p, transform, CC, sCC, f);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE half4 _ci_read_pixel_420_packed_h(texture2d<half, access::sample> Y, sampler sY, float2 p, float3x3 transform,
                                                   texture2d<half, access::sample> CC, sampler sCC, float2 f)
{
    return _read_pixel_420_packed<half,half2,half4>(Y, sY, p, transform, CC, sCC, f);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE void _ci_write_pixel(texture2d<float, access::write> outputTexture, const float4 result, const uint2 gid, const float4 outputRect)
{
    _write_pixel<float,float4>(outputTexture, result, gid, outputRect);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE void _ci_write_pixel_h(texture2d<half, access::write> outputTexture, const half4 result, const uint2 gid, const float4 outputRect)
{
    _write_pixel<half,half4>(outputTexture, result, gid, outputRect);
}
#if !TARGET_OS_OSX // imageblocks supported on iOS only
namespace coreimage
{
    struct PixelData
    {
        half4 c [[color(0)]];
    };
}
FUNCTION_ATTRIBUTES void _ci_write_block_h(imageblock<coreimage::PixelData> imageBlock, const half4 result, const ushort2 lid, const float4 outputRect)
{
    coreimage::PixelData output = { result };
    imageBlock.write(output, lid);
}
#endif
FUNCTION_ATTRIBUTES float4 _ci_group_output_00(float4 results)
{
    return float4(results[0], 0, 0, 1);
}
FUNCTION_ATTRIBUTES float4 _ci_group_output_10(float4 results)
{
    return float4(results[1], 0, 0, 1);
}
FUNCTION_ATTRIBUTES float4 _ci_group_output_01(float4 results)
{
    return float4(results[2], 0, 0, 1);
}
FUNCTION_ATTRIBUTES float4 _ci_group_output_11(float4 results)
{
    return float4(results[3], 0, 0, 1);
}
FUNCTION_ATTRIBUTES half4 _ci_group_output_00_h(half4 results)
{
    return half4(results[0], 0, 0, 1);
}
FUNCTION_ATTRIBUTES half4 _ci_group_output_10_h(half4 results)
{
    return half4(results[1], 0, 0, 1);
}
FUNCTION_ATTRIBUTES half4 _ci_group_output_01_h(half4 results)
{
    return half4(results[2], 0, 0, 1);
}
FUNCTION_ATTRIBUTES half4 _ci_group_output_11_h(half4 results)
{
    return half4(results[3], 0, 0, 1);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE void _ci_group_write_00(texture2d<float, access::write> outputTexture, float4 result, float2 dc, float4 outputRect, float4x4 transform)
{
    _destinationWrite(outputTexture, transform, outputRect, result, dc);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE void _ci_group_write_10(texture2d<float, access::write> outputTexture, float4 result, float2 dc, float4 outputRect, float4x4 transform)
{
    _destinationWrite(outputTexture, transform, outputRect, result, dc + float2(1,0));
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE void _ci_group_write_01(texture2d<float, access::write> outputTexture, float4 result, float2 dc, float4 outputRect, float4x4 transform)
{
    _destinationWrite(outputTexture, transform, outputRect, result, dc + float2(0,1));
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE void _ci_group_write_11(texture2d<float, access::write> outputTexture, float4 result, float2 dc, float4 outputRect, float4x4 transform)
{
    _destinationWrite(outputTexture, transform, outputRect, result, dc + float2(1,1));
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE void _ci_group_write_00_h(texture2d<half, access::write> outputTexture, half4 result, float2 dc, float4 outputRect, float4x4 transform)
{
    _destinationWrite(outputTexture, transform, outputRect, result, dc);
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE void _ci_group_write_10_h(texture2d<half, access::write> outputTexture, half4 result, float2 dc, float4 outputRect, float4x4 transform)
{
    _destinationWrite(outputTexture, transform, outputRect, result, dc + float2(1,0));
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE void _ci_group_write_01_h(texture2d<half, access::write> outputTexture, half4 result, float2 dc, float4 outputRect, float4x4 transform)
{
    _destinationWrite(outputTexture, transform, outputRect, result, dc + float2(0,1));
}
FUNCTION_ATTRIBUTES CI_ALWAYS_INLINE void _ci_group_write_11_h(texture2d<half, access::write> outputTexture, half4 result, float2 dc, float4 outputRect, float4x4 transform)
{
    _destinationWrite(outputTexture, transform, outputRect, result, dc + float2(1,1));
}
// for kIntermediateNeedsSRGBConversion support
FUNCTION_ATTRIBUTES float4 _ci_srgb_to_linear(float4 s)
{
    s.rgb = ci::srgb_to_linear(s.rgb);
    return s;
}
FUNCTION_ATTRIBUTES half4 _ci_srgb_to_linear_h(half4 s)
{
    s.rgb = ci::srgb_to_linear(s.rgb);
    return s;
}
FUNCTION_ATTRIBUTES float4 _ci_linear_to_srgb(float4 s)
{
    s.rgb = ci::linear_to_srgb(s.rgb);
    return s;
}
FUNCTION_ATTRIBUTES half4 _ci_linear_to_srgb_h(half4 s)
{
    s.rgb = ci::linear_to_srgb(s.rgb);
    return s;
}
// for half support
FUNCTION_ATTRIBUTES half4 _ci_float_to_half(float4 f)
{
    return static_cast<half4>(f);
}
FUNCTION_ATTRIBUTES float4 _ci_half_to_float(half4 h)
{
    return static_cast<float4>(h);
}
#if __CIKERNEL_METAL_VERSION__ >= 300
FUNCTION_ATTRIBUTES coreimage::sampler_h _ci_sampler_builder_h(texture2d<half, access::sample> t, 
                                                               metal::sampler s, 
                                                               constant float4x4& m, 
                                                               float2 dc )
{
    return coreimage::make_sampler( t, s, m, dc);
}
FUNCTION_ATTRIBUTES coreimage::sampler _ci_sampler_builder(texture2d<float, access::sample> t, 
                                                           metal::sampler s, 
                                                           constant float4x4& m, 
                                                           float2 dc )
{
    return coreimage::make_sampler( t, s, m, dc);
}
FUNCTION_ATTRIBUTES coreimage::group::destination _ci_group_destination_builder(float2 c, 
                                                                                uint2 gid, 
                                                                                float4 extent, 
                                                                                float4x4 transform, 
                                                                                metal::texture2d<float, access::write> t )
{
    return coreimage::group::make_destination( c, gid, extent, transform, t);
}
FUNCTION_ATTRIBUTES coreimage::group::destination_h _ci_group_destination_builder_h(float2 c, 
                                                                                    uint2 gid, 
                                                                                    float4 extent, 
                                                                                    float4x4 transform, 
                                                                                    metal::texture2d<half, access::write> t )
{
    return coreimage::group::make_destination( c, gid, extent, transform, t);
}
FUNCTION_ATTRIBUTES coreimage::priv::destination _ci_dest_struct_builder(float2 c, 
                                                                         uint2 gid, 
                                                                         float4 extent, 
                                                                         float4x4 transform,  
                                                                         metal::texture2d<float, access::write> texture,  
                                                                         metal::texture2d<float, access::write> texturePlane)
{
    return coreimage::priv::destination(c, gid, extent, transform, texture, texturePlane);
}
FUNCTION_ATTRIBUTES coreimage::priv::destination_h _ci_dest_struct_builder_h(float2 c, 
                                                                             uint2 gid, 
                                                                             float4 extent, 
                                                                             float4x4 transform, 
                                                                             metal::texture2d<half, access::write> texture, 
                                                                             metal::texture2d<half, access::write> texturePlane)
{
    return coreimage::priv::destination_h(c, gid, extent, transform, texture, texturePlane);
}
#endif //__CIKERNEL_METAL_VERSION__ >= 300
FUNCTION_ATTRIBUTES void _ci_writeSIMD_420 (coreimage::sample_t color, coreimage::priv::destination _dst)
{
    float2 p = static_cast<float2>(_dst.gid());
    // For each in every 2x2 cell, we write to the luminance plane
    _dst.write(color.rrrr, p);
    // The threadgroup size is hard-coded to 16x16 in MetalContext::render_node()
    // Instead we should use the option kCIKernelThreadsPerThreadgroup.
    // The 'simd_shuffle_down()' allows 'cc' to be read from other threads in the group.
    float2 cc = float2(color.gb);
    float2 ccSum;
    ccSum  = simd_shuffle_down(cc,  0);
    ccSum += simd_shuffle_down(cc,  1);
    ccSum += simd_shuffle_down(cc, 16);
    ccSum += simd_shuffle_down(cc, 17);
    // For once in every 2x2 cell, we write to the chroma plane
    const int x = int(p.x);
    const int y = int(p.y);
    if ((x % 2) == 0 && (y % 2) == 0)
        _dst.writePlane(ccSum.rgrg * 0.25, p / 2.0); 
}
FUNCTION_ATTRIBUTES void _ci_writeSIMD_422 (coreimage::sample_t color, coreimage::priv::destination _dst)
{
    float2 p = static_cast<float2>(_dst.gid());
    // For each in every 2x1 cell, we write to the luminance plane
    _dst.write(color.rrrr, p);
    // The threadgroup size is hard-coded to 16x16 in MetalContext::render_node()
    // Instead we should use the option kCIKernelThreadsPerThreadgroup.
    // The 'simd_shuffle_down()' allows 'cc' to be read from other threads in the group.
    float2 cc = float2(color.gb);
    float2 ccSum;
    ccSum  = simd_shuffle_down(cc,  0);
    ccSum += simd_shuffle_down(cc,  1);
    // For once in every 2x1 cell, we write to the chroma plane
    const int x = int(p.x);
    if ((x % 2) == 0)
        _dst.writePlane(ccSum.rgrg * 0.5, p / float2(2., 1.));
}
FUNCTION_ATTRIBUTES float4 _ci_writeSIMD_42X (coreimage::sample_t color, float4 shuffleMask, coreimage::priv::destination _dst)
{
    return color; // dummy return value
}
#define _CI_WRITETG_420_BODY_\
   /* The threadgroup size is hard-coded to 16x16 in MetalContext::render_node()
      Instead we should use the option kCIKernelThreadsPerThreadgroup.*/\
   const int grid = 16; \
   threadgroup float4 tgcc[grid * grid]; /* threadgroup array is initialized below */ \
    \
   const float2 py = static_cast<float2>(_dst.gid()); \
   const int x = int(py.x); \
   const int y = int(py.y); \
   const int index = ((y % grid) * grid) + (x % grid); \
   if (x % 2 == 0 && y % 2 == 0) \
   { \
       /* Initialize threadGroup memory */\
       tgcc[index]            = float4(0., 0., 0., 0.); \
       tgcc[index + grid]     = float4(0., 0., 0., 0.); \
       tgcc[index + 1]        = float4(0., 0., 0., 0.); \
       tgcc[index + 1 + grid] = float4(0., 0., 0., 0.); \
   } \
   /* Uses barriers to ensure order of read / write to threadgroup memory
      simdgroup_barrier are low overhead */\
   simdgroup_barrier(mem_flags::mem_threadgroup); \
   tgcc[index] = float4(color.g, color.b, 1.0, 1.0); \
   simdgroup_barrier(mem_flags::mem_threadgroup); \
    /* For once in every 2x2 cell, we write to the chroma plane */\
   if (x % 2 == 0 && y % 2 == 0) \
   { \
       /* Read from threadGroup memory*/\
       float4 ccvv = float4(tgcc[index]) \
                   + float4(tgcc[index + 1]) \
                   + float4(tgcc[index + grid]) \
                   + float4(tgcc[index + 1 + grid]); \
    \
        /* write subsampled output to chroma plane */\
       _dst.writePlane(ccvv.rgrg / ccvv.a, py / 2.0); \
   } \
    /* For each in every 2x2 cell, we write to the luminance plane */\
   _dst.write(color.rrrr, py);
#define _CI_WRITETG_422_BODY_\
    /* The threadgroup size is hard-coded to 16x16 in MetalContext::render_node()
      Instead we should use the option kCIKernelThreadsPerThreadgroup.*/ \
    const int grid = 16; \
    threadgroup float4 tgcc[grid * grid]; /* threadgroup array is initialized below */\
     \
    const float2 py = static_cast<float2>(_dst.gid()); \
    const int x = int(py.x); \
    const int y = int(py.y); \
    const int index = ((y % grid) * grid) + (x % grid); \
    if (x % 2 == 0 && y % 2 == 0) \
    { \
        /* Initialize threadGroup memory */\
        tgcc[index]            = float4(0., 0., 0., 0.); \
        tgcc[index + grid]     = float4(0., 0., 0., 0.); \
        tgcc[index + 1]        = float4(0., 0., 0., 0.); \
        tgcc[index + 1 + grid] = float4(0., 0., 0., 0.); \
    } \
     \
    /* Uses barriers to ensure order of read / write to threadgroup memory
       simdgroup_barrier are low overhead*/\
    simdgroup_barrier(mem_flags::mem_threadgroup); \
    tgcc[index] = float4(color.g, color.b, 1.0, 1.0); \
    simdgroup_barrier(mem_flags::mem_threadgroup); \
     \
    /*For once in every 2x1 cell, we write to the chroma plane*/\
    if (x % 2 == 0) \
    { \
        /* Read from threadGroup memory*/\
        float4 ccvv = float4(tgcc[index]) \
                    + float4(tgcc[index + 1]); \
     \
        /* write subsampled output to chroma plane */\
        _dst.writePlane(ccvv.rgrg / ccvv.a, py / float2(2., 1.)); \
    } \
     \
    /* For each in every 2x1 cell, we write to the luminance plane */\
    _dst.write(color.rrrr, py);
#ifndef _BUILDING_FOR_UBER_SHADER_
FUNCTION_ATTRIBUTES void _ci_writeTG_420 (coreimage::sample_t color, coreimage::priv::destination _dst)
{
    _CI_WRITETG_420_BODY_
}
FUNCTION_ATTRIBUTES void _ci_writeTG_422 (coreimage::sample_t color, coreimage::priv::destination _dst)
{
    _CI_WRITETG_422_BODY_
}
#endif //_BUILDING_FOR_UBER_SHADER_
#ifndef _BUILDING_FOR_UBER_SHADER_
} // extern "C"
#endif //_BUILDING_FOR_UBER_SHADER_
#pragma mark -
#if _CI_HALF_COLOR_
    #define sampler sampler_h
    #define destination destination_h
#endif
#endif //#ifndef _BUILDING_CORE_IMAGE_LIB_
// Additional CI kernels to be appended here...
#include <metal_stdlib>
#define sampler coreimage::sampler
#define sampler_h coreimage::sampler_h
#define hsampler sampler_h
#define __sample coreimage::sample_t;
#define destCoord() _dst.coord()
typedef float2 vec2;
typedef float3 vec3;
typedef float4 vec4;
typedef half2 hvec2;
typedef half3 hvec3;
typedef half4 hvec4;
typedef int2 ivec2;
typedef int3 ivec3;
typedef int4 ivec4;
typedef bool2 bvec2;
typedef bool3 bvec3;
typedef bool4 bvec4;
typedef coreimage::float2x2 mat2;
typedef coreimage::float3x3 mat3;
typedef coreimage::float4x4 mat4;
typedef coreimage::half2x2 hmat2;
typedef coreimage::half3x3 hmat3;
typedef coreimage::half4x4 hmat4;
typedef float4 __color;
using namespace metal;
using namespace coreimage;
#define cos_(x) cos(x)
#define sin_(x) sin(x)
#define tan_(x) tan(x)
#define cossin(x)  vec2(cos(x), sin(x))
#define cossin_(x) vec2(cos_(x), sin_(x))
#define sincos(x) vec2(sin(x), cos(x))
#define sincos_(x) vec2(sin_((x)), cos_(x))
#define radians(x) (x*3.14159265358979323846264338327950288/180.0)
#define degrees(x) (x*180.0/3.14159265358979323846264338327950288)
#define main main1
#define lowp
#define highp 
#define mediump
#define precision

// Equivalent to samplerExtent (src).xy.
#define samplerOrigin(src) samplerExtent(src).xy

// Equivalent to samplerExtent (src).zw.
#define samplerSize(src) samplerExtent(src).zw



cojrBTGaISF\Wb^biiii]ihjggggbbbb[b_bkkkk\pmvhrnxYh_qXdYlppppqqqqmlemqqqq~~~~azy}HbXpSh_q¦ª­­¶¶¶¶²²²²¦¦¦¦klil>J;WESG]kkhnbsrxzxwz~~~~~~~~Zmhxj{y¦¦¦¦¯¯¯¯¦¨¨¨rul{ssjs_pit[jarC]Pljjbnwwwwtxxxssosv|z|uusu;PCYSSGSaa_awwww{{{{Rc\p\icoqqqq]f`kWbZfcdadP_Y`ETIV?H8PARD[lvswbto~ookoiiiiXd[jmrkyv¦¬¬¸¸¸¸····®®®®££££SlitJ_YePTIV6E6Q<OC\onjoq|||{{{{ppppjjejqqqqsookoJXNbbgbndxwUe]qjibjXd\jghap~~~~n~s~sdmdw\og~p¢¢¢¢}}}}Wc\oyyyy~~~~rttunnnneeee8QE[X[Pb{{{{y||||bvvzSlhvJc]pdd^kig`ickfnxxxx}}}}vvvvmmim`f_o|{z}fohvXmdyJf]xGVGjrrrrvvvv?ZPe3G8XZ_V`ZiekWc^mJ]UhQ`Zehhhhssrsz    ³³³³³³³³||u|y~ycog{xxvxnsmsllfl]jbm{{{{~~||||^olo]f`jYkfry~~~zzzzL\Re9J;UXXSXaa\aXgam¡ªªªª¢¨¬£¨kohrsslsp§§§§¨¨¨¨qjzx||||rrrrSd]jmmgmttttpzw|jljdn,F7TTTOTddddnnnnqqqq_rrtzzzz}}}}zvqzaof}xxxxVbZj5THh]rpw¥¥¥¥auq|uuuujjfjEUK_O^V`iiiiYmjm^icnilgqfrnuwurwOg_tqi`qxunxr¨¨¨¨····¦ª¬¬rrgv^h\sg}zQpi}heZhBTE_ThbovvuvP[Oc@SC_=YJi\qiz~}yTf[vE_Qpssns]merA]Rnmnhsvqywbwu}ipppbbbbYYYYVVVVLZWZeeee_onoxxxxvzyzJkg}u~yyyyMdZrHf\{bh`j=N?[P^Uexxxx~~~~rrrr1QGbATK_ffcfnmjnkxyz{zv{Tldw{zv{w}Pe\p_f_gVc[dTaYhwwwwaxu`tomuoz¡¡¡¡qxxxdnlq~~~~ªªªª¢¢¢¢D^Wg7K=V8L>Wge^g}}}}gxuxsmx|~~~|jyylsqykkkkcc`cjjhj}}}}¥¥¥¥££££¡¡¡§§§§[qjylrmxezyvurvuuuuxxxxxxxxyyyy}}}}v}}tz}Voj<aZsppppjjjjX_X_OVK^lror|H^Tjcc]clllltttt]mlrgkgonnknRg`qgtq~{~~~cqluL`WoXharuvrxzwv|l{|~yyyyjjjj]]X]T]Uarttts`Yoiwce^jZZQ\TTNTWWSWff_f||||z¡§§§§~Y^PjAVFcsssscojrBTGa /* Error: Ran out of types for this method. */;

@end

