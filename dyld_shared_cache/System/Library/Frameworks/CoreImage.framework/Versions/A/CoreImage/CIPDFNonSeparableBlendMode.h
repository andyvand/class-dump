//
//     Generated by class-dump 3.5 (64 bit).
//
//  Copyright (C) 1997-2019 Steve Nygard.
//

#import <CoreImage/CIBlendModeFilter.h>

__attribute__((visibility("hidden")))
@interface CIPDFNonSeparableBlendMode : CIBlendModeFilter
{
}

- (id);
- (id);
- (int);
- (id);
- (int);
- (id);
- (void)_quotient = maxV * (1.0 - oldS);
      float q_quotient = maxV * (1.0 - (oldS * fraction));
      float t_quotient = maxV * (1.0 - (oldS * (1.0 - fraction)));
      if (sector == 0) {
        result.r = maxV;
        result.g = t_quotient;
        result.b = p_quotient;
      } else if (sector == 1) {
        result.r = q_quotient;
        result.g = maxV;
        result.b = p_quotient;
      } else if (sector == 2) {
        result.r = p_quotient;
        result.g = maxV;
        result.b = t_quotient;
      } else if (sector == 3) {
        result.r = p_quotient;
        result.g = q_quotient;
        result.b = maxV;
      } else if (sector == 4) {
        result.r = t_quotient;
        result.g = p_quotient;
        result.b = maxV;
      } else {
        result.r = maxV;
        result.g = p_quotient;
        result.b = q_quotient;
      }
    }
    result.r -= (result.r - minV) * brightness;
    result.g -= (result.g - minV) * brightness;
    result.b -= (result.b - minV) * brightness;
    float avg = ((result.r + result.g) + result.b) / 3.0;
    result.r -= (result.r - avg) * saturation;
    result.g -= (result.g - avg) * saturation;
    result.b -= (result.b - avg) * saturation;
  }
  return result;
}
;

@end

