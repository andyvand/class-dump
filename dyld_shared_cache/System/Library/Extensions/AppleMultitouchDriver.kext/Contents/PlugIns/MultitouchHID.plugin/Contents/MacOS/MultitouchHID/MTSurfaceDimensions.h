//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class MTSurfaceDimensions {
public: // (access info unavailable from symbols alone)
    MTSurfaceDimensions(MTRect, MTSize);
    auto convertMillimetersToPixels(MTPoint) const;
    auto convertMillimetersToSurfaceFraction(MTPoint) const;
    auto convertPixelDeltaToMillimeters(float, bool) const;
    auto convertPixelDeltaToMillimetersPerSecond(float, double) const;
    auto convertPixelDeltasToMillimetersPerSecond(MTPoint, double) const;
    auto convertPixelVelocityToMillimetersPerSecond(float) const;
    auto convertPixelsToMillimeters(MTPoint) const;
    auto convertPixelsToSurfaceFraction(MTPoint) const;
    auto convertSurfaceFractionToMillimeters(MTPoint, bool) const;
    auto convertSurfaceFractionToPixels(MTPoint) const;
    auto convertSurfaceFractionVelocityToPixelsPerSecond(MTPoint) const;
    auto deriveScreenPixelsFromResolution(MTSize);
    auto distanceToBottomEdge_mm(MTPoint) const;
    auto distanceToLeftEdge_mm(MTPoint) const;
    auto distanceToRightEdge_mm(MTPoint) const;
    auto distanceToTopEdge_mm(MTPoint) const;
    auto minDistanceToEdge_mm(MTPoint, bool, bool) const;
    auto updatePixelResolution_mm(MTSize);
    auto updateScreenBounds_mm(MTRect);
    ~MTSurfaceDimensions();
};
