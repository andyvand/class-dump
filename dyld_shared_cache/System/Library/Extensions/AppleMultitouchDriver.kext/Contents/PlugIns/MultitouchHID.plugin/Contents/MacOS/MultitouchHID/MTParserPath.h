//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class MTParserPath {
public: // (access info unavailable from symbols alone)
    MTParserPath(MTParserType, MTParserOptions, MTSLGLogger*);
    auto applyAbsoluteForceHysteresis(float, float&, float, MTPathFilterParameters const&) const;
    auto applyAbsolutePixelHysteresis(MTPoint, MTPoint&, MTPoint, float) const;
    auto applyMinimumIncrementHysteresis(float, float&, float, float) const;
    auto bypassUpToHysteresisOfMotion(float, float&, float) const;
    auto clear();
    auto clearPathSpeedSymmetries();
    auto clearTimestamps(MTPathStageTimestamps&);
    auto computeDebounceTimeInstability(MTPathFilterParameters const&, double);
    auto computeDivingButtonChangeInstability(MTPathStates const&, MTPathFilterParameters const&);
    auto computeZSignalInstability(MTPathStates const&, MTPathFilterParameters const&, bool, double);
    auto computeZonesAndEdgesMask(MTPoint, MTPoint, MTTouchZoneParameters const&);
    auto constructPathChangeEventMask();
    auto correctAndQuantizeRadius(MTPathFilterParameters const&);
    auto detectSustainedHoverAtEdge(MTPathFilterParameters const&, MTSurfaceDimensions const&);
    auto filterContactForScreenUI(MTPathStates const&, MTSurfaceDimensions const&, MTPathFilterParameters const&, MTHMFingerTipOffsetParameters const&);
    auto fingerLiftoffNearEdge(MTSurfaceDimensions const&, double) const;
    auto flushLiftoffAt(double);
    auto generateFlagsEventMaskBits(MTContact const&) const;
    auto getAverageVelocity_mm_s() const;
    auto getDisplacement_mm() const;
    auto getQuantizedMajorRadius_mm() const;
    auto getQuantizedMinorRadius_mm() const;
    auto getQuantizedRadiusAccuracy_mm() const;
    auto isBottomContact() const;
    auto isBottomOrTopContact() const;
    auto isEdgeContact() const;
    auto isEdgeContactTyping() const;
    auto isFingerContact() const;
    auto isFingerOrRestingContact() const;
    auto isPalmContact() const;
    auto isPalmHeelContact() const;
    auto isSideContact() const;
    auto isStuckOnDivingRegion(MTPathStates const&);
    auto isStylusContact() const;
    auto isThumbAlongEdgeContact() const;
    auto isThumbContact() const;
    auto isUnidentifiedContact() const;
    auto isValidClickSource() const;
    auto isValidEdgeStraddleClick() const;
    auto isValidForceSource(int) const;
    auto measureInstability(MTPathStates const&, MTPathFilterParameters const&, float*);
    auto pullHysteresisCenterCancelingEllipseTipMotion(MTHMFingerTipOffsetParameters const&, MTPoint, MTPoint&, MTPoint, MTPoint, MTSurfaceDimensions);
    auto pullHysteresisCenterCancelingEllipseTipMotionFromPressure(MTHMFingerTipOffsetParameters const&, MTPoint, MTPoint&, MTPoint, MTPoint, MTSurfaceDimensions);
    auto pullHysteresisCenterHidingUnstablePixelDeltasXY(MTPathFilterParameters const&, MTPoint, MTPoint&, float, float);
    auto rejectRestingThumbFromRestingHand();
    auto setPathSpeedSymmetries(float, float);
    auto stableAndMoved() const;
    auto updateAngularHysteresisCenter(float*, float, float);
    auto updateCurPrevContactsWith(MTContact&, double, MTSurfaceDimensions const&, MTTouchZoneParameters const&);
    auto updateDampedVelocity(MTPathStates const&, MTPathFilterParameters const&, float);
    auto updateHysteresisDragCenter_mm(MTPathFilterParameters);
    auto updatePathStageTimestamps(double);
    auto updateTotalDiscountedDistance(double);
    auto updateZonesAndEdges(double, MTSurfaceDimensions const&, MTTouchZoneParameters const&);
    auto wasFingerContact() const;
    auto wasFingerOrRestingContact() const;
    auto wasStylusContact() const;
    auto weakenDampedVelocity(float);
    ~MTParserPath();
};
