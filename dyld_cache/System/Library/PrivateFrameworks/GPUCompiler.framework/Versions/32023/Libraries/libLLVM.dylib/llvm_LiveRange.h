//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::LiveRange {
public: // (access info unavailable from symbols alone)
    MergeSegmentsInAsValue(llvm::LiveRange const&, llvm::VNInfo*);
    MergeValueInAsValue(llvm::LiveRange const&, llvm::VNInfo const*, llvm::VNInfo*);
    MergeValueNumberInto(llvm::VNInfo*, llvm::VNInfo*);
    RenumberValues();
    addSegment(llvm::LiveRange::Segment);
    addSegmentToSet(llvm::LiveRange::Segment);
    append(llvm::LiveRange::Segment);
    covers(llvm::LiveRange const&) const;
    createDeadDef(llvm::SlotIndex, llvm::BumpPtrAllocatorImpl<llvm::MallocAllocator, 4096ul, 4096ul, 128ul>&);
    createDeadDef(llvm::VNInfo*);
    dump() const;
    extendInBlock(llvm::ArrayRef<llvm::SlotIndex>, llvm::SlotIndex, llvm::SlotIndex);
    extendInBlock(llvm::SlotIndex, llvm::SlotIndex);
    find(llvm::SlotIndex);
    flushSegmentSet();
    isLiveAtIndexes(llvm::ArrayRef<llvm::SlotIndex>) const;
    join(llvm::LiveRange&, int const*, int const*, llvm::SmallVectorImpl<llvm::VNInfo*>&);
    markValNoForDeletion(llvm::VNInfo*);
    overlaps(llvm::LiveRange const&, llvm::CoalescerPair const&, llvm::SlotIndexes const&) const;
    overlaps(llvm::SlotIndex, llvm::SlotIndex) const;
    overlapsFrom(llvm::LiveRange const&, llvm::LiveRange::Segment const*) const;
    print(llvm::raw_ostream&) const;
    removeSegment(llvm::LiveRange::Segment*, bool);
    removeSegment(llvm::SlotIndex, llvm::SlotIndex, bool);
    removeValNo(llvm::VNInfo*);
    removeValNoIfDead(llvm::VNInfo*);
};
