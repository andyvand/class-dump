//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::detail::IEEEFloat {
public: // (access info unavailable from symbols alone)
    IEEEFloat(double);
    IEEEFloat(float);
    IEEEFloat(llvm::detail::IEEEFloat const&);
    IEEEFloat(llvm::detail::IEEEFloat&&);
    IEEEFloat(llvm::fltSemantics const&);
    IEEEFloat(llvm::fltSemantics const&, llvm::APFloatBase::uninitializedTag);
    IEEEFloat(llvm::fltSemantics const&, llvm::APInt const&);
    IEEEFloat(llvm::fltSemantics const&, unsigned long long);
    add(llvm::detail::IEEEFloat const&, llvm::RoundingMode);
    addOrSubtract(llvm::detail::IEEEFloat const&, llvm::RoundingMode, bool);
    addOrSubtractSignificand(llvm::detail::IEEEFloat const&, bool);
    addOrSubtractSpecials(llvm::detail::IEEEFloat const&, bool);
    addSignificand(llvm::detail::IEEEFloat const&);
    assign(llvm::detail::IEEEFloat const&);
    bitcastToAPInt() const;
    bitwiseIsEqual(llvm::detail::IEEEFloat const&) const;
    changeSign();
    compare(llvm::detail::IEEEFloat const&) const;
    compareAbsoluteValue(llvm::detail::IEEEFloat const&) const;
    convert(llvm::fltSemantics const&, llvm::RoundingMode, bool*);
    convertBFloatAPFloatToAPInt() const;
    convertDoubleAPFloatToAPInt() const;
    convertF80LongDoubleAPFloatToAPInt() const;
    convertFloat8E4M3APFloatToAPInt() const;
    convertFloat8E4M3FNAPFloatToAPInt() const;
    convertFloat8E5M2APFloatToAPInt() const;
    convertFloatAPFloatToAPInt() const;
    convertFromAPInt(llvm::APInt const&, bool, llvm::RoundingMode);
    convertFromDecimalString(llvm::StringRef, llvm::RoundingMode);
    convertFromHexadecimalString(llvm::StringRef, llvm::RoundingMode);
    convertFromSignExtendedInteger(unsigned long long const*, unsigned int, bool, llvm::RoundingMode);
    convertFromString(llvm::StringRef, llvm::RoundingMode);
    convertFromStringSpecials(llvm::StringRef);
    convertFromUnsignedParts(unsigned long long const*, unsigned int, llvm::RoundingMode);
    convertFromZeroExtendedInteger(unsigned long long const*, unsigned int, bool, llvm::RoundingMode);
    convertHalfAPFloatToAPInt() const;
    convertNormalToHexString(char*, unsigned int, bool, llvm::RoundingMode) const;
    convertPPCDoubleDoubleAPFloatToAPInt() const;
    convertQuadrupleAPFloatToAPInt() const;
    convertToDouble() const;
    convertToFloat() const;
    convertToHexString(char*, unsigned int, bool, llvm::RoundingMode) const;
    convertToInteger(llvm::MutableArrayRef<unsigned long long>, unsigned int, bool, llvm::RoundingMode, bool*) const;
    convertToSignExtendedInteger(llvm::MutableArrayRef<unsigned long long>, unsigned int, bool, llvm::RoundingMode, bool*) const;
    copySignificand(llvm::detail::IEEEFloat const&);
    divide(llvm::detail::IEEEFloat const&, llvm::RoundingMode);
    divideSignificand(llvm::detail::IEEEFloat const&);
    divideSpecials(llvm::detail::IEEEFloat const&);
    exponentInf() const;
    exponentNaN() const;
    exponentZero() const;
    freeSignificand();
    fusedMultiplyAdd(llvm::detail::IEEEFloat const&, llvm::detail::IEEEFloat const&, llvm::RoundingMode);
    getExactInverse(llvm::APFloat*) const;
    handleOverflow(llvm::RoundingMode);
    incrementSignificand();
    initFromAPInt(llvm::fltSemantics const*, llvm::APInt const&);
    initFromBFloatAPInt(llvm::APInt const&);
    initFromDoubleAPInt(llvm::APInt const&);
    initFromF80LongDoubleAPInt(llvm::APInt const&);
    initFromFloat8E4M3APInt(llvm::APInt const&);
    initFromFloat8E4M3FNAPInt(llvm::APInt const&);
    initFromFloat8E5M2APInt(llvm::APInt const&);
    initFromFloatAPInt(llvm::APInt const&);
    initFromHalfAPInt(llvm::APInt const&);
    initFromPPCDoubleDoubleAPInt(llvm::APInt const&);
    initFromQuadrupleAPInt(llvm::APInt const&);
    initialize(llvm::fltSemantics const*);
    isDenormal() const;
    isInteger() const;
    isLargest() const;
    isSignaling() const;
    isSignificandAllOnes() const;
    isSignificandAllOnesExceptLSB() const;
    isSignificandAllZeros() const;
    isSmallest() const;
    makeInf(bool);
    makeLargest(bool);
    makeNaN(bool, bool, llvm::APInt const*);
    makeQuiet();
    makeSmallest(bool);
    makeSmallestNormalized(bool);
    makeZero(bool);
    mod(llvm::detail::IEEEFloat const&);
    modSpecials(llvm::detail::IEEEFloat const&);
    multiply(llvm::detail::IEEEFloat const&, llvm::RoundingMode);
    multiplySignificand(llvm::detail::IEEEFloat const&);
    multiplySignificand(llvm::detail::IEEEFloat const&, llvm::detail::IEEEFloat);
    multiplySpecials(llvm::detail::IEEEFloat const&);
    next(bool);
    normalize(llvm::RoundingMode, llvm::lostFraction);
    operator=(llvm::detail::IEEEFloat const&);
    operator=(llvm::detail::IEEEFloat&&);
    partCount() const;
    remainder(llvm::detail::IEEEFloat const&);
    remainderSpecials(llvm::detail::IEEEFloat const&);
    roundAwayFromZero(llvm::RoundingMode, llvm::lostFraction, unsigned int) const;
    roundSignificandWithExponent(unsigned long long const*, unsigned int, int, llvm::RoundingMode);
    roundToIntegral(llvm::RoundingMode);
    shiftSignificandLeft(unsigned int);
    shiftSignificandRight(unsigned int);
    significandLSB() const;
    significandMSB() const;
    significandParts();
    significandParts() const;
    subtract(llvm::detail::IEEEFloat const&, llvm::RoundingMode);
    subtractSignificand(llvm::detail::IEEEFloat const&, unsigned long long);
    toString(llvm::SmallVectorImpl<char>&, unsigned int, unsigned int, bool) const;
    zeroSignificand();
    ~IEEEFloat();
};
