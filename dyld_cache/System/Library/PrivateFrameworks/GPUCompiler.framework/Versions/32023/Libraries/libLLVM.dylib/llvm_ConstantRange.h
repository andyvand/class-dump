//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::ConstantRange {
public: // (access info unavailable from symbols alone)
    ConstantRange(llvm::APInt);
    ConstantRange(llvm::APInt, llvm::APInt);
    ConstantRange(unsigned int, bool);
    abs(bool) const;
    add(llvm::ConstantRange const&) const;
    addWithNoWrap(llvm::ConstantRange const&, unsigned int, llvm::ConstantRange::PreferredRangeType) const;
    areInsensitiveToSignednessOfICmpPredicate(llvm::ConstantRange const&, llvm::ConstantRange const&);
    areInsensitiveToSignednessOfInvertedICmpPredicate(llvm::ConstantRange const&, llvm::ConstantRange const&);
    ashr(llvm::ConstantRange const&) const;
    binaryAnd(llvm::ConstantRange const&) const;
    binaryNot() const;
    binaryOp(llvm::Instruction::BinaryOps, llvm::ConstantRange const&) const;
    binaryOr(llvm::ConstantRange const&) const;
    binaryXor(llvm::ConstantRange const&) const;
    castOp(llvm::Instruction::CastOps, unsigned int) const;
    contains(llvm::APInt const&) const;
    contains(llvm::ConstantRange const&) const;
    difference(llvm::ConstantRange const&) const;
    dump() const;
    exactIntersectWith(llvm::ConstantRange const&) const;
    exactUnionWith(llvm::ConstantRange const&) const;
    fromKnownBits(llvm::KnownBits const&, bool);
    getActiveBits() const;
    getEquivalentICmp(llvm::CmpInst::Predicate&, llvm::APInt&) const;
    getEquivalentICmp(llvm::CmpInst::Predicate&, llvm::APInt&, llvm::APInt&) const;
    getEquivalentPredWithFlippedSignedness(llvm::CmpInst::Predicate, llvm::ConstantRange const&, llvm::ConstantRange const&);
    getMinSignedBits() const;
    getSignedMax() const;
    getSignedMin() const;
    getUnsignedMax() const;
    getUnsignedMin() const;
    icmp(llvm::CmpInst::Predicate, llvm::ConstantRange const&) const;
    intersectWith(llvm::ConstantRange const&, llvm::ConstantRange::PreferredRangeType) const;
    intrinsic(unsigned int, llvm::ArrayRef<llvm::ConstantRange>);
    inverse() const;
    isAllNegative() const;
    isAllNonNegative() const;
    isEmptySet() const;
    isFullSet() const;
    isIntrinsicSupported(unsigned int);
    isSignWrappedSet() const;
    isSizeLargerThan(unsigned long long) const;
    isSizeStrictlySmallerThan(llvm::ConstantRange const&) const;
    isUpperSignWrapped() const;
    isUpperWrapped() const;
    isWrappedSet() const;
    lshr(llvm::ConstantRange const&) const;
    makeAllowedICmpRegion(llvm::CmpInst::Predicate, llvm::ConstantRange const&);
    makeExactICmpRegion(llvm::CmpInst::Predicate, llvm::APInt const&);
    makeExactNoWrapRegion(llvm::Instruction::BinaryOps, llvm::APInt const&, unsigned int);
    makeGuaranteedNoWrapRegion(llvm::Instruction::BinaryOps, llvm::ConstantRange const&, unsigned int);
    makeSatisfyingICmpRegion(llvm::CmpInst::Predicate, llvm::ConstantRange const&);
    multiply(llvm::ConstantRange const&) const;
    overflowingBinaryOp(llvm::Instruction::BinaryOps, llvm::ConstantRange const&, unsigned int) const;
    print(llvm::raw_ostream&) const;
    sadd_sat(llvm::ConstantRange const&) const;
    sdiv(llvm::ConstantRange const&) const;
    sextOrTrunc(unsigned int) const;
    shl(llvm::ConstantRange const&) const;
    signExtend(unsigned int) const;
    signedAddMayOverflow(llvm::ConstantRange const&) const;
    signedSubMayOverflow(llvm::ConstantRange const&) const;
    smax(llvm::ConstantRange const&) const;
    smin(llvm::ConstantRange const&) const;
    smul_fast(llvm::ConstantRange const&) const;
    smul_sat(llvm::ConstantRange const&) const;
    srem(llvm::ConstantRange const&) const;
    sshl_sat(llvm::ConstantRange const&) const;
    ssub_sat(llvm::ConstantRange const&) const;
    sub(llvm::ConstantRange const&) const;
    subWithNoWrap(llvm::ConstantRange const&, unsigned int, llvm::ConstantRange::PreferredRangeType) const;
    subtract(llvm::APInt const&) const;
    truncate(unsigned int) const;
    uadd_sat(llvm::ConstantRange const&) const;
    udiv(llvm::ConstantRange const&) const;
    umax(llvm::ConstantRange const&) const;
    umin(llvm::ConstantRange const&) const;
    umul_sat(llvm::ConstantRange const&) const;
    unionWith(llvm::ConstantRange const&, llvm::ConstantRange::PreferredRangeType) const;
    unsignedAddMayOverflow(llvm::ConstantRange const&) const;
    unsignedMulMayOverflow(llvm::ConstantRange const&) const;
    unsignedSubMayOverflow(llvm::ConstantRange const&) const;
    urem(llvm::ConstantRange const&) const;
    ushl_sat(llvm::ConstantRange const&) const;
    usub_sat(llvm::ConstantRange const&) const;
    zeroExtend(unsigned int) const;
    zextOrTrunc(unsigned int) const;
};
