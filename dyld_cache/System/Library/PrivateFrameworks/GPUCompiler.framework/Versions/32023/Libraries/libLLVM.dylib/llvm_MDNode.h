//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::MDNode {
public: // (access info unavailable from symbols alone)
    MDNode(llvm::LLVMContext&, unsigned int, llvm::Metadata::StorageType, llvm::ArrayRef<llvm::Metadata*>, llvm::ArrayRef<llvm::Metadata*>);
    clone() const;
    concatenate(llvm::MDNode*, llvm::MDNode*);
    countUnresolvedOperands();
    decrementUnresolvedOperandCount();
    deleteAsSubclass();
    deleteTemporary(llvm::MDNode*);
    dropAllReferences();
    dropReplaceableUses();
    dumpTree() const;
    dumpTree(llvm::Module const*) const;
    eraseFromStore();
    getMostGenericAliasScope(llvm::MDNode*, llvm::MDNode*);
    getMostGenericAlignmentOrDereferenceable(llvm::MDNode*, llvm::MDNode*);
    getMostGenericFPMath(llvm::MDNode*, llvm::MDNode*);
    getMostGenericRange(llvm::MDNode*, llvm::MDNode*);
    getMostGenericTBAA(llvm::MDNode*, llvm::MDNode*);
    handleChangedOperand(void*, llvm::Metadata*);
    intersect(llvm::MDNode*, llvm::MDNode*);
    isTBAAVtableAccess() const;
    makeDistinct();
    makeUniqued();
    printTree(llvm::raw_ostream&, llvm::Module const*) const;
    printTree(llvm::raw_ostream&, llvm::ModuleSlotTracker&, llvm::Module const*) const;
    replaceOperandWith(unsigned int, llvm::Metadata*);
    replaceWithDistinctImpl();
    replaceWithPermanentImpl();
    replaceWithUniquedImpl();
    resolve();
    resolveAfterOperandChange(llvm::Metadata*, llvm::Metadata*);
    resolveCycles();
    setOperand(unsigned int, llvm::Metadata*);
    storeDistinctInContext();
    uniquify();
};
