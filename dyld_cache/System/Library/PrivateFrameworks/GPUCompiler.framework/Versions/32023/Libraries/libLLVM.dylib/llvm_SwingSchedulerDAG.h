//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::SwingSchedulerDAG {
public: // (access info unavailable from symbols alone)
    addConnectedNodes(llvm::SUnit*, llvm::NodeSet&, llvm::SetVector<llvm::SUnit*, std::__1::vector<llvm::SUnit*, std::__1::allocator<llvm::SUnit*>>, llvm::DenseSet<llvm::SUnit*, llvm::DenseMapInfo<llvm::SUnit*, void>>>&);
    addLoopCarriedDependences(llvm::AAResults*);
    applyInstrChange(llvm::MachineInstr*, llvm::SMSchedule&);
    calculateRecMII(llvm::SmallVector<llvm::NodeSet, 8u>&);
    calculateResMII();
    canUseLastOffsetValue(llvm::MachineInstr*, unsigned int&, unsigned int&, unsigned int&, long long&);
    changeDependences();
    checkNodeSets(llvm::SmallVector<llvm::NodeSet, 8u>&);
    checkValidNodeOrder(llvm::SmallVector<llvm::NodeSet, 8u> const&) const;
    colocateNodeSets(llvm::SmallVector<llvm::NodeSet, 8u>&);
    computeDelta(llvm::MachineInstr&, unsigned int&);
    computeNodeFunctions(llvm::SmallVector<llvm::NodeSet, 8u>&);
    computeNodeOrder(llvm::SmallVector<llvm::NodeSet, 8u>&);
    findCircuits(llvm::SmallVector<llvm::NodeSet, 8u>&);
    findDefInLoop(llvm::Register);
    finishBlock();
    fixupRegisterOverlaps(std::__1::deque<llvm::SUnit*, std::__1::allocator<llvm::SUnit*>>&);
    fuseRecs(llvm::SmallVector<llvm::NodeSet, 8u>&);
    groupRemainingNodes(llvm::SmallVector<llvm::NodeSet, 8u>&);
    isLoopCarriedDep(llvm::SUnit*, llvm::SDep const&, bool);
    postprocessDAG();
    registerPressureFilter(llvm::SmallVector<llvm::NodeSet, 8u>&);
    removeDuplicateNodes(llvm::SmallVector<llvm::NodeSet, 8u>&);
    schedule();
    schedulePipeline(llvm::SMSchedule&);
    setMAX_II();
    setMII(unsigned int, unsigned int);
    updatePhiDependences();
};
