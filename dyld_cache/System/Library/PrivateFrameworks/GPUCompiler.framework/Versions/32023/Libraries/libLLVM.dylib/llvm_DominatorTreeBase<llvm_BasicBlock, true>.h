//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class llvm::DominatorTreeBase<llvm::BasicBlock, true> {
public: // (access info unavailable from symbols alone)
    DominatorTreeBase(llvm::DominatorTreeBase<llvm::BasicBlock, true>&&);
    addNewBlock(llvm::BasicBlock*, llvm::BasicBlock*);
    addRoot(llvm::BasicBlock*);
    applyUpdates(llvm::ArrayRef<llvm::cfg::Update<llvm::BasicBlock*>>);
    applyUpdates(llvm::ArrayRef<llvm::cfg::Update<llvm::BasicBlock*>>, llvm::ArrayRef<llvm::cfg::Update<llvm::BasicBlock*>>);
    changeImmediateDominator(llvm::BasicBlock*, llvm::BasicBlock*);
    changeImmediateDominator(llvm::DomTreeNodeBase<llvm::BasicBlock>*, llvm::DomTreeNodeBase<llvm::BasicBlock>*);
    compare(llvm::DominatorTreeBase<llvm::BasicBlock, true> const&) const;
    createChild(llvm::BasicBlock*, llvm::DomTreeNodeBase<llvm::BasicBlock>*);
    createNode(llvm::BasicBlock*);
    deleteEdge(llvm::BasicBlock*, llvm::BasicBlock*);
    dominatedBySlowTreeWalk(llvm::DomTreeNodeBase<llvm::BasicBlock> const*, llvm::DomTreeNodeBase<llvm::BasicBlock> const*) const;
    dominates(llvm::BasicBlock const*, llvm::BasicBlock const*) const;
    dominates(llvm::DomTreeNodeBase<llvm::BasicBlock> const*, llvm::DomTreeNodeBase<llvm::BasicBlock> const*) const;
    eraseNode(llvm::BasicBlock*);
    findNearestCommonDominator(llvm::BasicBlock const*, llvm::BasicBlock const*) const;
    findNearestCommonDominator(llvm::BasicBlock*, llvm::BasicBlock*) const;
    getDescendants(llvm::BasicBlock*, llvm::SmallVectorImpl<llvm::BasicBlock*>&) const;
    getNode(llvm::BasicBlock const*) const;
    getRoot() const;
    getRootNode();
    getRootNode() const;
    insertEdge(llvm::BasicBlock*, llvm::BasicBlock*);
    isPostDominator() const;
    isReachableFromEntry(llvm::BasicBlock const*) const;
    isReachableFromEntry(llvm::DomTreeNodeBase<llvm::BasicBlock> const*) const;
    isVirtualRoot(llvm::DomTreeNodeBase<llvm::BasicBlock> const*) const;
    operator=(llvm::DominatorTreeBase<llvm::BasicBlock, true>&&);
    operator[](llvm::BasicBlock const*) const;
    print(llvm::raw_ostream&) const;
    properlyDominates(llvm::BasicBlock const*, llvm::BasicBlock const*) const;
    properlyDominates(llvm::DomTreeNodeBase<llvm::BasicBlock> const*, llvm::DomTreeNodeBase<llvm::BasicBlock> const*) const;
    recalculate(llvm::Function&);
    recalculate(llvm::Function&, llvm::ArrayRef<llvm::cfg::Update<llvm::BasicBlock*>>);
    reset();
    root_begin();
    root_begin() const;
    root_end();
    root_end() const;
    root_size() const;
    roots();
    roots() const;
    setNewRoot(llvm::BasicBlock*);
    splitBlock(llvm::BasicBlock*);
    updateDFSNumbers() const;
    verify(llvm::DominatorTreeBase<llvm::BasicBlock, true>::VerificationLevel) const;
    wipe();
};
