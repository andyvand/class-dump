//
//     Generated by class-dump 3.5 (64 bit).
//
//  Copyright (C) 1997-2019 Steve Nygard.
//

#import <CoreImage/CIKernel.h>

@interface CIWarpKernel : CIKernel
{
}

+ (id);
- (void);
- (id);
- (id);
- (_Bool);
- (id);
- (void);
- (id);
- (id);
- (id);
- (void);
- (_Bool);
- (struct CGRect);
- (id);
- (_Bool);
- (id);
- (id)ecMin:(id)arg1;
- (_Bool)el vec4 _smarttone_brightness_pos(vec4 c, float gamma) {
  vec3 neg = min(c.rgb, 0.0);
  vec3 pos = max(c.rgb, 1.0) - 1.0;
  c.rgb = clamp(c.rgb, 0.0, 1.0);
  vec3 m = 1.0 - c.rgb;
  float a = 6.000000e-01;
  vec4 result = c;
  result.rgb = 1.0 - (pow(m, vec3(gamma)) + (a * ((((gamma - 1.0) * m) * (1.0 - (m * m))) / (gamma * gamma))));
  c.rgb = pow(c.rgb, vec3(1.0 - ((min(gamma, 2.950) - 1.0) / 2.600)));
  result.rgb = mix(c.rgb, result.rgb, 8.500000e-01);
  result.rgb = (result.rgb + neg) + pos;
  return result;
}
;

// Remaining properties
@property(nonatomic) _Bool perservesAlpha;
@property(readonly, nonatomic) _Bool preservesOpacity;
@property(nonatomic) _Bool preservesRange;

@end

