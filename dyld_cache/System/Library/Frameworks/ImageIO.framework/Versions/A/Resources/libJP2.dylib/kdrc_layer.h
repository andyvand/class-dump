//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class kdrc_layer {
public: // (access info unavailable from symbols alone)
    activate(kdu_dims, kdu_dims, bool, bool, bool);
    activate(kdu_dims, kdu_dims, bool, bool, bool) (.cold.1);
    activate(kdu_dims, kdu_dims, bool, bool, bool) (.cold.2);
    activate(kdu_dims, kdu_dims, bool, bool, bool) (.cold.3);
    activate(kdu_dims, kdu_dims, bool, bool, bool, int, int);
    activate(kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.1);
    activate(kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.2);
    change_frame(int, bool);
    configure_overlay(bool, int, int);
    deactivate();
    find_optimal_scale(float, float, float);
    get_component_scale_factors(kdrc_stream*, double&, double&);
    get_component_scale_factors(kdrc_stream*, double&, double&) (.cold.1);
    get_visible_packet_stats(kdrc_stream*, kdu_dims, long long&, long long&, long long&);
    get_visible_packet_stats(kdrc_stream*, kdu_dims, long long&, long long&, long long&) (.cold.1);
    get_visible_packet_stats(kdrc_stream*, kdu_dims, long long&, long long&, long long&) (.cold.2);
    get_visible_packet_stats(kdrc_stream*, kdu_dims, long long&, long long&, long long&) (.cold.3);
    get_visible_packet_stats(kdrc_stream*, kdu_dims, long long&, long long&, long long&) (.cold.4);
    init(jpx_layer_source, kdu_dims, kdu_dims, bool, bool, bool);
    init(jpx_layer_source, kdu_dims, kdu_dims, bool, bool, bool) (.cold.1);
    init(mj2_video_source*, int, int, kdu_dims, bool, bool, bool);
    init(mj2_video_source*, int, int, kdu_dims, bool, bool, bool) (.cold.1);
    init(mj2_video_source*, int, int, kdu_dims, bool, bool, bool) (.cold.2);
    kdrc_layer(kdu_region_compositor*);
    map_region(int&, kdu_dims&);
    measure_visible_area(kdu_dims, bool);
    process_overlay(kdu_dims&);
    process_overlay(kdu_dims&) (.cold.1);
    reinit();
    reinit() (.cold.1);
    search_overlay(kdu_coords, int&, bool&);
    set_buffer_surface(kdu_dims, kdu_dims, kdu_compositor_buf*, bool);
    set_buffer_surface(kdu_dims, kdu_dims, kdu_compositor_buf*, bool) (.cold.1);
    set_buffer_surface(kdu_dims, kdu_dims, kdu_compositor_buf*, bool) (.cold.2);
    set_scale(bool, bool, bool, float, int&);
    take_layer_buffer(bool);
    take_layer_buffer(bool) (.cold.1);
    take_layer_buffer(bool) (.cold.2);
    update_composition(kdu_dims, unsigned int);
    update_composition(kdu_dims, unsigned int) (.cold.1);
    update_composition(kdu_dims, unsigned int) (.cold.2);
    update_overlay(bool, bool);
    update_overlay(bool, bool) (.cold.1);
    ~kdrc_layer();
};
