//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class kdu_region_compositor {
public: // (access info unavailable from symbols alone)
    access_codestream(kdrc_stream*);
    add_active_stream(int, int, bool);
    add_active_stream(int, int, bool) (.cold.1);
    add_active_stream(int, int, bool) (.cold.2);
    add_active_stream(int, int, bool) (.cold.3);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.1);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.2);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.3);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.4);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.5);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool) (.cold.6);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.1);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.2);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.3);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.4);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.5);
    add_compositing_layer(int, kdu_dims, kdu_dims, bool, bool, bool, int, int) (.cold.6);
    allocate_buffer(kdu_coords, kdu_coords&, bool);
    change_compositing_layer_frame(int, int);
    configure_overlays(bool, int, int);
    create(jpx_source*, int);
    create(kdu_compressed_source*, int);
    cull_inactive_layers(int);
    custom_paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords);
    delete_buffer(kdu_compositor_buf*);
    donate_compositing_buffer(kdu_compositor_buf*, kdu_dims, kdu_coords);
    donate_compositing_buffer(kdu_compositor_buf*, kdu_dims, kdu_coords) (.cold.1);
    donate_compositing_buffer(kdu_compositor_buf*, kdu_dims, kdu_coords) (.cold.2);
    donate_compositing_buffer(kdu_compositor_buf*, kdu_dims, kdu_coords) (.cold.3);
    find_codestream_region(int, int, bool);
    find_layer_region(int, int, bool);
    find_optimal_scale(kdu_dims, float, float, float, int*, int*, int*);
    find_point(kdu_coords, int&, int&);
    flush_composition_queue();
    get_codestream_info(kdrc_stream*, int&, int&, bounded::v1::bounded_ptr<int>, int*, float*, float*);
    get_codestream_info(kdrc_stream*, int&, int&, bounded::v1::bounded_ptr<int>, int*, float*, float*) (.cold.1);
    get_codestream_info(kdrc_stream*, int&, int&, int*, int*, float*, float*);
    get_codestream_info(kdrc_stream*, int&, int&, int*, int*, float*, float*) (.cold.1);
    get_codestream_packets(kdrc_stream*, kdu_dims, long long&, long long&, long long&);
    get_composition_buffer(kdu_dims&);
    get_composition_buffer(kdu_dims&) (.cold.1);
    get_max_available_quality_layers();
    get_next_codestream(kdrc_stream*, bool, bool);
    get_next_visible_codestream(kdrc_stream*, kdu_dims, bool);
    get_total_composition_dims(kdu_dims&);
    halt_processing();
    init();
    init(kdu_thread_env*, kdu_thread_queue*);
    inspect_composition_queue(int, long long*, int*);
    internal_allocate_buffer(kdu_coords, kdu_coords&, bool);
    internal_delete_buffer(kdu_compositor_buf*);
    kdu_region_compositor();
    kdu_region_compositor(jpx_source*, int);
    kdu_region_compositor(kdu_compressed_source*, int);
    kdu_region_compositor(kdu_thread_env*, kdu_thread_queue*);
    map_region(int&, kdu_dims&);
    paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords);
    paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.1);
    paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.2);
    paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.3);
    paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.4);
    paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.5);
    paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.6);
    paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.7);
    paint_overlay(kdu_compositor_buf*, kdu_dims, kdu_dims, int, jpx_metanode, int, kdu_coords, kdu_coords, bool, bool, bool, kdu_coords, kdu_coords, kdu_coords) (.cold.8);
    pop_composition_buffer();
    pre_destroy();
    process(int, kdu_dims&);
    process(int, kdu_dims&) (.cold.1);
    process(int, kdu_dims&) (.cold.2);
    push_composition_buffer(long long, int);
    push_composition_buffer(long long, int) (.cold.1);
    push_composition_buffer(long long, int) (.cold.2);
    refresh();
    remove_compositing_layer(int, bool);
    remove_compositing_layer(int, bool) (.cold.1);
    remove_compositing_layer(int, bool) (.cold.2);
    remove_compositing_layer(int, bool) (.cold.3);
    remove_stream(kdrc_stream*, bool);
    remove_stream(kdrc_stream*, bool) (.cold.1);
    remove_stream(kdrc_stream*, bool) (.cold.2);
    reset();
    retract_compositing_buffer(kdu_coords&);
    retract_compositing_buffer(kdu_coords&) (.cold.1);
    search_overlays(kdu_coords, int&);
    set_buffer_surface(kdu_dims, int);
    set_buffer_surface(kdu_dims, int) (.cold.1);
    set_buffer_surface(kdu_dims, int) (.cold.2);
    set_error_level(int);
    set_frame(jpx_frame_expander*);
    set_frame(jpx_frame_expander*) (.cold.1);
    set_frame(jpx_frame_expander*) (.cold.2);
    set_frame(jpx_frame_expander*) (.cold.3);
    set_frame(jpx_frame_expander*) (.cold.4);
    set_layer_buffer_surfaces();
    set_max_quality_layers(int);
    set_scale(bool, bool, bool, float);
    set_single_component(int, int, kdu_component_access_mode);
    set_single_component(int, int, kdu_component_access_mode) (.cold.1);
    set_single_component(int, int, kdu_component_access_mode) (.cold.2);
    set_single_component(int, int, kdu_component_access_mode) (.cold.3);
    set_thread_env(kdu_thread_env*, kdu_thread_queue*);
    update_composition();
    update_composition() (.cold.1);
    update_overlays(bool);
    ~kdu_region_compositor();
};
