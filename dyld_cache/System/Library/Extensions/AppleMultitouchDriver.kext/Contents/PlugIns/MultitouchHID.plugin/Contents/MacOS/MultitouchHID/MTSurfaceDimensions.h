//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class MTSurfaceDimensions {
public: // (access info unavailable from symbols alone)
    MTSurfaceDimensions(MTRect, MTSize);
    convertMillimetersToPixels(MTPoint) const;
    convertMillimetersToSurfaceFraction(MTPoint) const;
    convertPixelDeltaToMillimeters(float, bool) const;
    convertPixelDeltaToMillimetersPerSecond(float, double) const;
    convertPixelDeltasToMillimetersPerSecond(MTPoint, double) const;
    convertPixelVelocityToMillimetersPerSecond(float) const;
    convertPixelsToMillimeters(MTPoint) const;
    convertPixelsToSurfaceFraction(MTPoint) const;
    convertSurfaceFractionToMillimeters(MTPoint, bool) const;
    convertSurfaceFractionToPixels(MTPoint) const;
    convertSurfaceFractionVelocityToPixelsPerSecond(MTPoint) const;
    deriveScreenPixelsFromResolution(MTSize);
    distanceToBottomEdge_mm(MTPoint) const;
    distanceToLeftEdge_mm(MTPoint) const;
    distanceToRightEdge_mm(MTPoint) const;
    distanceToTopEdge_mm(MTPoint) const;
    minDistanceToEdge_mm(MTPoint, bool, bool) const;
    updatePixelResolution_mm(MTSize);
    updateScreenBounds_mm(MTRect);
    ~MTSurfaceDimensions();
};
