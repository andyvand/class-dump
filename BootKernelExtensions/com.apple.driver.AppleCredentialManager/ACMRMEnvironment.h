//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class ACMRMEnvironment {
public: // (access info unavailable from symbols alone)
    auto clear(ACMRMEnvironment::BoolKey);
    auto clear(ACMRMEnvironment::UInt32Key);
    auto clear(ACMRMEnvironment::UInt64Key);
    auto enumerate(void (ACMRMEnvironment::BoolKey) block_pointer) const;
    auto enumerate(void (ACMRMEnvironment::UInt32Key) block_pointer) const;
    auto enumerate(void (ACMRMEnvironment::UInt64Key) block_pointer) const;
    auto get(ACMRMEnvironment::UInt32Key) const;
    auto get(ACMRMEnvironment::UInt64Key) const;
    auto getOrDefault(ACMRMEnvironment::UInt32Key, unsigned int) const;
    auto getOrDefault(ACMRMEnvironment::UInt64Key, unsigned long long) const;
    auto getOrPanic(ACMRMEnvironment::UInt32Key) const;
    auto getOrPanic(ACMRMEnvironment::UInt32Key) const (.cold.1);
    auto getOrPanic(ACMRMEnvironment::UInt64Key) const;
    auto getOrPanic(ACMRMEnvironment::UInt64Key) const (.cold.1);
    auto inRangeOrPanic(ACMRMEnvironment::BoolKey);
    auto inRangeOrPanic(ACMRMEnvironment::BoolKey) (.cold.1);
    auto inRangeOrPanic(ACMRMEnvironment::UInt32Key);
    auto inRangeOrPanic(ACMRMEnvironment::UInt32Key) (.cold.1);
    auto inRangeOrPanic(ACMRMEnvironment::UInt64Key);
    auto inRangeOrPanic(ACMRMEnvironment::UInt64Key) (.cold.1);
    auto is(ACMRMEnvironment::BoolKey) const;
    auto isSet(ACMRMEnvironment::UInt32Key) const;
    auto isSet(ACMRMEnvironment::UInt64Key) const;
    auto load(void const*, unsigned long);
    auto name(ACMRMEnvironment::BoolKey);
    auto name(ACMRMEnvironment::UInt32Key);
    auto name(ACMRMEnvironment::UInt64Key);
    auto reset();
    auto save(void*, unsigned long) const;
    auto set(ACMRMEnvironment::BoolKey, bool);
    auto set(ACMRMEnvironment::UInt32Key, unsigned int);
    auto set(ACMRMEnvironment::UInt64Key, unsigned long long);
};
