//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANBusInterfacePCIe {
public: // (access info unavailable from symbols alone)
    auto D2HDebugRingCreateComplete(AppleBCMWLANPCIeCompletionRing*, int, void*);
    auto FWSetupDone();
    auto H2DDebugRingCreateComplete(AppleBCMWLANPCIeSubmissionRing*, int, void*);
    auto ackDSRequest();
    auto allocChipImage(unsigned long long, unsigned long long, unsigned long long, unsigned long long, unsigned long long, unsigned long long) const;
    auto allocateIPCDescriptorQueues(IOService*);
    auto allocateIPCPools(IOService*);
    auto allocateIPCTimers(IOService*);
    auto allocateImageDescriptor(IOService const*, OSDictionary*) const;
    auto allocateSkywalkBusResource();
    auto attachBusTimeout(IO80211TimerSource*);
    auto attachPCIeBus(IOPCIDevice*);
    auto attachPCIeBusGated(IOPCIDevice*);
    auto attachPCIeBusGated(IOPCIDevice*)::getInterruptAttempts;
    auto attachSoCRAMFile(CCFaultReport*);
    auto busSupportsACFlowControl();
    auto busSupportsCrashTracerTrapMiniDump();
    auto busSupportsFlowId();
    auto busSupportsTrapInfo();
    auto cancelRingMgmtTimers();
    auto changeDeepSleepStateSetClear(unsigned int, unsigned char, unsigned char);
    auto changeSleepState(PowerState, unsigned int);
    auto checkAPBAccessibility(bool, bool) const;
    auto checkErrors();
    auto checkSWICMPV6Packet(AppleBCMWLANPCIeSkywalkPacket*, PacketType, unsigned long long);
    auto checkTCMAccessibility() const;
    auto checkTransactionPending();
    auto clearPreS2RResetReason();
    auto collectDeferredFaultDataCallback(CCFaultReport*);
    auto collectImmediateFaultDataCallback(CCFaultReport*);
    auto commitRings();
    auto completeChipImage(AppleBCMWLANChipImage*) const;
    auto completeFaultReportCallback(CCFaultReport const*);
    auto configHMAP();
    auto configureDevice();
    auto configurePTM(bool);
    auto configureReport(IOReportChannelList*, unsigned int, void*, void*);
    auto configureTimeSyncCapability(bool, unsigned int);
    auto configureTimeSyncCapability(bool, unsigned int)::_os_log_fmt;
    auto convertFWToHostTimestamp(BCOMIPCTimestamp, unsigned long long*, bool, int);
    auto convertPTMToAbsolute(unsigned long long);
    auto copyCrashTracerTrapMiniDump(unsigned char*, unsigned long);
    auto copyTrapInfoBlob(unsigned char*, unsigned long);
    auto crashRetainCTSWaitCommitRingsTimeout();
    auto crashRetainCTSWaitPowerOffTimeout();
    auto createCompletionRing(AppleBCMWLANPCIeCompletionRing**, PCIeRingInfo const*, OSObject*, IO80211WorkQueue*, unsigned int (*)(OSObject*, AppleBCMWLANByteRing*, void*, unsigned int), void (*)(OSObject*, AppleBCMWLANByteRing*, int, void*));
    auto createCrashTracerLogger();
    auto createCrashTracerLogger()::kalloc_type_view_16676;
    auto createCrashTracerLogger()::kalloc_type_view_16700;
    auto createCrashTracerLogger()::kalloc_type_view_16708;
    auto createCrashTracerLogger()::kalloc_type_view_16730;
    auto createDARTMappings(AppleBCMWLANChipMemory*);
    auto createDebugRingTimeout(IO80211TimerSource*);
    auto createDestroyRequestTracker(IOService*);
    auto createDynamicCompletionRing(PCIeRingInfo*, DynamicRingOperationCompletion*, unsigned int (*)(OSObject*, AppleBCMWLANByteRing*, void*, unsigned int), void (*)(OSObject*, AppleBCMWLANByteRing*, int, void*));
    auto createDynamicCompletionRingGated(AppleBCMWLANDynamicRingOperationContext*, PCIeRingInfo const*, void*, void*);
    auto createDynamicRings();
    auto createDynamicSubmissionRingGated(AppleBCMWLANDynamicRingOperationContext*, PCIeRingInfo const*, void*, void*);
    auto createFirmwareEcounterLogger();
    auto createFirmwareEcounterLogger()::kalloc_type_view_16747;
    auto createFirmwareEcounterLogger()::kalloc_type_view_16792;
    auto createFirmwareEcounterLogger()::kalloc_type_view_16800;
    auto createFirmwareEcounterLogger()::kalloc_type_view_16838;
    auto createFirmwareLogger();
    auto createFirmwareLogger()::kalloc_type_view_16564;
    auto createFirmwareLogger()::kalloc_type_view_16614;
    auto createFirmwareLogger()::kalloc_type_view_16622;
    auto createFirmwareLogger()::kalloc_type_view_16660;
    auto createFirmwarePCIeIPC(AppleBCMWLANChipMemory*);
    auto createFlowCallback(int, unsigned short);
    auto createFlowRingTimeout(IO80211TimerSource*);
    auto createReporters();
    auto createStaticRings();
    auto createSubmissionRing(AppleBCMWLANPCIeSubmissionRing**, PCIeRingInfo const*, OSObject*, IO80211WorkQueue*, unsigned int (*)(OSObject*, AppleBCMWLANByteRing*, void*, unsigned int), void (*)(OSObject*, AppleBCMWLANByteRing*, int, void*));
    auto createTimestampLogPipe(char const*, char const*);
    auto createTimestampLogPipe(char const*, char const*)::kalloc_type_view_16855;
    auto createTimestampLogPipe(char const*, char const*)::kalloc_type_view_16885;
    auto createTimestampLogStream(CCLogPipe*, char const*);
    auto createTimestampLogStream(CCLogPipe*, char const*)::kalloc_type_view_16895;
    auto createTimestampLogStream(CCLogPipe*, char const*)::kalloc_type_view_16920;
    auto createTransmitFlowRing(AppleBCMWLANPCIeTransmitFlowRing**, PCIeRingInfo&, OSObject*, BusFlowInfo&, unsigned int (*)(OSObject*, AppleBCMWLANByteRing*, void*, unsigned int), void (*)(OSObject*, AppleBCMWLANByteRing*, int, void*), unsigned char, unsigned short);
    auto d0AckTimeout(IO80211TimerSource*);
    auto deassertDeviceWake();
    auto deferredStart(thread_call*);
    auto deferredStart(thread_call*)::_os_log_fmt;
    auto deleteFlow(unsigned short, unsigned short);
    auto deleteFlowCallback(int, unsigned short);
    auto deleteFlowTimeout(IO80211TimerSource*);
    auto destroyDynamicCompletionRing(AppleBCMWLANPCIeCompletionRing*, DynamicRingOperationCompletion*);
    auto destroyDynamicCompletionRingGated(AppleBCMWLANDynamicRingOperationContext*);
    auto destroyFirmwareIPCTrackers();
    auto destroyFirmwarePCIeIPC();
    auto destroyReporters();
    auto detachPCIeBusGated(IOPCIDevice*, bool);
    auto disableCompletionRingGated(AppleBCMWLANPCIeCompletionRing*, IO80211WorkQueue*);
    auto disablePacketTimestamping();
    auto disableRingEventSources();
    auto disableSubmissionRingGated(AppleBCMWLANPCIeSubmissionRing*, IO80211WorkQueue*);
    auto disableTimeSyncEngine();
    auto disableTimeSyncEngine()::_os_log_fmt;
    auto doesHWIdentifiersKeyContainString(char const*, char const*, unsigned long);
    auto doesHWIdentifiersKeyEqualString(char const*, char const*, unsigned long);
    auto drainControlCompleteRing(AppleBCMWLANRxItemRing*, void*, unsigned int);
    auto drainDebugCompleteRing(AppleBCMWLANRxItemRing*, void*, unsigned int);
    auto drainIOPool(IOCommandPool*);
    auto dsEnableDisable();
    auto dumpAPBRegisters() const;
    auto dumpDebugInfo(OSDictionary**);
    auto dumpRegisters();
    auto dumpRegistersFWDebug();
    auto dumpSocRAMToRegistry(char const*);
    auto dumpState(char*, unsigned int, unsigned int, bool) const;
    auto dumpTimeSyncInfo(char*, int, int);
    auto dumpUCodeRegistersDebugInfo(OSDictionary**);
    auto editImage(IO80211Buffer*, Ranges&, void*);
    auto enableCompletionRingGated(AppleBCMWLANPCIeCompletionRing*, IO80211WorkQueue*);
    auto enablePacketTimestamping();
    auto enableRingEventSources();
    auto enableSubmissionRingGated(AppleBCMWLANPCIeSubmissionRing*, IO80211WorkQueue*);
    auto enableTimeSyncEngine(bool);
    auto errnoFromReturn(int);
    auto externalResetTimeout(IO80211TimerSource*);
    auto fillControlSubmitRing(AppleBCMWLANTxItemRing*, void*, unsigned int);
    auto fillDebugSubmitRing(AppleBCMWLANTxItemRing*, void*, unsigned int);
    auto fillIOPool(IOCommandPool*, unsigned int, unsigned long, unsigned int);
    auto fillRingEndpointMemory(AppleBCMWLANPCIeCompletionRing*, unsigned short);
    auto fillRingEndpointMemory(AppleBCMWLANPCIeSubmissionRing*, unsigned short);
    auto filterPCIeMSI(IOFilterInterruptEventSource*);
    auto findAndReleaseFlowQueue(IO80211FlowQueueHash*);
    auto findAndReleaseFlowQueueWithFlowId(IO80211FlowQueueHash*, unsigned short);
    auto flr();
    auto flushPendingTxIOs();
    auto flushTrackedControlIO(short, OSObject*, void*);
    auto flushTrackedD2HRingCreationRequest(short, OSObject*, void*);
    auto flushTrackedD2HRingDeleteRequest(short, OSObject*, void*);
    auto flushTrackedFlowCreationRequest(short, OSObject*, void*);
    auto flushTrackedFlowDeleteRequest(short, OSObject*, void*);
    auto flushTrackedH2DRingCreationRequest(short, OSObject*, void*);
    auto flushTrackedH2DRingDeleteRequest(short, OSObject*, void*);
    auto flushTrackedRxDataIO(short, OSObject*, void*);
    auto flushTrackedRxDebugIO(short, OSObject*, void*);
    auto flushTrackedTxDataIO(short, OSObject*, void*);
    auto forcedSoCRAMRead(OSData**, void const**);
    auto free();
    auto free()::kalloc_type_view_3259;
    auto freeResources();
    auto freeResources()::_os_log_fmt;
    auto freeResources()::kalloc_type_view_3382;
    auto freeTrapMiniDumpBufferTimeout(IO80211TimerSource*);
    auto generateChipImage(AppleBCMWLANChipImage*) const;
    auto getActiveRxBufferCount();
    auto getActiveTxPacketCount();
    auto getBusSkywalk();
    auto getChipNumber();
    auto getDriver();
    auto getFilesDB();
    auto getMaxActiveCommandCount() const;
    auto getMaxControlFrameHeaders() const;
    auto getMaxOutstandingTxDataFrames() const;
    auto getMaxRxControlFramePayload() const;
    auto getMaxTxControlFramePayload() const;
    auto getMetaClass() const;
    auto getModuleInfo(OSDictionary*, OSString**);
    auto getPacketTSCounter(unsigned int*);
    auto getProvisioningRoot() const;
    auto getSoCRAMSize() const;
    auto getTimeSyncInfo(apple80211_timesync_info*);
    auto getTimestampingCapabilities(bool*, bool*);
    auto getTransmitFlowRing(unsigned short);
    auto getTrapInfoSequenceNum();
    auto getUCodeSCRSize() const;
    auto getUCodeSHMSize() const;
    auto getWorkLoop() const;
    auto getWorkQueue() const;
    auto halt(bool);
    auto handleAdjustBusy(IO80211TimerSource*);
    auto handleAdjustBusy(IO80211TimerSource*)::_os_log_fmt;
    auto handleFWTrap();
    auto handlePCIEvent(IOPCIEventSource*, IOPCIEvent const*);
    auto handleRxTimeSync(BCOMIPCRxCompleteMessageV7*, AppleBCMWLANPCIeSkywalkPacket*, int);
    auto handleTxTimeSync(BCOMIPCTxStatusMessageV7*, AppleBCMWLANPCIeSkywalkPacket*, int);
    auto hitDoorbell(char const*);
    auto init(OSDictionary*);
    auto init(OSDictionary*)::_os_log_fmt;
    auto init(OSDictionary*)::kalloc_type_view_1627;
    auto initMRUFlowIdList();
    auto initPCIeDatapathStreams();
    auto initPCIeDatapathStreams()::kalloc_type_view_16474;
    auto initPCIeDatapathStreams()::kalloc_type_view_16530;
    auto initRingInfo();
    auto initTransmitFlows();
    auto initialPowerStateForDomainState(unsigned long);
    auto interruptPCIeMSI(IOInterruptEventSource*, int);
    auto invalidateFlowId(unsigned short, unsigned int);
    auto isAPBAccessible(bool, bool) const;
    auto isBusAccessible(bool) const;
    auto isBusRegisteredAndPoweredOn();
    auto isEPTransactionPending(bool&);
    auto isFWLowLatencySupportEnabled();
    auto isFWTrap();
    auto isPacketTimestampingEnabledGated();
    auto isPortEnableFailure();
    auto isPowerOn() const;
    auto isSecureBootEnabled();
    auto isSystemHibernating(unsigned int&);
    auto isSystemWokenByWiFi(char*, char const*, unsigned long);
    auto isTCMAccessible();
    auto isWatchdogBlocked();
    auto linkdownDetachGated(IOPCIDevice*);
    auto loadChipImage(AppleBCMWLANChipImage const*);
    auto loadImage(IO80211Buffer*, Ranges const&, void*);
    auto logPowerStateTransition(PowerState, unsigned short, unsigned short);
    auto logSWICMPV6Timestamp(AppleBCMWLANPCIeSkywalkPacket*, unsigned short, PacketType, unsigned long long);
    auto logState() const;
    auto lowerWlanRegOn();
    auto modifyASPM(kAppleBCMWLANPCIeASPMCommands);
    auto modifyASPM(kAppleBCMWLANPCIeASPMCommands)::aspmSaved;
    auto nanoPowerCycleGated();
    auto newTxBuffer(unsigned long long, IO80211BufferPool*) const;
    auto notifyDriverFail();
    auto notifyDriverFail()::_os_log_fmt;
    auto notifyPCIeAttached(void*, IOService*, IONotifier*);
    auto notifyPCIeDetached(void*, IOService*, IONotifier*);
    auto overwriteMaxRxControlFramePayload(unsigned long long);
    auto parseOTP(OSObject*, int (*)(OSObject*, unsigned char, unsigned char, void const*, void*), void*);
    auto parseVersion1Tuple(unsigned char, unsigned char, HSICOTPTupleVers1 const*);
    auto pcapFillBusState(pcap_pcie_driver_state*);
    auto pciePortErrorDispatch(IOInterruptEventSource*, int);
    auto pciePortErrorDispatch(IOInterruptEventSource*, int)::kalloc_type_view_17888;
    auto pciePortErrorHandlerCallback(void*, AppleOLYHALPortInterfacePCIeErrorHandlerEvent, OSData*);
    auto pciePortErrorHandlerCallback(void*, AppleOLYHALPortInterfacePCIeErrorHandlerEvent, OSData*)::kalloc_type_view_17819;
    auto pciePortErrorHandlerGated(AppleOLYHALPortInterfacePCIeErrorHandlerEvent, OSData*);
    auto performDebugAction(int, unsigned int, void*, unsigned long long*);
    auto platformActionHandler(unsigned int, void*);
    auto platformActionHandlerGated(unsigned int, void*);
    auto pollRxIOs();
    auto powerStateDidChangeTo(unsigned long, unsigned long, IOService*);
    auto powerStateDidChangeToGated(unsigned long, unsigned long, IOService*);
    auto prepareFRCallback(CCFaultReport const*);
    auto prepareForRingSubmission(unsigned short);
    auto prepareForWoW();
    auto printVStatus();
    auto printWorkPendingForIPCStats();
    auto probe(IOService*, int*);
    auto processChipProvisioningData();
    auto ptmAckTimeout(IO80211TimerSource*);
    auto publishHWIdentifiers();
    auto readOTP();
    auto readSoCRAM(unsigned int, IOBufferMemoryDescriptor*, unsigned long long, unsigned int);
    auto readUCodeRAM(unsigned int, OSData*, unsigned long long, unsigned int);
    auto registerDriver(AppleBCMWLANCore*, AppleBCMWLANBusInterfaceCallbacks*);
    auto registerDriver(AppleBCMWLANCore*, AppleBCMWLANBusInterfaceCallbacks*)::_os_log_fmt;
    auto releaseCTS(unsigned int);
    auto releaseFlowQueue(IO80211FlowQueue*);
    auto removeDARTMappings();
    auto reportCompleted(IO80211WorkSource*);
    auto requestFlowId(ether_addr, ether_addr, unsigned int, unsigned char, apple80211_wme_ac, unsigned short*, unsigned int*, unsigned short*, OSObject*, unsigned int (*)(OSObject*, unsigned short), unsigned int (*)(OSObject*, AppleBCMWLANByteRing*, void*, unsigned int), void (*)(OSObject*, AppleBCMWLANByteRing*, int, void*), unsigned char);
    auto requestFlowQueue(ether_addr const&, ether_addr const&, unsigned int, unsigned char);
    auto requestIOTx(requestIOTxTypes);
    auto requestIOTxGated(requestIOTxTypes*);
    auto reset();
    auto retainCTS(unsigned int, DSWaitCondition, unsigned int);
    auto ringErrorAction(AppleBCMWLANByteRing*, int, void*);
    auto scheduleRxCompleteLegacy();
    auto secondaryInterruptExecutionTimeout(IO80211TimerSource*);
    auto sendHostTimeSyncMessage(unsigned short, IO80211Buffer*, unsigned int);
    auto setAMFMPropertyHelper(OSString*, OSString*);
    auto setChipUnrecoverable();
    auto setDeviceInDeepSleep(bool) const;
    auto setLlwTxRingSize(unsigned int);
    auto setMMIOLogState(bool);
    auto setOLYHALPropertyHelper(char const*, OSObject*);
    auto setPowerState(unsigned long, IOService*);
    auto setPowerStateGated(PowerState, IOService*);
    auto setPowerStateGated(PowerState, IOService*)::_os_log_fmt;
    auto setPowerStateGated(PowerState, IOService*)::firstPowerOnAfterStart;
    auto setPowerStateGatedAMFM(PowerState, IOService*);
    auto setPowerStateGatedAMFM(PowerState, IOService*)::_os_log_fmt;
    auto setPowerStateGatedAMFM(PowerState, IOService*)::firstPowerOnAfterStart;
    auto setPreS2RResetReason(AppleBCMWLANBusReturn, char const*);
    auto setRxRingSize(unsigned int);
    auto setShutdownActionBlock(int () block_pointer);
    auto setSkywalkPacketPoolSize(unsigned int);
    auto setTxRingSize(unsigned int);
    auto setTxSubmissionQueueSize(unsigned int);
    auto setVendorIdProperty(OSString*);
    auto setupBootMSI();
    auto setupForcedTrap(forcedFwTrapReason);
    auto shouldDeferError(int);
    auto shouldGenerateFileName();
    auto signalDriverEventGated(AppleBCMWLANPCIeEvents*);
    auto sleepWakeHandler(void*, unsigned int, IOService*, void*, unsigned long);
    auto start(IOService*);
    auto start(IOService*)::_os_log_fmt;
    auto stop(IOService*);
    auto stringFromReturn(int);
    auto submitControlBufferMsg(AppleBCMWLANTxItemRing*, BCOMIPCIOCtlBufferPostMessage*, unsigned char);
    auto submitCreateFlowRequestMsg(AppleBCMWLANTxItemRing*, BCOMIPCCreateFlowRingRequestMessage*);
    auto submitD2HRingCreateMsg(AppleBCMWLANTxItemRing*, BCOMIPCCreateD2HRingMessage*);
    auto submitD2HRingDeleteMsg(AppleBCMWLANTxItemRing*, BCOMIPCDeleteD2HRingMessage*);
    auto submitDeleteFlowRequestMsg(AppleBCMWLANTxItemRing*, BCOMIPCDeleteFlowRingRequestMessage*);
    auto submitH2DRingCreateMsg(AppleBCMWLANTxItemRing*, BCOMIPCCreateH2DRingMessage*);
    auto submitH2DRingDeleteMsg(AppleBCMWLANTxItemRing*, BCOMIPCDeleteH2DRingMessage*);
    auto submitIOCtlRequestMsg(AppleBCMWLANTxItemRing*, BCOMIPCIOCtlRequestMessage*);
    auto testTimeSyncEngine(unsigned int);
    auto toggleDeviceWake(bool&);
    auto trap(unsigned int);
    auto trapDevice(unsigned int);
    auto unregisterDriver(AppleBCMWLANCore*, AppleBCMWLANBusInterfaceCallbacks*);
    auto updateAllReports();
    auto updateReport(IOReportChannelList*, unsigned int, void*, void*);
    auto updateRxBufFillState();
    auto validateWorkQueue(char const*, unsigned int) const;
    auto verifyImages(OSDictionary const*, IOImageLoader const*) const;
    auto waitForBusAttachWithTimeoutGated(unsigned int*);
    auto wake();
    auto wakeupHost();
    auto watchdogPowerCheckBypass() const;
    auto workPendingForControl(unsigned short);
    auto workPendingForDebug(unsigned short);
    auto workPendingForEpoch(unsigned long long*);
    auto workaroundPTMIfNeeded();
    auto writeH2DMailbox(unsigned int);
    auto writeRTLV(AppleBCMWLANChipMemory*, unsigned char*, unsigned int, unsigned int);
    ~AppleBCMWLANBusInterfacePCIe();
};
