//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANPCIeSkywalkTxSubmissionQueue {
public: // (access info unavailable from symbols alone)
    auto batchedReenqueuePackets();
    auto clearFWRing(unsigned char*);
    auto clearFWRings();
    auto clearFlowIdInFlowQ(unsigned short);
    auto collectQueueStats(char*, unsigned long, unsigned long, unsigned long);
    auto dequeuePackets(OSObject*, IO80211NetworkPacket**, unsigned int, void*);
    auto disable();
    auto disable()::kalloc_type_view_193;
    auto dupAndTransmitMcastPacket(IO80211NetworkPacket*, unsigned char*, unsigned char*);
    auto enable();
    auto enable()::kalloc_type_view_178;
    auto findOrCreateFlowQueue(IO80211FlowQueueHash*);
    auto forwardPacket(IO80211NetworkPacket*);
    auto free();
    auto free()::kalloc_type_view_247;
    auto freeMulticastPacket(IO80211NetworkPacket*);
    auto getAvgTxCompLatency(unsigned short, unsigned long long);
    auto getLogger() const;
    auto getMetaClass() const;
    auto getPendingPacketCount();
    auto getRingFreeSpace();
    auto initProviderAndPoolAndQueueInfo(AppleBCMWLANBusInterfacePCIe*, OSObject*, AppleBCMWLANPCIeSkywalkPacketPool*, unsigned short, unsigned int, apple80211_wme_ac, unsigned int, CCLogStream*, CCFaultReporter*)::kalloc_type_view_125;
    auto p2pMulticastTx(IO80211NetworkPacket**, unsigned int);
    auto requestDequeue(void*, unsigned int);
    auto setFWRing(AppleBCMWLANPCIeTransmitFlowRing*, unsigned short, bool);
    auto signalTxPendingFlowQueue();
    auto triggerAWDLMulticastTx(unsigned int, unsigned char);
    auto validateMacAddr(AppleBCMWLANPCIeSkywalkPacket*, AppleBCMWLANSkywalkInterface*);
    auto withProviderAndQueueInfoAndPool(AppleBCMWLANBusInterfacePCIe*, OSObject*, unsigned short, unsigned int, apple80211_wme_ac, unsigned int, AppleBCMWLANPCIeSkywalkPacketPool*, CCLogStream*, CCFaultReporter*);
    ~AppleBCMWLANPCIeSkywalkTxSubmissionQueue();
};
