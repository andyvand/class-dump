//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class ACIPCControl {
public: // (access info unavailable from symbols alone)
    ACIPCControl();
    cancelTimer();
    cancelTimesync();
    cancelTimesyncTimer();
    checkPendingMessage();
    clearChip();
    deRegisterPort(ACIPCPort*);
    deviceAlive();
    deviceAwakeCheck();
    deviceDead();
    deviceRunning();
    deviceWakeAsync();
    disableMMIOCoredumpOnError(acipcErrorType);
    enterLowPower();
    exitLowPower(bool, bool);
    getChip();
    getLinkState() const;
    getName() const;
    getSleep() const;
    hostExitSleepCheck();
    hostExitSleepCompletion();
    initialize(acipcControlParams*);
    initializeDeviceWakeDoorbell(unsigned long long*);
    linkDownCheck(int);
    linkQuiesceUnQuiesceAck(bool);
    linkUpCheck(int, bool);
    mmioRead(unsigned int volatile*, bool);
    mmioReadBar0(unsigned int, bool);
    mmioReadBar1(unsigned int, bool);
    mmioReadMemory(unsigned char volatile*, unsigned char*, unsigned int, bool);
    mmioReadMemoryBar0(unsigned int, unsigned char*, unsigned int, bool);
    mmioReadMemoryBar1(unsigned int, unsigned char*, unsigned int, bool);
    mmioReadOTP(unsigned int, unsigned char*, unsigned int, bool);
    mmioWriteBar0(unsigned int, unsigned int);
    mmioWriteBar1(unsigned int, unsigned int);
    mmioWriteMemoryBar0(unsigned int, unsigned int*, unsigned int);
    mmioWriteMemoryBar1(unsigned int, unsigned int*, unsigned int);
    msiInterrupt(unsigned char) const;
    nextTimesyncExists(bool);
    prepareShutDown();
    quiesce();
    readBar0Register(acipcPeripheralRegister, bool, unsigned int);
    readBar1Register(acipcPeripheralRegister, bool);
    registerPort(ACIPCPort*);
    scan();
    sendTimesyncMessage();
    setChip(ACIPCChip*);
    setChipInaccessible(bool);
    setDARBasedTrap(bool);
    setLowPowerSleepDetected(bool);
    setTimesyncCompleted(bool);
    sleepCheck(int);
    startQuiesceActivityTimer(unsigned long long, bool);
    startSleepTimer(unsigned long long, void*);
    startTimer(unsigned long long, acipcControlTimerMode, void*);
    startTimesyncTimer();
    terminate();
    timer(void*);
    timesyncCompletion(unsigned long long, unsigned int, int);
    timesyncTimer(void*);
    unquiesce();
    writeBar0Register(acipcPeripheralRegister, unsigned int, unsigned int);
    writeBar1Register(acipcPeripheralRegister, unsigned int);
    ~ACIPCControl();
};
