//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class ACIPCControl {
public: // (access info unavailable from symbols alone)
    ACIPCControl();
    auto cancelTimer();
    auto cancelTimesync();
    auto cancelTimesyncTimer();
    auto checkPendingMessage();
    auto clearChip();
    auto deRegisterPort(ACIPCPort*);
    auto deviceAlive();
    auto deviceAwakeCheck();
    auto deviceDead();
    auto deviceRunning();
    auto deviceWakeAsync();
    auto disableMMIOCoredumpOnError(acipcErrorType);
    auto enterLowPower();
    auto exitLowPower(bool, bool);
    auto getChip();
    auto getLinkState() const;
    auto getName() const;
    auto getSleep() const;
    auto hostExitSleepCheck();
    auto hostExitSleepCompletion();
    auto initialize(acipcControlParams*);
    auto initializeDeviceWakeDoorbell(unsigned long long*);
    auto linkDownCheck(int);
    auto linkQuiesceUnQuiesceAck(bool);
    auto linkUpCheck(int, bool);
    auto mmioRead(unsigned int volatile*, bool);
    auto mmioReadBar0(unsigned int, bool);
    auto mmioReadBar1(unsigned int, bool);
    auto mmioReadMemory(unsigned char volatile*, unsigned char*, unsigned int, bool);
    auto mmioReadMemoryBar0(unsigned int, unsigned char*, unsigned int, bool);
    auto mmioReadMemoryBar1(unsigned int, unsigned char*, unsigned int, bool);
    auto mmioReadOTP(unsigned int, unsigned char*, unsigned int, bool);
    auto mmioWriteBar0(unsigned int, unsigned int);
    auto mmioWriteBar1(unsigned int, unsigned int);
    auto mmioWriteMemoryBar0(unsigned int, unsigned int*, unsigned int);
    auto mmioWriteMemoryBar1(unsigned int, unsigned int*, unsigned int);
    auto msiInterrupt(unsigned char) const;
    auto nextTimesyncExists(bool);
    auto prepareShutDown();
    auto quiesce();
    auto readBar0Register(acipcPeripheralRegister, bool, unsigned int);
    auto readBar1Register(acipcPeripheralRegister, bool);
    auto registerPort(ACIPCPort*);
    auto scan();
    auto sendTimesyncMessage();
    auto setChip(ACIPCChip*);
    auto setChipInaccessible(bool);
    auto setDARBasedTrap(bool);
    auto setLowPowerSleepDetected(bool);
    auto setTimesyncCompleted(bool);
    auto sleepCheck(int);
    auto startQuiesceActivityTimer(unsigned long long, bool);
    auto startSleepTimer(unsigned long long, void*);
    auto startTimer(unsigned long long, acipcControlTimerMode, void*);
    auto startTimesyncTimer();
    auto terminate();
    auto timer(void*);
    auto timesyncCompletion(unsigned long long, unsigned int, int);
    auto timesyncTimer(void*);
    auto unquiesce();
    auto writeBar0Register(acipcPeripheralRegister, unsigned int, unsigned int);
    auto writeBar1Register(acipcPeripheralRegister, unsigned int);
    ~ACIPCControl();
};
