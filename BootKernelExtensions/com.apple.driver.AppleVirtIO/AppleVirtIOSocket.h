//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleVirtIOSocket {
public: // (access info unavailable from symbols alone)
    auto allocatePacket(unsigned int);
    auto attachSocket();
    auto constructTransaction(AppleVirtIOQueue*, AppleVirtIOTransaction* (*)(OSObject*));
    auto constructTransactions(unsigned int, AppleVirtIOQueue*, AppleVirtIOTransaction* (*)(OSObject*));
    auto detachSocket();
    auto didTerminate(IOService*, unsigned int, bool*);
    auto eventQueueEventHandler(AppleVirtIOQueue*, void*);
    auto flushOutputQueue();
    auto flushOutputQueue() (.cold.1);
    auto flushOutputQueue() (.cold.2);
    auto getCID();
    auto getMetaClass() const;
    auto newEventTransaction(OSObject*);
    auto newReceiveTransaction(OSObject*);
    auto newTransmitTransaction();
    auto processEventTransaction(AppleVirtIOSocketEventTransaction*);
    auto processReceiveTransaction(AppleVirtIOSocketTransaction*, unsigned int, bool*);
    auto putMessage(vsock_address, vsock_address, vsock_operation, unsigned int, unsigned int, __mbuf*);
    auto queueMessageGated(vsock_address, vsock_address, virtio_vsock_op, unsigned int, __mbuf*, unsigned int, unsigned int);
    auto queueMessageGated(vsock_address, vsock_address, virtio_vsock_op, unsigned int, __mbuf*, unsigned int, unsigned int) (.cold.1);
    auto queueMessageWithTagGated(vsock_tag*);
    auto receiveQueueEventHandler(AppleVirtIOQueue*, void*);
    auto recycleTXTransactions();
    auto start(IOService*);
    auto start(IOService*)::kalloc_type_view_184;
    auto stop(IOService*);
    auto stop(IOService*)::kalloc_type_view_225;
    auto transmitQueueEventHandler(AppleVirtIOQueue*, void*);
    ~AppleVirtIOSocket();
};
