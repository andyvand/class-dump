//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IOAHCIPortMultiplier {
public: // (access info unavailable from symbols alone)
    IOAHCIPortMultiplier();
    IOAHCIPortMultiplier(OSMetaClass const*);
    auto AbortRequests(unsigned int, int);
    auto AbortRequests(unsigned int, int) (.cold.1);
    auto AddDeviceToDeviceList(IOAHCIDevice*);
    auto AllocatePMRequest();
    auto AllocatePMRequest()::kalloc_type_view_588;
    auto ApplyWorkarounds();
    auto ComResetPMPort(unsigned int);
    auto ComResetPortMultiplier();
    auto CompleteRequest(OSObject*);
    auto CompleteRequest(OSObject*) (.cold.1);
    auto Create();
    auto CreatePMDevice(unsigned int);
    auto CreatePMDevice(unsigned int) (.cold.1);
    auto DeallocatePMRequest();
    auto DeallocatePMRequest()::kalloc_type_view_641;
    auto Destroy();
    auto DestroyPMDevice(unsigned int);
    auto DestroyPMDevice(unsigned int) (.cold.1);
    auto DetectDevicePresence(unsigned int);
    auto DisableAggressiveLinkPowerManagement();
    auto EnableAggressiveLinkPowerManagement();
    auto EnumeratePMPorts();
    auto ExecuteAHCIRequest(OSObject*);
    auto ExecuteAHCIRequest(OSObject*) (.cold.1);
    auto ExecuteAHCIRequest(OSObject*) (.cold.2);
    auto ExecuteRequest(ApplePortMultiplierIORequest*, unsigned int);
    auto FreeAHCIRequest(OSObject*);
    auto GetAHCIRequest(bool);
    auto GetDeviceForDevicePort(unsigned int);
    auto InitializePowerManagement();
    auto InitiateRecovery();
    auto PortMultiplierPowerThread();
    auto PortMultiplierSNotificationThread();
    auto PortMultiplierSNotificationThread() (.cold.1);
    auto ReadBlock0(unsigned int);
    auto ReadBlock0(unsigned int) (.cold.1);
    auto ReadBlock0(unsigned int) (.cold.2);
    auto ReadPortMultiplierRegister(unsigned int, unsigned int, unsigned int*);
    auto ReadPortMultiplierRegister(unsigned int, unsigned int, unsigned int*) (.cold.1);
    auto RecoverPortMultiplierLink();
    auto RecoverPortMultiplierLink() (.cold.1);
    auto RefreshDeviceSet();
    auto ReleaseTag(unsigned int, unsigned int);
    auto RemoveDeviceFromDeviceList(IOAHCIDevice*);
    auto ReportAHCISpecificDeviceDataSize();
    auto ReportAggressiveLinkPowerManagementState();
    auto ReportAggressiveLinkPowerManagementSupport();
    auto RequestCallback(OSObject*);
    auto RequestComplete(ApplePortMultiplierIORequest*);
    auto RequestTag(unsigned int);
    auto ScanForDevices();
    auto SendFromQueue();
    auto SendFromQueue() (.cold.1);
    auto SendFromQueue() (.cold.2);
    auto SendFromQueue() (.cold.3);
    auto SendFromQueue() (.cold.4);
    auto SendFromQueue() (.cold.5);
    auto SetAHCIRequestCount(unsigned int);
    auto SetInitialDeviceProperties(OSDictionary*);
    auto SetRecoveryState(bool);
    auto SetSNotificationState(bool);
    auto SoftResetDevicePort(unsigned int, unsigned int*);
    auto WaitForRequest(ApplePortMultiplierIORequest*, unsigned int);
    auto WritePortMultiplierRegister(unsigned int, unsigned int, unsigned int);
    auto WritePortMultiplierRegister(unsigned int, unsigned int, unsigned int) (.cold.1);
    auto attach(IOService*);
    auto attachToChild(IORegistryEntry*, IORegistryPlane const*);
    auto detach(IOService*);
    auto detachFromChild(IORegistryEntry*, IORegistryPlane const*);
    auto didTerminate(IOService*, unsigned int, bool*);
    auto finalize(unsigned int);
    auto free();
    auto getMetaClass() const;
    auto handleClose(IOService*, unsigned int);
    auto handleIsOpen(IOService const*) const;
    auto handleOpen(IOService*, unsigned int, void*);
    auto message(unsigned int, IOService*, void*);
    auto setPowerState(unsigned long, IOService*);
    auto start(IOService*);
    auto start(IOService*) (.cold.1);
    auto start(IOService*) (.cold.2);
    auto start(IOService*) (.cold.3);
    auto stop(IOService*);
    auto terminate(unsigned int);
    auto willTerminate(IOService*, unsigned int);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IOAHCIPortMultiplier();
};
