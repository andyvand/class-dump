//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANNANInterface {
public: // (access info unavailable from symbols alone)
    auto bringdownLink();
    auto bringupLink();
    auto buildNAN_CommittedAvailability(apple80211_nan_committed_availability*, unsigned char*, int);
    auto convertNANFWFrameType(unsigned char, unsigned char*, unsigned char*);
    auto copyDeviceInfo(apple80211_nan_device_info*, wl_nan_conf_status*);
    auto debugHexdump(char const*, unsigned char const*, unsigned int);
    auto disable(unsigned int);
    auto disableDatapath();
    auto disableNAN();
    auto enable(unsigned int);
    auto enableDatapath();
    auto free();
    auto free()::kalloc_type_view_460;
    auto getAWDL_DFSP_CONFIG(apple80211_awdl_dfsp_config*);
    auto getBSDNamePrefix();
    auto getHP2P_CAPS(apple80211_hp2p_caps*);
    auto getHP2P_PARAMS(void*, int, unsigned char);
    auto getIOVARString(unsigned short);
    auto getLogger() const;
    auto getMCS(unsigned int);
    auto getMetaClass() const;
    auto getNAN_ADVERTISERS(apple80211_nan_advertisers*);
    auto getNAN_BCN_TX_CONTROL(apple80211_nan_bcn_control*);
    auto getNAN_CHANNEL_SEQUENCE(apple80211_nan_chan_sequence*);
    auto getNAN_CLUSTER_ID(apple80211_nan_cluster_id*);
    auto getNAN_COMMITTED_AVAILABILITY(apple80211_nan_committed_availability*);
    auto getNAN_CURRENT_ANCHOR_MASTER_RECORD(apple80211_nan_current_amr_record_data*);
    auto getNAN_ControlExtBits(unsigned int*, unsigned int*);
    auto getNAN_DEVICE_CAPABILITY(apple80211_nan_device_capability*);
    auto getNAN_DEVICE_INFO(apple80211_nan_device_info*);
    auto getNAN_DISC_BCN_PERIOD(apple80211_nan_disc_bcn_period*);
    auto getNAN_DW_AWAKE_PERIOD(apple80211_nan_dw_awake_period*);
    auto getNAN_ENABLED(apple80211_nan_enable*);
    auto getNAN_ElectionMetric(apple80211_nan_election_metric*);
    auto getNAN_FwCapability();
    auto getNAN_GCR_LINK_CONDITION(apple80211_nan_gcr_link_condition*);
    auto getNAN_HOP_COUNT_LIMIT(apple80211_nan_hop_count_limit*);
    auto getNAN_HOP_COUNT_TO_ANCHOR_MASTER(apple80211_nan_hop_count*);
    auto getNAN_HOST_ELECTION(apple80211_nan_host_election*);
    auto getNAN_LAST_ANCHOR_MASTER_RECORD(apple80211_nan_last_amr_record_data*);
    auto getNAN_LLW_PARAMS(apple80211_llw_params*);
    auto getNAN_MASTER_CHANNEL(apple80211_nan_primary_master_channel*);
    auto getNAN_MASTER_PREFERENCE(apple80211_nan_master_pref*);
    auto getNAN_MAX_PEERS(apple80211_nan_maxpeers*);
    auto getNAN_PUBLISHED_SERVICE_LIST(apple80211_nan_published_service_list_data*);
    auto getNAN_RANDOM_FACTOR(apple80211_nan_random_factor*);
    auto getNAN_RANDOM_FACTOR_ROTATION(apple80211_nan_random_factor_rotation*);
    auto getNAN_RSSI_5G_CLOSE(apple80211_nan_rssi_5G_close*);
    auto getNAN_RSSI_5G_MIDDLE(apple80211_nan_rssi_5G_mid*);
    auto getNAN_RSSI_CLOSE(apple80211_nan_rssi_close*);
    auto getNAN_RSSI_MIDDLE(apple80211_nan_rssi_mid*);
    auto getNAN_SECONDARY_MASTER_CHANNEL(apple80211_nan_secondary_master_channel*);
    auto getNAN_SERVICE_DISCOVERY_STATS(apple80211_nan_service_disc_stats*);
    auto getNAN_SUBSCRIBED_SERVICE_LIST(apple80211_nan_subscribed_service_list_data*);
    auto getNAN_SYNC_BCN_PERIOD(apple80211_nan_sync_bcn_period*);
    auto getNAN_SYNC_STATS(apple80211_nan_sync_stats*);
    auto getNAN_TX_RATE(apple80211_nan_min_tx_rate*);
    auto getNAN_WARMUP_TIME(apple80211_nan_warmup_time*);
    auto getNAN_deviceCapability(apple80211_p2p_device_capability*);
    auto getRLLW_STATS(apple80211_rllw_stats*);
    auto getRxCompQueue();
    auto getTransmissionMode(unsigned int);
    auto getTxCompQueue();
    auto getTxPacketPool();
    auto getTxSubQueue(apple80211_wme_ac);
    auto handleCacheControlAsyncCallBack(CommandID&, int, CommandRxPayload&, void*)::kalloc_type_view_6449;
    auto handleEvent(wl_event_msg_t*);
    auto handleEventNANClusterIDChanged(wl_event_msg_t*);
    auto handleEventNANDiscoveryResultReceived(wl_event_msg_t*);
    auto handleEventNANFWDataPathTerminated(wl_event_msg_t*);
    auto handleEventNANFollowupReceived(wl_event_msg_t*);
    auto handleEventNANHostAssistRequest(wl_event_msg_t*);
    auto handleEventNANMasterChanged(wl_event_msg_t*);
    auto handleEventNANPeerScheduleUpdate(wl_event_msg_t*);
    auto handleEventNANPublishReplied(wl_event_msg_t*);
    auto handleEventNANPublishTerminated(wl_event_msg_t*);
    auto handleEventNANStateChanged(wl_event_msg_t*);
    auto handleEventNANSubscribeTerminated(wl_event_msg_t*);
    auto handleNANEventAuthFrameRx(wl_event_msg_t*);
    auto handleNANEventBeaconReceived(wl_event_msg_t*);
    auto handleNANEventChanBoundary(wl_event_msg_t*);
    auto handleNANEventDWEnd(wl_event_msg_t*);
    auto handleNANEventDWStart(wl_event_msg_t*);
    auto handleNANEventDataPathEnd(wl_event_msg_t*);
    auto handleNANEventDataPathEstablished(wl_event_msg_t*);
    auto handleNANEventNAFTxComplete(wl_event_msg_t*);
    auto handleNANEventOobActionFrameRx(wl_event_msg_t*);
    auto handleNANEventOobActionFrameTxStatus(wl_event_msg_t*);
    auto handleNANEventPeerDataPathConfirm(wl_event_msg_t*);
    auto handleNANEventPeerDataPathIndication(wl_event_msg_t*);
    auto handleNANEventPeerDataPathResponse(wl_event_msg_t*);
    auto handleNANEventRangeReportIndication(wl_event_msg_t*);
    auto handleNANEventRangeRequestIndication(wl_event_msg_t*);
    auto handleNANEventRangeResponseIndication(wl_event_msg_t*);
    auto handleNANEventRangeTerminateIndication(wl_event_msg_t*);
    auto handleNANIovarAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleNANIovarAsyncCallBack(CommandID&, int, CommandRxPayload&, void*)::kalloc_type_view_3663;
    auto handleProxIovarAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSlotBssIovarAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto init();
    auto init()::kalloc_type_view_208;
    auto init(AppleBCMWLANCore*, ether_addr*, unsigned int, char*);
    auto isChipInterfaceReady();
    auto issueGetIOVAR(unsigned short, void*, unsigned int);
    auto issueSetIOVAR(unsigned short, void*, unsigned int);
    auto issueSyncSetIOVAR(unsigned short, void*, unsigned int);
    auto postEventNANDataPathEnd(void*, apple80211_nan_data_path_end_reason);
    auto reset();
    auto resetInterface();
    auto sendNANSetIOVAR(void*, unsigned int, unsigned char)::kalloc_type_view_3886;
    auto sendNANSetIOVAR(void*, unsigned int, unsigned char)::kalloc_type_view_3894;
    auto sendNANSetIOVAR(void*, unsigned int, unsigned char)::kalloc_type_view_3918;
    auto setAWDL_DFSP_CONFIG(apple80211_awdl_dfsp_config*);
    auto setCOMPANION_SKYWALK_IF_CREATE(apple80211_companion_skywalk_if_create*);
    auto setInterfaceEnable(bool);
    auto setMacAddress(ether_addr&);
    auto setNANLLW_PARAMSCallback(CommandID&, int, CommandRxPayload&, void*);
    auto setNAN_ADDITIONAL_ATTR(apple80211_nan_vendor_payload*);
    auto setNAN_AF_TX(apple80211_nan_af_tx*);
    auto setNAN_BCN_TX_CONTROL(apple80211_nan_bcn_control*);
    auto setNAN_CANCEL_PUBLISH(apple80211_nan_cancel_publish_data*);
    auto setNAN_CANCEL_SUBSCRIBE(apple80211_nan_cancel_subscribe_data*);
    auto setNAN_CHANNEL_SEQUENCE(apple80211_nan_chan_sequence*);
    auto setNAN_CLUSTER_ID(apple80211_nan_cluster_id*);
    auto setNAN_COMMITTED_AVAILABILITY(apple80211_nan_committed_availability*);
    auto setNAN_CREATE_GCR_SESSION(apple80211_nan_create_gcr_session*);
    auto setNAN_ControlBits(unsigned int*);
    auto setNAN_ControlExtBits(unsigned int*, unsigned int*);
    auto setNAN_DATA_PATH_RESTORE(apple80211_nan_restore_data_path_restore_info*);
    auto setNAN_DISC_BCN_PERIOD(apple80211_nan_disc_bcn_period*);
    auto setNAN_DP_CONF(apple80211_nan_dp_confirm*);
    auto setNAN_DP_END(apple80211_nan_dp_terminate*);
    auto setNAN_DP_REQ(apple80211_nan_dp_request*);
    auto setNAN_DP_RESP(apple80211_nan_dp_response*);
    auto setNAN_DW_AWAKE_PERIOD(apple80211_nan_dw_awake_period*);
    auto setNAN_ELECTION_ROLE(apple80211_nan_role_cfg*);
    auto setNAN_ENABLED(apple80211_nan_enable*);
    auto setNAN_ElectionMetric(apple80211_nan_election_metric*);
    auto setNAN_EventMask(unsigned char*);
    auto setNAN_FAST_DISCOVERY(apple80211_nan_forced_disc_beacon_transmission*);
    auto setNAN_FOLLOW_UP_TRANSMIT(apple80211_nan_follow_up_transmit_data*);
    auto setNAN_GCR_RATE(apple80211_nan_gcr_rate*);
    auto setNAN_HOP_COUNT_LIMIT(apple80211_nan_hop_count_limit*);
    auto setNAN_HOP_COUNT_TO_ANCHOR_MASTER(apple80211_nan_hop_count*);
    auto setNAN_HOST_ELECTION(apple80211_nan_host_election*);
    auto setNAN_IMMUTABLE_AVAILABILITY(apple80211_nan_immutable_availability*);
    auto setNAN_INIT(apple80211_nan_init*);
    auto setNAN_JOIN_GCR_SESSION(apple80211_nan_join_gcr_session*);
    auto setNAN_LLW_PARAMS(apple80211_llw_params*);
    auto setNAN_MASTER_CHANNEL(apple80211_nan_primary_master_channel*);
    auto setNAN_MASTER_PREFERENCE(apple80211_nan_master_pref*);
    auto setNAN_MAX_PEERS(apple80211_nan_maxpeers*);
    auto setNAN_NDC_AVAILABILITY(apple80211_nan_data_cluster_availability*);
    auto setNAN_OOB_AF_TX(apple80211_nan_oob_act_frm_data*);
    auto setNAN_PAIRING_KEYS(apple80211_nan_encryption_keys*);
    auto setNAN_POTENTIAL_AVAILABILITY(apple80211_nan_potential_availability*);
    auto setNAN_PUBLISH(apple80211_nan_publish_data*);
    auto setNAN_RANDOM_FACTOR(apple80211_nan_random_factor*);
    auto setNAN_RANDOM_FACTOR_ROTATION(apple80211_nan_random_factor_rotation*);
    auto setNAN_RANGING_AVAILABILITY(apple80211_nan_ranging_availability*);
    auto setNAN_RANGING_CANCEL(apple80211_nan_ranging_cancel*);
    auto setNAN_RANGING_REQUEST(apple80211_nan_ranging_request*);
    auto setNAN_RANGING_RESPONSE(apple80211_nan_ranging_response*);
    auto setNAN_RSSI_5G_CLOSE(apple80211_nan_rssi_5G_close*);
    auto setNAN_RSSI_5G_MIDDLE(apple80211_nan_rssi_5G_mid*);
    auto setNAN_RSSI_CLOSE(apple80211_nan_rssi_close*);
    auto setNAN_RSSI_MIDDLE(apple80211_nan_rssi_mid*);
    auto setNAN_RemoveCommittedAvailability(unsigned char);
    auto setNAN_SCHEDULE_UPDATE(apple80211_nan_schedule_update*);
    auto setNAN_SECONDARY_MASTER_CHANNEL(apple80211_nan_secondary_master_channel*);
    auto setNAN_SERVICE_DESCRIPTOR_EXTENSION(apple80211_nan_service_descriptor_extension*);
    auto setNAN_SUBSCRIBE(apple80211_nan_subscribe_data*);
    auto setNAN_SYNC_BCN_PERIOD(apple80211_nan_sync_bcn_period*);
    auto setNAN_TX_RATE(apple80211_nan_min_tx_rate*);
    auto setNAN_VENDOR_PAYLOAD(apple80211_nan_vendor_payload*);
    auto setNAN_VENDOR_PAYLOAD_COMPACT(apple80211_nan_vendor_payload*);
    auto setNAN_WARMUP_TIME(apple80211_nan_warmup_time*);
    auto setP2P_COEX_PROTECTION(apple80211_p2p_coex_protection*);
    auto setPromiscuousModeEnable(bool, unsigned int);
    auto setRADIO_CHANSEQ_CONTROL(apple80211_radio_chan_seq*);
    auto setRunningState(bool);
    auto start(AppleBCMWLANCore*, IOSkywalkEthernetInterface::RegistrationInfo*);
    auto start(IOService*);
    auto stop(IOService*);
    auto stringForFrameType(unsigned char);
    auto withOptions(AppleBCMWLANCore*, ether_addr*, char*);
    ~AppleBCMWLANNANInterface();
};
