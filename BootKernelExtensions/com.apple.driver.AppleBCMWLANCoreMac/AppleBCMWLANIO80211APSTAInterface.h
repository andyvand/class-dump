//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANIO80211APSTAInterface {
public: // (access info unavailable from symbols alone)
    auto checkForAppleIE(unsigned char*, unsigned int);
    auto checkForStationListMismatch(apple80211_sta_data*);
    auto checkIfAllStaAreInLPM()::_os_log_fmt;
    auto configureBeaconDutyCycleParams(apple80211_softap_ps_dynamic_level);
    auto configureLowPowerModeExit();
    auto configureMPDUSize(unsigned int);
    auto configureManagementFrameProtectionForSoftAP(unsigned int);
    auto configureSoftAPPeerStats(bool);
    auto configureSoftAPPeerStats(bool)::kalloc_type_view_5486;
    auto configureSoftAPPeerStats(bool)::kalloc_type_view_5496;
    auto convertBCMAssocListToAppleAssocList(maclist*, apple80211_sta_data*);
    auto deleteIPv4PktFiltersAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto disable(unsigned int);
    auto disableDatapath();
    auto enable(unsigned int);
    auto enableAPInterface();
    auto enableDatapath();
    auto forwardPacket(IO80211NetworkPacket*);
    auto free();
    auto free()::kalloc_type_view_721;
    auto freeResources();
    auto getAssocListAsyncCallback(CommandID&, int, CommandRxPayload&, void*);
    auto getBSDNamePrefix();
    auto getBSDUnitNumber();
    auto getCHANNEL(apple80211_channel_data*);
    auto getHOST_AP_MODE_HIDDEN(apple80211_host_ap_mode_hidden_t*);
    auto getInterfaceSubFamily();
    auto getKEY_RSC(apple80211_key*);
    auto getLogger() const;
    auto getMetaClass() const;
    auto getMultiCastQueue();
    auto getNumTxQueues();
    auto getOP_MODE(apple80211_opmode_data*);
    auto getPEER_CACHE_MAXIMUM_SIZE(apple80211_peer_cache_maximum_size*);
    auto getRxCompQueue();
    auto getRxPacketPool();
    auto getRxQueueCapacity();
    auto getSOFTAP_PARAMS(apple80211_softap_params*);
    auto getSOFTAP_STATS(apple80211_softap_stats*);
    auto getSSID(apple80211_ssid_data*);
    auto getSTATE(apple80211_state_data*);
    auto getSTATION_LIST(apple80211_sta_data*);
    auto getSTA_IE_LIST(apple80211_sta_ie_data*);
    auto getSTA_STATS(apple80211_sta_stats_data*);
    auto getTxCompQueue();
    auto getTxHeadroom();
    auto getTxPacketPool();
    auto getTxQueueDepth();
    auto getTxSubQueue(apple80211_wme_ac);
    auto handleAPStatsUpdates(IO80211TimerSource*);
    auto handleActionFrame(wl_event_msg_t*)::_os_log_fmt;
    auto handleEvent(wl_event_msg_t*);
    auto handleSetARPHostIPAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSetARPHostIPClearAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSetARPOffloadAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSetBcnDTIMPeriodAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSetBcnIntervalAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSetBcnWaitPeriodAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSetLowPowerModeAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSetRpsNoaAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSetScbProbeAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSoftAPStatsConfigAsyncCallback(CommandID&, int, CommandRxPayload&, void*);
    auto handleSoftAPStatsConfigAsyncCallback(CommandID&, int, CommandRxPayload&, void*)::kalloc_type_view_5532;
    auto holdSoftAPPowerAssertion();
    auto hostAPPowerOff();
    auto init();
    auto init()::kalloc_type_view_210;
    auto init(AppleBCMWLANCore*, ether_addr*, unsigned int, char*);
    auto init(AppleBCMWLANCore*, ether_addr*, unsigned int, char*)::kalloc_type_view_228;
    auto initSoftAPParameters();
    auto isSoftAPConcurrencyEnabled();
    auto monitorAPInterface(IO80211TimerSource*);
    auto parseRSNXE(unsigned char*, unsigned int, unsigned char*);
    auto postMessageForSTA(unsigned int, void*, unsigned short);
    auto powerStateChangeReasonToString(apple80211_softap_ps_change_reason);
    auto printDataPath(userPrintCtx*);
    auto programAppleVendorIE();
    auto programVendorIEList(unsigned char*, unsigned int);
    auto releaseSoftAPPowerAssertion();
    auto removeStaFromStaTable(unsigned char);
    auto reset();
    auto runPowerSaveStateMachine()::_os_log_fmt;
    auto setBeaconDutyCycle(unsigned int);
    auto setBeaconInterval(unsigned short);
    auto setCHANNEL(apple80211_channel_data*);
    auto setCIPHER_KEY(apple80211_key*);
    auto setHOST_AP_MODE(apple80211_network_data*);
    auto setHOST_AP_MODE_HIDDEN(apple80211_host_ap_mode_hidden_t*);
    auto setHostApModeInternal(apple80211_network_data*);
    auto setMIS_MAX_STA(apple80211_mis_max_sta*);
    auto setMacAddress(ether_addr&);
    auto setMaxAssoc(unsigned int);
    auto setPEER_CACHE_CONTROL(apple80211_peer_cache_control*);
    auto setPowerSaveState(apple80211_softap_power_state, apple80211_softap_ps_change_reason);
    auto setPowerSaveState(apple80211_softap_power_state, apple80211_softap_ps_change_reason)::_os_log_fmt;
    auto setRSN_CONF(apple80211_rsn_conf_data*);
    auto setSOFTAP_EXTENDED_CAPABILITIES_IE(apple80211_softap_extended_capabilities_info*);
    auto setSOFTAP_PARAMS(apple80211_softap_params*);
    auto setSOFTAP_TRIGGER_CSA(apple80211_softap_csa_params*);
    auto setSOFTAP_WIFI_NETWORK_INFO_IE(apple80211_softap_wifi_network_info*);
    auto setSSID(apple80211_ssid_data*);
    auto setSTA_AUTHORIZE(apple80211_sta_authorize_data*);
    auto setSTA_DEAUTH(apple80211_sta_disassoc_data*);
    auto setSTA_DISASSOCIATE(apple80211_sta_disassoc_data*);
    auto softApStatsAccumulatePowerStateDuration(apple80211_softap_power_state, unsigned long long);
    auto start(AppleBCMWLANCore*, IOSkywalkEthernetInterface::RegistrationInfo*);
    auto start(IOService*);
    auto stop(IOService*);
    auto updateRxCounter(unsigned long long);
    auto updateSTAAssocInfo(wl_event_msg_t*);
    auto withOptions(AppleBCMWLANCore*, ether_addr*, unsigned int, char*);
    ~AppleBCMWLANIO80211APSTAInterface();
};
