//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANCommandQueue {
public: // (access info unavailable from symbols alone)
    AppleBCMWLANCommandQueue();
    AppleBCMWLANCommandQueue(OSMetaClass const*);
    auto dequeue();
    auto dequeueBlocking();
    auto dequeueLast();
    auto enqueue(AppleBCMWLANCommandQueue*);
    auto enqueue(AppleBCMWLANCommandQueue*, unsigned int);
    auto enqueue(IOCommand*);
    auto enqueueFirst(IOCommand*);
    auto filter(bool (*)(OSObject*, IOCommand*, void*), queue_entry*, OSObject*, void*);
    auto filter(bool (IOCommand*) block_pointer, queue_entry*);
    auto filterInternal(bool (*)(OSObject*, IOCommand*, void*), bool (IOCommand*) block_pointer, OSObject*, void*, queue_entry*);
    auto flush();
    auto free();
    auto free()::kalloc_type_view_118;
    auto getCapacity() const;
    auto getMetaClass() const;
    auto getSize() const;
    auto initWithGateAndCapacity(IO80211CommandGate*, unsigned int);
    auto initWithGateAndCapacity(IO80211CommandGate*, unsigned int)::kalloc_type_view_75;
    auto lockDequeue();
    auto lockDequeueBlocking();
    auto lockEnqueue(AppleBCMWLANCommandQueue*);
    auto lockEnqueue(AppleBCMWLANCommandQueue*, unsigned int);
    auto lockEnqueue(IOCommand*);
    auto lockEnqueueFirst(IOCommand*);
    auto lockFilter(bool (*)(OSObject*, IOCommand*, void*), queue_entry*, OSObject*, void*);
    auto lockFilter(bool (IOCommand*) block_pointer, queue_entry*);
    auto lockFlush();
    auto lockMap(AppleBCMWLANCommandQueueMapAction_t (*)(OSObject*, IOCommand*, void*), OSObject*, void*);
    auto lockPeek();
    auto lockRemove(IOCommand*);
    auto map(AppleBCMWLANCommandQueueMapAction_t (*)(OSObject*, IOCommand*, void*), OSObject*, void*);
    auto map(queue_entry*, AppleBCMWLANCommandQueueMapAction_t (*)(OSObject*, IOCommand*, void*), OSObject*, void*);
    auto peek();
    auto remove(IOCommand*);
    auto withCapacity(unsigned int);
    auto withGateAndCapacity(IO80211CommandGate*, unsigned int);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~AppleBCMWLANCommandQueue();
};
