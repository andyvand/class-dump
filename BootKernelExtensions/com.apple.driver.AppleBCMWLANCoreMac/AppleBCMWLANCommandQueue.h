//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANCommandQueue {
public: // (access info unavailable from symbols alone)
    AppleBCMWLANCommandQueue();
    AppleBCMWLANCommandQueue(OSMetaClass const*);
    dequeue();
    dequeueBlocking();
    dequeueLast();
    enqueue(AppleBCMWLANCommandQueue*);
    enqueue(AppleBCMWLANCommandQueue*, unsigned int);
    enqueue(IOCommand*);
    enqueueFirst(IOCommand*);
    filter(bool (*)(OSObject*, IOCommand*, void*), queue_entry*, OSObject*, void*);
    filter(bool (IOCommand*) block_pointer, queue_entry*);
    filterInternal(bool (*)(OSObject*, IOCommand*, void*), bool (IOCommand*) block_pointer, OSObject*, void*, queue_entry*);
    flush();
    free();
    free()::kalloc_type_view_118;
    getCapacity() const;
    getMetaClass() const;
    getSize() const;
    initWithGateAndCapacity(IO80211CommandGate*, unsigned int);
    initWithGateAndCapacity(IO80211CommandGate*, unsigned int)::kalloc_type_view_75;
    lockDequeue();
    lockDequeueBlocking();
    lockEnqueue(AppleBCMWLANCommandQueue*);
    lockEnqueue(AppleBCMWLANCommandQueue*, unsigned int);
    lockEnqueue(IOCommand*);
    lockEnqueueFirst(IOCommand*);
    lockFilter(bool (*)(OSObject*, IOCommand*, void*), queue_entry*, OSObject*, void*);
    lockFilter(bool (IOCommand*) block_pointer, queue_entry*);
    lockFlush();
    lockMap(AppleBCMWLANCommandQueueMapAction_t (*)(OSObject*, IOCommand*, void*), OSObject*, void*);
    lockPeek();
    lockRemove(IOCommand*);
    map(AppleBCMWLANCommandQueueMapAction_t (*)(OSObject*, IOCommand*, void*), OSObject*, void*);
    map(queue_entry*, AppleBCMWLANCommandQueueMapAction_t (*)(OSObject*, IOCommand*, void*), OSObject*, void*);
    peek();
    remove(IOCommand*);
    withCapacity(unsigned int);
    withGateAndCapacity(IO80211CommandGate*, unsigned int);
    ~AppleBCMWLANCommandQueue();
};
