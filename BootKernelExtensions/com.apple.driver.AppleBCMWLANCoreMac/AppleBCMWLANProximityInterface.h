//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANProximityInterface {
public: // (access info unavailable from symbols alone)
    auto bringdownLink();
    auto bringupLink();
    auto buildAWDLChanSequence(apple80211_awdl_sync_channel_sequence*, awdl_channel_sequence*, unsigned short*);
    auto buildChanSequenceCommand(apple80211_awdl_sync_channel_sequence*, apple80211_nan_committed_availability*);
    auto clearTrafficRegistrationState();
    auto deleteChipInterface();
    auto disable(unsigned int);
    auto disableDatapath();
    auto doSetSyncState(bool, bool);
    auto dump(char*, unsigned int, int);
    auto dumpAwdlPeers(char*, unsigned int, int);
    auto dumpAwdlStats(char*, unsigned int, unsigned int);
    auto dumpPeerCache(char*, unsigned int, unsigned int);
    auto dumpSyncParams(char*, unsigned int, unsigned int);
    auto enable(unsigned int);
    auto enableDatapath();
    auto enableRoamEvents();
    auto flushFlowQueues(ether_addr*);
    auto free();
    auto free()::kalloc_type_view_1076;
    auto freeMulticastPacket(IO80211NetworkPacket*);
    auto freePendingPackets(IO80211NetworkPacket**, unsigned int);
    auto freeResources();
    auto getAWDL_AF_SECONDARY_PAYLOAD_CAPABILITIES(apple80211_awdl_cap*);
    auto getAWDL_AF_SECONDARY_PAYLOAD_CAPABILITIES(apple80211_awdl_cap*)::kalloc_type_view_2887;
    auto getAWDL_AF_SECONDARY_PAYLOAD_CAPABILITIES(apple80211_awdl_cap*)::kalloc_type_view_2914;
    auto getAWDL_AF_TX_MODE(apple80211_awdl_af_tx_mode*);
    auto getAWDL_CCA(apple80211_awdl_cca*);
    auto getAWDL_DEVICE_CAPABILITIES(apple80211_awdl_device_capabilities*);
    auto getAWDL_DEV_STATS(apple80211_awdl_dev_stats*);
    auto getAWDL_DFSP_CONFIG(apple80211_awdl_dfsp_config*);
    auto getAWDL_ELECTION_ID(apple80211_awdl_election_id*);
    auto getAWDL_ELECTION_METRIC(apple80211_awdl_election_metric*);
    auto getAWDL_ELECTION_PARAMS(apple80211_awdl_election_params*);
    auto getAWDL_ELECTION_RSSI_THRESHOLDS(apple80211_awdl_election_rssi_thresholds*);
    auto getAWDL_ENCRYPTION_TYPE(apple80211_encrypt*);
    auto getAWDL_EXTENSION_STATE_MACHINE_PARAMETERS(apple80211_awdl_extension_state_machine_parameter*);
    auto getAWDL_MASTER_CHANNEL(apple80211_awdl_master_channel*);
    auto getAWDL_MIN_RATE(apple80211_awdl_min_rate*);
    auto getAWDL_PRESENCE_MODE(apple80211_awdl_presence_mode*);
    auto getAWDL_SECONDARY_MASTER_CHANNEL(apple80211_awdl_secondary_master_channel*);
    auto getAWDL_SYNCHRONIZATION_CHANNEL_SEQUENCE(apple80211_awdl_sync_channel_sequence*);
    auto getAWDL_SYNC_ENABLED(apple80211_awdl_sync_enabled*);
    auto getAWDL_SYNC_PARAMS(apple80211_awdl_sync_params*);
    auto getAWDL_SYNC_STATE(apple80211_awdl_sync_state*);
    auto getAWDL_TXCAL_PERIOD(apple80211_awdl_txcal_period*);
    auto getBSDNamePrefix();
    auto getBSSID(apple80211_bssid_data*);
    auto getCHANNEL(apple80211_channel_data*);
    auto getElectionParams(awdl_election_tree_info*);
    auto getLLW_PARAMS(apple80211_llw_params*);
    auto getLogger() const;
    auto getMetaClass() const;
    auto getPEER_CACHE_MAXIMUM_SIZE(apple80211_peer_cache_maximum_size*);
    auto getRxCompQueue();
    auto getSSID(apple80211_ssid_data*);
    auto getSTATE(apple80211_state_data*);
    auto getTX_RATE(apple80211_tx_rate*);
    auto getTxCompQueue();
    auto getTxPacketPool();
    auto getTxSubQueue(apple80211_wme_ac);
    auto handleActionFrame_rx(IO80211BufferCursor const&);
    auto handleAvailabilityWindowEnd(IO80211BufferCursor const&);
    auto handleAvailabilityWindowExtension(IO80211BufferCursor const&);
    auto handleAvailabilityWindowStart(IO80211BufferCursor const&);
    auto handleCacheControlAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleElectionTreeIovarAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleEvent(IO80211BufferCursor const&);
    auto handleFlowControlledProxIovarAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleMasterEvent();
    auto handleNonElectionMasterEvent();
    auto handleOobAfIovarAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleOobAfIovarAsyncCallBack(CommandID&, int, CommandRxPayload&, void*)::kalloc_type_view_2427;
    auto handleProbeResponse_rx(wl_event_msg_t const*);
    auto handleProxIovarAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleProxSlotBSSCompletion(CommandID&, int, CommandRxPayload&, void*);
    auto handleSlaveEvent();
    auto handleSlotBssIovarAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto init();
    auto init()::kalloc_type_view_904;
    auto init(AppleBCMWLANCore*, ether_addr*, unsigned int, char*);
    auto isChipInterfaceReady();
    auto logElectionTree(char const*);
    auto p2pMulticastTx(IO80211NetworkPacket**, unsigned int);
    auto printDataPath(userPrintCtx*);
    auto reset();
    auto resetInterface();
    auto sendIOVarSetCommand(iovec*, unsigned int, char const*);
    auto sendPendingPacketsToStack(IO80211NetworkPacket**, unsigned int);
    auto sendUnicastAFIOVar(iovec*, unsigned int)::kalloc_type_view_2475;
    auto sendUnicastAFIOVar(iovec*, unsigned int)::kalloc_type_view_2492;
    auto setAWDL_AF_TX_MODE(apple80211_awdl_af_tx_mode*);
    auto setAWDL_AWDL_ADVERTISERS(awdl_peer_advert_add*);
    auto setAWDL_BSSID(apple80211_awdl_bssid*);
    auto setAWDL_CONFIG(apple80211_awdl_config_request*);
    auto setAWDL_DFSP_CONFIG(apple80211_awdl_dfsp_config*);
    auto setAWDL_DFSP_UCSA_CONFIG(apple80211_awdl_dfsp_ucsa_config*);
    auto setAWDL_ELECTION_ID(apple80211_awdl_election_id*);
    auto setAWDL_ELECTION_METRIC(apple80211_awdl_election_metric*);
    auto setAWDL_ELECTION_PARAMS(apple80211_awdl_election_params*);
    auto setAWDL_ELECTION_RSSI_THRESHOLDS(apple80211_awdl_election_rssi_thresholds*);
    auto setAWDL_ENCRYPTION_KEYS(apple80211_key*);
    auto setAWDL_ENCRYPTION_TYPE(apple80211_encrypt*);
    auto setAWDL_EXTENSION_STATE_MACHINE_PARAMETERS(apple80211_awdl_extension_state_machine_parameter*);
    auto setAWDL_MASTER_CHANNEL(apple80211_awdl_master_channel*);
    auto setAWDL_MIN_RATE(apple80211_awdl_min_rate*);
    auto setAWDL_OOB_AUTO_REQUEST(apple80211_awdl_oob_request*);
    auto setAWDL_OOB_REQUEST(apple80211_awdl_oob_request*);
    auto setAWDL_OOB_SEC_PAYLOAD_REQUEST(apple80211_awdl_af_sec_payload_request*);
    auto setAWDL_PEER_TRAFFIC_REGISTRATION(apple80211_awdl_peer_traffic_registration*);
    auto setAWDL_PRESENCE_MODE(apple80211_awdl_presence_mode*);
    auto setAWDL_RTG_PEER_STATS_CONFIG(apple80211_rtg_peer_stats_cfg*);
    auto setAWDL_SECONDARY_MASTER_CHANNEL(apple80211_awdl_secondary_master_channel*);
    auto setAWDL_SYNCHRONIZATION_CHANNEL_SEQUENCE(apple80211_awdl_sync_channel_sequence*);
    auto setAWDL_SYNC_ENABLED(apple80211_awdl_sync_enabled*);
    auto setAWDL_SYNC_FRAME_TEMPLATE(apple80211_awdl_sync_frame_template*);
    auto setAWDL_SYNC_PARAMS(apple80211_awdl_sync_params*);
    auto setAWDL_SYNC_STATE(apple80211_awdl_sync_state*);
    auto setAWDL_TXCAL_PERIOD(apple80211_awdl_txcal_period*);
    auto setAfTxMode();
    auto setCHANNEL(apple80211_channel_data*);
    auto setCOMPANION_SKYWALK_IF_CREATE(apple80211_companion_skywalk_if_create*);
    auto setChannelAsyncCallback(CommandID&, int, CommandRxPayload&, void*);
    auto setInterfaceEnable(bool);
    auto setLLW_PARAMS(apple80211_llw_params*);
    auto setLLW_PARAMSCallback(CommandID&, int, CommandRxPayload&, void*);
    auto setLowLatencyTrafficState(bool, bool);
    auto setMacAddress(ether_addr&);
    auto setP2P_COEX_PROTECTION(apple80211_p2p_coex_protection*);
    auto setPEER_CACHE_CONTROL(apple80211_peer_cache_control*);
    auto setPSF_DWELL(apple80211_awdl_psf_dwell_params*);
    auto setPromiscuousModeEnable(bool, unsigned int);
    auto setSSID(apple80211_ssid_data*);
    auto setSYNC_ENABLED_internal(apple80211_awdl_sync_enabled*);
    auto setSyncParams();
    auto setTX_RATE(apple80211_tx_rate*);
    auto signalDriverEvent(AppleBCMWLANProximityInterfaceEvents);
    auto start(AppleBCMWLANCore*, IOSkywalkEthernetInterface::RegistrationInfo*);
    auto start(IOService*);
    auto stop(IOService*);
    auto triggerAWDLMulticastTx(unsigned int, unsigned char);
    auto waitForDriverEvent(AppleBCMWLANProximityInterfaceEvents, unsigned int);
    auto withOptions(AppleBCMWLANCore*, ether_addr*, char*);
    ~AppleBCMWLANProximityInterface();
};
