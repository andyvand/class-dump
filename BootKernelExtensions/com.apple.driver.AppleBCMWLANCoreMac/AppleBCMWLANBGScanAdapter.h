//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANBGScanAdapter {
public: // (access info unavailable from symbols alone)
    auto collectBGScanCachedResults(apple80211_bgscan_cached_network_data_list*);
    auto configEPNO(unsigned char);
    auto configPNO(unsigned char);
    auto configureBGScanParams(unsigned char);
    auto configureDynamicScanFreq(unsigned char, scan_freq_params*);
    auto configureMotionProfileEPNO(apple80211_bg_motion_profile*);
    auto configureMotionProfileMapping();
    auto configureMotionProfilePNO(apple80211_bg_motion_profile*);
    auto configurePFN(unsigned int);
    auto configurePFNSuspend(unsigned int);
    auto configurePrivateMACForScans(unsigned int);
    auto configureUnAssociatedScanTime(unsigned char, unsigned int);
    auto disablePrivateMACForScans();
    auto enablePrivateMACForScans();
    auto free();
    auto free()::kalloc_type_view_172;
    auto generateAndApplyNewPrivateMACForScans();
    auto getBGScanMacRandomMask(unsigned char*);
    auto getBGScanPrivateMac(unsigned char*);
    auto getMetaClass() const;
    auto getMotionProfileState();
    auto getPrevAndCurrentBGScanPrivateMac(unsigned char*, unsigned char*);
    auto getWCL_BGSCAN_CACHE_RESULT(apple80211_bgscan_cached_network_data_list*);
    auto handleGetMotionStateAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleGetScanMacStateAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handlePFNSuspendAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSetDynamicScanFreqAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto handleSetPrivateMACForScansAsyncCallBack(CommandID&, int, CommandRxPayload&, void*);
    auto initWithDriver(AppleBCMWLANCore*)::kalloc_type_view_113;
    auto parseBGScanEvent(wl_event_msg_t*);
    auto parsePFNAllGoneExtEvent(wl_event_msg_t*);
    auto retrieveBGScanCachedInfo(bool*, apple80211_bgscan_cached_network_data_list*);
    auto scanBackoffEvent(wl_event_msg_t*);
    auto scanPrivacyMACTimer(IO80211TimerSource*);
    auto setWCL_CONFIG_BGSCAN(apple80211_bg_scan*);
    auto setWCL_CONFIG_BG_MOTIONPROFILE(apple80211_bg_motion_profile*);
    auto setWCL_CONFIG_BG_NETWORK(apple80211_bg_network*);
    auto setWCL_CONFIG_BG_PARAMS(apple80211_bg_params*);
    auto withDriver(AppleBCMWLANCore*);
    ~AppleBCMWLANBGScanAdapter();
};
