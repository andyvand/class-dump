//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANSkywalkInterface {
public: // (access info unavailable from symbols alone)
    AppleBCMWLANSkywalkInterface();
    AppleBCMWLANSkywalkInterface(OSMetaClass const*);
    auto bpfTap(unsigned int, unsigned int);
    auto disable(unsigned int);
    auto disableDatapath();
    auto enable(unsigned int);
    auto enableDatapath();
    auto findPeer(ether_addr&);
    auto flushFlowQueues(ether_addr*);
    auto free();
    auto free()::kalloc_type_view_181;
    auto getController();
    auto getHardwareAssists();
    auto getInterfaceUnitNumber();
    auto getLogger() const;
    auto getMetaClass() const;
    auto getMultiCastQueue();
    auto getNumTxQueues();
    auto getRunningState();
    auto getRxCompQueue();
    auto getRxPacketPool();
    auto getRxQueueCapacity();
    auto getSupportedMediaArray(unsigned int*, unsigned int*);
    auto getTxCompQueue();
    auto getTxHeadroom();
    auto getTxPacketPool();
    auto getTxQueueDepth();
    auto getTxSubQueue(apple80211_wme_ac);
    auto getTxSubQueue(unsigned int);
    auto handleChosenMedia(unsigned int);
    auto hwConfigNicProxyData(nicproxy_info_s*);
    auto init();
    auto init()::kalloc_type_view_104;
    auto initWithProviderAndParameters(AppleBCMWLANCore*, unsigned int, SkywalkInterfaceRole, bool, unsigned char, apple80211_wme_ac*, unsigned int, CCLogStream*, CCFaultReporter*);
    auto isDataPathConnected();
    auto isEnabled();
    auto notifyQueueState(apple80211_wme_ac, unsigned short);
    auto packetSpace(unsigned char);
    auto pendingPackets(unsigned char);
    auto powerOffNotification();
    auto powerOnNotification();
    auto printDataPath(userPrintCtx*);
    auto removePacketQueue(IO80211FlowQueueHash*);
    auto reportDatapathIssue(int, void*, unsigned long long);
    auto reportNicProxyLimits(nicproxy_limits_info_s&);
    auto reset();
    auto retrievePendingPacketsOnRoam(unsigned int*, unsigned int*);
    auto setAllMulticastModeEnable(bool);
    auto setCurrentApAddress(ether_addr*);
    auto setInterfaceUnitNumber(int);
    auto setMacAddress(mloAddrArray&);
    auto setMulticastAddresses(ether_addr const*, unsigned int);
    auto setPromiscuousModeEnable(bool, unsigned int);
    auto setRunningState(bool);
    auto start(AppleBCMWLANCore*, IOSkywalkEthernetInterface::RegistrationInfo*);
    auto start(IOService*);
    auto statsDump();
    auto stop(IOService*);
    auto updateRxCounter(unsigned long long);
    auto withProvider(AppleBCMWLANCore*, unsigned int, SkywalkInterfaceRole, CCLogStream*, CCFaultReporter*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~AppleBCMWLANSkywalkInterface();
};
