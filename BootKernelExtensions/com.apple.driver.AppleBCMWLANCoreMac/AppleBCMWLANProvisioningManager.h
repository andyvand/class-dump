//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANProvisioningManager {
public: // (access info unavailable from symbols alone)
    auto addSInt16ValToImage(char const*, short, ProvisionControl&) const;
    auto addStringValToImage(char const*, char*);
    auto checkNormalizedNVRAM(IO80211Buffer*, Ranges&, void*) const;
    auto checkRegulatorTable(IO80211Buffer*, Ranges&, void*) const;
    auto checkTxCapTable(IO80211Buffer*, Ranges&, void*) const;
    auto free();
    auto free()::kalloc_type_view_284;
    auto gather24GHzRxCal(ProvisionedData&) const;
    auto gather24GHzTxCal(ProvisionedData&) const;
    auto gather50GHzRxCal(ProvisionedData&) const;
    auto gather50GHzTxCal(ProvisionedData&) const;
    auto gatherBaudRate(ProvisionedData&) const;
    auto gatherDeviceTreeData(ProvisionedData&) const;
    auto gatherMacAddress(ProvisionedData&) const;
    auto gatherRxFreqGroup2G(ProvisionedData&) const;
    auto gatherRxTempCal(ProvisionedData&) const;
    auto generateData(ProvisionedData&) const;
    auto generateProvisionedNVRAMParameters(IO80211Buffer*, Ranges&, void*);
    auto getMetaClass() const;
    auto getProvisionedData(ProvisionedData*);
    auto initWithProvisioningRoot(IOService*, AppleBCMWLANBusInterface*);
    auto initWithProvisioningRoot(IOService*, AppleBCMWLANBusInterface*)::kalloc_type_view_221;
    auto logNVRAM(IO80211Buffer*, Ranges&, void*) const;
    auto normalizeNVRAM(IO80211Buffer*, Ranges&, void*) const;
    auto parseOTPTuple(unsigned char, unsigned char, void const*, void*) const;
    auto populateDeviceTree(ProvisionedData const&);
    auto prepareNVRAMString(char*, unsigned short, signed char*, unsigned short, char const*, ProvisionControl&);
    auto processProvisionedData();
    auto validateData(ProvisionedData const&) const;
    auto withProvisioningRoot(IOService*, AppleBCMWLANBusInterface*);
    ~AppleBCMWLANProvisioningManager();
};
