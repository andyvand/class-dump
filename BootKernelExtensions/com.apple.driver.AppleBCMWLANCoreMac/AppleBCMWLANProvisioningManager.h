//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleBCMWLANProvisioningManager {
public: // (access info unavailable from symbols alone)
    addSInt16ValToImage(char const*, short, ProvisionControl&) const;
    addStringValToImage(char const*, char*);
    checkNormalizedNVRAM(IO80211Buffer*, Ranges&, void*) const;
    checkRegulatorTable(IO80211Buffer*, Ranges&, void*) const;
    checkTxCapTable(IO80211Buffer*, Ranges&, void*) const;
    free();
    free()::kalloc_type_view_284;
    gather24GHzRxCal(ProvisionedData&) const;
    gather24GHzTxCal(ProvisionedData&) const;
    gather50GHzRxCal(ProvisionedData&) const;
    gather50GHzTxCal(ProvisionedData&) const;
    gatherBaudRate(ProvisionedData&) const;
    gatherDeviceTreeData(ProvisionedData&) const;
    gatherMacAddress(ProvisionedData&) const;
    gatherRxFreqGroup2G(ProvisionedData&) const;
    gatherRxTempCal(ProvisionedData&) const;
    generateData(ProvisionedData&) const;
    generateProvisionedNVRAMParameters(IO80211Buffer*, Ranges&, void*);
    getMetaClass() const;
    getProvisionedData(ProvisionedData*);
    initWithProvisioningRoot(IOService*, AppleBCMWLANBusInterface*);
    initWithProvisioningRoot(IOService*, AppleBCMWLANBusInterface*)::kalloc_type_view_221;
    logNVRAM(IO80211Buffer*, Ranges&, void*) const;
    normalizeNVRAM(IO80211Buffer*, Ranges&, void*) const;
    parseOTPTuple(unsigned char, unsigned char, void const*, void*) const;
    populateDeviceTree(ProvisionedData const&);
    prepareNVRAMString(char*, unsigned short, signed char*, unsigned short, char const*, ProvisionControl&);
    processProvisionedData();
    validateData(ProvisionedData const&) const;
    withProvisioningRoot(IOService*, AppleBCMWLANBusInterface*);
    ~AppleBCMWLANProvisioningManager();
};
