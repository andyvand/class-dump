//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class compositedisk_manager {
public: // (access info unavailable from symbols alone)
    alg_cancel_range(ioreq_ent*, bool);
    alg_demoted_range(phy_ext_addr const&, phy_ext_addr const&, long long);
    alg_is_pinned_range(phy_ext_addr const&, long long);
    alg_pin_range(phy_ext_addr const&, long long);
    alg_promoted_range(phy_ext_addr const&, phy_ext_addr const&, long long);
    alg_unpin_range(phy_ext_addr const&, long long);
    boost_mover_work();
    calc_aligned_unit_range(ioreq_ent const&, ioreq_ent*, unsigned long long*, unsigned long long*, bool);
    calc_superset_unit_range(ioreq_ent const&, ioreq_ent*, unsigned long long*, unsigned long long*);
    call_monitor_io(ioreq_ent const&, bool*);
    clear_stat();
    compositedisk_manager(lvg*, unsigned long, unsigned int, char*);
    cpdk_enabled();
    cpdk_is_cf();
    data_monitor_thread();
    data_mover_thread();
    fill_stat(CompositeStat*);
    get_ext_lv_info(ioreq_ent*, vector<ioreq_ent_ex*>&, bool*);
    handle_move_work(bool*);
    handle_move_work_init_tx(tx**, lv_handle*, hash_set<unsigned long long, &hash_uint64(unsigned long long const&), equal_to<unsigned long long>>&, bool, bool);
    move_blocks(phy_ext_addr const&, unsigned long long, int, phy_ext_addr*);
    move_lv_blocks(unsigned long long, unsigned long long, lv_key, unsigned long long, int);
    on_fast_dev(phy_ext_addr const&);
    pin_blocks(CoreStorageLogical*, OSArray*, long long);
    pin_unpin_exts(CoreStorageLogical*, OSArray*, long long);
    prepare_move_work();
    process_move_work(cpdk_data_move_info*, __throttle_info_handle*&, char*);
    record_io(ioreq_ent const&, vector<phy_ext_simple>&, int);
    set_fast_dev_size(unsigned long long);
    set_fast_dev_unavailable(long long);
    set_lv_pin_unpin_flags(CoreStorageLogical*, OSArray*, long long);
    set_mig_unit_nblks(unsigned long long);
    start_moving_data();
    stop_maintenance_threads();
    stop_mover_thread(bool);
    sync_current_round(cpdk_data_move_info*, tx**, CoreStorageLogical*, rl_node**, lv_handle*, __throttle_info_handle*&, long long*);
    sync_move_work(tx**);
    sync_move_work(tx**) (.cold.1);
    validate_lv_pin_unpin_request(CoreStorageLogical*, OSArray*);
    wait_mover_work_done();
    ~compositedisk_manager();
};
