//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class compositedisk_manager {
public: // (access info unavailable from symbols alone)
    auto alg_cancel_range(ioreq_ent*, bool);
    auto alg_demoted_range(phy_ext_addr const&, phy_ext_addr const&, long long);
    auto alg_is_pinned_range(phy_ext_addr const&, long long);
    auto alg_pin_range(phy_ext_addr const&, long long);
    auto alg_promoted_range(phy_ext_addr const&, phy_ext_addr const&, long long);
    auto alg_unpin_range(phy_ext_addr const&, long long);
    auto boost_mover_work();
    auto calc_aligned_unit_range(ioreq_ent const&, ioreq_ent*, unsigned long long*, unsigned long long*, bool);
    auto calc_superset_unit_range(ioreq_ent const&, ioreq_ent*, unsigned long long*, unsigned long long*);
    auto call_monitor_io(ioreq_ent const&, bool*);
    auto clear_stat();
    auto cpdk_enabled();
    auto cpdk_is_cf();
    auto data_monitor_thread();
    auto data_mover_thread();
    auto fill_stat(CompositeStat*);
    auto get_ext_lv_info(ioreq_ent*, vector<ioreq_ent_ex*>&, bool*);
    auto handle_move_work(bool*);
    auto handle_move_work_init_tx(tx**, lv_handle*, hash_set<unsigned long long, &hash_uint64(unsigned long long const&), equal_to<unsigned long long>>&, bool, bool);
    auto move_blocks(phy_ext_addr const&, unsigned long long, int, phy_ext_addr*);
    auto move_lv_blocks(unsigned long long, unsigned long long, lv_key, unsigned long long, int);
    auto on_fast_dev(phy_ext_addr const&);
    auto pin_blocks(CoreStorageLogical*, OSArray*, long long);
    auto pin_unpin_exts(CoreStorageLogical*, OSArray*, long long);
    auto prepare_move_work();
    auto process_move_work(cpdk_data_move_info*, __throttle_info_handle*&, char*);
    auto record_io(ioreq_ent const&, vector<phy_ext_simple>&, int);
    auto set_fast_dev_size(unsigned long long);
    auto set_fast_dev_unavailable(long long);
    auto set_lv_pin_unpin_flags(CoreStorageLogical*, OSArray*, long long);
    auto set_mig_unit_nblks(unsigned long long);
    auto start_moving_data();
    auto stop_maintenance_threads();
    auto stop_mover_thread(bool);
    auto sync_current_round(cpdk_data_move_info*, tx**, CoreStorageLogical*, rl_node**, lv_handle*, __throttle_info_handle*&, long long*);
    auto sync_move_work(tx**);
    auto sync_move_work(tx**) (.cold.1);
    auto validate_lv_pin_unpin_request(CoreStorageLogical*, OSArray*);
    auto wait_mover_work_done();
    compositedisk_manager(lvg*, unsigned long, unsigned int, char*);
    ~compositedisk_manager();
};
