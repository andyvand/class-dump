//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleHPMDevice {
public: // (access info unavailable from symbols alone)
    AppleHPMDevice();
    AppleHPMDevice(OSMetaClass const*);
    auto changeSSAMTransmitReceiver(unsigned int, unsigned long long, unsigned int);
    auto finalize(unsigned int);
    auto free();
    auto getCommunicationsBusProvider();
    auto getI2CAddress();
    auto getMetaClass() const;
    auto getRID();
    auto hpmEnableOptions(unsigned int);
    auto hpmEnterExitFirmwareUpdateMode(unsigned int);
    auto hpmForceMode(unsigned char, unsigned int, unsigned int);
    auto hpmForceUSBDeviceMode(unsigned int, unsigned long long);
    auto hpmForceUpdateMode(unsigned char);
    auto initWithHPMBus(AppleHPM*);
    auto message(unsigned int, IOService*, void*);
    auto setBoardID(unsigned int);
    auto setDeviceID(unsigned int);
    auto setI2CAddress(unsigned int);
    auto setI2CFailure(bool);
    auto setRID(unsigned char);
    auto setRevision(unsigned char);
    auto setRevisionID(unsigned char);
    auto setRouteString(unsigned long long);
    auto setUUID(unsigned char*);
    auto setVendorID(unsigned int);
    auto setVersion(unsigned int);
    auto updateSSAMConnection(unsigned int, unsigned long long, unsigned int);
    auto withHPMBus(AppleHPM*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~AppleHPMDevice();
};
