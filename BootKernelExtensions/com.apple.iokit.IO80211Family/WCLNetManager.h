//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class WCLNetManager {
public: // (access info unavailable from symbols alone)
    WCLNetManager();
    WCLNetManager(OSMetaClass const*);
    auto assocTimer(IO80211TimerSource*);
    auto assocTimerAction(void*);
    auto checkIfNeedToLeaveDueToSapActive();
    auto checkTrafficCounters();
    auto clearAssocReadyStatus(bool);
    auto clearLinkChangedEventData();
    auto clearReassoCmd();
    auto colocatedCacheEvent(OSObject*, apple80211_colocated_cache_data&);
    auto configureWnmDMS();
    auto connectComplete(void*);
    auto considerChannelForBandSpecifiedRoam(unsigned short, AppleChannelSpec_t&);
    auto csaReceived(bulletinBoardMessage&);
    auto debounceEnd(bool);
    auto debounceFail(bulletinBoardMessage&);
    auto debounceStart(unsigned int);
    auto driverResetInIpValid(void*);
    auto driverResetNotificationHandler(bulletinBoardMessage&);
    auto enableRoaming(bulletinBoardMessage&);
    auto firstLinkUp(void*);
    auto forceLinkDown(void*);
    auto forceNeiborCacheRoamWith6EDisablement(bool);
    auto forceNeiborCacheRoamWith6EDisablement(bool)::_os_log_fmt;
    auto forceRoam(bool, unsigned char);
    auto forceRoam(bulletinBoardMessage&);
    auto forceRoamUsingNeighborCache(apple80211_reassoc*, unsigned char);
    auto forceRoamUsingNeighborCache(apple80211_reassoc*, unsigned char)::_os_log_fmt;
    auto free();
    auto free()::kalloc_type_view_212;
    auto freeResources();
    auto fwDebounceTimeout(OSObject*, unsigned int, void*);
    auto fwLoadCompleteNotificationHandler(bulletinBoardMessage&);
    auto getAssocReadyStatus(bulletinBoardMessage&);
    auto getBssidChnageReason(WCLBSSBeacon*, WCLBSSBeacon*);
    auto getDEAUTH(bulletinBoardMessage&);
    auto getDisassocWaitTime();
    auto getEXTENDED_STATS(bulletinBoardMessage&);
    auto getLINK_CHANGED_EVENT_DATA(bulletinBoardMessage&);
    auto getLinkChangeEventInternal(apple80211_link_changed_event_data*);
    auto getLogger() const;
    auto getMetaClass() const;
    auto getP2P_STEERING_METRIC(bulletinBoardMessage&);
    auto getPOWERSAVE(bulletinBoardMessage&);
    auto getPowerMode();
    auto handleBeaconMuteMitigation(bulletinBoardMessage&);
    auto handleBssTransEvent(bulletinBoardMessage&);
    auto handleDeauthDisassoc(bulletinBoardMessage&);
    auto handleDeauthDisassocFrame(bulletinBoardMessage&);
    auto handleFwDebounceFailure();
    auto handleGetStateInfo(bulletinBoardMessage&);
    auto handleLqmUpdate(bulletinBoardMessage&);
    auto handleMissedBeacons();
    auto handleReassociateWithCoreCapture(bulletinBoardMessage&);
    auto handleRoamDoneEvent(bulletinBoardMessage&);
    auto handleSupplicantEvent(bulletinBoardMessage&);
    auto handleUserDisassoc(bulletinBoardMessage&);
    auto ignore(void*);
    auto inactivityStatusEvent(bulletinBoardMessage&);
    auto inactivityStatusInternal(bool);
    auto initWithOptions(WCLController*, WCLBulletinBoard*);
    auto initWithOptions(WCLController*, WCLBulletinBoard*)::kalloc_type_view_267;
    auto ipStatus(void*);
    auto ipStatusCommon(apple80211_assoc_ready*);
    auto ipStatusInWaitForIp(void*);
    auto isConncetCompleted();
    auto isDataPathCleanOnGoing();
    auto isDebounceDueToDissassoc();
    auto isIPv4AddressValid();
    auto isIPv4AddressValid()::_os_log_fmt;
    auto isScanOrJoinBusy();
    auto isSetCountryNeedDissconnect(apple80211_country_code_data*);
    auto isWaitingForIPAddress();
    auto joinAbort(void*);
    auto joinDone(bulletinBoardMessage&);
    auto leaveNetwork(void*);
    auto leaveNetworkCommand(unsigned int, unsigned int, unsigned short, apple80211LinkChangeReason, unsigned char, unsigned char, bool, bool, ether_addr*, char const*);
    auto linkDownComplete(void*);
    auto linkDownCompleteInSleep(void*);
    auto linkDownInd(void*);
    auto linkDownInd(void*)::_os_log_fmt;
    auto linkStatusInd(bulletinBoardMessage&);
    auto linkSuppress(void*);
    auto linkSuppressCommand(wclVoluntaryNetworkDisconnect*, char const*);
    auto linkUp(void*);
    auto linkUpInSleep(void*);
    auto loadTrafficCounters(apple80211_wcl_traffic_counters*);
    auto logTransition(char const*, char const*, char const*, char const*, unsigned int, unsigned int);
    auto moveFrom6GIfNeeded(bool);
    auto protectIpTimer(IO80211TimerSource*);
    auto receivedIPv4Address(unsigned int, unsigned int);
    auto receivedIPv6Address(in6_addr*, unsigned int);
    auto removeBandChannels(apple80211_reassoc*, unsigned int);
    auto saveReassoc(apple80211_reassoc*, bool);
    auto scanDone(bulletinBoardMessage&);
    auto sendArpCmd(unsigned int, bool);
    auto sendBeaconMitigationParams(IO80211BcnMuteMode, unsigned int);
    auto sendProductIeTemplate(bulletinBoardMessage&);
    auto sendReassocCommand(apple80211_reassoc*, bool, unsigned short, bool);
    auto sendReassocToDriver(apple80211_reassoc*, bool, bool);
    auto sendUpdateBssFlags();
    auto set4wayHsTimeout(unsigned long long);
    auto set6GDisableClient(unsigned int, apple80211_6G_mode_flag);
    auto set6GMode(apple80211_6G_mode_flag);
    auto set6G_MODE(bulletinBoardMessage&);
    auto setBTSCOState(bulletinBoardMessage&);
    auto setCountryCode(bulletinBoardMessage&);
    auto setCurrentBSS(WCLBSSBeacon*, bool, apple80211_bssid_change_reason&);
    auto setDEAUTH(bulletinBoardMessage&);
    auto setDISASSOCIATE(bulletinBoardMessage&);
    auto setDenyListBSS(bulletinBoardMessage&);
    auto setDhcpRenewalData(bulletinBoardMessage&);
    auto setGarp(bool);
    auto setIPV4_PARAMS(bulletinBoardMessage&);
    auto setIPV6_PARAMS(bulletinBoardMessage&);
    auto setIbssMode(bulletinBoardMessage&);
    auto setInternalReassoc(bulletinBoardMessage&);
    auto setKey(bulletinBoardMessage&);
    auto setLinkChangedEventData(bool, unsigned int, apple80211LinkChangeReason);
    auto setNANPHS_ASSOCIATION(bulletinBoardMessage&);
    auto setNANPHS_TERMINATED(bulletinBoardMessage&);
    auto setOffLoadArp(bulletinBoardMessage&);
    auto setOffLoadNdp(bulletinBoardMessage&);
    auto setPM_MODE(bulletinBoardMessage&);
    auto setPOWERSAVE(bulletinBoardMessage&);
    auto setPowerProfile(bulletinBoardMessage&);
    auto setProtocolCleanDone(bulletinBoardMessage&);
    auto setROAM(bulletinBoardMessage&);
    auto setROAMWithBand(apple80211_sta_roam_data*);
    auto setROAMWithBssid(ether_addr const&, bool);
    auto setROAMWithBssid(ether_addr const&, bool)::_os_log_fmt;
    auto setROAMWithChannel(apple80211_sta_roam_data*);
    auto setROAMWithChannel(apple80211_sta_roam_data*)::_os_log_fmt;
    auto setVirtualIfState(bulletinBoardMessage&);
    auto setWoWEapolFilterConfig(bulletinBoardMessage&);
    auto sleep(void*);
    auto sleepBeforeConnectComplete(void*);
    auto slowWifiRecovery(bulletinBoardMessage&);
    auto startProtectIp(unsigned int, bool);
    auto startRoamScan(bool);
    auto steerFrom6G();
    auto steerToBand(Bands);
    auto stopProtectIp();
    auto systemAwakeNotificationHandler(bulletinBoardMessage&);
    auto systemPowerOFFNotificationHandler(bulletinBoardMessage&);
    auto systemWillSleepNotificationHandler(bulletinBoardMessage&);
    auto triggerBSSIDSteering(unsigned char, bool, unsigned int);
    auto triggerBSSIDSteering(unsigned char, bool, unsigned int)::_os_log_fmt;
    auto triggerCcIfNeededOnSupplicantEvent(apple80211_message_supplicant_event&);
    auto triggerHostInBandLazyRoam(Bands);
    auto triggerHostInBandLazyRoamIfNeeded();
    auto triggerNeighborCacheReAssociation(unsigned int, bool, bool);
    auto triggerNeighborCacheReAssociation(unsigned int, bool, bool)::_os_log_fmt;
    auto triggerNeighborCacheSteering(unsigned char);
    auto triggerNeighborCacheSteering(unsigned char)::_os_log_fmt;
    auto triggerReAssociationOnBand(unsigned short);
    auto triggerReAssociationOnBand(unsigned short)::_os_log_fmt;
    auto triggerSteering(unsigned char);
    auto tryReassoc();
    auto unconfigureWnmDMS();
    auto unhandledEvent(void*);
    auto updateBeaconCounter(bulletinBoardMessage&);
    auto updateBss(bool, bool, bool, int, apple80211_bssid_change_reason&);
    auto updateLinkState(bool, bool, bool, unsigned int, unsigned int);
    auto updateSupplicantEvent(apple80211_message_supplicant_event&);
    auto validateSSid();
    auto verifyLinkDown(void*);
    auto wake(void*);
    auto wakeLeave(void*);
    auto withOptions(WCLController*, WCLBulletinBoard*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~WCLNetManager();
};
