//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211ScanCacheStore {
public: // (access info unavailable from symbols alone)
    IO80211ScanCacheStore();
    IO80211ScanCacheStore(OSMetaClass const*);
    actionCachedNetworksMatchingAuth(unsigned char*, unsigned char, unsigned int, unsigned int, unsigned short, unsigned short, unsigned int, bool (OSObject*) block_pointer);
    addToScanCache(IO80211BSSBeacon*, IO80211BandChannelSet&);
    checkIfBeaconExistInCache(IO80211BSSBeacon*);
    copyCachedNetworksMatching(unsigned char*, unsigned char, unsigned short, unsigned short, unsigned int);
    copyCachedNetworksMatchingAuth(unsigned char*, unsigned char, unsigned int, unsigned int, unsigned short, unsigned short, unsigned int, unsigned int, unsigned int);
    copyCachedNetworksMatchingColocatedGroup(unsigned short, bool);
    copyCachedNetworksMatchingColocatedGroup(unsigned short, bool, ether_addr);
    copyCachedNetworksMatchingColocatedRNR(unsigned char*, unsigned char, unsigned short, unsigned short, bool, ether_addr);
    createIO80211ScanCacheStore(IO80211ScanCacheStoreParams&);
    free();
    free()::kalloc_type_view_126;
    freeResource();
    getCacheSize();
    getCachedBeacon(BeaconMsg*);
    getCachedBeacon(unsigned char*, unsigned char, ether_addr*);
    getCachedBeacons(bool (OSObject const*) block_pointer);
    getLogger();
    getMetaClass() const;
    initIO80211ScanCacheStore(IO80211ScanCacheStoreParams&);
    initIO80211ScanCacheStore(IO80211ScanCacheStoreParams&)::_os_log_fmt;
    initIO80211ScanCacheStore(IO80211ScanCacheStoreParams&)::kalloc_type_view_562;
    isPnoNetworkFound();
    isPnoNetworkFound(IO80211BSSBeacon*);
    logScanCache(char*, unsigned int, unsigned int);
    obtainObjectFromCache(bool (OSObject const*) block_pointer);
    performActionOnCache(bool (OSObject*) block_pointer);
    pruneInvalidBeacons(IO80211BandChannelSet&, bandBitmap, bool);
    purgeScanCache();
    reapOldBeacons(unsigned int);
    removeBeacon(IO80211BSSBeacon*);
    removeBeaconsMatching(IO80211BSSBeacon*);
    removeBeaconsMatching(unsigned char*, unsigned long long, ether_addr*);
    setBSSPreference(BSSPreference*);
    setCachedBGScanData(apple80211_bgscan_data*);
    setCachedOffloadData(apple80211_offload_scan_data*);
    updateOrAddBeacon(BeaconMetaData&, unsigned char*, IO80211BandChannelSet&, IO80211BSSBeacon* (BeaconMetaData&, unsigned char*, CCLogStream*, CommonFaultReporter*) block_pointer);
    updateScanCache(BeaconMetaData&, unsigned char*, IO80211BandChannelSet&);
    validateAndAdd(IO80211BSSBeacon*, IO80211BandChannelSet&);
    ~IO80211ScanCacheStore();
};
