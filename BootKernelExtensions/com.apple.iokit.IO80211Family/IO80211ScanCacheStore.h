//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211ScanCacheStore {
public: // (access info unavailable from symbols alone)
    IO80211ScanCacheStore();
    IO80211ScanCacheStore(OSMetaClass const*);
    auto actionCachedNetworksMatchingAuth(unsigned char*, unsigned char, unsigned int, unsigned int, unsigned short, unsigned short, unsigned int, bool (OSObject*) block_pointer);
    auto addToScanCache(IO80211BSSBeacon*, IO80211BandChannelSet&);
    auto checkIfBeaconExistInCache(IO80211BSSBeacon*);
    auto copyCachedNetworksMatching(unsigned char*, unsigned char, unsigned short, unsigned short, unsigned int);
    auto copyCachedNetworksMatchingAuth(unsigned char*, unsigned char, unsigned int, unsigned int, unsigned short, unsigned short, unsigned int, unsigned int, unsigned int);
    auto copyCachedNetworksMatchingColocatedGroup(unsigned short, bool);
    auto copyCachedNetworksMatchingColocatedGroup(unsigned short, bool, ether_addr);
    auto copyCachedNetworksMatchingColocatedRNR(unsigned char*, unsigned char, unsigned short, unsigned short, bool, ether_addr);
    auto createIO80211ScanCacheStore(IO80211ScanCacheStoreParams&);
    auto free();
    auto free()::kalloc_type_view_126;
    auto freeResource();
    auto getCacheSize();
    auto getCachedBeacon(BeaconMsg*);
    auto getCachedBeacon(unsigned char*, unsigned char, ether_addr*);
    auto getCachedBeacons(bool (OSObject const*) block_pointer);
    auto getLogger();
    auto getMetaClass() const;
    auto initIO80211ScanCacheStore(IO80211ScanCacheStoreParams&);
    auto initIO80211ScanCacheStore(IO80211ScanCacheStoreParams&)::_os_log_fmt;
    auto initIO80211ScanCacheStore(IO80211ScanCacheStoreParams&)::kalloc_type_view_562;
    auto isPnoNetworkFound();
    auto isPnoNetworkFound(IO80211BSSBeacon*);
    auto logScanCache(char*, unsigned int, unsigned int);
    auto obtainObjectFromCache(bool (OSObject const*) block_pointer);
    auto performActionOnCache(bool (OSObject*) block_pointer);
    auto pruneInvalidBeacons(IO80211BandChannelSet&, bandBitmap, bool);
    auto purgeScanCache();
    auto reapOldBeacons(unsigned int);
    auto removeBeacon(IO80211BSSBeacon*);
    auto removeBeaconsMatching(IO80211BSSBeacon*);
    auto removeBeaconsMatching(unsigned char*, unsigned long long, ether_addr*);
    auto setBSSPreference(BSSPreference*);
    auto setCachedBGScanData(apple80211_bgscan_data*);
    auto setCachedOffloadData(apple80211_offload_scan_data*);
    auto updateOrAddBeacon(BeaconMetaData&, unsigned char*, IO80211BandChannelSet&, IO80211BSSBeacon* (BeaconMetaData&, unsigned char*, CCLogStream*, CommonFaultReporter*) block_pointer);
    auto updateScanCache(BeaconMetaData&, unsigned char*, IO80211BandChannelSet&);
    auto validateAndAdd(IO80211BSSBeacon*, IO80211BandChannelSet&);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IO80211ScanCacheStore();
};
