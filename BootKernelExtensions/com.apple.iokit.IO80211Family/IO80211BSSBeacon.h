//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211BSSBeacon {
public: // (access info unavailable from symbols alone)
    IO80211BSSBeacon();
    IO80211BSSBeacon(OSMetaClass const*);
    auto addressMatches(IO80211BSSBeacon const&) const;
    auto addressMatches(ether_addr const&) const;
    auto addressMatches(unsigned char*) const;
    auto allocateIEListBufferAndCopy(unsigned long, unsigned char*);
    auto calculateRates();
    auto dumpBeacon();
    auto free();
    auto free()::kalloc_type_view_1348;
    auto getAKMs();
    auto getAPMode() const;
    auto getAPMode() const::capToMode;
    auto getATIMWindow() const;
    auto getAddress() const;
    auto getAddress(ether_addr*) const;
    auto getAddress(unsigned char*) const;
    auto getAgeInMS() const;
    auto getAgeInSeconds() const;
    auto getApAppleAndRsnIeList(unsigned char*, unsigned int&) const;
    auto getBand() const;
    auto getBeaconPeriod() const;
    auto getCCA(signed char&);
    auto getCCAStats(apple80211_cca_stats&) const;
    auto getCapabilities() const;
    auto getChanPrimarySWSpec() const;
    auto getChanSWSpec() const;
    auto getChannel() const;
    auto getCurrentBSSAKMs();
    auto getDTIMPeriod() const;
    auto getEncryptionMode() const;
    auto getExtendedRates(unsigned char&);
    auto getExtendedRates(unsigned char*) const;
    auto getHtRxRates();
    auto getIeListLength() const;
    auto getListenInterval() const;
    auto getLogger() const;
    auto getMaxRate();
    auto getMetaClass() const;
    auto getMinRate();
    auto getMldAddress() const;
    auto getMldAddress(ether_addr&) const;
    auto getMultiBssidOffset();
    auto getNoise() const;
    auto getNoiseDeltaOverTwoCores() const;
    auto getNonTransmittedBssidIndex();
    auto getOWETransAddress(ether_addr*) const;
    auto getOWETransAddress(unsigned char*) const;
    auto getOWETransSSID() const;
    auto getOWETransSSIDLength() const;
    auto getObjectFromChain(queue_entry*);
    auto getPMKSAExpiration() const;
    auto getPnoNetwork();
    auto getQBSS(apple80211_qbss_load*);
    auto getQueueChain();
    auto getQueueChainForObject(OSObject*);
    auto getRSSI() const;
    auto getRSSIAgeInSeconds() const;
    auto getRnRBSSInfo(unsigned int, unsigned char*) const;
    auto getRnRContext() const;
    auto getRxRate() const;
    auto getRxRatePercent();
    auto getSNR() const;
    auto getSSID() const;
    auto getSSIDCStr() const;
    auto getSSIDLength() const;
    auto getShortSsid() const;
    auto getSupportedRates(unsigned char&);
    auto getSupportedRates(unsigned char*) const;
    auto getTimestamp() const;
    auto getWPSInfo(WPSInfo_t*);
    auto getWeightedAvgChannelMetric(apple80211_weight_avg_lqm_event_data*, unsigned long long*);
    auto getiOSCapabilityIE(iOSIESubTypeCapabilities&);
    auto getiOSWiFiNetworkInfoIE(iOSIESubTypePHInfo&);
    auto hasLQMResult() const;
    auto initWithChanSpec(CCLogStream*, CommonFaultReporter*);
    auto initWithChanSpec(CCLogStream*, CommonFaultReporter*)::kalloc_type_view_1288;
    auto initWithChanSpec(CCLogStream*, CommonFaultReporter*)::kalloc_type_view_1332;
    auto is11axAsrCapableAp() const;
    auto is80211rEnabledInCCXIE() const;
    auto is8021xEnabled();
    auto isAP() const;
    auto isAppleNetwork();
    auto isBSSBeaconProtectionCapable() const;
    auto isBSSOCVCapable() const;
    auto isBSSQoSMgmtMSCSSupported() const;
    auto isBSSSAEPKCapable() const;
    auto isBSSSAEPKPwdExclsivelyUsed() const;
    auto isBSSTransMgmtSupported() const;
    auto isBeaconAtHeRate() const;
    auto isBlacklisted() const;
    auto isBssMfpCapable();
    auto isCarPlayDongle();
    auto isCurrent() const;
    auto isDMSSupported() const;
    auto isDirectedProbeNetwork() const;
    auto isEhtEnabled() const;
    auto isFILSDiscoveryFrame() const;
    auto isFastBSSTransitionSupported() const;
    auto isFtEnabled();
    auto isHEEnabled() const;
    auto isHTEnabled() const;
    auto isHidden() const;
    auto isIOSDevice();
    auto isInternetAccessible() const;
    auto isInterworkingIEPresent() const;
    auto isLikelyAlpineBMWNetwork();
    auto isLikelyOrbiNetwork();
    auto isMld() const;
    auto isNBTEvoBMWNetwork();
    auto isNeighborReportSupported() const;
    auto isNewBssBetter(int, bool);
    auto isNonTransmittedBssid() const;
    auto isNwAssuranceEnabledInCCXIE() const;
    auto isOWETrans() const;
    auto isOnColocatedAP(unsigned short, ether_addr) const;
    auto isOnColocatedGroup(unsigned short, bool) const;
    auto isOnColocatedGroup(unsigned short, bool, ether_addr) const;
    auto isOnNetwork(IO80211BSSBeacon const*) const;
    auto isOnNetwork(unsigned char const*, unsigned char, unsigned short, unsigned short) const;
    auto isOnRSNNetwork(unsigned char const*, unsigned char, unsigned int, unsigned int, unsigned short, unsigned int) const;
    auto isOnRSNNetworkOWE(unsigned int, unsigned int, unsigned int, unsigned int, bool*, bool*) const;
    auto isPatternFound(char const*, char const*);
    auto isPrivacyCompatible(IO80211BSSBeacon const*) const;
    auto isPrivacyEnabled() const;
    auto isProxyARPSupported() const;
    auto isQosFastLaneEnabled() const;
    auto isSameSSIDCoLocatedAP() const;
    auto isScoreComputed() const;
    auto isSplitSSIDCoLocatedAP() const;
    auto isTIMBroadcastSupported() const;
    auto isVHTEnabled() const;
    auto isWACNetwork();
    auto isWEPEnabled() const;
    auto isWPAEnabled() const;
    auto isWiFiNetworkFullyLoaded() const;
    auto isWiFiNetworkInfoAvailable() const;
    auto logBeacon(char const*, char const*, char*, unsigned int, unsigned int, bool);
    auto logBeacon(char const*, char const*, char*, unsigned int, unsigned int, bool)::_os_log_fmt;
    auto logBeaconWithOSLog(char const*, char const*, char*, unsigned int, unsigned int);
    auto mldAddressMatches(IO80211BSSBeacon*) const;
    auto mldAddressMatches(ether_addr const&) const;
    auto overrideChannel(io80211EhtOperationIe const*, io80211HeOperationIe const*, io80211VhtOperationIe const*, io80211HtInfo const*, io80211DsParams const*);
    auto parseAppleDeviceIE(IEEEStandardIE const*, unsigned long);
    auto parseDsParams(io80211DsParams const*, overrideChannelContext&);
    auto parseEhtOp(io80211EhtOperationIe const*, overrideChannelContext&);
    auto parseHeOp(io80211HeOperationIe const*, overrideChannelContext&);
    auto parseHtInfo(io80211HtInfo const*, overrideChannelContext&);
    auto parseMloIe(IEEEExtensionIEHeader*);
    auto parseOWETransIE(IEEEStandardIE const*);
    auto parseRSNIe(rsnIeEntry&, rsnIeOutput&);
    auto parseRSNIeContext(rsnIeContext&);
    auto parseRnrIe(IEEEStandardIE*);
    auto parseVhtOp(io80211VhtOperationIe const*, overrideChannelContext&);
    auto parseWpsIE(IEEEStandardIE const*);
    auto removeAsCurrent();
    auto searchAlpineBMWAppleDeviceIE(unsigned char*, unsigned int*);
    auto searchNetgearOrbiVendorIE(unsigned char*, unsigned int*);
    auto setAddress(ether_addr*);
    auto setAsCurrent();
    auto setBeaconDataEx(unsigned char*, unsigned int, unsigned char*, unsigned char, bool);
    auto setBeaconDataFromMsg(BeaconMetaData&, unsigned char*);
    auto setBeaconPeriod(unsigned short);
    auto setBlacklisted(bool);
    auto setCapabilities(unsigned short);
    auto setCardCap(apple80211_capability_data&);
    auto setDirectedProbeNetwork(directedNetwork);
    auto setFlags(bool, bool, bool, bool, bool, bool, bool, bool, bool);
    auto setHEMCSIndexSet(apple80211_he_mcs_index_set_data&);
    auto setHidden(bool);
    auto setInternetAccess(bool);
    auto setInterworkingIEPresent(bool);
    auto setMCSIndexSet(apple80211_mcs_index_set_data&);
    auto setNonTransmittedBssidIndex(unsigned char);
    auto setPMKSAExpiration(unsigned int);
    auto setPnoNetwork(bool);
    auto setRateSet(apple80211_rate_set_data&);
    auto setSSID(unsigned char const*, unsigned char);
    auto setVHTMCSIndexSet(apple80211_vht_mcs_index_set_data&);
    auto shortSSIDMatches(IO80211BSSBeacon const&) const;
    auto shortSSIDMatches(unsigned int) const;
    auto ssidMatches(IO80211BSSBeacon const&) const;
    auto ssidMatches(unsigned char const*, unsigned char) const;
    auto ssidPrefixMatches(unsigned char const*, unsigned char) const;
    auto toScanResult(apple80211_scan_result&) const;
    auto updateCCA(signed char);
    auto updateCCAStats(apple80211_cca_stats const&);
    auto updateChannel(unsigned char);
    auto updateChannelInfo(AppleChannelSpec_t, unsigned char);
    auto updateLegacyCCAStats(apple80211_legacy_cca_stats const&);
    auto updateNoise(short);
    auto updateNoiseDeltaOverTwoCores(signed char);
    auto updateRSSI(int);
    auto updateRxRate(int);
    auto updateSNR(short);
    auto updateWeightAvgLQM(apple80211_weight_avg_lqm_event_data*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IO80211BSSBeacon();
};
