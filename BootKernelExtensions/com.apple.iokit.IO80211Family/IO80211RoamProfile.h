//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211RoamProfile {
public: // (access info unavailable from symbols alone)
    IO80211RoamProfile(OSMetaClass const*);
    auto addRoamOffset(unsigned int);
    auto applyRoam5GBandBoost(apple80211_wifi_roam_5g_bandboost_t&, Bands);
    auto applyRoamingProfile(ExtendedRoamParameters*, Bands);
    auto applyRoamingProfile(ExtendedRoamParametersV1*, ExtendedRoamParametersV1*, ExtendedRoamParametersV1*);
    auto configureExtendedRoamParams(apple80211_roam_profile_all_bands*);
    auto configureExtendedRoamParamsV1(apple80211_roam_profile_all_bands*);
    auto configureRoamingProfile(bool, char const*);
    auto configureRoamingProfile(bool, char const*)::_os_log_fmt;
    auto configureRoamingProfile(char const*);
    auto dumpState(char*, unsigned int, unsigned int) const;
    auto free();
    auto free()::kalloc_type_view_124;
    auto freeResources();
    auto gatherParameterData(IO80211RoamProfileOptions const&);
    auto get5ghPreferenceProfileState();
    auto get5ghSteeringProfileState();
    auto getAllowedScanBands(bool, bool, bool, bool);
    auto getCurrentRoamProfile();
    auto getLimitedRoamProfile();
    auto getLogger() const;
    auto getMetaClass() const;
    auto getMultiAPEnvironment();
    auto getNextRoamProfile();
    auto getRoamProfileCommonPart(apple80211_roam_profile_config_common&, Bands);
    auto getRoamProfileLowRssiTrigger(Bands);
    auto getRoamProfilePerBand(ExtendedRoamParametersV1*, Bands, apple80211_roam_profile_band&);
    auto getRoamProfilePerBracket(apple80211_roam_profile_config_common&, ExtendedRoamParametersV1&, apple80211_roam_profile_config_bracket&);
    auto ignoreRoamDelta(bool);
    auto initRoamingProfile();
    auto initWithOptions(IO80211RoamProfileOptions&);
    auto initWithOptions(IO80211RoamProfileOptions&)::kalloc_type_view_139;
    auto is2GRoamingAllowed();
    auto is5GRoamingAllowed();
    auto is6GRoamingAllowed();
    auto is6GRoamingAllowedPerRssi(int);
    auto isAllowOverride();
    auto isAntennaInEfficiencyMitigationSupported();
    auto isCurrentProfileMultipleAP();
    auto isOpBtmAllowed();
    auto printRoamProfile(char*, unsigned int, unsigned int);
    auto printRoamingBand(apple80211_roam_profile_band&, int, int);
    auto printRoamingBracket(apple80211_roam_profile_config_bracket&, int, int, int);
    auto printRoamingProfile(apple80211_roam_profile_config&);
    auto removeRoamOffset(unsigned int);
    auto setBlockedBands(apple80211_blocked_bands&);
    auto setDeviceConfig(bool, bool, bool, bool, bool);
    auto setLimitedRoamProfile(unsigned int, bool);
    auto setNbRangingSession(bool);
    auto setOriginalRoamProfile(unsigned int);
    auto setROAM_PARAMS(apple80211_roam_params*);
    auto setROAM_PROFILE(apple80211_roam_profile_all_bands*);
    auto updateBtCoexState(unsigned int, bool);
    auto updateExtendedRoamParamsBand(apple80211_roam_profile_band_data*);
    auto updateExtendedRoamParamsBandV1(apple80211_roam_profile_band_data*);
    auto updateLowLatencyTraffic(bool, bool, bool);
    auto validateRoamParams(unsigned char);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IO80211RoamProfile();
};
