//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211P2PDataPathManager {
public: // (access info unavailable from symbols alone)
    IO80211P2PDataPathManager();
    IO80211P2PDataPathManager(OSMetaClass const*);
    auto addNANPeerToCachedList(IO80211Peer*);
    auto addPeerToFWCache(IO80211Peer*, bool);
    auto allocateWithPeerManager(IO80211PeerManager*);
    auto clearFlagAWDLHotSpotConcurrency();
    auto clearP2PMaxPeerCacheSizeConfigured();
    auto configureMaxPeerCacheSize();
    auto configureP2PPeerCacheSize();
    auto configureSoftAPPeerCacheSize();
    auto decrementCachedPeerCount();
    auto decrementCurrentPeerCacheSize();
    auto deleteNANPeerFromCachedList(IO80211Peer*);
    auto deletePeerFromFWCache(IO80211Peer*, peerDeleteReason);
    auto enqueuePeerToFwCachedPeerList(IO80211Peer*);
    auto findCachedPeer(ether_addr const*);
    auto findCachedPeer(unsigned int);
    auto free();
    auto free()::kalloc_type_view_101;
    auto getCardCapableMaxPeerCacheSize();
    auto getCurrentPeerCacheSize();
    auto getCurrentSoftapPeerCacheSize();
    auto getMaxPeerCacheSize();
    auto getMaxSoftapPeerCacheSize();
    auto getMetaClass() const;
    auto getMyCachedPeerCount();
    auto getPeerManager();
    auto getPeerToBeUncached();
    auto getSizeFwCachedPeerList();
    auto handleCacheControlMessages(apple80211_peer_cache_control*);
    auto handlePeerAddIndication(apple80211_peer_cache_control*);
    auto handlePeerDeleteIndication(apple80211_peer_cache_control*);
    auto incrementCachedPeerCount();
    auto incrementCurrentPeerCacheSize();
    auto initCommonResources();
    auto initWithPeerManager(IO80211PeerManager*);
    auto initWithPeerManager(IO80211PeerManager*)::kalloc_type_view_60;
    auto isP2PMaxPeerCacheSizeConfigured();
    auto isPeerCacheFull();
    auto isSoftapPeerCacheFull();
    auto logDebug(char const*, ...);
    auto logDebug(unsigned long long, char const*, ...);
    auto printFwCachedPeerList();
    auto removeAllCachedPeers();
    auto removeAllPeerFromFWCachedPeerList();
    auto removeLRUPeerFromFWCache();
    auto removePeerFromFWCachedPeerList(IO80211Peer*);
    auto sanityCheckFWCachedPeerListSize();
    auto sendIoctlToCachePeer(apple80211_peer_cache_control*, IO80211Peer*);
    auto setCachedPeerCount(unsigned int);
    auto setCurrentPeerCacheSize(unsigned int);
    auto setCurrentSoftapPeerCacheSize(unsigned int);
    auto setFlagAWDLHotSpotConcurrency();
    auto setMaxPeerCacheSize(unsigned int);
    auto setMaxSoftapPeerCacheSize(unsigned int);
    auto setP2PMaxPeerCacheSizeConfigured();
    auto setPeerManager(IO80211PeerManager*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IO80211P2PDataPathManager();
};
