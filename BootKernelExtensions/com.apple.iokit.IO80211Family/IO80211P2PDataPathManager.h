//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211P2PDataPathManager {
public: // (access info unavailable from symbols alone)
    IO80211P2PDataPathManager();
    IO80211P2PDataPathManager(OSMetaClass const*);
    addNANPeerToCachedList(IO80211Peer*);
    addPeerToFWCache(IO80211Peer*, bool);
    allocateWithPeerManager(IO80211PeerManager*);
    clearFlagAWDLHotSpotConcurrency();
    clearP2PMaxPeerCacheSizeConfigured();
    configureMaxPeerCacheSize();
    configureP2PPeerCacheSize();
    configureSoftAPPeerCacheSize();
    decrementCachedPeerCount();
    decrementCurrentPeerCacheSize();
    deleteNANPeerFromCachedList(IO80211Peer*);
    deletePeerFromFWCache(IO80211Peer*, peerDeleteReason);
    enqueuePeerToFwCachedPeerList(IO80211Peer*);
    findCachedPeer(ether_addr const*);
    findCachedPeer(unsigned int);
    free();
    free()::kalloc_type_view_101;
    getCardCapableMaxPeerCacheSize();
    getCurrentPeerCacheSize();
    getCurrentSoftapPeerCacheSize();
    getMaxPeerCacheSize();
    getMaxSoftapPeerCacheSize();
    getMetaClass() const;
    getMyCachedPeerCount();
    getPeerManager();
    getPeerToBeUncached();
    getSizeFwCachedPeerList();
    handleCacheControlMessages(apple80211_peer_cache_control*);
    handlePeerAddIndication(apple80211_peer_cache_control*);
    handlePeerDeleteIndication(apple80211_peer_cache_control*);
    incrementCachedPeerCount();
    incrementCurrentPeerCacheSize();
    initCommonResources();
    initWithPeerManager(IO80211PeerManager*);
    initWithPeerManager(IO80211PeerManager*)::kalloc_type_view_60;
    isP2PMaxPeerCacheSizeConfigured();
    isPeerCacheFull();
    isSoftapPeerCacheFull();
    logDebug(char const*, ...);
    logDebug(unsigned long long, char const*, ...);
    printFwCachedPeerList();
    removeAllCachedPeers();
    removeAllPeerFromFWCachedPeerList();
    removeLRUPeerFromFWCache();
    removePeerFromFWCachedPeerList(IO80211Peer*);
    sanityCheckFWCachedPeerListSize();
    sendIoctlToCachePeer(apple80211_peer_cache_control*, IO80211Peer*);
    setCachedPeerCount(unsigned int);
    setCurrentPeerCacheSize(unsigned int);
    setCurrentSoftapPeerCacheSize(unsigned int);
    setFlagAWDLHotSpotConcurrency();
    setMaxPeerCacheSize(unsigned int);
    setMaxSoftapPeerCacheSize(unsigned int);
    setP2PMaxPeerCacheSizeConfigured();
    setPeerManager(IO80211PeerManager*);
    ~IO80211P2PDataPathManager();
};
