//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211LinkRecovery {
public: // (access info unavailable from symbols alone)
    IO80211LinkRecovery();
    IO80211LinkRecovery(OSMetaClass const*);
    auto actionToString(apple80211_link_recovery_action);
    auto clearRecoveryParams();
    auto clearRecoveryParams()::_os_log_fmt;
    auto completeFaultReportCallback(CCFaultReport const*);
    auto confirmAction(apple80211_link_recovery_user_input);
    auto confirmAction(apple80211_link_recovery_user_input)::_os_log_fmt;
    auto discardLinkProbeResult(ether_addr);
    auto fgAppNotificationOnly(apple80211_link_recovery_action);
    auto free();
    auto free()::kalloc_type_view_290;
    auto getInducedSlowWiFiState();
    auto getMetaClass() const;
    auto getPeerMacAddress();
    auto getPeerStats();
    auto getQuotaInfo(QuotaType, unsigned long long**, unsigned long long**);
    auto getRecoveryQuotaType(apple80211_link_recovery_reason, bool);
    auto getTimeSinceLastRecoveryActionInSecs();
    auto initWithMacAddressAndController(ether_addr, IO80211Controller*, bool, IO80211LQMCrashTracer*);
    auto initWithMacAddressAndController(ether_addr, IO80211Controller*, bool, IO80211LQMCrashTracer*)::_os_log_fmt;
    auto initWithMacAddressAndController(ether_addr, IO80211Controller*, bool, IO80211LQMCrashTracer*)::kalloc_type_view_150;
    auto isAssociatedToMovingNetwork();
    auto isFGTrafficActive();
    auto postLinkRecoveryEvent();
    auto postLinkRecoveryEvent()::_os_log_fmt;
    auto recordAppState(apple80211_app_state*);
    auto recordAppState(apple80211_app_state*)::_os_log_fmt;
    auto recordBSSBlacklistEvent(apple80211_bss_blacklist_event*);
    auto recordBSSBlacklistEvent(apple80211_bss_blacklist_event*)::_os_log_fmt;
    auto recordLinkProbeResult(int);
    auto reset(ether_addr const*);
    auto reset(ether_addr const*)::_os_log_fmt;
    auto resetRecoveryLimits();
    auto setInducedSlowWiFiState(bool);
    auto setInducedSlowWiFiState(bool)::_os_log_fmt;
    auto setLinkQualityMonitor(IO80211LinkQualityMonitor*);
    auto setScreenState(bool);
    auto setScreenState(bool)::_os_log_fmt;
    auto stop();
    auto triggerAction(apple80211_link_recovery_action, apple80211_link_recovery_reason, char const*, bool, char const*, bool, bool);
    auto triggerAction(apple80211_link_recovery_action, apple80211_link_recovery_reason, char const*, bool, char const*, bool, bool)::_os_log_fmt;
    auto triggerFaultReporter();
    auto triggerFaultReporter()::_os_log_fmt;
    auto triggerLinkProbe(bool);
    auto triggerLinkProbe(bool)::_os_log_fmt;
    auto updateStats(apple80211_link_recovery_action, IO80211LinkRecoveryActionState, apple80211_link_recovery_reason, apple80211_link_recovery_user_input);
    auto withMacAddressAndController(ether_addr, IO80211Controller*, bool, IO80211LQMCrashTracer*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IO80211LinkRecovery();
};
