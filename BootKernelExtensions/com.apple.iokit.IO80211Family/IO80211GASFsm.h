//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211GASFsm {
public: // (access info unavailable from symbols alone)
    IO80211GASFsm(OSMetaClass const*);
    auto busy(void*);
    auto cleanupAndIndicateGASDone(int, apple80211_gas_query_state);
    auto collectGASResultsOnGASComplete();
    auto findIndexForPeer(ether_addr, bool&);
    auto flushGasPeerTable();
    auto free();
    auto free()::kalloc_type_view_100;
    auto freeResources();
    auto gasQueryTimerAction(IO80211TimerSource*);
    auto getFsmState();
    auto getGASMaxRspLen();
    auto getGASResults();
    auto getLogger() const;
    auto getMetaClass() const;
    auto handleABORT();
    auto handleGAS_COMPLETE(apple80211_gas_complete_t*);
    auto handleGAS_FRAGMENT_RX(apple80211_gas_rsp_t*);
    auto handleGAS_MULTI_PEER_ERROR(apple80211_gas_multi_peer_error_t*);
    auto handleGAS_REQUEST_IOC(apple80211_gas_query_t*);
    auto handleGAS_RESULTS_IOC(apple80211_gas_result_t*);
    auto handleQUIESCE();
    auto handleWAKE();
    auto handleWATCHDOG();
    auto ignore(void*);
    auto initGasPeerTable();
    auto initWithOptions(IO80211GASFsmOptions*);
    auto initWithOptions(IO80211GASFsmOptions*)::kalloc_type_view_125;
    auto isBusy();
    auto issueGASAbort();
    auto postGASDoneEvent(int, int);
    auto processAbort(void*);
    auto processDriverReset(void*);
    auto processGasComplete(void*);
    auto processGasCompleteError(void*);
    auto processGasCompleteInIdleOrAborting(void*);
    auto processGasFragRx(void*);
    auto processGasQuerySendError(void*);
    auto processGasQueryTimeout(void*);
    auto processQuiesce(void*);
    auto removeDups(apple80211_gas_peers_list_t&);
    auto resetGASManagerState();
    auto resetGASPeerEntry(gas_response_entry_t*);
    auto resetGASTimer();
    auto sendGasQuery(void*);
    auto shuffleGasPeers(apple80211_gas_peers_list_t&);
    auto unhandledEvent(void*);
    auto updateGASResponseInfo(gas_response_entry_t*, bool, unsigned char*, apple_event_gas_rsp_t*);
    auto updatePeerGASResponseTable(apple80211_gas_rsp_t*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IO80211GASFsm();
};
