//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211LinkQualityMonitor {
public: // (access info unavailable from symbols alone)
    IO80211LinkQualityMonitor();
    IO80211LinkQualityMonitor(OSMetaClass const*);
    auto analyzeMeasurements(bool);
    auto analyzeMeasurements(bool)::_os_log_fmt;
    auto analyzeTxLatency();
    auto armMeasurementTimer();
    auto checkLimits(Limits*);
    auto checkLimits(Limits*)::_os_log_fmt;
    auto checkMeasurementTimeout();
    auto checkRecoveryReason(Limits*);
    auto checkRecoveryReason(Limits*)::_os_log_fmt;
    auto checkRxPhyRateLimits(unsigned int, unsigned long long, bool*);
    auto checkRxPhyRateLimits(unsigned int, unsigned long long, bool*)::_os_log_fmt;
    auto checkTxPhyRateLimits(unsigned int, unsigned long long, bool*);
    auto checkTxPhyRateLimits(unsigned int, unsigned long long, bool*)::_os_log_fmt;
    auto disArmMeasurementTimer();
    auto free();
    auto free()::kalloc_type_view_682;
    auto getBandIndex(apple80211_channel_band);
    auto getCCAIndex(signed char);
    auto getCCAIndex(signed char)::ccaLookup;
    auto getCcaExtCaps();
    auto getChannelWidth();
    auto getChannelWidthIndex(apple80211_channel_width);
    auto getConcurrentInterfaceActiveDurationMS();
    auto getElapsedPeriodMS();
    auto getExpectedPhyRate(signed char, signed char);
    auto getExpectedThroughput(signed char, signed char);
    auto getExpectedTxAMPDUDensity(signed char, signed char);
    auto getLastLimits(LimitType);
    auto getLastMeasurements();
    auto getLinkRecovery();
    auto getLinkRecoveryReasonString(apple80211_link_recovery_reason);
    auto getMaxQueueFullDurationMS();
    auto getMetaClass() const;
    auto getNSSIndex(unsigned int);
    auto getOffChannelDurationUS();
    auto getPeerMacAddress();
    auto getPhyModeIndex(apple80211_phymode);
    auto getRSSIIndex(signed char);
    auto getRSSIIndex(signed char)::rssiLookup;
    auto getRealTimeAWDLActiveState();
    auto getSummary(apple80211_lqm_summary*);
    auto getTVPMActiveDurationMS();
    auto getThroughputTestResults(long long&, long long&);
    auto getUIEventStr(UIEvent);
    auto getUIStateStr(UIState);
    auto getWorstAvgLatencyCoP();
    auto getWorstMaxLatencyCop();
    auto incrementTxLatency(unsigned long long, io80211BinCounters*);
    auto induceRxAmpduDupErrors();
    auto induceRxBadPhy();
    auto induceRxHighDecryptError();
    auto induceRxHighMCDecryptError();
    auto induceRxHighOverflow();
    auto induceRxHighPNReplay();
    auto induceSlowWiFiIfDebugTriggered();
    auto induceTxBadPhy();
    auto induceTxLatency();
    auto induceTxLowPhyRate();
    auto initWithProviderAndOptions(IOService*, Options const*, IO80211LinkRecovery*, IO80211LQMCrashTracer*);
    auto initWithProviderAndOptions(IOService*, Options const*, IO80211LinkRecovery*, IO80211LQMCrashTracer*)::_os_log_fmt;
    auto initWithProviderAndOptions(IOService*, Options const*, IO80211LinkRecovery*, IO80211LQMCrashTracer*)::kalloc_type_view_530;
    auto is2GBand();
    auto measurementTimeoutCallback(IO80211TimerSource*);
    auto printLogs(Limits*);
    auto printLogs(Limits*)::_os_log_fmt;
    auto recordAMPDUDensity(unsigned int);
    auto recordAWDLInfraDutyCycle(unsigned int);
    auto recordAmpduCounters(AmpduCounters_t*);
    auto recordAvgCCA(apple80211_homechan_qual_data*);
    auto recordAvgCCA(apple80211_homechan_qual_data*)::_os_log_fmt;
    auto recordAvgLQM(apple80211_lqm_event_data*);
    auto recordAvgLQM(apple80211_lqm_event_data*)::_os_log_fmt;
    auto recordBTAntennaInfo(unsigned long long, unsigned int);
    auto recordChannelSwitchEvent(apple80211_channel_switch_data*);
    auto recordDPSSymptomsInput(apple80211_dps_symptoms_input*);
    auto recordDataQueueState(apple80211_data_queue_state_event*);
    auto recordEscoTrafficIndication(unsigned int);
    auto recordEscoTrafficIndication(unsigned int)::_os_log_fmt;
    auto recordIPAddressEvent(apple80211_assoc_ready*);
    auto recordInterfaceConcurrencyState(bool);
    auto recordLTECoexActivity(apple80211_lteCoex_report*);
    auto recordLinkProbeResult(int);
    auto recordLinkProbeResult(int)::_os_log_fmt;
    auto recordPhyActivity(long long, bool);
    auto recordPhyRate(unsigned int, bool);
    auto recordPhyRate(unsigned int, bool)::_os_log_fmt;
    auto recordRC1CoexActivity(apple80211_rc1_coex_stats_report*);
    auto recordRC2CoexActivity(apple80211_rc2_coex_stats_report*);
    auto recordRxDataErrors(RxErrors*);
    auto recordRxDataFrames(RxErrors*);
    auto recordRxOverflowErrors(RxErrors*);
    auto recordRxPacket(IO80211NetworkPacket*, packet_info_tag*);
    auto recordRxPacket(unsigned long long, unsigned long long);
    auto recordRxPhyErrors(RxErrors*);
    auto recordRxSecurityErrors(RxErrors*, bool);
    auto recordScanClientUpdate(apple80211_scan_client_update*);
    auto recordSymptomsInput(unsigned long long);
    auto recordSymptomsInput(unsigned long long)::_os_log_fmt;
    auto recordTVPMState(apple80211_tvpm_event*);
    auto recordTVPMState(apple80211_tvpm_event*)::_os_log_fmt;
    auto recordTxErrors(TxErrors*);
    auto recordTxLatency(unsigned long long);
    auto recordTxPacketCompletion(IO80211NetworkPacket*, PacketSkywalkScratch*, int);
    auto recordTxPacketCompletion(unsigned long long, unsigned long long, unsigned long long);
    auto recordTxPacketSubmission(IO80211NetworkPacket*, PacketSkywalkScratch*);
    auto recordTxPacketSubmission(unsigned long long, unsigned long long, unsigned long long);
    auto recordUserInputResult(int);
    auto recordUserInputResult(int)::_os_log_fmt;
    auto recordWeightedAvgLQM(apple80211_weight_avg_lqm_event_data*);
    auto recordWeightedAvgLQM(apple80211_weight_avg_lqm_event_data*)::_os_log_fmt;
    auto recordedAllMandatoryTypes();
    auto requestUserInput();
    auto resetLatencyCoP();
    auto resetMeasurements();
    auto runUIStateMachine(UIEvent, UIEventData);
    auto runUIStateMachine(UIEvent, UIEventData)::_os_log_fmt;
    auto saveCurrentMeasurementsAndLimits();
    auto setTriggerToInduceSlowWiFi(slowWiFiInduceTrigger);
    auto snapshotHostRxPacketStats();
    auto snapshotHostTxPacketStats();
    auto triggerIPFailRecovery();
    auto triggerLinkProbe(bool);
    auto triggerLinkProbe(bool)::_os_log_fmt;
    auto triggerUserInput();
    auto triggerUserInput()::_os_log_fmt;
    auto tweakTxCompletedPackets();
    auto tweakWLANTxRxDuration();
    auto updateCcaExtCaps(bool);
    auto updateRealTimeAWDLActiveState(bool);
    auto userInputTimeoutCallback(IO80211TimerSource*);
    auto withProviderAndOptions(IOService*, Options const*, IO80211LinkRecovery*, IO80211LQMCrashTracer*);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IO80211LinkQualityMonitor();
};
