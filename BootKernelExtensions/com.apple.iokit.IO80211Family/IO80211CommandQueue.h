//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211CommandQueue {
public: // (access info unavailable from symbols alone)
    IO80211CommandQueue();
    IO80211CommandQueue(OSMetaClass const*);
    auto bumpCapacity(unsigned int);
    auto countEntries(unsigned char);
    auto dequeue(unsigned int);
    auto dequeueBlocking(unsigned char);
    auto dump();
    auto enqueue(IO80211QueueDescriptor*);
    auto enqueueBefore(IO80211QueueDescriptor*, IO80211QueueDescriptor*);
    auto flush();
    auto free();
    auto free()::kalloc_type_view_140;
    auto getCapacity() const;
    auto getFirst(unsigned char);
    auto getHashBucket(unsigned char);
    auto getLast(unsigned char);
    auto getLock();
    auto getMetaClass() const;
    auto getNext(IO80211QueueDescriptor*);
    auto getNumBuckets() const;
    auto getSize() const;
    auto initWithGateAndCapacity(IO80211CommandGate*, unsigned int, unsigned int);
    auto initWithGateAndCapacity(IO80211CommandGate*, unsigned int, unsigned int)::kalloc_type_view_78;
    auto initWithGateAndCapacity(IO80211CommandGate*, unsigned int, unsigned int)::kalloc_type_view_88;
    auto insert(comparisonResult (*)(IO80211QueueDescriptor*, IO80211QueueDescriptor*, userPrintCtx*), IO80211QueueDescriptor*, userPrintCtx*, insertMethod, int (*)(OSObject*, IO80211QueueDescriptor*, void*, userPrintCtx*));
    auto isFull() const;
    auto lock(char const*, int);
    auto lockCountEntries(unsigned char);
    auto lockDequeue(unsigned char);
    auto lockDequeueBlocking(unsigned char);
    auto lockEnqueue(IO80211QueueDescriptor*);
    auto lockFlush();
    auto lockInsert(comparisonResult (*)(IO80211QueueDescriptor*, IO80211QueueDescriptor*, userPrintCtx*), IO80211QueueDescriptor*, userPrintCtx*, insertMethod, int (*)(OSObject*, IO80211QueueDescriptor*, void*, userPrintCtx*));
    auto lockMap(MapAction (*)(OSObject*, IO80211QueueDescriptor*, void*, mapExtraParam*), OSObject*, void*, mapExtraParam*, unsigned int);
    auto map(MapAction (*)(OSObject*, IO80211QueueDescriptor*, void*, mapExtraParam*), OSObject*, void*, mapExtraParam*, unsigned int);
    auto print(userPrintCtx*);
    auto releaseResources();
    auto releaseResources()::kalloc_type_view_129;
    auto unlock(char const*, int);
    auto withGateAndCapacity(IO80211CommandGate*, unsigned int, unsigned int);
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IO80211CommandQueue();
};
