//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class IO80211ScanRequest {
public: // (access info unavailable from symbols alone)
    IO80211ScanRequest();
    IO80211ScanRequest(OSMetaClass const*);
    auto createIO80211ScanRequest(apple80211_scan_data*, scanType);
    auto free();
    auto free()::kalloc_type_view_60;
    auto getBSSType();
    auto getBandInfo(unsigned int);
    auto getChannels();
    auto getMetaClass() const;
    auto getMinTimestamp();
    auto getNthChannelBand(unsigned long);
    auto getNthPrimaryChannel(unsigned long);
    auto getNumChannels();
    auto getSSID();
    auto getSSIDLength();
    auto getScanData() const;
    auto getScanFlags();
    auto getScanID();
    auto getScanRequestForDriver(apple80211ScanRequest&, unsigned int&, unsigned long long&, bool);
    auto getScanRequestForDriver(apple80211ScanRequest&, unsigned int&, unsigned long long&, bool)::_os_log_fmt;
    auto getScanType();
    auto getShortSSID();
    auto has6EChannel();
    auto initIO80211ScanRequest(apple80211_scan_data*, scanType);
    auto initIO80211ScanRequest(apple80211_scan_data*, scanType)::_os_log_fmt;
    auto initIO80211ScanRequest(apple80211_scan_data*, scanType)::kalloc_type_view_430;
    auto is2GScanRequest();
    auto isNeedToInformPnoFound();
    auto storeScanData(apple80211_scan_data*, scanType);
    auto storeScanData(apple80211_scan_data*, scanType)::_os_log_fmt;
    auto updateScanFlags(unsigned long long);
    auto updateScanFlags(unsigned long long)::_os_log_fmt;
    void * operator new(unsigned long);
    void operator delete(void*, unsigned long);
    ~IO80211ScanRequest();
};
