//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class AppleUSBXHCI {
public: // (access info unavailable from symbols alone)
    AppleUSBXHCI(OSMetaClass const*);
    auto abortPipeGated(IOUSBHostPipe*, int, IOService*);
    auto abortStreamGated(IOUSBHostStream*, int);
    auto adjustDeviceMaxExitLatencyGated(IOUSBHostDevice*, unsigned int);
    auto adjustPipeGated(IOUSBHostPipe*, IOUSBHostPipe::StandardUSBDescriptors&);
    auto cacheCapabilityRegisters();
    auto clearPipeStallGated(IOUSBHostPipe*);
    auto closePipeGated(IOUSBHostPipe*);
    auto closeStreamGated(IOUSBHostStream*);
    auto createCommandRing(AppleUSBXHCIInterrupter*, unsigned int, IOMapper*);
    auto createDevice(tInternalUSBHostConnectionSpeed, unsigned int, unsigned int);
    auto createIOBuffer(unsigned int, unsigned long long);
    auto createInternalDevice(unsigned int);
    auto createInternalEndpoint(AppleUSBXHCIDevice*, StandardUSB::EndpointDescriptor const*, StandardUSB::ConfigurationDescriptor const*);
    auto createIsochronousRequestPool();
    auto createIsochronousTransferRing(AppleUSBXHCIEndpoint*);
    auto createPipeGated(AppleUSBHostController::CreatePipeArgs*, IOUSBHostPipe*&);
    auto createPorts();
    auto createRequestPool();
    auto createStreamGated(IOUSBHostDevice*, IOUSBHostPipe*, unsigned int, IOUSBHostStream*&);
    auto createTDPool();
    auto createTransferRing(AppleUSBXHCIEndpoint*, unsigned int);
    auto destroyDevice(IOUSBHostDevice*);
    auto disableStreamsGated(IOUSBHostPipe*);
    auto enableStreamsGated(IOUSBHostPipe*);
    auto executePendingCommands(AppleUSBXHCIDevice*);
    auto filterOccurred(IOFilterInterruptEventSource*);
    auto generateRawReferenceMicroframe(unsigned long long&);
    auto getCompanionPort(AppleUSBHostPort*, unsigned int);
    auto getCompanionPortGated(AppleUSBHostPort*, unsigned int, AppleUSBHostPort*&);
    auto getDMACommand(unsigned int, unsigned int);
    auto getNextCapability(void*);
    auto getNextLegacyCapability(void*);
    auto getNextProtocolCapability(void*);
    auto getRawMicroFrameCount();
    auto getSlotForDevice(AppleUSBXHCIDevice*);
    auto getSlotForDevice(IOUSBHostDevice*);
    auto hardwareException(unsigned int);
    auto hardwareExceptionThreadCall(unsigned int);
    auto hasCapability(AppleUSBXHCI::tCapability);
    auto init(OSDictionary*);
    auto interruptOccurred(IOInterruptEventSource*, int);
    auto io(AppleUSBHostRequest*);
    auto ioFilterInterrupt(IOFilterInterruptEventSource*);
    auto ioGated(AppleUSBHostRequest*);
    auto ioInterrupt(IOInterruptEventSource*, int);
    auto isochronousFilterInterrupt(IOFilterInterruptEventSource*);
    auto isochronousInterrupt(IOInterruptEventSource*, int);
    auto makeMicroframeNumber(unsigned long long, unsigned long long) const;
    auto manuallyProcessInterrupts();
    auto portInterrupt(StandardUSBXHCI::StandardUSBXHCITRB const*);
    auto primaryFilterInterrupt(IOFilterInterruptEventSource*);
    auto primaryInterrupt(IOInterruptEventSource*, int);
    auto regAnd32(unsigned long long, unsigned int, unsigned int);
    auto regAnd64(unsigned long long, unsigned int, unsigned long long);
    auto regOr32(unsigned long long, unsigned int, unsigned int);
    auto regOr64(unsigned long long, unsigned int, unsigned long long);
    auto registerHubGated(IOUSBHostDevice*, StandardUSB::HubDescriptor const*, tRegisterHubFlags);
    auto registerPeriodicTransferRing(AppleUSBXHCITransferRing*);
    auto registerSuperSpeedHubGated(IOUSBHostDevice*, StandardUSB::SuperSpeedHubDescriptor const*);
    auto reset();
    auto restoreState();
    auto resumePipesGated(IOUSBHostDevice*);
    auto returnDMACommand(IODMACommand*);
    auto ringDoorbell(unsigned int, unsigned int);
    auto saveState();
    auto stringFromHardwareException(unsigned int);
    auto suspendPipesGated(IOUSBHostDevice*);
    auto testMode(unsigned int, StandardUSBXHCI::tXHCITestMode);
    auto unregisterPeriodicTransferRing(AppleUSBXHCITransferRing*);
    ~AppleUSBXHCI();
};
