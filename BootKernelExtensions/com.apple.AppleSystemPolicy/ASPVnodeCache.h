//
//     Generated by class-dump 3.5 (64 bit) — C++ symbol-derived view.
//

class ASPVnodeCache {
public: // (access info unavailable from symbols alone)
    ASPVnodeCache();
    ASPVnodeCache()::_os_log_fmt;
    auto checkAndSetTrackingUpcalled(vnode*);
    auto clearLoggingComplete(vnode*);
    auto enableTestMode();
    auto getDeveloperToolState(vnode*, int*);
    auto getEvaluationResults(vnode*, evaluation_results*);
    auto getProvenanceData(vnode*, TrackingAttributes*);
    auto needsLogging(vnode*);
    auto setDeveloperToolState(vnode*, int);
    auto setDeveloperToolState(vnode*, int)::_os_log_fmt;
    auto setEvaluationResults(vnode*, evaluation_results*);
    auto setEventPresentIfNeeded(vnode*, unsigned char, unsigned long long);
    auto setLoggingComplete(vnode*);
    auto setProvenanceData(vnode*, TrackingAttributes*);
    ~ASPVnodeCache();
};
